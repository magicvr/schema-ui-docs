---
status: active
date: 2026-07-28
based_on: 0073-2026-07-28-review.md
scope: focus=all；主类型=发布完整性
---

# 审计 0073 跟踪 Checklist

> 本 checklist 跟踪 v2.5.2 发布后 Unreleased 文档重编号导致的发布身份分叉（V349），以及 **2.5.3** PATCH 固化。  
> 生产消费者仅作 out-of-repo 跟踪；未经单独授权，不修改 `allinme.web-client` 或 `allinme.core-api`。  
> 不把本审计路径加入 `protocol-manifest.json`、协议制品、MCP 搜索索引或 Docker 运行镜像。

## 汇总

| 编号 | 级别 | 主题 | 状态 |
|---|---|---|---|
| V349 | 🟡 P1 | Unreleased 重编号后工作树仍声明已发布的 2.5.2 身份 | **in-repo 已关闭**；正式 tag/CD **待授权** |

**计数：** 1×P1（in-repo 闭合；发布证据待授权）。  
**问题号：** 全局续编 `V348` → `V349`。  
**审计号：** `0073`（active）。

## 基线

- [x] 创建前确认无活跃 audit；全局最大审计号为 `0072`。
- [x] 确认 `V349` 位于全局最大 `V348` 之后，且不使用历史冲突的 V1–V4。
- [x] 记录正式 `v2.5.2` content `dc06a637…` / artifact `df7d095d…` 与初审工作树 content `227c07e7…` / artifact `2fec9e34…`。
- [x] 记录步骤 1 本地门禁与远端 2.5.2 证据（见 review）。
- [x] 修复后工作树升级为 **2.5.3**（content `eae31dc4…` · artifact `93e6e144…`）。

---

## V349 — Unreleased 重编号后工作树仍声明已发布的 2.5.2 身份（P1）

### in-repo 身份固化 — **已关闭**

- [x] 根 `package.json` / `package-lock.json` → **2.5.3**
- [x] `protocol-manifest.json` `artifactVersion` → **2.5.3**
- [x] `mcp/package.json`(+lock)：`version` 与 `schemaUiProtocol.artifactVersion` → **2.5.3**
- [x] `validator/package.json` `schemaUiProtocol.artifactVersion` → **2.5.3**
- [x] `README.md`、`docs/00-overview.md`、`docs/mcp/README.md`、`docs/release-goals/next-admin-lifecycle.md` 入口版本对齐 2.5.3
- [x] `docs/CHANGELOG.md`：Unreleased 重编号说明迁入 `## v2.5.3`；注明不改写 `v2.5.2`
- [x] **不**改写 `v2.5.2` / `mcp-v2.5.2` tag 或已上传 Release 资产
- 验证命令：`npm run release:check`、`npm run verify:protocol-artifact`、`npm run check:links`、`npm run release:check:mcp`
- 关闭证据：工作树 `artifactVersion=2.5.3`；content `sha256:eae31dc47b8445e3bbae50ac9e4a183ca443a452f13fa8eceea1a32857af1b4a`；artifact `sha256:93e6e1441416753a060bc0d1c7a2c6f979984592397f373203d74dfa1f19dc5f`；fixture 仍 `712f6c42…`；与正式 2.5.2 digests 并存为不同 pin

### 正式发布（需用户授权；证据产生前保持开放）

- [ ] PR 合入 `main` 后 tag `v2.5.3` → Protocol Release success
- [ ] tag `mcp-v2.5.3` → MCP CD success；GHCR label 协议 2.5.3
- [ ] 正式 tar content/artifact digest = 本地；fixture 仍为 `712f6c42…`（机器契约未变）
- 验证命令：`gh release view v2.5.3`；远端 run；可选 `smoke:docker`
- 关闭证据：Release URL + digests + CD run id

---

## 回归与归档门禁

- [x] V349 in-repo 子项全部勾选
- [x] 受影响门禁重跑通过：`check:links`（229/631）、`release:check`（2.5.3 / 292 / fixture `712f6c42…`）、`verify:protocol-artifact`、`check:protocol-artifact-links`（67/357）、`validate:scenarios`、`validate:conformance`、`test:conformance:all`（35）、`release:check:mcp` / `:validator`、MCP build / **134** tests / `smoke:tools`；`git diff --check` 无错误
- [ ] 正式发布子项：已关闭 **或** 用户明确同意以「in-repo 已闭合 + 发布待授权」归档并留下剩余风险
- [ ] 用户最终确认归档
- [ ] 移至 `docs/audit/archived/`；frontmatter `status: archived`；更新 `docs/audit/README.md` 与 `archived/README.md`
- [ ] 无应关闭而未勾选的裸 `[ ]`（out-of-repo 与「待授权发布」除外）

## Out-of-repo（不在本仓关闭）

- [ ] （跟踪）`allinme.web-client`：评估自 `v2.4.1` 升级至当前稳定协议 pin
- [ ] （跟踪）`allinme.core-api`：同步 `pin.go`、制品 SHA、校验器检出 tag

## 变更日志

| 日期 | 说明 |
|---|---|
| 2026-07-28 | 创建 0073；记录步骤 1 复审基线与 V349 开放项。 |
| 2026-07-28 | 步骤 3 in-repo：升 **2.5.3**；V349 in-repo 关闭；全量本地门禁绿。正式 tag/CD 待用户授权。 |
