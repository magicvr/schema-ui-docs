---
status: active
date: 2026-07-28
based_on: 0072-2026-07-28-review.md
scope: focus=all；主类型=发布完整性与门禁卫生
---

# 审计 0072 跟踪 Checklist

> 本 checklist 跟踪 v2.5.1 发布后入口卫生与门禁观察面（V345–V348），以及修复后的 **2.5.2** PATCH 身份。  
> 生产消费者仅作 out-of-repo 跟踪；未经单独授权，不修改 `allinme.web-client` 或 `allinme.core-api`。  
> 不把本审计路径加入 `protocol-manifest.json`、协议制品、MCP 搜索索引或 Docker 运行镜像。

## 汇总

| 编号 | 级别 | 主题 | 状态 |
|---|---|---|---|
| V345 | 🟡 P2 | MCP 文档仍写「需等 `mcp-v2.5.1` 发布」 | **已关闭** |
| V346 | 🟡 P2 | `release-check` 未对权威 `17` 做 `applies_to` 门禁 | **已关闭** |
| V347 | ⚪ P3 | Protocol CI 未观察 L0–L4 等校验脚本路径 | **已关闭** |
| V348 | ⚪ P3 | CHANGELOG 措辞 + 2.5.2 身份（in-repo 已闭合） | **in-repo 已关闭；正式 tag/CD 待办** |

**计数：** 2×P2 + 1×P3 in-repo 全关；V348 发布证据链待 `v2.5.2` / `mcp-v2.5.2`。  
**问题号：** 全局续编 `V344` → `V345`–`V348`。  
**审计号：** `0072`（active）。  
**工作树身份：** 协议 / MCP **2.5.2** / `"2.5"`；fixture 仍 `712f6c42…`（机器契约相对 2.5.1 未变）。

## 基线

- [x] 创建前确认无活跃 audit；全局最大审计号为 `0071`。
- [x] 确认 `V345`–`V348` 位于全局最大 `V344` 之后，且不使用历史冲突的 V1–V4。
- [x] 记录工作树（初审）：协议 `2.5.1` / `"2.5"`，MCP `2.5.1`；正式 `v2.5.1` / `mcp-v2.5.1` @ `6191c14`。
- [x] 记录初审本地门禁与远端 2.5.1 证据（见 review）。
- [x] 修复后工作树升级为 **2.5.2**（见变更日志）。

---

## V345 — MCP 文档仍写「需等 `mcp-v2.5.1` 发布」（P2）— 已关闭

- [x] 更新 `docs/mcp/README.md`「当前版本示例」段：去掉错误的「需等已发布 tag」口径；区分工作树与已发布 pin。
- [x] 全文检索 `docs/mcp/**`：无残留「需等」。
- [x] 示例版本与 `mcp/package.json` **2.5.2** 对齐；并注明 `mcp-v2.5.2` 发布前生产可 pin 已发布 `2.5.1`。
- 验证命令：`npm run check:links`（227 MD / 633 链接通过）。
- 关闭证据：`docs/mcp/README.md` 已改；`docs/mcp` 不进协议制品。

---

## V346 — `release-check` 未对权威 `17` 做 `applies_to` 门禁（P2）— 已关闭

- [x] 将 `docs/17-app-manifest.md` 纳入 `scripts/release-check.js` 的 `coreSpecPaths`。
- [x] 将 `docs/00-overview.md` 纳入同一循环。
- [x] 扩展后 `npm run release:check` 通过（2.5.2）。
- [x] 负向探针：临时将 `17` 的 `applies_to` 改为 `v2.4` → `release:check` exit 1；已恢复。
- 验证命令：`npm run release:check`。
- 关闭证据：`coreSpecPaths` 含 `00`/`17`；负向 fail-closed。

---

## V347 — Protocol CI 未观察 L0–L4 等校验脚本路径（P3）— 已关闭

- [x] `.github/workflows/protocol-ci.yml`：`pull_request` 与 `push` 的 scripts 观察面均改为 `scripts/**`。
- [x] 两处 paths 一致。
- [x] 与 CONTRIBUTING 本地门禁对齐：校验/release/link/conformance 脚本变更将触发 Protocol CI；validator-ci / mcp-ci 仍可并行兜底。
- 验证命令：workflow 文件审查。
- 关闭证据：protocol-ci paths 使用 `scripts/**`。

---

## V348 — CHANGELOG 措辞与 2.5.2 身份（P3）— in-repo 已关闭；tag 待办

- [x] `v2.5.1` 版本说明改为「PATCH **已发布**」。
- [x] `v2.5.0` 节「见 Unreleased 292」改为「见 **v2.5.1** 的 292 cases」。
- [x] 评估 digest：fixture 不变 `712f6c42…`；content/artifact 因 informative 变更而更新 → 工作树升为 **2.5.2**（根包、manifest、lockfile、MCP、validator、README、overview、CHANGELOG `v2.5.2` 节）。
- [x] **不**改写 `v2.5.1` / `mcp-v2.5.1` tag。
- [ ] **发布证据（归档前）：** PR → `main`；tag `v2.5.2` Protocol Release success；tag `mcp-v2.5.2` MCP CD success；记录正式 tar/镜像 digest（应与本地 `content 6bd5796b…` / `artifact 36cc47ae…` 对齐，以 Release 为准）。
- 验证命令（in-repo）：`npm run release:check`；`npm run verify:protocol-artifact`。
- 关闭证据（in-repo）：CHANGELOG + 2.5.2 版本对齐；本地 digest 见下。  
  - contentDigest：`sha256:dc06a6370aba18ceb28b6fecd898320f4e9c0f2a0219734cc17ccf583031c3d8`  
  - artifactDigest：`sha256:df7d095de218934b542de00ccea2c065f9dd84d9e3f696b19bee4e08615db4f0`  
  - fixtureDigest：`sha256:712f6c4293ccd7e7c0355d9fe9e4b264eecbd64fc5cac68251a4e8e8458ecf7e`（与 2.5.1 相同）

---

## 回归与归档门禁

- [x] V345–V347 全部勾选；V348 in-repo 勾选。
- [x] 重跑：`check:links`、`release:check`、`verify:protocol-artifact`、`check:protocol-artifact-links`、`validate:scenarios`、`validate:conformance`、`test:conformance:all`（35/35）、MCP build / 134 tests / smoke:tools。
- [ ] V348 正式 tag/CD 证据（见上）。
- [ ] 用户确认归档后：移至 `docs/audit/archived/`，frontmatter `status: archived`，更新 `docs/audit/README.md` 与 `archived/README.md`。
- [ ] `git diff --check`；确认相对链接有效；checklist 无应关闭而未勾选的裸 `[ ]`（发布证据项除外，直至 tag）。

## Out-of-repo（不在本仓关闭）

- [ ] （跟踪）`allinme.web-client`：评估自 `v2.4.1` 升级至 `v2.5.1`/`v2.5.2` 的 pin、suite 数、CI ref。
- [ ] （跟踪）`allinme.core-api`：同步 `pin.go`、制品 SHA、校验器检出 tag。

## 变更日志

| 日期 | 说明 |
|---|---|
| 2026-07-28 | 创建 0072；记录步骤 1 复审基线与 V345–V348 开放项。 |
| 2026-07-28 | **步骤 3 in-repo：** V345–V347 关闭；V348 措辞修复并升 **2.5.2**；本地全量门禁绿；正式 tag/CD 待用户发版。 |
