---
status: active
date: 2026-07-18
based_on: 0061-2026-07-18-review.md
---

# 审计 0061 跟踪清单

> 本清单只验收前后端共享协议的字段、语义、Schema、L0-L4、JS/Python conformance 和协议发布边界。生产 Renderer、业务后端、MCP/Validator 作为产品的能力不属于本轮协议条目。

## 基线与范围

- [x] 确认本项目只交付共享协议；不把缺少生产 Renderer/Host 计为协议缺陷。
- [x] 确认视觉层边界：CSS、DOM、主题、动画和像素级响应式由前端决定。
- [x] 当前基线记录为 `main` / HEAD `1726f7c`，创建前工作树干净。
- [x] 新审计编号使用 `0061`；新增问题从全局 `V242` 起连续编号。
- [x] 已检查此前 0059/0060 结论，未将已关闭问题直接重复编号。

## V242（P1）bodyMapping 源字段与目标字段

- [ ] 明确 `bodyMapping` source key 必须属于调用 form 的有效字段集合。
- [ ] Form 字段收集包含普通 `field` 以及 `dateRangePicker.startField/endField`。
- [ ] L2 拒绝不存在的 source key，并给出稳定错误码/路径。
- [ ] L2 拒绝多个 source 写入同一 target key；补充冲突路径诊断。
- [ ] 定义已声明字段运行时为 `undefined` 时的提交行为。
- [ ] JS/Python request reference 使用 presence sentinel，统一缺失字段行为。
- [ ] 增加未知 source、重复 target、显式 null、缺失 runtime value 的 request fixtures。
- [ ] 同步 `07-actions-contract.md`、Action Schema、L2、JS/Python reference、MCP 回归和 CHANGELOG。

## V243（P1）普通表单 Action URL 模板

- [ ] 明确普通 `form.props.submitAction` 不允许未绑定 `{identifier}` 模板。
- [ ] L2 对 form action 引用路径拒绝含模板的 request URL。
- [ ] Request reference 对绕过 L2 的 form template fail-closed。
- [ ] 增加合法 RowAction template、非法普通 form template、孤立花括号负例。
- [ ] 同步 Action/Renderer 文档和迁移说明。

## V244（P1）Form 字段命名空间与 `$deps` 符号表

- [ ] 定义 form scope 内字段非空、唯一性和日期范围端点规则。
- [ ] L2 建立 form 字段符号表，覆盖嵌套 children、tabs content 和 modal content。
- [ ] L3a 校验 reaction/visibleWhen/data.params/optionsSource.params 的 `$deps` 根字段存在。
- [ ] 拒绝重复字段、空字段、范围端点冲突和未知 `$deps` 字段。
- [ ] 增加重复字段、范围冲突、拼写错误 `$deps` 的负例 fixtures。
- [ ] 同步 `01`、`02`、`03`、`04`、`06`、Schema、L2/L3a 和 CHANGELOG。

## V245（P1）server pageSize

- [ ] 决定 `pagination.mode: server` 下 `pageSize` 必填或固定默认值。
- [ ] 将决定同步到 component registry DSL、组件文档、ADR-0011 和 Renderer 规范。
- [ ] L2/normalizer/Renderer 对首次请求物化同一结果。
- [ ] 增加省略 pageSize、边界值和非法小数/非正数 fixtures。

## V246（P1）static/ref 和组件数据形状

- [ ] 定义 static datasource 被 `source: ref` 引用时的同步消费语义。
- [ ] 定义 static ref 不产生网络 loading/error 的规则。
- [ ] 分别定义 table、statCard、chart、text 的 static 输入形状和取值路径。
- [ ] 明确 static/ref 与 responseMapping 的关系。
- [ ] 增加四类组件的 static/ref 正反 fixtures。

## V247（P1）标量组件 responseMapping

- [ ] 决定 v2 是否禁止 statCard/text responseMapping，或新增 `value` mapping。
- [ ] 同步 L2 条件约束和 Schema/DSL 描述。
- [ ] 修正 JS/Python response reference 对标量输入的处理。
- [ ] 增加标量包装响应的正反 fixture。

## V248（P1）空 local responseMapping

- [ ] 决定 `{}` 是非法输入还是“清除继承并恢复默认映射”。
- [ ] 若非法：删除/改写 `empty-local-mapping` 成功 fixture，reference fail-closed。
- [ ] 若合法：移除/调整 `minProperties`，补充规范、L2 和完整 fixtures。
- [ ] 重新运行 response mapping、scenario、release digest 门禁。

## V249（P1）RowAction 缺失值

- [ ] 定义缺失/undefined 与显式 null 的区别。
- [ ] Path 缺失、query 缺失、body 缺失统一为原子 fail-closed 行为。
- [ ] Query null tombstone 继续按 ADR-0010 删除参数。
- [ ] JS/Python 统一返回 `UNRESOLVED_ROW_VALUE` 和路径。
- [ ] 增加 path/query/body 缺失与显式 null fixtures。

## V250（P1）RowAction retry/idempotency

- [ ] 明确 RowAction 继承目标 request Action 的 retryPolicy。
- [ ] 定义一次点击的 invocation 边界和 timeout/network unknown 结果。
- [ ] idempotent retry 复用同一 Idempotency-Key。
- [ ] 增加行级 never/idempotent/timeout/network fixtures。
- [ ] 同步 `07`、`08`、ADR-0014、JS/Python reference 和 CHANGELOG。

## V251（P1）401/403 fallback 优先级

- [ ] 固定 401/403 高于 states.error.fallbackText 的优先级。
- [ ] 401 抑制具体 fallback/response message。
- [ ] 403 使用固定无权限占位。
- [ ] 增加带自定义 fallback 的 401/403 fixtures 或行为向量。
- [ ] 同步 `04`、`07`、`08` 和 Action outcome reference。

## V252（P1）requestInterceptor 与 DataRef GET-only

- [ ] 限制 DataRef/optionsSource GET interceptor 不得增加 body。
- [ ] 明确 interceptor 可修改和不可修改的传输字段。
- [ ] 增加 interceptor 试图修改 DataRef body 的负例/运行时向量。
- [ ] 同步 Renderer 请求配置文档和 conformance 说明。

## V253（P1）请求 latest-wins

- [ ] 定义 per-node request epoch/generation。
- [ ] 定义旧请求取消、旧响应丢弃和 hidden/unmounted 状态处理。
- [ ] 覆盖 `$deps`、搜索、分页、排序连续变化。
- [ ] 增加乱序响应 fixtures 和 Renderer 行为说明。

## V254（P1）Form hidden/disabled 提交投影

- [ ] 定义 hidden、disabled、unmounted 字段是否参与校验。
- [ ] 定义上述字段是否进入 bodyMapping 和默认提交 body。
- [ ] 定义上传失败字段的提交行为。
- [ ] 定义 hidden required 的提交失败/跳过规则。
- [ ] 增加 hidden/disabled/required/bodyMapping 组合 fixtures。

## V255（P1）严格 URL query 和 Unicode 校验

- [ ] L2 复用 canonical query parser 拒绝 `%zz`、非法 UTF-8 等 URL query。
- [ ] L2 对 query key/value 执行 Unicode scalar validation。
- [ ] 覆盖 data.params、datasources.params、optionsSource.params、RowAction query。
- [ ] MCP/CLI 返回统一错误码和路径。
- [ ] 增加 MCP/L2 回归测试，确认不影响合法 query。

## V256（P1）JSON own-property traversal

- [ ] 统一 JS responseMapping、reaction `$deps`、RowAction `$row` 的 own-property 读取。
- [ ] 禁止通过原型链解析 `toString`、`constructor`、`__proto__`、`prototype`。
- [ ] 确认 Python reference 与 JS reference 的缺失结果一致。
- [ ] 增加 response/reaction/row 三类跨语言 fixtures。

## V257（P1）非法 GET body reference fail-closed

- [ ] Form Action GET 在 request construction 前返回统一拒绝。
- [ ] RowAction GET/DELETE 非空 body 在 request construction 前返回统一拒绝。
- [ ] 定义错误码和路径，并与 L2 保持一致。
- [ ] 增加 form GET、row GET body、row DELETE body 负例。

## V258（P1）官方场景 reference metadata/path

- [ ] Python scenario reference 读取并校验 `scenarioPath`。
- [ ] JS/Python 对官方场景 pageId/protocolVersion metadata 使用同一来源。
- [ ] 增加不存在 path 和 metadata mismatch 负例。
- [ ] 重新运行 JS/Python scenario runners。

## V259（P1）JSON number 1/1.0 reaction

- [ ] 定义语言无关 JSON number 比较语义。
- [ ] 定义 reaction commit equality 和是否触发下一轮的数字规则。
- [ ] 修正 JS/Python reference 一致性。
- [ ] 增加 `1`、`1.0`、指数形式和跨类型比较 fixtures。

## V260（P1）Reaction 初始求值与 true→false 复位

- [ ] 定义 mount 时是否强制初始 snapshot/evaluate。
- [ ] 定义每个状态键的基线值。
- [ ] 定义无 otherwise 时条件变 false 的复位行为。
- [ ] 定义多 reaction 对同一状态键的复位/覆盖顺序。
- [ ] 增加初始求值、true→false 和多轮状态转换 fixtures。

## V261（P1）表达式类型和 coercion

- [ ] 定义 `==`、`!=`、`>`、`>=`、`<`、`<=` 的类型矩阵。
- [ ] 定义 null、undefined、缺失、数字、布尔和字符串比较。
- [ ] 禁止直接依赖宿主语言默认 coercion。
- [ ] 同步表达式规范、Renderer 指南、JS/Python reference 和 fixtures。

## V262（P2）Reaction 下一轮与 loop limit

- [ ] 对齐 Renderer 文档、ADR-0006 和 reference 的下一轮触发条件。
- [ ] 定义默认最大轮次 N 和 off-by-one 行为。
- [ ] 定义第 N 轮仍需继续时的结构化结果。
- [ ] 增加无依赖写入、依赖写入和循环超限 fixtures。

## V263（P2）Core Registry 与 Host Extension

- [ ] 明确 `Renderer.register` 是否仅为宿主内部 API。
- [ ] 定义 extension manifest 的 namespace/version/Schema/capability。
- [ ] 明确未安装扩展时的拒绝或降级结果。
- [ ] L2 和 runtime 使用同一扩展描述输入，或明确扩展不属于核心 CI 页面。
- [ ] 同步 `03`、`06`、`08`、ADR/CHANGELOG。

## V264（P2）dependencies/visibleField 结构约束

- [ ] Schema 对 dependencies 增加 `uniqueItems`。
- [ ] 拒绝 `$deps.`/`$row.` 前缀和原型保留路径。
- [ ] `visibleField` 限制为合法 row path，拒绝表达式拼接。
- [ ] 在 L3a 前完成 shorthand normalization。
- [ ] 增加未使用错误 dependency 和非法 visibleField 负例。

## V265（P2）默认值、baseURL 和必填 props fallback

- [ ] 定义 schema default 是否由 normalizer 物化。
- [ ] 定义请求型 Renderer 缺少 baseURL 的行为。
- [ ] 缺少必填 props 时统一 invalid-component placeholder 或固定默认值。
- [ ] 补充 DataRef method、upload fieldName/multiple 等默认值测试。

## V266（P2）组件边界语义

- [ ] 定义 searchable select 的 keyword 冲突、优先级、清空和 tombstone。
- [ ] 定义 upload 空文件、ASCII whitespace 和多文件 idempotency key 派生。
- [ ] 定义 currency code、datetime timezone 和非法值 fallback。
- [ ] 增加 columns/span/pageSize 等正整数/范围约束。
- [ ] 为每个变更同步组件 DSL、Schema、L2、reference、fixtures 和 CHANGELOG。

## 完整 Admin 生命周期（后续范围，不计入本轮缺陷）

- [ ] 另开协议设计：页面级 ActionTrigger/Toolbar。
- [ ] 另开协议设计：标准行级详情/编辑导航。
- [ ] 另开协议设计：编辑记录加载、initialValues 和表单回填。
- [ ] 另开协议设计：recordView/details 组件。
- [ ] 另开协议设计：批量操作和完整权限继承。

## 协议门禁

- [ ] 修复完成后运行 `npm run validate:conformance`。
- [ ] 修复完成后运行 `npm run test:conformance:all`。
- [ ] 修复完成后运行 `npm run validate:scenarios`。
- [ ] 修复完成后运行 `npm run release:check`。
- [ ] 修复完成后运行 `npm run check:links`。
- [ ] 若修改 MCP 校验，运行 MCP build、tests 和 tools smoke。
- [ ] 若修改 fixtures/协议内容，核对 protocol content digest 和 fixture digest。
- [ ] 所有问题关闭后，等待用户明确确认，再将本套文档移入 `docs/audit/archived/`。

## 归档判定

- [ ] V242–V266 均有规范、Schema/L2、reference 和 fixture 证据，或明确记录为后续范围。
- [ ] 当前审计范围内 P0/P1/P2 均已关闭或有用户确认的范围决策。
- [ ] checklist 无裸 `[ ]` 后才允许归档。
- [ ] 归档时同步 `docs/audit/README.md` 与 `docs/audit/archived/README.md`。
