---
status: active
date: 2026-07-24
based_on: 0067-2026-07-24-review.md
---

# 审计 0067 跟踪 Checklist

> 本 checklist 跟踪 **v2.4 recordView 交付原子一致性**。生产 Renderer、Host、业务鉴权不属于本轮范围。  
> 不借本清单修改 ADR-0024 已接受语义；语义权威仍以 accepted ADR、核心规范、Schema 与 versioned fixtures 为准。  
> 本轮创建时 **未** 跑全量 `release:check` / `test:conformance:all` / `verify:protocol-artifact`。

## 基线

- [x] 审计编号 `0067` 延续已归档 `0066`（活跃区无活跃审计；归档区最大号 0066 → 下一号 0067）。
- [x] 问题编号 `V300`–`V308` 延续 `V299`。
- [x] 确认本轮 **2×P0 + 2×P1 + 4×P2 + 1×P3 簇**；机器双重门控与 key⊆mapping 有 L2 探针，非「未实现」类 P0。
- [x] 确认 0066 已归档且无并行活跃审计。
- [x] 记录复审基线：工作树 `2.4.0` / `2.4`；ADR-0024 accepted；versioned fixtures 静态 213。
- [x] 将 L2/L3a 探针结论与「全量未跑」写入 review 基线表。

## V300（P0）权威层当前版本仍为 2.3 — 开放

- [ ] `docs/01-node-protocol.md` 中「当前协议/制品使用 `"2.3"`」改为与 **`2.4` / `2.4.0`** 一致（含 YAML 示意注释与 `meta.protocolVersion` 说明段）。
- [ ] `docs/schemas/page.schema.json` `meta.protocolVersion.description` 中「当前协议制品为 `"2.3"`」改为 **`"2.4"`**，并覆盖或交叉引用 2.4 / `recordView` 纪律。
- [ ] 全文检索核心权威层无残留「当前…2.3」作为**现行**制品锚点（历史迁移/CHANGELOG 节除外）。
- [ ] 关闭说明：路径 + 一句话摘要。

## V301（P0）`01` 未写 recordView 版本下限 — 开放

- [ ] 在 `01` `meta.protocolVersion` 纪律中增加：使用 `type: recordView` 时必须 `meta.protocolVersion >= "2.4"`，并声明 `record.view.load`。
- [ ] 与 `00` / `06` / ADR-0024 D1 双重门控表述交叉一致（版本 + capability 两端均出现在权威链）。
- [ ] 关闭说明：路径 + 一句话摘要。

## V302（P1）registry `required` 键冲突 — 开放

- [ ] 修复 `component-registry.json` 中字段级布尔 `required` 与对象级 `required: [...]` 键冲突（至少 `recordView.props.recordSource`；评估 `form.props.recordSource` 同构）。
- [ ] 确认 `JSON.parse` 后字段必填语义可被 L2 `validateProps`（或文档约定的唯一规则）读取。
- [ ] 回归：缺 `recordSource` 的 `recordView` 仍 fail-closed；合法页仍通过。
- [ ] 关闭说明。

## V303（P1）双重门控无 L2 负例向量 — 开放

- [ ] 择一或组合：
  - (A) 增加可自动执行的 L2 负例（例如：`protocolVersion: "2.3"` + `recordView`；`2.4` 无 `record.view.load` + `recordView`）；并纳入 CI / release 路径；或
  - (B) 书面豁免并改写 `15-v2.4-release-goals` 相关勾选措辞（默认不推荐）。
- [ ] 若选 (A)：记录 suite/脚本名与 case id。
- [ ] 关闭说明。

## V304（P2）`02` §13 recordView 路由 undefined — 开放

- [ ] `docs/02-reaction-expression.md` §13（或等价节）将「route 键 undefined → 拒绝构造加载请求」扩到 `recordView.props.recordSource`。
- [ ] 关闭说明。

## V305（P2）`08` recordView 执行节 — 开放

- [ ] 在 `docs/08-renderer-spec.md` 增加 `record.view.load` / `recordView` 执行摘要（挂载 GET、显式 mapping、缺路径 null、节点错误态、与 form.record.load 边界）。
- [ ] 关闭说明。

## V306（P2）key ⊆ mapping 仅 L2 — 开放

- [ ] 择一：
  - (A) 在 `06`/`03` 明确「跨字段 ⊆ 仅 L2，L0/L1 不足」并视需要加 L2 金样例（可与 V303 合并）；或
  - (B) 接受历史分层模式，本项信息关闭并记决议。
- [ ] 关闭说明。

## V307（P2）场景阅读顺序 — 开放

- [ ] `docs/05-scenarios/README.md` 阅读顺序补入 `admin-list-detail-lifecycle`。
- [ ] 关闭说明。

## V308（P3）信息簇 — 开放

- [ ] states empty：ADR vs `03` 对齐或接受差异。
- [ ] response-mapping：可选增加 `recordView` 空 mapping 负例（或接受共享路径覆盖）。
- [ ] `protocol-manifest` informative 是否列入 `order-detail-lifecycle` 样例。
- [ ] `06` `last_updated` 与 2.4 内容一致。
- [ ] 各子项关闭或「本 MINOR 不改」决议。

## 文档与入口（关闭本审计时）

- [ ] `docs/audit/README.md` 移除 0067 活跃记录（或标归档）。
- [ ] review + checklist 移入 `docs/audit/archived/`。
- [ ] `docs/audit/archived/README.md` 顶部登记 0067 摘要。
- [ ] 未将审计路径写入 `protocol-manifest.json` 发布清单（保持 excluded）。

## 完成门禁（关闭本审计时）

- [ ] V300–V301 全部勾选关闭。
- [ ] V302–V303 全部勾选关闭，或书面豁免已登记。
- [ ] V304–V308 全部勾选关闭或明确推迟。
- [ ] 关闭时执行并记录：
  - [ ] `npm run release:check`
  - [ ] `npm run verify:protocol-artifact`（若发运制品）
  - [ ] 至少：`test:conformance` 中 response-mapping / version-negotiation / scenarios（或 `test:conformance:all`）
- [ ] 无新增未编号的 recordView / 2.4 发布阻断项。

## 决议记录

| 日期 | 决议 |
|---|---|
| 2026-07-24 | **新建 0067**。只读审计 v2.4 recordView 原子一致性。结论：机器层主路径已落地；权威 `01`/`page.schema` 仍锚 2.3 且 `01` 缺 recordView 版本下限 → **P0 阻断**。开放 V300–V308。全量测试未跑。 |
