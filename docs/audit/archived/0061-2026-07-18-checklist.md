---
status: archived
date: 2026-07-18
based_on: 0061-2026-07-18-review.md
---

# 审计 0061 跟踪清单

> 本清单只验收前后端共享协议的字段、语义、Schema、L0-L4、JS/Python conformance 和协议发布边界。生产 Renderer、业务后端、MCP/Validator 作为产品的能力不属于本轮协议条目。

## 基线与范围

- [x] 确认本项目只交付共享协议；不把缺少生产 Renderer/Host 计为协议缺陷。
- [x] 确认视觉层边界：CSS、DOM、主题、动画和像素级响应式由前端决定。
- [x] 当前基线记录为 `main` / HEAD `1726f7c`，创建前工作树干净。
- [x] 新审计编号使用 `0061`；新增问题从全局 `V242` 起连续编号。
- [x] 已检查此前 0059/0060 结论，未将已关闭问题直接重复编号。

## V242（P1）bodyMapping 源字段与目标字段

- [x] 明确 `bodyMapping` source key 必须属于调用 form 的有效字段集合。
- [x] Form 字段收集包含普通 `field` 以及 `dateRangePicker.startField/endField`。
- [x] L2 拒绝不存在的 source key，并给出稳定错误码/路径。
- [x] L2 拒绝多个 source 写入同一 target key；补充冲突路径诊断。
- [x] 定义已声明字段运行时为 `undefined` 时的提交行为。
- [x] JS/Python request reference 使用 presence sentinel，统一缺失字段行为。
- [x] 增加未知 source与缺失 runtime value 的 request fixture。
- [x] 同步 `07-actions-contract.md`、Action Schema、L2、JS/Python reference、MCP 回归和 CHANGELOG。

## V243（P1）普通表单 Action URL 模板

- [x] 明确普通 `form.props.submitAction` 不允许未绑定 `{identifier}` 模板。
- [x] L2 对 form action 引用路径拒绝含模板的 request URL。
- [x] Request reference 对绕过 L2 的 form template fail-closed。
- [x] 增加合法 RowAction template、非法普通 form template、孤立花括号负例。
- [x] 同步 Action/Renderer 文档和迁移说明。

## V244（P1）Form 字段命名空间与 `$deps` 符号表

- [x] 定义 form scope 内字段非空、唯一性和日期范围端点规则。
- [x] L2 建立 form 字段符号表，覆盖嵌套 children、tabs content 和 modal content。
- [x] L3a 校验 reaction/visibleWhen/data.params/optionsSource.params 的 `$deps` 根字段存在。
- [x] 拒绝重复字段、空字段、范围端点冲突和未知 `$deps` 字段。
- [x] 增加重复字段、范围冲突、拼写错误 `$deps` 的静态校验覆盖。
- [x] 同步 `01`、`02`、`03`、`04`、`06`、Schema、L2/L3a 和 CHANGELOG。

## V245（P1）server pageSize

- [x] 决定 `pagination.mode: server` 下 `pageSize` 必填或固定默认值。
- [x] 将决定同步到 component registry DSL、组件文档、ADR-0011 和 Renderer 规范。
- [x] L2 对首次请求所需的 pageSize 执行同一正整数约束。
- [x] Schema/组件文档明确 client/none 不使用 pageSize。

## V246（P1）static/ref 和组件数据形状

- [x] 定义 static datasource 被 `source: ref` 引用时的同步消费语义。
- [x] 定义 static ref 不产生网络 loading/error 的规则。
- [x] 分别定义 table、statCard、chart、text 的 static 输入形状和取值路径。
- [x] 明确 static/ref 与 responseMapping 的关系。
- [x] 增加四类组件的 static/ref 正反 fixtures；覆盖 ref 解引用、缺失目标与 responseMapping 拒绝。

## V247（P1）标量组件 responseMapping

- [x] 决定 v2 禁止 statCard/text responseMapping。
- [x] 同步 L2 条件约束和 Schema/DSL 描述。
- [x] JS/Python response reference 对未定义标量 mapping fail-closed。
- [x] 增加标量组件 responseMapping 的负向覆盖说明。

## V248（P1）空 local responseMapping

- [x] 决定 `{}` 是非法输入并保持 `minProperties: 1`。
- [x] 空/null local mapping 的 reference 统一 fail-closed。
- [x] 补充空/null mapping 与原型属性路径负例。
- [x] 重新运行 response mapping、scenario、release digest 门禁。

## V249（P1）RowAction 缺失值

- [x] 定义缺失/undefined 与显式 null 的区别。
- [x] Path/query/body 缺失统一为原子 fail-closed 行为。
- [x] Query null tombstone 继续按 ADR-0010 删除参数。
- [x] JS/Python 统一返回 `UNRESOLVED_ROW_VALUE` 和路径。
- [x] 增加 path/query/body 缺失、显式 null 与 query tombstone 覆盖。

## V250（P1）RowAction retry/idempotency

- [x] 明确 RowAction 继承目标 request Action 的 retryPolicy。
- [x] 定义一次点击的 invocation 边界和 timeout/network unknown 结果。
- [x] idempotent retry 复用同一 Idempotency-Key。
- [x] 增加行级 idempotent fixture。
- [x] 同步 `07`、`08`、ADR-0014、JS/Python reference 和 CHANGELOG。

## V251（P1）401/403 fallback 优先级

- [x] 固定 401/403 高于 states.error.fallbackText 的优先级。
- [x] 401 抑制具体 fallback/response message。
- [x] 403 使用固定无权限占位。
- [x] 增加带自定义 fallback 的 401/403 行为向量。
- [x] 同步 `04`、`07`、`08` 和 Action outcome reference。

## V252（P1）requestInterceptor 与 DataRef GET-only

- [x] 限制 DataRef/optionsSource GET interceptor 不得增加 body。
- [x] 明确 interceptor 可修改和不可修改的传输字段。
- [x] 增加 interceptor 试图修改 DataRef body、method 或 query 的负例/运行时向量。
- [x] 同步 Renderer 请求配置文档和 conformance 说明。

## V253（P1）请求 latest-wins

- [x] 定义 per-node request epoch/generation。
- [x] 定义旧请求取消、旧响应丢弃和 hidden/unmounted 状态处理。
- [x] 覆盖依赖变化对应的连续请求。
- [x] 增加乱序响应 fixtures 和 Renderer 行为说明。

## V254（P1）Form hidden/disabled 提交投影

- [x] 定义 hidden、disabled、unmounted、上传失败字段是否参与校验。
- [x] 定义上述字段是否进入 bodyMapping 和默认提交 body。
- [x] 定义上传失败字段的提交行为。
- [x] 定义 hidden required 的提交失败/跳过规则。
- [x] 增加 hidden/disabled/required/bodyMapping 组合 fixture。

## V255（P1）严格 URL query 和 Unicode 校验

- [x] L2 以 canonical query 规则拒绝 `%zz`、非法 UTF-8 等 URL query。
- [x] L2 对 query key/value 执行 Unicode scalar validation。
- [x] 覆盖 data.params、datasources.params、optionsSource.params、RowAction query。
- [x] MCP/CLI 沿用统一错误码和路径；本轮未修改 MCP 实现。
- [x] 增加 L2/conformance 回归并确认合法 query 不受影响。

## V256（P1）JSON own-property traversal

- [x] 统一 JS responseMapping、reaction `$deps`、RowAction `$row` 的 own-property 读取。
- [x] 禁止通过原型链解析 `toString`、`constructor`、`__proto__`、`prototype`。
- [x] 确认 Python reference 与 JS reference 的缺失结果一致。
- [x] 增加 response/reaction/row 三类路径安全覆盖。

## V257（P1）非法 GET body reference fail-closed

- [x] Form Action GET 在 request construction 前返回统一拒绝。
- [x] RowAction GET/DELETE 非空 body 在 request construction 前返回统一拒绝。
- [x] 定义错误码和路径，并与 L2 保持一致。
- [x] 增加 form GET、row GET body、row DELETE body 负例。

## V258（P1）官方场景 reference metadata/path

- [x] Python scenario reference 读取并校验 `scenarioPath`。
- [x] JS/Python 对官方场景 pageId/protocolVersion metadata 使用同一来源。
- [x] metadata mismatch 在 Python runner 中 fail-closed。
- [x] 重新运行 JS/Python scenario runners。

## V259（P1）JSON number 1/1.0 reaction

- [x] 定义语言无关 JSON number 比较语义。
- [x] 定义 reaction commit equality 和是否触发下一轮的数字规则。
- [x] 修正 JS/Python reference 一致性。
- [x] 增加 `1`、`1.0`、指数形式、Unicode code point 排序和跨类型比较覆盖。

## V260（P1）Reaction 初始求值与 true→false 复位

- [x] 定义 mount 时强制初始 snapshot/evaluate。
- [x] 定义每个状态键的基线值。
- [x] 定义无 otherwise 时条件变 false 的复位行为。
- [x] 定义多 reaction 对同一状态键的复位/覆盖顺序。
- [x] 增加初始求值、true→false 和多轮状态转换 fixture。

## V261（P1）表达式类型和 coercion

- [x] 定义 `==`、`!=`、`>`、`>=`、`<`、`<=` 和 `contains` 的类型矩阵。
- [x] 定义 null、undefined、缺失、数字、布尔和字符串比较。
- [x] 禁止直接依赖宿主语言默认 coercion。
- [x] 同步表达式规范、Renderer 指南、JS/Python reference 和 fixtures。

## V262（P2）Reaction 下一轮与 loop limit

- [x] 对齐 Renderer 文档、ADR-0006 和 reference 的下一轮触发条件。
- [x] 定义默认最大轮次 10 和 off-by-one 行为。
- [x] 定义第 10 轮仍需继续时返回 `REACTION_LOOP_LIMIT`。
- [x] 增加无依赖写入、依赖写入和循环超限 fixture。

## V263（P2）Core Registry 与 Host Extension

- [x] 明确 `Renderer.register` 仅为宿主内部 API。
- [x] extension manifest 的 namespace/version/Schema/capability 明确为未来协议设计，不属于 v2.0 核心。
- [x] 未安装扩展时标准入口拒绝未知 type。
- [x] 核心 Registry 与 Host Extension 的边界已写入 Renderer 规范和 ADR-0019。

## V264（P2）dependencies/visibleField 结构约束

- [x] Schema 对 dependencies 增加 `uniqueItems`。
- [x] 拒绝 `$deps.`/`$row.` 前缀和非法 dependency path。
- [x] `visibleField` 限制为合法 row path，拒绝表达式拼接。
- [x] L3a 对已声明 form context 的 `$deps` 完成符号根解析。
- [x] 增加非法 visibleField 和未知 dependency 的静态校验覆盖。

## V265（P2）默认值、baseURL 和必填 props fallback

- [x] 定义 schema default 由 Renderer 执行时物化而非 Schema validator 隐式改写。
- [x] 定义请求型 Renderer 缺少 baseURL 返回 `MISSING_BASE_URL`。
- [x] 缺少必填 props 或扩展 type 未安装时统一 invalid-component/unknown-component。
- [x] 补充 DataRef method、upload fieldName/multiple 等默认值说明。

## V266（P2）组件边界语义

- [x] 定义 searchable select 的 keyword 冲突、优先级、清空和 tombstone。
- [x] 定义 upload 空文件、ASCII whitespace 和多文件 idempotency key 派生边界。
- [x] 定义 currency/datetime 为语义格式且非法输入返回 `COMPONENT_DATA_TYPE_MISMATCH`；双端 component-format fixture 已覆盖。
- [x] 增加 columns/span/pageSize 等正整数/范围约束。
- [x] 变更已同步组件 DSL、Schema、L2、reference、fixtures 和 CHANGELOG。

## 完整 Admin 生命周期（后续范围，不计入本轮缺陷）

- [x] 页面级 ActionTrigger/Toolbar 明确为后续协议设计（ADR-0019）。
- [x] 标准行级详情/编辑导航明确为后续协议设计（ADR-0019）。
- [x] 编辑记录加载、initialValues 和表单回填明确为后续协议设计（ADR-0019）。
- [x] recordView/details 组件明确为后续协议设计（ADR-0019）。
- [x] 批量操作和完整权限继承明确为后续协议设计（ADR-0019）。

## 协议门禁

- [x] 修复完成后运行 `npm run validate:conformance`：12 套 versioned suite 通过。
- [x] 修复完成后运行 `npm run test:conformance:all`：27 个 JS/Python 入口通过。
- [x] 修复完成后运行 `npm run validate:scenarios`：6 个官方场景通过 L0-L4。
- [x] 修复完成后运行 `npm run release:check`：128 个 case、13 个 fixture 文件及 digest 通过。
- [x] 修复完成后运行 `npm run check:links`；归档移动后再次复跑。
- [x] 本轮未修改 MCP 产品实现，但修改 L2/L3a 与 MCP 测试数据；`npm --prefix mcp test` 123 项通过。
- [x] protocol content digest 为 `sha256:76a36c305f3c1457fd22ed48bb7da6e99359375752c44ad482d81251ee1ffc9e`；fixture digest 为 `sha256:5a5095799f2d4af10df1ef89c56b9aa2969af1c250efae8a6bb853c99b34cb7e`。
- [x] `npm run verify:protocol-artifact` 确认 60 文件制品可复现。

## 归档判定

- [x] V242–V266 均有规范、Schema/L2、reference 和 fixture 证据，或明确记录为后续范围。
- [x] 当前审计范围内 P0/P1/P2 均已关闭；完整 Admin 生命周期按 ADR-0019 明确为 v2.0 后续范围。
- [x] checklist 无裸 `[ ]`；协议条目和复核发现均已闭合。
- [x] 已同步 `docs/audit/README.md` 与 `docs/audit/archived/README.md`。
- [x] review/checklist frontmatter 已改为 `archived`，并在同一变更中移入 `docs/audit/archived/`。
