---
status: checklist
date: 2026-07-09
based_on: 0027-2026-07-09-review.md
---

# 审计 0027 跟踪清单

## V45（🔴）`grid` 直接子节点通用 `span` 约定与组件 DSL 冲突

| 字段 | 值 |
|---|---|
| **位置** | `docs/03-component-registry.md` 通用约定；`docs/schemas/component-registry.json`；`scripts/validate-l2-components.js` |
| **问题** | 正文声明任意 `grid` 直接子节点可声明 `props.span`，但部分组件 DSL 未声明 `span`，L2 会拒绝 |
| **操作** | 统一 `span` 的协议口径，并同步正文、DSL 与验证样例 |
| **文件** | `docs/03-component-registry.md`；`docs/schemas/component-registry.json`；必要时 `scripts/validate-l2-components.js` |

- [x] V45a 决定最终口径：保留 B4 的“`grid` 直接子节点通用 `span`”能力。处理：不收窄正文能力。
- [x] V45b 按最终口径更新 `docs/03-component-registry.md` 的相关组件字段表。修改：为 `grid`、`tabs`、`form` 与字段类组件补充 `span` 行。
- [x] V45c 按最终口径更新 `docs/schemas/component-registry.json`。修改：为缺失组件补充 `span` 字段声明，保持 L2 与正文一致。
- [x] V45d 用最小样例验证：`grid.children[]` 中的 `form.props.span` 与 `input.props.span` 均已通过 L2。

---

## V46（🟡）`08-renderer-spec.md` 的 `$deps` 数据依赖示例不满足当前 `form` 组件契约

| 字段 | 值 |
|---|---|
| **位置** | `docs/08-renderer-spec.md` §2.3；`docs/schemas/component-registry.json` `form.props.allOf` |
| **问题** | 示例使用 `body.type: form` 但缺少默认模式必填的 `submitAction`，也没有声明 `mode: search` / `targetTable` |
| **操作** | 将示例改写为符合当前组件契约的可验证结构 |
| **文件** | `docs/08-renderer-spec.md` |

- [x] V46a 选择示例修复方式：补齐默认表单 `submitAction`，并为内层表格补齐当前契约必填 props。
- [x] V46b 更新 `docs/08-renderer-spec.md` §2.3 的 YAML 示例。修改：新增 `form.props.submitAction`，并补齐 `table.props.rowKey`、`pagination`、`columns`。
- [x] V46c 抽取等价最小页面运行 L2，确认不再报 `body.props.submitAction` 缺失。验证：`validate-l2-components.js` 返回 `violations: []`。

---

## V47（🔴）`select.optionsSource` 响应体格式在 `04-datasource-contract.md` 内部自相矛盾

| 字段 | 值 |
|---|---|
| **位置** | `docs/04-datasource-contract.md` §4.1 与 §9；`docs/decisions/0005-response-mapping.md` |
| **问题** | §4.1 将 `select` 远程数据源归入 `{ list, total }` 列表类接口，但 §9 与 ADR-0005 明确 `select.optionsSource` 默认返回裸数组 |
| **操作** | 将 `select.optionsSource` 从 `DataRef` 列表响应契约中拆清，统一远程选项响应格式 |
| **文件** | `docs/04-datasource-contract.md`；必要时同步 ADR 说明 |

- [x] V47a 修改 `04-datasource-contract.md` §4.1 标题和说明，移除“配合 `select` 远程数据源”的包裹列表暗示。
- [x] V47b 在 `04-datasource-contract.md` §4.1 和 §9 明确：`select.optionsSource` 是组件私有远程选项契约，默认响应体为裸数组，不复用 `data.responseMapping`。
- [x] V47c 核对 ADR-0005 中 `select.optionsSource` 的说明，确认与修复后的 `04-datasource-contract.md` 一致。

---

## V48（🟡）`chart` 与 `responseMapping.list` 的适用口径在 ADR、校验文档和 L2 实现之间不一致

| 字段 | 值 |
|---|---|
| **位置** | `docs/decisions/0005-response-mapping.md`；`docs/06-validation.md`；`scripts/validate-l2-components.js` |
| **问题** | ADR/校验文档暗示 `chart` 可作为 `responseMapping.list` 消费者，但 L2 当前明确只把 `table` 视为列表类接口 |
| **操作** | 选择并同步 `chart` 的 `responseMapping.list` 支持边界 |
| **文件** | `docs/decisions/0005-response-mapping.md`；`docs/06-validation.md`；`docs/04-datasource-contract.md` / `docs/08-renderer-spec.md`；必要时 `scripts/validate-l2-components.js` |

- [x] V48a 决定 `chart` 支持通过 `data.responseMapping.list` 从包裹响应中取数组，延续 ADR-0005 D3 口径。
- [x] V48b 在 `04-datasource-contract.md` / `08-renderer-spec.md` / `06-validation.md` 明确 `chart` 的映射语义，并更新 L2 对 `chart` 声明 `responseMapping` 时的 `list` 校验。
- [x] V48c 不适用：本轮选择支持 `chart.responseMapping.list`，无需移除 ADR-0005 中 “需要数组数据的 `chart`” 口径。
- [x] V48d 用最小 `chart.data.responseMapping` 样例验证最终口径：缺少 `list` 的 chart 映射被 L2 拒绝，提供 `list` 的 chart 映射通过。

---

## 汇总

| 编号 | 严重度 | 状态 |
|---|---|---|
| V45 | 🔴 | [x] 已修复并验证 |
| V46 | 🟡 | [x] 已修复并验证 |
| V47 | 🔴 | [x] 已修复 |
| V48 | 🟡 | [x] 已修复并验证 |