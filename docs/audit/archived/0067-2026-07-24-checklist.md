---
status: archived
date: 2026-07-24
based_on: 0067-2026-07-24-review.md
---

# 审计 0067 跟踪 Checklist

> 本 checklist 跟踪 **v2.4 recordView 交付原子一致性**。生产 Renderer、Host、业务鉴权不属于本轮范围。  
> 不借本清单修改 ADR-0024 已接受语义；语义权威仍以 accepted ADR、核心规范、Schema 与 versioned fixtures 为准。  
> 创建时 **未** 跑全量门禁；关闭时已补跑（见完成门禁）。

## 基线

- [x] 审计编号 `0067` 延续已归档 `0066`（活跃区无活跃审计；归档区最大号 0066 → 下一号 0067）。
- [x] 问题编号 `V300`–`V308` 延续 `V299`。
- [x] 确认本轮 **2×P0 + 2×P1 + 4×P2 + 1×P3 簇**；机器双重门控与 key⊆mapping 有 L2 探针，非「未实现」类 P0。
- [x] 确认 0066 已归档且无并行活跃审计。
- [x] 记录复审基线：工作树 `2.4.0` / `2.4`；ADR-0024 accepted；versioned fixtures 静态 213（响应后 214）。
- [x] 将 L2/L3a 探针结论与「全量未跑」写入 review 基线表。

## V300（P0）权威层当前版本仍为 2.3 — 已关闭

- [x] `docs/01-node-protocol.md` 中「当前协议/制品使用 `"2.3"`」改为与 **`2.4` / `2.4.0`** 一致（含 YAML 示意注释与 `meta.protocolVersion` 说明段）。
- [x] `docs/schemas/page.schema.json` `meta.protocolVersion.description` 中「当前协议制品为 `"2.3"`」改为 **`"2.4"`**，并覆盖 2.4 / `recordView` 纪律。
- [x] 全文检索核心权威层无残留「当前…2.3」作为**现行**制品锚点（历史迁移/CHANGELOG/审计正文除外）。
- [x] 关闭说明：`01` L28/L47 + `page.schema.json` protocolVersion.description → 现行锚点 `"2.4"`。

## V301（P0）`01` 未写 recordView 版本下限 — 已关闭

- [x] 在 `01` `meta.protocolVersion` 纪律中增加：使用 `type: recordView` 时必须 `meta.protocolVersion >= "2.4"`，并声明 `record.view.load`。
- [x] 与 `00` / `06` / ADR-0024 D1 双重门控表述交叉一致（版本 + capability 两端均出现在权威链）。
- [x] 关闭说明：`01` protocolVersion 段补 recordView 下限 + capability；`06` v2.4 注记交叉引用 `01`。

## V302（P1）registry `required` 键冲突 — 已关闭

- [x] 修复 `component-registry.json` 中字段级布尔 `required` 与对象级 `required: [...]` 键冲突（`recordView.props.recordSource` + 同构 `form.props.recordSource`）。
- [x] 确认 `JSON.parse` 后字段必填语义可被 L2 `validateProps` / MCP `deriveRequiredProps` 读取（布尔 `true`/`false`）。
- [x] 回归：缺 `recordSource` 的 `recordView` 仍 fail-closed；合法页仍通过（MCP + L2 探针）。
- [x] 关闭说明：删除覆盖布尔的 `required` 数组；嵌套 method/url/responseMapping 仍由各自 `required: true` 表达；MCP components 测试断言 `recordSource.required === true`。

## V303（P1）双重门控无 L2 负例向量 — 已关闭

- [x] 选 (A)：增加可自动执行的 L2 负例并纳入 CI。
  - `docs/05-scenarios/_samples/audit-0067-recordview-on-23-bad.yaml`（`2.3` + recordView → `PROTOCOL_VERSION_TOO_LOW`）
  - `docs/05-scenarios/_samples/audit-0067-recordview-missing-capability-bad.yaml`（`2.4` 无 `record.view.load`）
  - MCP `validate_content`：`recordView on protocolVersion 2.3` / `without record.view.load`
- [x] suite/脚本名与 case id：MCP tests + `_samples/audit-0067-*`；L2 脚本探针通过。
- [x] 关闭说明：方案 A 落地；`06` 指向 samples 与 MCP 测试。

## V304（P2）`02` §13 recordView 路由 undefined — 已关闭

- [x] `docs/02-reaction-expression.md` §13 将「route 键 undefined → 拒绝构造加载请求」扩到 `recordView.props.recordSource`（与 form 同构）。
- [x] 关闭说明：§13 对称 form + recordView；交叉 ADR-0021/0024。

## V305（P2）`08` recordView 执行节 — 已关闭

- [x] 在 `docs/08-renderer-spec.md` 增加 §2.5.1 `form.record.load` / `record.view.load` 执行摘要（挂载 GET、显式 mapping、缺路径 null、节点错误态、与 DataRef 边界）。
- [x] 关闭说明：`08` §2.5.1。

## V306（P2）key ⊆ mapping 仅 L2 — 已关闭

- [x] 选 (A)：在 `06`/`03` 明确「跨字段 ⊆ 仅 L2，L0/L1 不足」；并加 L2 负例（与 V303 合并）。
  - sample：`audit-0067-recordview-key-not-in-mapping-bad.yaml`
  - MCP：`fields key outside responseMapping`
- [x] 关闭说明：文档 + CI 负例。

## V307（P2）场景阅读顺序 — 已关闭

- [x] `docs/05-scenarios/README.md` 阅读顺序补入 `admin-list-detail-lifecycle`（edit 之后、batch 之前）。
- [x] 关闭说明：阅读顺序现为 1–10，含 detail 生命周期。

## V308（P3）信息簇 — 已关闭

- [x] states empty：`03` recordView 摘要改为 loading/error/**empty 可选**，与 ADR D4 对齐。
- [x] response-mapping：增加 `record-view-empty-mapping-rejected`（对称 form 空 mapping）；versioned count 213→214。
- [x] `protocol-manifest` informative 列入 `order-detail-lifecycle.yaml`。
- [x] `06` `last_updated` 更新为 2026-07-24（与 2.4 内容一致）；相关核心规范 frontmatter 同步。
- [x] 各子项已关闭。

## 文档与入口（关闭本审计时）

- [x] `docs/audit/README.md` 移除 0067 活跃记录。
- [x] review + checklist 移入 `docs/audit/archived/`。
- [x] `docs/audit/archived/README.md` 顶部登记 0067 摘要。
- [x] 未将审计路径写入 `protocol-manifest.json` 发布清单（保持 excluded）。

## 完成门禁（关闭本审计时）

- [x] V300–V301 全部勾选关闭。
- [x] V302–V303 全部勾选关闭。
- [x] V304–V308 全部勾选关闭。
- [x] 关闭时执行并记录：
  - [x] `npm run release:check`（`2.4.0` / `2.4`，versionedCaseCount **214**，fixtureDigest `sha256:474a3c091bbac3d1f0a9737e27bfa790d2cf25846706d67afdf3fc45fe8e754e`，artifactDigest `sha256:ea1f0b6fa379e8c161eea974170f67505ea03560c5b5d2b95956d92a2563294b`，83 文件）
  - [x] `npm run verify:protocol-artifact`（reproducible；同上 digests）
  - [x] `npm run test:conformance:all`（29 入口全绿；response 19、version 29、scenarios 12 等）
  - [x] MCP `validate-content` + `components` 测试 118 passed（含 V302/V303/V306 新负例）
- [x] 无新增未编号的 recordView / 2.4 发布阻断项。

## 决议记录

| 日期 | 决议 |
|---|---|
| 2026-07-24 | **新建 0067**。只读审计 v2.4 recordView 原子一致性。结论：机器层主路径已落地；权威 `01`/`page.schema` 仍锚 2.3 且 `01` 缺 recordView 版本下限 → **P0 阻断**。开放 V300–V308。全量测试未跑。 |
| 2026-07-24 | **响应 V300–V301**：`01` 与 `page.schema` 现行制品锚点改为 `"2.4"`；`01` 补 recordView ≥2.4 + `record.view.load` 双重门控文档。**关闭 V300/V301。** |
| 2026-07-24 | **响应 V302**：registry 去掉覆盖字段级布尔的 `required` 数组；`recordView.recordSource.required === true` 可被 L2/MCP 读取；缺 recordSource 仍 fail-closed。**关闭 V302。** |
| 2026-07-24 | **响应 V303 方案 A**：`_samples/audit-0067-recordview-on-23-bad.yaml` / `…-missing-capability-bad.yaml` + MCP validate_content 负例纳入 CI。**关闭 V303。** |
| 2026-07-24 | **响应 V304–V305**：`02` §13 对称 recordView；`08` §2.5.1 执行摘要。**关闭 V304/V305。** |
| 2026-07-24 | **响应 V306 方案 A**：`03`/`06` 明确 ⊆ 仅 L2；负例 sample + MCP 测试。**关闭 V306。** |
| 2026-07-24 | **响应 V307–V308**：阅读顺序补 detail；states empty 对齐 ADR；空 mapping 对称 case；manifest 列 order-detail-lifecycle；`06` last_updated。**关闭 V307/V308。** |
| 2026-07-24 | **归档 0067**：全量门禁绿；review + checklist 移入 archived；活跃区清空；审计路径仍 excluded。 |
