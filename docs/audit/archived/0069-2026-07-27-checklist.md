---
status: archived
date: 2026-07-27
based_on: 0069-2026-07-27-review.md
---

# 审计 0069 跟踪 Checklist

> 本 checklist 跟踪 **ADR-0025 / 0026 / 0027 接受前** 必须闭合的协议层问题（V313–V330）。  
> 不包含生产 Renderer / Host / 业务鉴权实现；不因「产品常见」新增面包屑/badge/登录协议。  
> 接受三 ADR 后的 Schema / M0–M1 / L2 / fixtures 落地走各 ADR 交付清单，可另开 v2.5 发布审计。

## 基线

- [x] 审计编号 `0069` 延续已归档 `0068`（活跃区原无活跃审计；归档区最大号 0068 → 下一号 0069）。
- [x] 问题编号 `V313`–`V330` 延续全局最大 `V312`。
- [x] 确认本轮 **6×P1（阻断 accepted）+ 6×P2 + 6×P3**；**无**会静默破坏 v2.4 合法页的 P0。
- [x] 确认 0025 / 0026 / 0027 均为 `proposed`，未进入 `protocol-manifest.json` / 2.4 制品。
- [x] 确认 0068 已归档且创建本轮前无并行活跃审计。
- [x] 对照材料已扫：章程、0003、0009、0011、0013、0021、0022、0023、`03`/`04`/`07`/`08`/`11`。

## 维护者裁决（关闭 P1 前须先勾选）

### Q1 — V313 D4a 消歧键序

- [x] 选定方案（**须先勾选其一**）：
  - **(A) 字面量段数降序 → 字符长度降序 → 声明序（推荐）** ← **已选定（2026-07-27）**；或
  - (B) 保持纯字符长度，并 M1 静态拒绝可重叠模板对；或
  - (C) 其他（写明）：________________

### Q2 — V314 navigate 命中注册表

- [x] 选定方案（**须先勾选其一**）：
  - **(A) 命中则必须按 D4a 注入 `$context.route`；未命中仍允许 navigate（推荐）** ← **已选定（2026-07-27）**；或
  - (B) 维持「宜」+ 仅正例 fixture；或
  - (C) 其他（写明）：________________

### Q3 — V317 `TABLE_SORT_FIELD_UNKNOWN`

- [x] 选定方案（**须先勾选其一**）：
  - **(A) 保留稳定码，收紧触发集并补 fixture（推荐）** ← **已选定（2026-07-27）**；或
  - (B) 删除稳定码，仅靠 L2；或
  - (C) 其他（写明）：________________

---

## 阻断 accepted（P1）

### V313（🔴）D4a 字符长度最长优先

- [x] Q1 已选定。
- [x] ADR-0025 D4a 步骤 4 已按选定方案改写规范性消歧键。
- [x] informative 示例与算法一致（若选 A：明确 `/orders/new` 胜于 `/orders/{id}`）。
- [x] 交付/conformance 意图列出该对正例与「两参数模板仅声明序」向量。
- [x] 开放问题节：若曾写「无」，已与本裁决一致（见 V330）。
- [x] 关闭说明：2026-07-27 方案 A 写入 0025 D4a；开放问题表已更新。

### V314（🔴）navigate 命中必须注入

- [x] Q2 已选定。
- [x] ADR-0025 D4 navigate 接缝已改写（若选 A：命中 → **必须** 填充 `$context.route`）。
- [x] 明确未命中注册表时不拒绝 navigate（v2.x 兼容保留）。
- [x] conformance 意图：命中正例为门禁；未注入不得再仅标「宜」。
- [x] 关闭说明：2026-07-27 方案 A 写入 0025 D4 / D5。

### V315（🔴）baseURL / 应用路由根术语

- [x] ADR-0025 D1b 增加规范性术语句：`RendererConfig.baseURL` 仅 HTTP；`route` / navigate url / `$context.route.path` 相对**应用路由根**。
- [x] 交付清单显式要求 accept 时同步修订：
  - [x] `docs/07-actions-contract.md` navigate「baseURL 下」措辞（**accept 时落地**，已写入 0025 交付清单）
  - [x] ADR-0021 D2 path「baseURL 之后」措辞（**accept 时落地**，已写入 0025 交付清单）
  - [x] 如需要：`docs/08` 交叉引用避免混称（交付清单覆盖）
- [x] 删除或改写 D1b 中易复述旧口径的含混句。
- [x] 关闭说明：2026-07-27 ADR 层闭合；07/0021 正文修订列为 accept 原子项（非本轮实现落地）。

### V316（🟡）path 段解码算法

- [x] ADR-0025 D4a 钉死：按段 RFC 3986 percent-decode；`+` 字面；非法 `%` → 候选失败；无 NFC/NFKC。
- [x] conformance 意图至少含：编码段、非法 `%`、字面 `+`。
- [x] 关闭说明：2026-07-27 写入 0025 D4a。

### V317（🟡）`TABLE_SORT_FIELD_UNKNOWN`

- [x] Q3 已选定。
- [x] 若选 A：0027 D4 触发集收紧为「非 null sortKey ∉ 可排序集合 → 禁止发请求」。
- [x] 若选 B：正文删除该稳定码及交付引用。→ **N/A（未选 B）**
- [x] 错误码权威登记位置写入交付清单（与 V318 一并）。
- [x] 关闭说明：2026-07-27 方案 A 写入 0027 D4 + 交付。

### V318（🟡）原子交付：`08` capability + 错误码投影

- [x] ADR-0025 交付清单增加：`08` §3.4 `app.manifest`；清单级稳定错误码表；accept 后 manifest authority。
- [x] ADR-0026 交付清单增加：`08` §3.4 `app.navigation`。
- [x] ADR-0027 交付清单增加：`TABLE_SORT_FIELD_UNKNOWN`（若保留）登记处。
- [x] 三 ADR 均声明：proposed 阶段不入 `protocol-manifest.json`。
- [x] 关闭说明：2026-07-27 三 ADR 交付节已补；实际 `08`/schema 文件仍待 accept 后落地。

---

## 建议接受前关闭（P2）

### V319 匹配输入 `P` 前置条件

- [x] 0025 写明：`P` 以 `/` 开头、无 query/fragment、已剥 basename、非空（根为 `/`）。
- [x] home 落地 `$context.route.path` **等于**目标 `route` 字面量。
- [x] 关闭说明：2026-07-27 写入 0025 D3a / D4a。

### V320 `MANIFEST_ROUTE_AMBIGUOUS`

- [x] 选定：删除稳定码 / 标 non-normative diagnostic / 其他：________ ← **删除稳定互操作码**
- [x] 0025 D6 与交付错误码列表一致。
- [x] 关闭说明：2026-07-27 自 D6 移除；非目标节明示不做。

### V321 空 `pages` + 导航

- [x] 选定：允许壳（仅 `url` 项）/ M1 拒绝空 pages 且有 navigation / 其他：________ ← **允许壳**
- [x] M1 规则与 0025/0026 正文一致。
- [x] 关闭说明：2026-07-27 写入 0025 D3a/D4 与 0026 D3。

### V322 菜单可见性公式

- [x] 0026 D4 独立写出缺省 true 的 AND 公式（无 reactions）。
- [x] 不再仅依赖「沿 §3.10」而不解释菜单差异。
- [x] 关闭说明：2026-07-27 写入 0026 D4。

### V323 应用壳 `$context` 生命周期

- [x] 0026（或 0025）写明：菜单过滤用应用实例 boot 快照；与清单同生命周期；更新须重建实例。
- [x] 关闭说明：2026-07-27 写入 0025 D5 与 0026 D4。

### V324 0011 ↔ 0027 原子性

- [x] 门禁句写入 0027 交付或 v2.5 发布目标：accept 同 tag；拒绝 0027 则回滚 0011 D3 声明面句子。
- [x] 关闭说明：2026-07-27 写入 0027 交付 + 0011 D3 注记。

---

## 文档卫生（P3，可跟版）

### V325 well-known 前缀

- [x] 可选：0025 D1 informative 补「多前缀 API 宜显式 manifest URL」。
- [x] 关闭说明：2026-07-27 已写入 D1。

### V326 defaultSort 首次点击

- [x] 0027 补例句：初始 `field:desc` 时首次点击该列 → `null`。
- [x] 关闭说明：2026-07-27 写入 0027 D4。

### V327 M 系列跳号

- [x] 0025 校验层级表脚注「有意无 M2」。
- [x] 关闭说明：2026-07-27 已写入。

### V328 0026 引用 0023

- [x] 改为沿 ADR-0003 view；注明 0023 不定义菜单键。
- [x] 关闭说明：2026-07-27 写入 0026 D3/D5。

### V329 0027 路由 query 非目标措辞

- [x] 半句区分「≠ 0011 表格 API `sort` query」。
- [x] 关闭说明：2026-07-27 写入 0027 D5。

### V330 「开放问题无」与重开裁决

- [x] V313（及若有）裁决写入正文后，开放问题节与「已裁决表」一致。
- [x] 关闭说明：2026-07-27 三 ADR 开放问题表已含 0069 裁决。

---

## Conformance 意图清单（接受后落地；本审计只跟踪「条款已可测」）

> 下列在对应 P1/P2 关闭后，应能无歧义写入 fixtures。勾选表示「条款已足够向量化」，**不**要求本审计内实现 suite。  
> **归档说明：** 下列均已标「条款可测」；**fixture 实现**已推迟至 ADR accept / v2.5 发布审计。

### suite `app-manifest`（0025）

- [x] 清单版本精确协商正负例（`UNSUPPORTED_PROTOCOL_VERSION` / `MISSING_PROTOCOL_VERSION`）— **条款可测**；fixture 推迟至 accept
- [x] 清单 2.5 + 页 2.1/2.4 解耦 — **条款可测**；fixture 推迟至 accept
- [x] 缺 `app.manifest` / 有 `navigation` 无 `app.navigation` → M1 — **条款可测**；fixture 推迟至 accept
- [x] `homePageRef` 未知 id / 含参 route / 深链接优先 / 空 pages 禁 home — **条款可测**；fixture 推迟至 accept
- [x] D4a：字面量、命名参数、V313 消歧对、声明序并列、尾 `/` 不归一 — **条款可测**；fixture 推迟至 accept
- [x] path 解码向量（V316）— **条款可测**；fixture 推迟至 accept
- [x] `schemaUrl` 基址拼接与占位 ⊆ route；未解析 → `MISSING_PATH_BINDING` — **条款可测**；fixture 推迟至 accept
- [x] `pageId` 与 schema `meta.pageId` 不匹配 — **条款可测**；fixture 推迟至 accept
- [x] logo 相对 path / `https:` / 拒 `http:` `data:` — **条款可测**；fixture 推迟至 accept
- [x] manifest 加载失败 fail-closed — **条款可测**；fixture 推迟至 accept
- [x] navigate 命中注册表注入（V314）/ 未命中不拒绝 — **条款可测**；fixture 推迟至 accept

### suite `app-navigation`（0026）

- [x] 三槽位；未知槽位拒绝 — **条款可测**；fixture 推迟至 accept
- [x] `pageRef` 完整性；`pageRef`/`url` 互斥；组不嵌组 — **条款可测**；fixture 推迟至 accept
- [x] `permissions.view` / `visibleWhen` 过滤与缺省 true（V322）— **条款可测**；fixture 推迟至 accept
- [x] 空组剪枝；数组序；icon 降级 — **条款可测**；fixture 推迟至 accept
- [x] 高亮：`pageRef` 单项 D4a；`url` 全等；query 不参与；多 active 允许 — **条款可测**；fixture 推迟至 accept

### suite `table-sort` 或扩 `search-table`（0027）

- [x] 无 `table.sort` / 无 `sortable`：无协议排序 UI 义务（v2.4 兼容）— **条款可测**；fixture 推迟至 accept
- [x] 字段集无 capability 或 version&lt;2.5 → L2 拒绝 — **条款可测**；fixture 推迟至 accept
- [x] 三态 asc→desc→清；`sortField` 映射；保留名拒绝；sortKey 唯一 — **条款可测**；fixture 推迟至 accept
- [x] `defaultSort` 初始请求；搜索保留 sort；清除后不恢复 default — **条款可测**；fixture 推迟至 accept
- [x] 仅 `pagination.mode: server`；client/none → L2 — **条款可测**；fixture 推迟至 accept
- [x] 未知 sortKey（V317）— **条款可测**；fixture 推迟至 accept
- [x] 与 `table.selection`：sort 变化清空选中 — **条款可测**；fixture 推迟至 accept

---

## 文档与轨道

- [x] `docs/audit/README.md` 登记 0069 为当前活跃审计（创建时）。
- [x] 三 ADR 吸收本轮 P1（及已选关闭的 P2）后，状态仍为 `proposed` 直至单独 accept 决议。
- [x] 不将审计路径写入 `protocol-manifest.json`。
- [x] `11-next-admin-lifecycle-goals.md` 不强制收录应用级轨道（预期）；若 v2.5 另有 release-goals 文档再链 ADR。
- [x] 关闭本审计时：review + checklist 移入 `docs/audit/archived/`，更新 `audit/README.md` 与 `archived/README.md`。
- [x] 归档 checklist 无裸 `[ ]`（conformance fixture 实现已显式推迟至 accept；无未说明的开放勾选）。

## 完成门禁（关闭本审计时）

- [x] V313–V318 全部在 ADR 正文关闭（或等价降级写清），**不得**据此直接把 ADR 标为 `accepted`。
- [x] V319–V324 已关闭或明确推迟至 v2.5 发布门禁且不改变「可接受性」结论。
- [x] V325–V330 已关闭或显式推迟。
- [x] Q1–Q3 均有选定记录。
- [x] Conformance 意图清单中，与已关闭 P1 相关的行已标「条款可测」。
- [x] 0025/0026/0027 仍为 proposed（除非维护者另作 accept 并另开落地审计）。
- [x] 用户确认无误后执行归档。

## 决议记录

| 日期 | 决议 |
|---|---|
| 2026-07-27 | **新建 0069**。交叉审计 0025/0026/0027：有条件接受；开放 V313–V330。阻断 accepted：V313–V318。维护者裁决点 Q1–Q3 待勾选。 |
| 2026-07-27 | **接受推荐并改 ADR**。Q1=A、Q2=A、Q3=A。V313–V330 提案层正文已写回 0025/0026/0027（及 0011 D3 原子性注记）。**仍为 proposed**；`08`/schema/fixtures/07·0021 术语修订待 accept 原子落地。本审计可在用户确认后归档。 |
| 2026-07-27 | **归档 0069**。用户确认归档；V313–V330 提案层全部关闭；fixture/`08` 落地推迟至 accept；review + checklist 移入 `archived/`；活跃区清空。 |
