---
status: checklist
date: 2026-07-10
based_on: 0036-2026-07-10-review.md
---

# 审计 0036 跟踪清单

## V91（🟡）将 `responseMapping` 生效映射（本地优先 / 继承 datasources）写回 04 / 08 正文

| 字段 | 值 |
|---|---|
| **位置** | `docs/04-datasource-contract.md` §4.1.1；`docs/08-renderer-spec.md` §2.5 |
| **问题** | L2/`06`/V88 已规定生效映射解析顺序，但数据源契约与 Renderer 规范仍只写节点本地映射 |
| **操作** | 在 04/08 写明：本地 `data.responseMapping` 优先，否则 `source: ref` 继承 `datasources[ref].responseMapping`；无映射沿用默认字段名 |
| **文件** | `docs/04-datasource-contract.md`；`docs/08-renderer-spec.md` |

- [x] V91a 更新 `docs/04-datasource-contract.md` §4.1.1：补充生效映射解析顺序与本地覆盖语义。
- [x] V91b 更新 `docs/08-renderer-spec.md` §2.5：Renderer 应用生效映射（含继承），且映射不得进入请求参数。
- [x] V91c 交叉引用 `06-validation.md` v0.2.8 / ADR-0005（如需要）。
- [x] V91d 同步触达文档 `last_updated`（若修改日期需更新）。
- [x] V91e 验证：人工对照 L2 `getEffectiveResponseMapping()` 与 04/08 正文口径一致。
- 完成记录：2026-07-10
- 修改摘要：04/08 写明生效映射三步解析；验证时修正 04 示例中“字段级继承 total”误述为“本地映射整体覆盖”

---

## V92（🟡）实现禁止 `params.responseMapping` 的静态校验

| 字段 | 值 |
|---|---|
| **位置** | ADR-0005 D1；`docs/01-node-protocol.md` §3.3；`docs/schemas/node.schema.json`；`scripts/*`；MCP 测试 |
| **问题** | 协议禁止将 `responseMapping` 放入 `params`，但 Schema/L2/L3a/L4 均不拒绝 |
| **操作** | L2（推荐）拒绝 `data.params.responseMapping` 与 `datasources.*.params.responseMapping`；可选 L1 Schema 加固；同步 06 与 MCP 反例 |
| **文件** | `scripts/validate-l2-components.js`；可选 `docs/schemas/node.schema.json`；`docs/06-validation.md`；`mcp/tests/validate-content.test.ts`；`mcp/tests/test-utils.ts` |

- [x] V92a 在 L2 增加对 `node.data.params.responseMapping` 的拒绝规则（明确 path/message）。
- [x] V92b 在 L2 同步扫描页面级 `doc.datasources[*].params.responseMapping` 并拒绝。
- [x] V92c （可选但建议）在 `node.schema.json` 的 `params` 增加 `not: { required: ["responseMapping"] }` 或等价约束，使 L1 也能拦截。
- [x] V92d 更新 `docs/06-validation.md`：L2 规则表 / 变更说明写明禁止 `params.responseMapping`。
- [x] V92e 新增 MCP `validate_content` 反例：节点级与 `datasources` 级 `params.responseMapping` 均应失败。
- [x] V92f 验证：
  - 临时 YAML：`body.data.params.responseMapping` → L2 拒绝（exit 1）
  - 临时 YAML：`datasources.*.params.responseMapping` → L2 拒绝（exit 1）
  - 合法 `data.responseMapping` + 同级 `params` 字面量 → L2 通过
  - `npm --prefix mcp run build` → 通过
  - `npm --prefix mcp test` → 31/31 通过
- 完成记录：2026-07-10
- 修改摘要：L2 `validateParamsResponseMappingBan()` + Schema `params.not` + 06 v0.2.8 变更段 + MCP 三组反例

---

## V93（🟡）补齐 Renderer 对 `source: ref` / `datasources` 的请求协调规格

| 字段 | 值 |
|---|---|
| **位置** | `docs/08-renderer-spec.md` §2.1–§2.4（及可与 V91 合并的 §2.5） |
| **问题** | 数据加载章节几乎只覆盖 `source: api`；缺少 ref 解析、同 ref 请求协调、错误归属等约定 |
| **操作** | 在 §2 增补 `source: ref` 行为：解析 datasources、加载/错误态等价、同 ref 请求策略、与本地 responseMapping 覆盖的衔接 |
| **文件** | `docs/08-renderer-spec.md`；可选 `docs/04-datasource-contract.md` §1 |

- [x] V93a 在 `08` §2 明确 `source: ref` 解析 `datasources[ref]` 的请求配置（url/method/params 等）。
- [x] V93b 明确加载态 / 错误态与 `source: api` 等价，错误归属消费节点。
- [x] V93c 选定并写死同 `ref` 的默认请求协调策略（共享进行中请求结果）。
- [x] V93d 与 V91 的 §2.5 生效映射描述交叉引用，避免两处口径分叉。
- [x] V93e （可选）增加最小示例或交叉引用 `05-scenarios/grid-dashboard.md`。
- [x] V93f 验证：人工通读 §2.1.1 / §2.5 后，`api` 与 `ref` 两条路径均有明确约定。
- 完成记录：2026-07-10
- 修改摘要：新增 `08` §2.1.1；默认共享同 ref 进行中请求；失败各节点独立错误态

---

## V94（🟢）同步 `smoke-docker.mjs` 默认镜像 tag 至 0.2.8

| 字段 | 值 |
|---|---|
| **位置** | `mcp/tests/smoke-docker.mjs` |
| **问题** | 默认 `schema-ui-mcp:0.2.7`，包与文档已是 0.2.8 |
| **操作** | 默认 tag 改为 `schema-ui-mcp:0.2.8` |
| **文件** | `mcp/tests/smoke-docker.mjs` |

- [x] V94a 将默认镜像改为 `schema-ui-mcp:0.2.8`。
- [x] V94b 验证：默认 tag 与 `mcp/package.json` version `0.2.8`、文档示例一致。
- [x] V94c Docker smoke 未在本环境执行（依赖本地镜像）；默认 tag 已对齐，剩余风险：无本地镜像时需显式 `docker build` 后再跑 smoke。
- 完成记录：2026-07-10

---

## V95（🟢）同步 `06` L2 总览表与 L2 脚本头注释为“生效映射”用语

| 字段 | 值 |
|---|---|
| **位置** | `docs/06-validation.md` §1 L2 行；`scripts/validate-l2-components.js` 文件头 §5 |
| **问题** | 仍写“本地 responseMapping 条件必填”，与实现/变更段的生效映射不一致 |
| **操作** | 改为生效映射（本地优先，否则继承）措辞 |
| **文件** | `docs/06-validation.md`；`scripts/validate-l2-components.js` |

- [x] V95a 更新 `06` L2 表格描述：改为生效映射（本地优先，否则继承），并写入 params 禁令。
- [x] V95b 更新 `validate-l2-components.js` 文件头注释 §5 / §9。
- [x] V95c 验证：与 `getEffectiveResponseMapping()` / v0.2.8 变更段用语一致。
- 完成记录：2026-07-10

---

## 汇总

| 编号 | 严重度 | 主题 | 涉及文件 | 状态 |
|---|---|---|---|---|
| V91 | 🟡 | 生效 responseMapping 写回 04/08 | `04` / `08` | ✅ |
| V92 | 🟡 | 禁止 params.responseMapping 的机器校验 | L2 + L1 Schema / `06` / MCP 测试 | ✅ |
| V93 | 🟡 | Renderer source:ref 请求协调规格 | `08` §2.1.1 | ✅ |
| V94 | 🟢 | smoke-docker 默认 tag 0.2.8 | `mcp/tests/smoke-docker.mjs` | ✅ |
| V95 | 🟢 | L2 文档/注释生效映射措辞 | `06` / L2 脚本头 | ✅ |

## 建议验证命令（修复后）

```bash
# L2 / 临时反例（params.responseMapping 与生效映射回归）
node scripts/validate-l2-components.js ".tmp-audit-check/*.yaml" --json
npm run validate -- ".tmp-audit-check/*.yaml"

# MCP 构建与测试
npm --prefix mcp run build
npm --prefix mcp test

# Docker smoke（需本地已有镜像；可选）
# npm --prefix mcp run smoke:docker -- schema-ui-mcp:0.2.8

# 文档一致性（人工抽查）
# - 04 §4.1.1 / 08 §2.5 含生效映射（本地优先 / 继承）
# - 08 §2 覆盖 source: ref
# - 06 L2 行与脚本头注释为生效映射用语
# - smoke-docker 默认 tag 0.2.8

git diff --check
```