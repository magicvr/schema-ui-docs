---
status: active
date: 2026-07-11
based_on: 0054-2026-07-11-review.md
---

# 审计 0054 跟踪清单

## V213（🟡）L3a 修正 `datasources.*.params` 违规文案标签

- [x] V213a `scanDataParams`：由三类扫描入口传入固定 `paramsLabel`（`optionsSource.params` / `datasources.*.params` / `data.params`），递归时原样保持，避免业务键名影响标签。
  - 文件：`scripts/validate-l3a-expressions.js`
- [x] V213b 文件头注释列出 `data.params` / `optionsSource.params` / `datasources.*.params` 三类扫描面。
  - 文件：`scripts/validate-l3a-expressions.js`
- [x] V213c MCP 反例断言 datasources 违规 message 含 `datasources`（或标签），不仅 `path`/`rule`。
  - 文件：`mcp/tests/validate-content.test.ts`
- [x] V213d 验证：datasources `$deps` / 模板拼接探针 → L3a 失败且 message 标签正确；`npm --prefix mcp test` 相关用例通过。

## V214（🟡）闭合三面 params 文档与 §2 交叉引用

- [x] V214a `02` §10.7 标题/正文纳入页面级 `datasources.*.params`（永远非 form；字面量合法；完整 `$deps.*` → `NON_FORM_DATA_PARAMS`；模板 → `DATA_PARAMS_VARIABLE`）。
  - 文件：`docs/02-reaction-expression.md`
- [x] V214b 附录 A 增补 `datasources.*.params` 行（`$deps` ❌ / `$self` ❌ / `$context` ❌ / `$row` ❌）。
  - 文件：`docs/02-reaction-expression.md`
- [x] V214c `04` §1 / §3.1–§3.2 明确页面级 `datasources.*.params` 与节点级同一整值替换语法，且无 form 上下文不得出现 `$deps.*`。
  - 文件：`docs/04-datasource-contract.md`
- [x] V214d 确认 `02` §2「见附录 A / §10.7」在补齐后覆盖 datasources 声明。
- [x] V214e 按需同步 `last_updated`（`02` / `04` — 均为当日无需改）。

## V215（🟡）对齐 VisibleWhen.dependencies 无前缀路径描述

- [x] V215a `node.schema.json` `VisibleWhen.dependencies.description`：与 reaction/V209 同口径（form 字段名；row 为 `$row.` 后缀完整点路径；可空数组；禁止带 `$deps.`/`$row.` 前缀）。
  - 文件：`docs/schemas/node.schema.json`
- [x] V215b `01` §3.8 YAML 注释与 §3.5 reactions 注释对齐（含表格 `scope: row` 场景提示）。
  - 文件：`docs/01-node-protocol.md`
- [x] V215c Schema JSON 可解析；抽查描述无「仅字段名」歧义。

## V216（🟢）统一 `$parentRow`「静态拒绝」措辞（V211 残留）

- [x] V216a `07` §3.1：`暂不支持` → 静态拒绝口径。
  - 文件：`docs/07-actions-contract.md`
- [x] V216b L2 requestMapping 违规 message：`暂不支持` → `静态拒绝`。
  - 文件：`scripts/validate-l2-components.js`
- [x] V216c L3a 文件头 + `PARENT_ROW_UNSUPPORTED` message 同步。
  - 文件：`scripts/validate-l3a-expressions.js`
- [x] V216d 可选：MCP 测试断言 message 含「静态拒绝」或不再含「暂不支持」。
  - 文件：`mcp/tests/validate-content.test.ts`
- [x] V216e 验证：含 `$parentRow` 的 L2/L3a 探针失败且文案正确。

## V217（🟢）MCP 临时目录清理失败发本地警告

- [x] V217a `validation-runner.ts` finally：`rmSync` 失败时 `console.warn`（或等价），不改变返回结构。
  - 文件：`mcp/src/core/validation-runner.ts`
- [x] V217b 注入临时目录清理器，自动化验证清理失败仅发 `console.warn`，不改变成功结果与 `internalError`。
- [x] V217c `npm --prefix mcp run build` + 相关测试通过。

## 元数据与验证

- [x] M1 `docs/CHANGELOG.md` Unreleased 记录 0054 / V213–V217，并列入审计路径与触达文件。
- [x] M2 更新 `docs/audit/README.md` 当前活跃审计与跟踪项。
- [x] M3 修复完成后最小验证：
  - 文档/脚本：datasources params L3a 探针；可选 `npm run validate:l3a` 相关样例
  - MCP：`npm --prefix mcp run build`；`npm --prefix mcp test`；`npm --prefix mcp run smoke:tools`（若触达工具路径）

## 汇总

| 编号 | 严重度 | 主题 | 状态 |
|---|---|---|---|
| V213 | 🟡 | L3a datasources params 文案标签 | ✅ |
| V214 | 🟡 | 三面 params 文档 / §2 交叉引用 | ✅ |
| V215 | 🟡 | VisibleWhen.dependencies 描述 | ✅ |
| V216 | 🟢 | parentRow 静态拒绝措辞残留 | ✅ |
| V217 | 🟢 | MCP 清理失败本地警告 | ✅ |
