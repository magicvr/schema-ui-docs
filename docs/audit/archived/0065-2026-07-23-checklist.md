---
status: archived
date: 2026-07-23
based_on: 0065-2026-07-23-review.md
---

# 审计 0065 跟踪 Checklist

> 本 checklist 跟踪 **ADR-0023 接受前** 必须闭合的协议层问题。不包含生产 Renderer / Host / 业务鉴权。  
> 接受 0023 后的 Schema/L2/fixtures 落地走 ADR 正文 M1–M6，可另开发布审计。

## 基线

- [x] 审计编号 `0065` 延续已归档 `0064`。
- [x] 问题编号 `V287`–`V294` 延续 `V286`。
- [x] 确认无 P0；本轮 **4×P1 + 4×P2**。
- [x] 确认 0023 为 `proposed`，未进入 `protocol-manifest.json` / 2.2 制品。

## 阻断 accepted（P1）

### V287 结构路径

- [x] ADR-0023 定义「权限结构树」边集合（Node 嵌套 + 操作挂载）。
- [x] 写明允许声明 `permissionCascade` 的 type / 位置白名单（含 `table` 是否在列）。
- [x] 写明 `modal` / 跨 page 根切断规则与公式前提一致。
- [x] 示例：table 上 cascade → toolbar / RowAction 的路径可读。

### V288 表单 edit 目标

- [x] ADR 给出可编辑目标判定（type 白名单或可机器执行规则）。
- [x] 明确 `form.mode=search` 不参与 `edit` 继承。
- [x] 明确默认 `submitAction` 入口与 cascade `edit` 的关系。
- [x] 与既有 `permissions.edit=false` 呈现语义对齐（只读/禁用 + 不提交）。

### V289 permissionIntent 矩阵

- [x] 挂载点白名单（RowAction / toolbar / actionButton / form implicit submit）。
- [x] 与 `request` / `navigate` / `modal` / 本地 key 的关系已写明，intent 与 HTTP 解耦。
- [x] form 提交免 `permissionIntent`，改吃 form 级 edit。
- [x] 非法挂载点 L2 拒绝意图已写清。

### V290 无 cascade 的容器 permissions

- [x] 明确「仅本地、不传后代」与容器自效应（无自效应时为 no-op）。
- [x] 已提供本地 vs cascade 的对比示例。
- [x] 与 D1「不追溯 v2.2」无矛盾表述。

## 建议接受前关闭（P2）

### V291 OQ 表

- [x] ADR-0023 增加 `OQ-23-*` 裁决表；当前均为 Closed（提案层），接受仍需独立决议。

### V292 执行时序

- [x] ADR 写明 effectivePermission 插入点；接受时同步 `08`（及 toolbar/submit）。

### V293 capability 门控

- [x] 仅 intent / 仅 cascade / 两者皆有的 L2 与 capability 规则。
- [x] 版本下限策略句已对齐 V282 纪律，具体 MINOR 号接受时定。

### V294 呈现 vs 可执行

- [x] 互操作只约束不可执行；隐藏/禁用不升格为第二权限通道。
- [x] `03` 推荐呈现已写入 ADR，接受时同步。

## 文档与轨道

- [x] `docs/audit/README.md` 移除活跃记录，并在 `docs/audit/archived/README.md` 登记归档 0065。
- [x] ADR-0023 吸收本轮 P1 及 P2，已更新 OQ 状态与接受前复核点。
- [x] `11` Phase C.1 保留 `accepted` 项未勾选；本审计不代替接受决议。
- [x] 0023 未被拒绝或大幅收窄；ADR 与 `11` 均明确保持 `proposed`，接受另行裁决。

## 完成门禁（关闭本审计时）

- [x] V287–V290 已在 ADR 中关闭；不得据此直接将 ADR 标为 accepted。
- [x] V291–V294 已关闭或明确推迟至接受后的 MINOR 落地，均不改变当前 proposed 状态。
- [x] 0023 仍为 proposed；M1–M6 未启动，后续接受后再移交发布目标文档；本审计只关闭「可接受性」范围。
- [x] review + checklist 移入 `docs/audit/archived/`，并更新 `audit/README.md` 与 `archived/README.md`。

## 当前决议记录

| 日期 | 决议 |
|---|---|
| 2026-07-23 | **不可直接 accepted**。方向作为 post-v2.2 MINOR 提案保留。阻断项 V287–V290。 |
| 2026-07-23 | **响应 0065**。V287–V294 已写回 ADR-0023 并关闭为提案层裁决；ADR 仍 `proposed`，接受/拒绝另行决定。 |
