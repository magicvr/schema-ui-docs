---
status: archived
date: 2026-07-11
based_on: 0048-2026-07-11-review.md
---

# 审计 0048 跟踪清单

## V197（🟡）收紧 params `$deps.*` 为整值替换

- [x] V197a 更新 `scripts/validate-l3a-expressions.js`：params 字符串包含协议变量语法时，必须完整匹配单个 `$deps.<path>`；禁止前后缀拼接。
  - 修改：`scanDataParams()` 改为「含 `$` 则必须整段匹配 `^$deps.<path>$`」，与 requestMapping 口径一致。
- [x] V197b 确认同一规则递归覆盖 `data.params`、`select.props.optionsSource.params` 与 `datasources.*.params` 的对象/数组值，并保留页面级 datasource 的 `NON_FORM_DATA_PARAMS` 语义。
  - 验证：完整 `$deps.*` 在非表单上下文仍报 `NON_FORM_DATA_PARAMS`；模板拼接报 `DATA_PARAMS_VARIABLE`。
- [x] V197c 更新 `docs/02-reaction-expression.md` §10.7、`docs/04-datasource-contract.md` §3.1/§3.2 和 `docs/06-validation.md`，明确仅支持整值替换，不提供字符串模板拼接。
  - 同步：`docs/08-renderer-spec.md` 相关措辞。
- [x] V197d 更新 `mcp/tests/validate-content.test.ts`：完整 `$deps.*` 通过；前缀/后缀拼接以 `DATA_PARAMS_VARIABLE` 失败；普通字面量通过；覆盖 data/optionsSource/datasources 路径。
- [x] V197e 更新 `docs/CHANGELOG.md` Unreleased，记录 params 值替换校验边界修复。
- [x] V197f 执行并记录验证：
  - `node --check scripts/validate-l3a-expressions.js` ✅
  - 最小 L3a 正反例：`$deps.ownerId` / 字面量通过；`prefix-$deps.ownerId` / `$deps.ownerId-suffix` 以 `DATA_PARAMS_VARIABLE` 失败；非表单完整 `$deps.*` 以 `NON_FORM_DATA_PARAMS` 失败 ✅
  - `npm --prefix mcp run build` ✅
  - `npm --prefix mcp test` ✅（108/108）

## V198（🟢）修复 `04` 指向表达式 §10.1 的 fragment

- [x] V198a 将 `docs/04-datasource-contract.md` §3.2 的旧 fragment 更新为当前 `docs/02-reaction-expression.md` §10.1 标题 fragment。
  - 新 fragment：`#101-非表单--表单-visiblewhen-的变量白名单`（与仓库既有 `/` → `--` 锚点风格一致）。
- [x] V198b 搜索当前活跃文档，确认不再残留旧 fragment `#101-deps-出现在非表单-visiblewhen-中`（仅审计正文历史描述保留）。
- [x] V198c 对当前 Markdown 相对链接执行文件目标与 fragment 双重检查并记录结果。
  - 用与仓库已知锚点（`#32-dataparams--...`、`#51-401--403-...`）一致的 slug 规则校验 §10.1 新链接通过。

## 汇总

| 编号 | 严重度 | 主题 | 状态 |
|---|---|---|---|
| V197 | 🟡 | params `$deps.*` 整值替换边界 | ✅ |
| V198 | 🟢 | `04` → 表达式 §10.1 fragment | ✅ |

## 已执行验证

```text
node --check scripts/validate-l3a-expressions.js
# L3a fixtures: ok-full / bad-prefix / bad-suffix / non-form-full
npm --prefix mcp run build
npm --prefix mcp test   # 108/108
# fragment: #101-非表单--表单-visiblewhen-的变量白名单
git status --short
```
