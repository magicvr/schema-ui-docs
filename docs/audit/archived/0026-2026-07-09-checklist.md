---
status: checklist
date: 2026-07-09
based_on: 0026-2026-07-09-review.md
---

# 审计 0026 跟踪清单

## V43（🔴）L3a 未按变量可见性矩阵拒绝非法 `$parentRow.*` 使用

| 字段 | 值 |
|---|---|
| **位置** | `docs/02-reaction-expression.md` 附录 A；`scripts/validate-l3a-expressions.js` |
| **问题** | `$parentRow.*` 仅应允许嵌套表格内 `scope: row` 表达式，但 L3a 当前放行非行级位置的 `$parentRow.*` |
| **操作** | 更新 L3a 作用域校验并补充回归验证 |
| **文件** | `scripts/validate-l3a-expressions.js`；必要时同步 `docs/06-validation.md` 或测试说明 |

- [x] V43a 补充一个非法样例：非表格 / 非行级 `visibleWhen.when` 使用 `$parentRow.*` 时，L3a 必须报错。验证：临时样例 `body.visibleWhen.when: "$parentRow.status == 'active'"` 已返回 `SCOPE_ISOLATION` 与 `PARENT_ROW_SCOPE`。
- [x] V43b 修改 `validate-l3a-expressions.js`，将 `$parentRow.*` 纳入行级上下文变量集合，不再只检查 `$row.*`。修改：新增 `hasParentRowRef` 与 `PARENT_ROW_SCOPE` 规则。
- [x] V43c 按 `02-reaction-expression.md` 附录 A 区分普通行级与嵌套行级场景；若当前协议结构无法表达嵌套表格上下文，需在校验器或文档中明确保守拒绝策略。修改：遍历时记录 `tableDepth`，仅内层表格行级表达式允许 `$parentRow.*`；L3a 嵌套表格正例已通过。
- [x] V43d 复跑 L3a 反例、官方 4 个场景示例和相关校验链，确认非法 `$parentRow.*` 被拒绝且现有示例不受影响。验证：官方 4 个场景示例抽取后执行 `node scripts/validate-all.js <正斜杠 glob>`，L0/L1、L2、L3a、L4 全部通过。

---

## V44（🟢）`01-node-protocol.md` 表达式求值时序章节引用漂移

| 字段 | 值 |
|---|---|
| **位置** | `docs/01-node-protocol.md` §3.10 |
| **问题** | 链接仍写 `02-reaction-expression.md §13` 与旧 `#13-...` 锚点，但当前章节已是 §14 |
| **操作** | 更新章节号和锚点 |
| **文件** | `docs/01-node-protocol.md` |

- [x] V44a 将链接展示文本 `02-reaction-expression.md §13` 更新为 `02-reaction-expression.md §14`。修改：`docs/01-node-protocol.md` §3.10。
- [x] V44b 将链接锚点 `#13-表达式求值时序模型since-024` 更新为 `#14-表达式求值时序模型since-024`。修改：`docs/01-node-protocol.md` §3.10。
- [x] V44c 搜索确认当前协议正文中不再残留指向该旧 §13 锚点的引用。验证：旧 §13 命中仅剩本审计文档的问题描述与 checklist 记录。

---

## 汇总

| 编号 | 严重度 | 状态 |
|---|---|---|
| V43 | 🔴 | [x] 已修复并验证 |
| V44 | 🟢 | [x] 已修复 |