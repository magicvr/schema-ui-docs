---
status: archived
date: 2026-07-28
based_on: 0075-2026-07-28-review.md
scope: focus=all；主类型=实现/契约漂移；次类型=发布后入口/门禁卫生
---

# 审计 0075 跟踪 Checklist

> 本 checklist 跟踪 v2.6.0 正式发布后的总纲交付叙事、F1 扩展示例 frontmatter 门禁与 CHANGELOG 历史指针（V359–V361）。  
> 生产消费者仅作 out-of-repo 跟踪；未经单独授权，不修改 `allinme.web-client` 或 `allinme.core-api`。  
> 不把本审计路径加入 `protocol-manifest.json`、协议制品、MCP 搜索索引或 Docker 运行镜像。

## 汇总

| 编号 | 级别 | 主题 | 状态 |
|---|---|---|---|
| V359 | 🟢 P2 | `00-overview` §5 交付叙事停在 2.5，未写 ADR-0028 / 2.6 | **已关闭** |
| V360 | 🟢 P2 | F1 扩展示例未进入 `release-check` frontmatter 门禁 | **已关闭** |
| V361 | ⚪ P3 | CHANGELOG `v2.5.3` 仍写 F1「见 Unreleased」 | **已关闭** |

**计数：** 0×P0 + 0×P1 + **2×P2** + **1×P3** **全部关闭**。  
**问题号：** 全局续编 `V358` → `V359`–`V361`。  
**审计号：** `0075`（archived）。

## 基线

- [x] 创建前确认无活跃 audit；全局最大审计号为 `0074`（archived）。
- [x] 确认 `V359`–`V361` 位于全局最大 `V358` 之后，且不使用历史冲突的 V1–V4。
- [x] 记录正式 `v2.6.0` content `a2c6b4a4…` / artifact `e132d225…` / fixture `3a9cd233…`。
- [x] 记录正式 MCP GHCR index `fafeacb0…`；`2.6.0` / `2.6` / `latest` 同 digest。
- [x] 步骤 1 本地全量门禁绿（含 conformance 35/35、MCP 134、制品链接、GHCR smoke）。
- [x] 修复后重跑受影响门禁并勾选关闭证据。
- [x] 用户确认后归档（步骤 3）。

---

## V360 — F1 扩展示例未进入 `release-check` frontmatter 门禁（P2）— 已关闭

**优先：** 先于 V359，避免只改正文不补门禁。

- [x] `scripts/release-check.js` 的 `scenarioDocsForFrontmatter` 增加 `docs/05-scenarios/form-controls-extended.md`
- [x] `docs/05-scenarios/README.md` 说明块补充 F1：扩展示例；进 frontmatter 一致性门禁；未进官方六场景 / 默认 CONFORMANCE 白名单（算法由 request/response/version 等 suite 覆盖）
- [x] 确认 Markdown 首 fence 与 `_samples/user-profile-extended.yaml` 一致（步骤 1 已 equal；本轮未改 YAML）
- 验证命令：
  - `npm run release:check` → 通过（303 cases；fixture `3a9cd233…` 不变）
- 关闭证据：
  - 门禁列表含 F1 路径；`release:check` 绿

## V359 — `00-overview` §5 交付叙事停在 2.5（P2）— 已关闭

- [x] `docs/00-overview.md` §5 交付段追加 ADR-0028 / `form.controls.extended` / `2.6.0` 已正式发布
- [x] 同节场景覆盖句补「扩展表单控件面」
- [x] `last_updated` → `2026-07-28`
- 验证命令：
  - `npm run check:links` → 237 MD / 662 链接通过
  - `npm run release:check` → overview 版本字符串仍绿
- 关闭证据：
  - 总纲交付叙事与 capability/L2/v2.6 门禁一致

## V361 — CHANGELOG `v2.5.3` F1「见 Unreleased」（P3）— 已关闭

- [x] `docs/CHANGELOG.md` `## v2.5.3` 演进轨道 bullet：改为指向 **v2.6.0** 节
- [x] **未**改写 2.5.3 其余语义与 digests 表述；Unreleased 仍为空
- 验证命令：
  - 人工阅读；`npm run release:check`（首节仍为 `v2.6.0`）
- 关闭证据：
  - 历史指针不再指向空 Unreleased

---

## Out-of-repo（不编号，不阻塞本审计归档）

- [ ] （跟踪）`allinme.web-client` / `allinme.core-api` 仍 pin `v2.4.1` — 升级需单独授权与消费者 PR
- 说明：本仓 2.5/2.6 契约已发布；消费者未跟版属已知残留风险，**不**在本 checklist 强制关闭

## 回归与归档门禁

- [x] `npm run check:links`（237 / 662）
- [x] `npm run release:check`（303；fixture 不变）
- [x] `npm run validate:scenarios`（官方 6 场景 L0–L4）
- [x] checklist 无应关闭而未勾选的裸 `[ ]`（out-of-repo 除外）
- [x] 用户最终确认后：移动 review/checklist → `archived/`；frontmatter `status: archived`；更新 `docs/audit/README.md` 与 `archived/README.md`

### 修复后 digests 备注（不改正式 tag）

| 项 | 正式 `v2.6.0` | 本轮修复后工作树 |
|---|---|---|
| fixture | `3a9cd233…` | **相同**（机器契约未变） |
| content | `a2c6b4a4…` | `6d80bcf2…`（informative：`00` / CHANGELOG / 场景 README） |
| artifact | `e132d225…` | `ec13e28d…` |

**未**改写 `v2.6.0` / `mcp-v2.6.0`。若需工作树与 pin 身份再对齐，另开 **PATCH** 发布（本审计不强制）。
