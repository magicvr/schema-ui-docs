---
status: checklist
date: 2026-07-10
based_on: 0031-2026-07-10-review.md
---

# 审计 0031 跟踪清单

## V67（🔴）表格内嵌 `visibleWhen` / `reactions` 结构约束未实际校验

| 字段 | 值 |
|---|---|
| **位置** | `docs/schemas/component-registry.json`；`scripts/validate-l2-components.js`；`scripts/validate-l3a-expressions.js`；MCP `validate_content` |
| **问题** | table `props.columns[]` / `props.actions[]` 内嵌表达式对象写了 `$ref`，但实际 L0/L1/L2/L3a 未执行完整结构与状态键约束 |
| **操作** | 让校验链拒绝缺 `when`、row scope 的 `required/value`，并补 MCP 回归 |
| **文件** | `scripts/validate-l2-components.js`；`scripts/validate-l3a-expressions.js`（若适用）；`docs/06-validation.md`；`mcp/tests/*` |

- [x] V67a 在 L2 中解析或等价实现 `VisibleWhen` / `Reaction` `$ref` 校验，覆盖 `table.props.columns[].visibleWhen`、`columns[].reactions[]`、`actions[].visibleWhen`、`actions[].reactions[]`。修改：`scripts/validate-l2-components.js` 增加受控 `$ref` 校验器，覆盖 `VisibleWhen` / `Reaction` / `Permissions`。
- [x] V67b 确保 `visibleWhen.when` 缺失会报错，且路径指向对应 table column/action 字段。验证：`body.props.columns[0].visibleWhen.when` 报 `必填字段 "when" 缺失`。
- [x] V67c 确保 `scope: row` 的 `fulfill` / `otherwise` 出现 `required` 或 `value` 会报错，覆盖列和行内 action。验证：`body.props.columns[0].reactions[0].fulfill.required` 与 `body.props.actions[0].reactions[0].fulfill.value` 均被 L2 拒绝。
- [x] V67d 补充正例：合法的 `scope: row` 表格列/行内操作 `visibleWhen` / `reactions` 仍通过。验证：`node scripts/validate-all.js <good-table-visiblewhen.yaml> --json` 返回 `passed: true`。
- [x] V67e 同步 `docs/06-validation.md`，说明 table props 内嵌表达式结构和状态键约束由哪一层校验。修改：L2 行补充受控 `$ref` 与 row scope 状态键结构校验说明。
- [x] V67f 补充 MCP `validate_content` 回归测试，至少覆盖一条 table 内嵌表达式非法配置返回 `passed: false`。修改：`mcp/tests/test-utils.ts`、`mcp/tests/validate-content.test.ts`。
- [x] V67g 执行验证：`node scripts/validate-l2-components.js <3 invalid fixtures> --json`（3 处 L2 违规）；`node scripts/validate-all.js <bad/good fixture> --json`（非法失败、合法通过）；`npm --prefix mcp test -- validate-content.test.ts`（11 通过）；`npm --prefix mcp run build`；`npm --prefix mcp test`；`npm --prefix mcp run smoke:tools`。

---

## V68（🟡）Renderer §5.5 L3 静态校验清单落后

| 字段 | 值 |
|---|---|
| **位置** | `docs/08-renderer-spec.md` §5.5 |
| **问题** | 只列出早期三条 L3 静态校验，遗漏当前 02/06/L3a 的完整规则 |
| **操作** | 将 §5.5 改为引用权威规则并补齐当前必须覆盖的规则清单 |
| **文件** | `docs/08-renderer-spec.md`；可选 `docs/06-validation.md` |

- [x] V68a 更新 `08-renderer-spec.md` §5.5，明确 Renderer 静态校验应覆盖 `02` §10 与附录 A、`06` L3a 的当前完整规则。修改：`docs/08-renderer-spec.md`。
- [x] V68b 在 §5.5 补充 scope 隔离、`$parentRow`、表格 action 禁 `$self`、row scope 状态键限制、表格 `scope: form` 的 form 上下文、params 变量限制与 `datasources.*.params` 非 form 规则。修改：同上。
- [x] V68c 若 V67 明确了 table 内嵌表达式结构校验层级，同步在 §5.5 或相关段落中说明。修改：`08` §5.5 补充“L2 先保证 table 内嵌表达式结构合法，L3a 再校验表达式语义”。
- [x] V68d 执行验证：`node scripts/validate-all.js <bad/good fixture> --json` 覆盖 table 表达式边界；最终执行 `git diff --check` 覆盖 Markdown 空白检查。

---

## V69（🟡）MCP `internalError` 返回结构与 ADR-0007 不一致

| 字段 | 值 |
|---|---|
| **位置** | `docs/decisions/0007-mcp-protocol-reader-validator.md`；`mcp/src/types.ts`；`mcp/src/core/validation-runner.ts` |
| **问题** | ADR 要求 `internalError` 为结构化对象，当前类型与实现为字符串 |
| **操作** | 将 `internalError` 改为对象结构，补测试并构建验证 |
| **文件** | `mcp/src/types.ts`；`mcp/src/core/validation-runner.ts`；`mcp/tests/validate-content.test.ts` |

- [x] V69a 将 MCP 类型中的 `internalError` 改为结构化对象（可复用 `ParseError` 或新增 `ToolError`），最小字段 `message`。修改：`mcp/src/types.ts` 新增 `ToolError`，`internalError: ToolError | null`。
- [x] V69b 修改 `validation-runner.ts` 中 `format` 非法、content 超限、catch 异常等路径，统一写入 `{ message }`。修改：`mcp/src/core/validation-runner.ts`。
- [x] V69c 修改 `summarize()` 读取 `internalError.message`，保持 summary 文案不退化。修改：同上。
- [x] V69d 补充测试：触发 content 超限或等价内部错误路径时，`internalError` 为对象且不包含原始堆栈。修改：`mcp/tests/validate-content.test.ts`。
- [x] V69e 执行验证：`npm --prefix mcp run build`；`npm --prefix mcp test`（15 通过）；`npm --prefix mcp run smoke:tools`（5 个工具全部列出）。

---

## V70（🟢）0030 checklist 未勾选项与归档索引状态不自洽

| 字段 | 值 |
|---|---|
| **位置** | `docs/audit/archived/0030-2026-07-10-checklist.md`；`docs/audit/archived/README.md` |
| **问题** | 0030 索引写“全部已完成”，但 checklist 中 V65c 仍为空复选框 |
| **操作** | 明确 V65c 是否执行；若不执行，勾选并记录决策原因；若执行则补文档后勾选 |
| **文件** | `docs/audit/archived/0030-2026-07-10-checklist.md`；可选 `docs/02-reaction-expression.md` / `docs/04-datasource-contract.md` |

- [x] V70a 决定 V65c 处理方式：补充 `02` / `04`，或明确不额外修改且原因是 `06` 已作为校验边界权威补充。决策：不额外修改 `02`/`04`，以 `06-validation.md` L3a 作为页面级 `datasources.*.params` 静态校验边界说明。
- [x] V70b 将 0030 checklist 中 V65c 标记为 `[x]`，并记录实际处理或不执行原因。修改：`docs/audit/archived/0030-2026-07-10-checklist.md`。
- [x] V70c 确认 `docs/audit/archived/README.md` 的“跟踪清单（全部已完成 ✅）”与 checklist 状态一致。结果：0030 checklist 已无未勾选项。
- [x] V70d 执行验证：搜索空复选框模式，确认 `docs/audit/archived/0030-2026-07-10-checklist.md` 无剩余未勾选项。

---

## 汇总

| 编号 | 严重度 | 状态 |
|---|---|---|
| V67 | 🔴 | [x] 已修复并验证 |
| V68 | 🟡 | [x] 已修复并验证 |
| V69 | 🟡 | [x] 已修复并验证 |
| V70 | 🟢 | [x] 已修复并验证 |

## 建议验证命令（修复后）

```bash
# 表格表达式与文档/脚本
npm run validate:l2 -- "<tmp-or-fixture>.yaml"
npm run validate:l3a -- "<tmp-or-fixture>.yaml"
npm run validate -- "<tmp-or-fixture>.yaml"

# MCP 实现与回归
npm --prefix mcp run build
npm --prefix mcp test
npm --prefix mcp run smoke:tools
```

若未触及 Dockerfile 或镜像内容，可不执行 `npm --prefix mcp run smoke:docker -- <image>`；若后续改动 Docker 打包或镜像文档，再补充 Docker smoke 状态。