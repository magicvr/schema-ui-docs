---
status: checklist
date: 2026-07-08
based_on: 0015-2026-07-08-review.md
---

# 审计 0015 跟踪清单

> 本清单覆盖 5 个在 0014 归档后再次全量复审发现的问题，编号 V9-V13（延续 0014 的 V4-V8 编号体系）。

## 使用说明

- `[ ]` 待修复 / `[x]` 已完成
- 每项修复完成后，在右侧填写对应文件的修改摘要和 commit hash

---

## V9（🔴）`08-renderer-spec.md` 示例仍引用 `$deps.dateRange`

| 字段 | 值 |
|---|---|
| **位置** | `08-renderer-spec.md` §2.3 数据依赖声明示例 |
| **问题** | `dateRangePicker` 声明 `startField: dateFrom`、`endField: dateTo`，但表格参数仍写 `dateFrom: $deps.dateRange` |
| **操作** | 改为分别引用 `$deps.dateFrom` 和 `$deps.dateTo` |
| **文件** | `docs/08-renderer-spec.md` |

- [x] V9a 将 `dateFrom: $deps.dateRange` 改为 `dateFrom: $deps.dateFrom`
- [x] V9b 补充 `dateTo: $deps.dateTo`
- [x] V9c 确认示例的 `dependencies` 覆盖 `dateFrom` / `dateTo`

---

## V10（🟡）根 `README.md` 目录树漏列当前关键文档

| 字段 | 值 |
|---|---|
| **位置** | 根 `README.md` 目录树 |
| **问题** | `decisions/` 只列到 0004，未列 ADR-0005/0006；`05-scenarios/README.md` 也未列出 |
| **操作** | 更新目录树，与当前实际文档结构一致 |
| **文件** | `README.md` |

- [x] V10a 在 `05-scenarios/` 下补充 `README.md`
- [x] V10b 在 `decisions/` 下补充 `0005-response-mapping.md`
- [x] V10c 在 `decisions/` 下补充 `0006-expression-evaluation-order.md`

---

## V11（🟡）`scope: form` 默认场景表述容易误导非表单节点 `visibleWhen`

| 字段 | 值 |
|---|---|
| **位置** | `02-reaction-expression.md` §5 作用域表 |
| **问题** | `scope: form` 默认场景写“页面 `visibleWhen`”，容易误导读者认为非表单节点可访问 `$deps.*` |
| **操作** | 收紧默认场景表述，并补充非表单节点 `visibleWhen` 只能访问 `$context.*` 的说明 |
| **文件** | `docs/02-reaction-expression.md` |

- [x] V11a 将 `scope: form` 默认场景改为更精确的表单内场景表述
- [x] V11b 补充非表单节点 `visibleWhen` 不得访问 `$deps.*` 的说明
- [x] V11c 与 `01-node-protocol.md` 的非表单 `visibleWhen` 规则交叉核对

---

## V12（🟡）表格列 `scope: form` 下 `$self` 可见性语义未定义

| 字段 | 值 |
|---|---|
| **位置** | `02-reaction-expression.md` §6 变量可见性矩阵 |
| **问题** | 变量矩阵允许表格列 `scope: form` 表达式访问 `$self`，但正文没有定义该场景下 `$self` 的绑定对象 |
| **操作** | 将该场景下 `$self` 标为不可用，或补充明确绑定语义 |
| **文件** | `docs/02-reaction-expression.md` |

- [x] V12a 将表格列 `scope: form` 表达式的 `$self` 可见性改为不可用
- [x] V12b 确认 `scope: row` 下 `$self` 仍表示当前列单元格原始值
- [x] V12c 检查静态校验规则是否需要同步说明

---

## V13（🟢）`06-validation.md` 校验命令路径与仓库实际目录不一致

| 字段 | 值 |
|---|---|
| **位置** | `06-validation.md` §3.1 AJV 示例与 VS Code YAML 配置示例 |
| **问题** | 示例写 `schemas/page.schema.json`，但仓库实际路径是 `docs/schemas/page.schema.json` |
| **操作** | 改为仓库根目录可直接执行/引用的路径，或明确命令需在 `docs/` 目录执行 |
| **文件** | `docs/06-validation.md` |

- [x] V13a 修正 AJV 命令中的 schema 路径
- [x] V13b 修正 VS Code YAML 配置中的 schema 路径
- [x] V13c 明确示例命令的执行目录

---

## 汇总

| 编号 | 严重度 | 类型 | 状态 |
|------|--------|------|------|
| V9 | 🔴 | 示例契约错误 | [x] |
| V10 | 🟡 | 入口索引漂移 | [x] |
| V11 | 🟡 | 表达式作用域表述歧义 | [x] |
| V12 | 🟡 | `$self` 语义未定义 | [x] |
| V13 | 🟢 | 校验命令路径错误 | [x] |

> **全部项目完成后，** 将本审计文件移入 `audit/archived/` 并在 `archived/README.md` 添加索引。

---

*生成时间：2026-07-08*