---
status: archived
date: 2026-07-28
based_on: schema-ui-protocol 2.6.0 / protocolVersion 2.6；初审 HEAD 47eff94；正式 tag v2.6.0 / mcp-v2.6.0 @ cf65b96；延续已归档 0074
scope: focus=all；主类型=实现/契约漂移；次类型=发布后入口/门禁卫生；不修改生产消费者仓库
---

# 审计报告 0075：v2.6.0 发布后总纲交付叙事与 F1 场景门禁投影

## 结论

本轮全量复审确认：正式 **`v2.6.0` / `mcp-v2.6.0`** 证据链完整，本地全量门禁与 0074 关闭结论一致（303 fixtures / 35 conformance / 134 MCP tests / 制品与 GHCR digests 对齐）。**无 P0/P1**；未发现机器契约或发布证据链回归。

新发现问题 **2×P2 + 1×P3（V359–V361）**：**全部关闭**（步骤 3 修复）。均为 v2.6.0 正式发布后的**入口叙事与扩展示例门禁投影**缺口，不阻断已发布制品身份；**未**改写正式 tag；**不**强制 PATCH。于 2026-07-28 归档。

| 判定 | 说明 |
|---|---|
| 正式 2.6.0 身份 | content `a2c6b4a4…` · artifact `e132d225…` · fixture `3a9cd233…`（99 文件）— **不可变 pin** |
| 修复后工作树 | content `6d80bcf2…` · artifact `ec13e28d…` · fixture **仍** `3a9cd233…`（仅 informative 文档） |
| MCP 镜像 | index `sha256:fafeacb0…`（`2.6.0` / `2.6` / `latest` 同 digest）；GHCR smoke 通过 |
| 0074 已关项 | V350–V358 **未回归** |
| 本轮关闭 | V359 总纲；V360 F1 frontmatter 门禁；V361 CHANGELOG 指针 |

有效修复已沉淀到 `docs/00-overview.md`、`scripts/release-check.js`、`docs/05-scenarios/README.md` 与 `docs/CHANGELOG.md`；**本文不是协议权威来源**。`docs/audit/**` 不进入 `protocol-manifest.json`、协议制品、MCP 索引或 Docker 镜像。

## 范围与基线

### 审计类型

| 类型 | 本轮角色 |
|---|---|
| **实现/契约漂移** | **主类型** — 权威总纲交付叙事与 release 场景 frontmatter 门禁相对 v2.6 线不完整 |
| 发布完整性 | **已闭合**（本仓）— 正式 tag/CD/digests 齐备；本轮不重开 V352 |
| 提案评审 | 不适用 — 无 proposed ADR |

### 当前基线（步骤 1）

| 项 | 结果 |
|---|---|
| 工作树 / 分支 | `dev` @ `47eff94`（工作树干净；`origin/main` 含 PR #31 合并） |
| 工作树版本 | 根包与 manifest **2.6.0**；页面协议 **`"2.6"`**；MCP **2.6.0**；validator **1.0.0** |
| 正式 tag | `v2.6.0` / `mcp-v2.6.0` → 发布 commit `cf65b96` |
| 活跃审计 | 创建前 **无**；本轮 **0075** |
| 问题编号 | 全局最大 **V358**（0074），本轮续编 **V359–V361** |

### 基线验证（步骤 1）

| 命令或证据 | 结果 |
|---|---|
| `npm run check:links` | 235 Markdown、658 相对链接通过 |
| `npm run release:check` | 303 cases、17 fixture 文件；fixture `3a9cd233…`；content `a2c6b4a4…`；artifact `e132d225…` |
| `npm run verify:protocol-artifact` | reproducible=true；99 文件 |
| `npm run check:protocol-artifact-links` | 71 MD / 381 链接通过 |
| `npm run validate:scenarios` / `validate:conformance` | 官方 6 场景 L0–L4；16 versioned suites 绿 |
| `npm run validate -- …/user-profile-extended.yaml` | L0–L4 通过；Markdown fence ≡ sample |
| `npm run test:conformance:all` | **35/35** JS+Python |
| `release:check:mcp` / `:validator` | 通过（线对齐 2.6） |
| MCP `build` / `test` / `smoke:tools` | **134/134**；5 tools |
| GHCR `smoke:docker` `2.6.0` | 通过（`appManifestDocOk`；index `fafeacb0…`） |
| 远端 Release / CD | `v2.6.0` Protocol Release success；`mcp-v2.6.0` MCP CD success |
| 生产消费者 | 相邻仓只读：仍 pin **`v2.4.1`**（out-of-repo） |

## 覆盖率与验证

| 批次 | 已覆盖 | 未覆盖 / 低置信度 | 置信度 |
|---|---|---|---|
| A 规范与权威边界 | charter、manifest、`00`–`09`、Schema、ADR 状态、v2.6 门禁、F1 场景/sample、迁移 | 未逐字通读全部历史 ADR | 高 |
| B 校验与 conformance | 全量 runner、release-check 计数、L2 F1 路径抽样 | reference 未全文 diff | 高 |
| C MCP 与 validator | 版本对齐、DOC_MAP→v2.6、build/test/smoke、GHCR smoke | 本地无 tag 镜像（已用 GHCR 替代） | 高 |
| D 自动化与发布 | RELEASE 惯例、正式 digests、镜像 tag 同 digest | — | 高 |
| E 生产消费者 | web-client / core-api pin 只读 | 未跑消费者 CI | 中 |
| 历史审计 | 索引 + 0070–0074 查重；最大 NNNN=0074 / V=358 | 未通读 0001–0069 全文 | 索引高 |

## 问题清单（汇总表）

| 编号 | 级别 | 主题 | 状态 | 类型 |
|---|---|---|---|---|
| V359 | 🟢 P2 | `00-overview` §5 交付叙事停在 2.5，未写 ADR-0028 / 2.6 | **已关闭** | 实现/契约漂移（入口） |
| V360 | 🟢 P2 | F1 扩展示例未进入 `release-check` frontmatter 门禁 | **已关闭** | 实现/契约漂移（门禁） |
| V361 | ⚪ P3 | CHANGELOG `v2.5.3` 仍写 F1「见 Unreleased」 | **已关闭** | 文案卫生 |

**计数：** 0×P0、0×P1、**2×P2**、**1×P3**；**全部关闭**。  
**Out-of-repo（不编号）：** 生产消费者仍 pin `v2.4.1`。

## 问题详情

### V359（🟢 P2）`00-overview` §5 交付叙事停在 2.5，未写 ADR-0028 / 2.6 — 已关闭

**位置：** `docs/00-overview.md` §5「版本与稳定性」Admin 生命周期交付段与场景覆盖句。

**初审行为：** capability/L2 已含 F1，交付叙事与场景覆盖句停在 2.5 / 批量多选。

**修复（步骤 3）：** 交付段追加 ADR-0028 / `form.controls.extended` / `2.6.0`；场景覆盖补「扩展表单控件面」；`last_updated: 2026-07-28`。

**关闭证据：** `npm run check:links`、`npm run release:check` 绿。

**查重：** 非 0074/V356 同类；总纲交付清单滞后。

---

### V360（🟢 P2）F1 扩展示例未进入 `release-check` frontmatter 门禁 — 已关闭

**位置：** `scripts/release-check.js` → `scenarioDocsForFrontmatter`；`docs/05-scenarios/README.md`。

**初审行为：** V342 门禁含 batch/permission，**缺** `form-controls-extended.md`；场景 README 未说明 F1 门禁状态。

**修复（步骤 3）：** 门禁列表加入 F1 路径；README 说明块补 F1 frontmatter 门禁与「未进 OFFICIAL/默认 CONFORMANCE」口径。

**关闭证据：** `npm run release:check` 绿（F1 frontmatter `v2.6` 与 YAML 一致）；`validate:scenarios` 绿。

**查重：** 模式自 **0071/V342**；0074/V358 仅阅读顺序。

---

### V361（⚪ P3）CHANGELOG `v2.5.3` 仍写 F1「见 Unreleased」 — 已关闭

**位置：** `docs/CHANGELOG.md` → `## v2.5.3` 演进轨道 bullet。

**修复（步骤 3）：** 改为「随 **v2.6.0** 正式发布（见上文 `## v2.6.0` 节）」；未改 2.5.3 其余语义。

**关闭证据：** 人工阅读；`release:check` 首节仍为 `v2.6.0`。

---

## 与历史审计关系

| 历史 | 关系 |
|---|---|
| **0074**（V350–V358） | 2.6.0 发布与 in-repo 闭合已归档；本轮确认 digests/门禁**未回归**；本轮为发布后剩余入口/门禁投影 |
| **0071**（V342） | 场景 frontmatter 门禁 → 本轮 **V360** 为 F1 场景漏网 |
| **0072**（V345–V348） | 入口卫生模式 → 本轮 **V359** 为总纲交付叙事对称缺口 |
| **0070–0073** | 发布身份链已闭合；本轮不重开 |
| 0010/0013/0014 | 历史 V1–V4 冲突；本轮 **不**复用，自 **V359** 递增 |

## 处理顺序与防复发

1. **V360**（门禁）→ 先补 `release-check`，防止后续只改文案仍漏投影。  
2. **V359**（总纲）→ 对齐交付叙事与场景覆盖。  
3. **V361**（CHANGELOG 指针）→ 低成本卫生。  

**防复发：**

- 新增扩展示例 Markdown 时，默认同步：`scenarioDocsForFrontmatter`（若有 YAML `protocolVersion`）+ 场景 README 门禁说明。  
- MINOR 交付后检查 `00-overview` §5 交付段是否与最新 accepted ADR / 制品版本同表。  
- 消费者 pin 升级仍 out-of-repo，不在本审计强制落地。

## 关闭条件（步骤 3）

- [x] V359–V361 可关闭项全部 `[x]`，并有本地命令证据。  
- [x] 已重跑：`check:links`、`release:check`、`validate:scenarios`。  
- [x] 用户确认后归档至 `docs/audit/archived/`；重置活跃区 README。  
- [x] **未**自动打 tag、**未**改已发布 digests、**未**改生产消费者。

### 步骤 3 关闭摘要

| 项 | 结果 |
|---|---|
| V360 | `release-check` + 场景 README 门禁说明 |
| V359 | `00-overview` 交付叙事与场景覆盖 |
| V361 | CHANGELOG `v2.5.3` 指针 → v2.6.0 |
| 机器契约 | fixture digest **未变** |
| 剩余风险 | 工作树 content/artifact 与正式 `v2.6.0` 分叉（informative）；可选后续 PATCH；消费者仍 pin `v2.4.1` |
