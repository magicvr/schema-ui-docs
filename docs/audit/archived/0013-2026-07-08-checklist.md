---
status: checklist
date: 2026-07-08
based_on: 0013-2026-07-08-review.md
---

# 审计 0013 跟踪清单

## 使用说明

- `[ ]` 待修复 / `[x]` 已完成
- 每项修复完成后，在右侧填写对应文件的修改摘要和 commit hash

---

## V1（🔴）`00-overview.md` §5 版本声明同步

| 字段 | 值 |
|---|---|
| **位置** | `00-overview.md` §5 |
| **问题** | 声明"最新补丁版本 `v0.2.3`"，但 CHANGELOG 已有 v0.2.4 |
| **操作** | `v0.2.3` → `v0.2.4`；同步更新 frontmatter `last_updated: 2026-07-07` → `2026-07-08` |
| **文件** | `docs/00-overview.md` |

- [x] V1 同步版本声明（§5：`v0.2.3` → `v0.2.4`）
- [x] V1 同步 frontmatter `last_updated`（`2026-07-07` → `2026-07-08`）

---

## V2（🟡）`modal.content` `since` 标注统一

| 字段 | 值 |
|---|---|
| **位置** | `07-actions-contract.md` §5 + `schemas/action.schema.json` |
| **问题** | 代码注释写 `(since 0.2)`，正文写 `(since 0.2.1)`，Schema 描述写 `since 0.2`——三处不一致 |
| **操作** | 统一为 `(since 0.2.1)`（与 CHANGELOG v0.2.1 记录一致） |
| **文件** | `docs/07-actions-contract.md`、`docs/schemas/action.schema.json` |

- [x] V2 修正 `07-actions-contract.md` §5 代码块注释（L65：`(since 0.2)` → `(since 0.2.1)`）
- [x] V2 修正 `schemas/action.schema.json` `content.description`（L42：`since 0.2` → `since 0.2.1`）

---

## V3（🟡）`component-registry.json` 中 table 列/行操作 `since` 标注批量修正

| 字段 | 值 |
|---|---|
| **位置** | `schemas/component-registry.json` |
| **问题** | 7 处 description 仍写 `(since 0.2)`，正文 `03-component-registry.md` 已改为 `(since 0.2.1)` |
| **操作** | 全部 7 处 `(since 0.2)` → `(since 0.2.1)`（详见下方子项） |
| **文件** | `docs/schemas/component-registry.json` |

- [x] V3a `columns[].visibleWhen` description（L164）：`(since 0.2)` → `(since 0.2.1)`
- [x] V3b `columns[].reactions` description（L165）：`(since 0.2)` → `(since 0.2.1)`
- [x] V3c `columns[].permissions` description（L166）：`(since 0.2)` → `(since 0.2.1)`
- [x] V3d `actions[].visibleField` description（L190）：`(since 0.2)` → `(since 0.2.1)`
- [x] V3e `actions[].visibleWhen` description（L192）：`(since 0.2)` → `(since 0.2.1)`
- [x] V3f `actions[].reactions` description（L193）：`(since 0.2)` → `(since 0.2.1)`
- [x] V3g `actions[].permissions` description（L194）：`(since 0.2)` → `(since 0.2.1)`

---

## O1（🟢）`last_updated` frontmatter 策略决策

| 字段 | 值 |
|---|---|
| **位置** | `01-node-protocol.md`、`02-reaction-expression.md`、`04-datasource-contract.md`、`08-renderer-spec.md` |
| **问题** | 0012 审计修改了内容，但四篇文档 `last_updated` 仍为 `2026-07-07` |
| **操作** | 先决策策略，再执行更新 |

### 决策选项

- **策略 A（严格维护）：** 每次内容变更同步更新 `last_updated`。本次将四篇文档更新为 `2026-07-08`。
- **策略 B（放弃维护）：** 移除所有文档的 `last_updated` frontmatter 字段，避免未来再次出现此问题。
- **策略 C（仅审计时更新）：** 只有在审计驱动的批量修改后更新该字段，小修小改不更新。

### 决策结果（审计 0014 回补）

采用 **策略 C（仅审计时更新）**。理由：策略 A 维护成本高、容易遗漏；策略 B 会丢失历史追踪信息。策略 C 折中——审计驱动的批量修改后统一更新 `last_updated`，日常小修小改不要求同步。本次四篇文档的 `last_updated` 已更新为 `2026-07-08`。

决策后：

- [x] O1 明确 `last_updated` 维护策略（C）
- [x] O1 按选定策略执行更新

---

## 汇总

| 编号 | 严重度 | 状态 |
|------|--------|------|
| V1 | 🔴 | [x] |
| V2 | 🟡 | [x] |
| V3 | 🟡 | [x] |
| O1 | 🟢 | [x] |

> **全部项目完成后，** 将本审计文件移入 `audit/archived/` 并在 `archived/README.md` 添加索引。

---

*生成时间：2026-07-08*
