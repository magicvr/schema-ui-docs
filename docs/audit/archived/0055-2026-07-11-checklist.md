---
status: completed
date: 2026-07-11
based_on: 0055-2026-07-11-review.md
---

# 审计 0055 跟踪清单

## V218（🔴）严格版本协商

- [x] 新增 ADR-0009，精确匹配 `supportedVersions`，删除最近版本猜测和 `minCompatibleVersion`。
- [x] 标准入口拒绝缺失、畸形、未知 MAJOR 和未知 MINOR。
- [x] 定义显式 legacy adapter 边界和输出契约。
- [x] 增加版本与 capability 一致性 fixtures、JavaScript reference 和 14 个正反例向量。

## V219（🔴）query 线级序列化

- [x] 新增 ADR-0010，采用标量-only RC 方案。
- [x] 定义 `null` / `undefined` / 空字符串 / number / boolean 规则。
- [x] 定义 UTF-8、RFC 3986、顺序、重复 key、已有 query 和 fragment 规则。
- [x] Schema/L2 拒绝 query 对象、数组、空 key 和非有限数值。
- [x] 四类 query 来源复用同一算法；JavaScript 与 Python reference 通过同一 16 个字节级向量。

## V220（🔴）保留参数与冲突优先级

- [x] 新增 ADR-0011，将 `page` / `pageSize` / `sort` 定为保留参数。
- [x] L2 拒绝静态 params 和搜索字段与保留参数冲突。
- [x] 固定 URL query、静态 params、搜索字段、Renderer 状态的合并顺序。
- [x] 固定提交、清空、翻页、排序时的页码和筛选状态转换。

## V221（🔴）跨实现一致性套件

- [x] 建立 fixture Schema、目录、runner 和 JavaScript reference。
- [x] 覆盖版本、请求、映射、搜索、reaction、Action、错误、上传和六场景（共 65 个版本化 fixtures）。
- [x] 指定生产消费者仓库与负责人：前端 `magicvr/allinme.web-client`、后端 `magicvr/allinme.core-api`，负责人均为 `magicvr`。
- [x] 两个生产仓库已实现版本协商、请求构造、响应映射、搜索表格状态、Reaction 调度、Action/error、上传执行与六个官方场景，直接消费全部 65 个共享 fixtures；正式 v1 CI 固定协议 `main` 提交 `d2f0fc0877dc6550c9fe7e3635b25c7ec72b4ddd`。前端 `6527bbff9aad1faf70e985988d4573afc7ae2e03`（Actions run `29154337497`，job `86548882378`）与后端 `d31d358f1e3c3a733ab3a7ad4282078f310d8a8f`（Actions run `29154339108`，job `86548886778`）远端 CI 均为 `completed/success`。
- [x] 两端直接消费同一版本化 fixtures，无 skip 或私有解释分支。

## V222（🟡）独立合法性 CI 门禁

- [x] 建立六场景公共 YAML 提取器和 `validate:scenarios`。
- [x] CI 独立执行根 L0-L4；MCP 测试复用同一提取器；CD 发布前执行同一门禁。
- [x] 保持 MCP build、119 tests、tools smoke 和 Docker smoke 为绿。

## V223（🟡）可复现发布

- [x] 新增 `release:check`，校验协议、包、lockfile、fixtures、迁移文档和 CHANGELOG，并输出 65 个版本化 fixtures 的确定性 digest。
- [x] 完成 `0.2` / `0.3` 到 `1.0` 迁移说明。
- [x] 全部门禁通过后原子切换 `"1.0"` / `1.0.0`；本地 `release:check`、65 fixtures 双语言 runners、六场景 L0-L4、MCP build/119 tests/tools smoke 与 `schema-ui-mcp:1.0.0` Docker smoke 全部通过，fixture digest 为 `sha256:7bf6db4ced24ed59f68a32f3072540b4be179c6aadf5c23042350e6668c66f6b`。
- [x] `v1.0.0` 指向 `d2f0fc0877dc6550c9fe7e3635b25c7ec72b4ddd`；MCP CD run `29154389128` / job `86549015732` 为 `completed/success`，远端 `1.0.0` 与 Git SHA 镜像均 smoke 通过并解析到 `sha256:190453685beded5872f24336c9b1ca1051960602f7d66d67bad6d42de40f997e`，fixture digest 为 `sha256:7bf6db4ced24ed59f68a32f3072540b4be179c6aadf5c23042350e6668c66f6b`。

## V224（🟡）清理 MCP 同目录 CommonJS 生成物

- [x] 删除 `mcp/src` / `mcp/tests` 中 7 个会遮蔽 TypeScript 的跟踪 `.js` 文件。
- [x] `.gitignore` 阻止同目录 `.js` 再次生成，构建仍只输出到 `mcp/dist/`。
- [x] MCP build、119 tests 和 tools smoke 通过。

## 当前基线

- [x] `npm --prefix mcp run build`
- [x] `npm --prefix mcp test`：3 files / 119 tests
- [x] `npm --prefix mcp run smoke:tools`
- [x] MCP 测试覆盖六个官方场景的 YAML fence
- [x] `npm run validate:scenarios`：根 L0-L4 独立消费六个官方场景
- [x] `npm --prefix mcp run smoke:docker -- schema-ui-mcp:0.3.0-rc.1`
- [x] `npm --prefix mcp run smoke:docker -- schema-ui-mcp:1.0.0`