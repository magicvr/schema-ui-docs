---
status: checklist
date: 2026-07-08
based_on: 0022-2026-07-08-review.md
---

# 审计 0022 跟踪清单

> 本清单覆盖 2 个在 0021 归档后再次全量复审发现的问题，编号 V30-V31（延续 0021 的 V28-V29 编号体系）。

## 使用说明

- `[ ]` 待修复 / `[x]` 已完成
- 每项修复完成后，在右侧填写对应文件的修改摘要和 commit hash

---

## V30（🔴）`07-actions-contract.md` 将 `table.props.actions[].key` 误写为顶层 `actions` 引用，和 `RowAction`/场景示例直接冲突

| 字段 | 值 |
|---|---|
| **位置** | `docs/07-actions-contract.md` §1；`docs/03-component-registry.md` `RowAction` 表；`docs/05-scenarios/data-table.md` 主示例 |
| **问题** | `07-actions-contract.md` 声称 `table.props.actions[].key` 按顶层 `actions` 的 id 引用，但 `RowAction.key` 仅定义为“操作标识”，场景示例也未提供对应顶层 `actions` 定义，形成执行语义冲突 |
| **操作** | 统一 `RowAction.key` 与顶层 `actions` 的关系，并同步修正文档/示例，确保 Action 引用语义全局一致 |
| **文件** | `docs/07-actions-contract.md`、`docs/03-component-registry.md`、`docs/05-scenarios/data-table.md`（是否都需要改，取决于最终统一口径） |

- [x] V30a 确认协议最终口径：`RowAction.key` 为本地操作标识，不引用顶层 `actions`；该结论与现有 `RowAction` 正文和数据表格示例保持一致。
- [x] V30b 按统一口径修改 `docs/07-actions-contract.md` §1 的“使用位置”说明：仅保留 `form.props.submitAction`、`upload.props.actionRef` 等真实 id 引用，并补充说明 `table.props.actions[].key` 不引用顶层 `actions`。
- [x] V30c 在 `docs/03-component-registry.md` 的 `RowAction.key` 说明中补充“仅供前端识别行内操作类型，不引用顶层 `actions`”限定。
- [x] V30d 核对并修正 `docs/05-scenarios/data-table.md` 主示例：保留无顶层 `actions` 的写法，并补充说明 `actions[].key` 仅为行内操作标识。

---

## V31（🟡）`02-reaction-expression.md` 权限示例误用 `permissions.visible`

| 字段 | 值 |
|---|---|
| **位置** | `docs/02-reaction-expression.md` §11.1 `$context.user` 最小字段集示例 |
| **问题** | 示例写成 `permissions.visible`，但 `01-node-protocol.md` 已定义协议标准动作键为 `view` / `edit` / `delete` |
| **操作** | 将示例中的 `visible` 改为 `view`，与权威定义保持一致 |
| **文件** | `docs/02-reaction-expression.md` |

- [x] V31a 将权限示例中的 `permissions.visible` 改为 `permissions.view`。修改文件：`docs/02-reaction-expression.md`。
- [x] V31b 复核 `01-node-protocol.md`、`02-reaction-expression.md` 其余权限示例与说明，确认不存在同类键名漂移残留。

---

## 汇总

| 编号 | 严重度 | 状态 |
|---|---|---|
| V30a | 🔴 严重 | [x] 已完成 |
| V30b | 🔴 严重 | [x] 已完成 |
| V30c | 🔴 严重 | [x] 已完成 |
| V30d | 🔴 严重 | [x] 已完成 |
| V31a | 🟡 中等 | [x] 已完成 |
| V31b | 🟡 中等 | [x] 已完成 |
