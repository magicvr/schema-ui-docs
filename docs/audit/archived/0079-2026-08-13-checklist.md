---
status: archived
date: 2026-08-13
based_on: schema-ui-protocol 2.8.0 / protocolVersion 2.8；tag v2.8.0 @ 593f625；HEAD 8ba128a（上游）、消费者 schema-ui-core f929120
scope: focus=all；主类型=发布完整性 + 实现/契约漂移（Host/App 互操作轨道 H1–H4 全量闭环）；审计轮 A-001（self）/ A-002（independent，grok build）
---

# 审计清单 0079：v2.8.0 Host/App 互操作发布闭环

## 门禁核对（A-001 self）

| # | 检查项 | 证据 | 结果 |
|---|---|---|---|
| 1 | ADR-0034–0037 accepted 且列入 semanticSpecs | `protocol-manifest.json` `authority.semanticSpecs`（593f625） | pass |
| 2 | B0/F0/C0 封闭 Schema 与 ADR 字段表逐字一致 | `docs/schemas/host-*.schema.json` + `host-conformance-claim.schema.json`；if/then 条件字段（availability mode / retry after / recovery url） | pass |
| 3 | capability registry 17 项、依赖闭包、DAG、removedIn | `docs/schemas/capability-registry.json` + C1 `validateRegistry` fixtures | pass |
| 4 | JS/Python reference 逐字段一致 | host-bootstrap 23 / host-failure 43 / host-conformance-claim 30 fixtures 双语言通过；claim digest 跨语言交叉验证一致 | pass |
| 5 | capability ID 语法增宽全量同步 | page/app-manifest Schema + 6 处 reference pattern + ADR-0034 H2 追加说明 | pass |
| 6 | 2.8 扫版完整 | 16 个算法 suite 302 cases → 2.8；version-negotiation 保留历史 +5；场景 17 文件；核心规范 frontmatter 00–10；README/overview；MCP DOC_MAP | pass |
| 7 | release-check 门禁 | release:check（421 cases、fixture digest 7aacf133… pin）/ release:check:mcp / release:check:validator 全绿；tag 模式 GITHUB_REF 校验通过 | pass |
| 8 | 制品可复现 | build + verify 双一致：content 40690917… / artifact 594207e0…；122 files | pass |
| 9 | 消费者生产证据 | GOAL-004 E-004；apps/web vitest 862；e2e 7 通过；Go handler/composition/kernel 测试通过；claim-artifact 5 门禁 | pass |
| 10 | claim 绑定 | artifact contentSha256 + fixture 树 digest + buildId 三处逐字一致（正式发布 digest 重 pin 后） | pass |
| 11 | residual 如实登记 | E-004 五条 residual（页面 2.7 reactions 子集、return intent 端到端、304 优化、session 终态来源、候选→正式绑定过程） | pass |
| 12 | 防漂移规则 6 条 | §5 逐条核对（无平行权威、document 无 secret、不按后端 code 驱动、无 partial 冒充、reserve 无空 schema、proposed 不入 manifest） | pass |

## A-001 self 发现问题（已关闭）

| ID | 严重级 | 问题 | 处置 |
|---|---|---|---|
| V375 | P2 | 消费者 boot.ts 未把 bootstrap document 的 `retryAfterSeconds` 传播进 maintenance 终态的 `retry`（ADR-0035 D2/D7 语义不完整） | 已修复（消费者 f929120）：maintenance 带 retryAfterSeconds → `after`（重建完整实例），缺省 manual；rate-limited/timeout/offline/unavailable → manual；forbidden/protocol-rejected → none（对齐 ADR-0036 D4 默认） |
| V376 | P2 | 消费者 kernel `core.manifest-route` 的 Contributions.Routes 未登记新 bootstrap 路由（模块注册表与实现漂移） | 已修复（消费者 f929120） |
| V377 | P3 | 消费者 E-004 residual 未登记「session adapter 尚无 reauth-required/locked 生产来源」 | 已修复（消费者 f929120，residual #5） |
| V378 | P3 | B1 对 `bootstrapVersion !== "1.0"` 的防御性重检路径无 fixture（B0 const 已结构性覆盖；新增 fixture 将改变已发布 digest，不做发布后改动） | 记录不修（P3）；下一 MINOR 增补用例时随行 |

## 待办

- [x] A-001 self 清单与本表落盘
- [x] A-002 independent（grok build，grok 4.5，reasoning high）执行并合并意见（首轮 F1–F7 全部处置；复核轮全数 Pass，BLOCKING_COUNT=0）
- [x] 阻断性意见归零后归档 0079
