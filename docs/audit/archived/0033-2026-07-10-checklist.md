---
status: checklist
date: 2026-07-10
based_on: 0033-2026-07-10-review.md
---

# 审计 0033 跟踪清单

## V79（🟡）`upload.props.actionRef` 的 `actions.upload` 能力声明提示未同步到入口文档

| 字段 | 值 |
|---|---|
| **位置** | `docs/03-component-registry.md`；`docs/schemas/component-registry.json`；`docs/07-actions-contract.md` |
| **问题** | L2 已检查 `actions.upload` 能力声明，但组件文档/DSL/action 入口文档缺少提示 |
| **操作** | 在组件表格、DSL description 和 upload action 说明中追加 `requiredCapabilities` 提示 |
| **文件** | `docs/03-component-registry.md`；`docs/schemas/component-registry.json`；`docs/07-actions-contract.md` |

- [x] V79a 在 `docs/03-component-registry.md` upload 组件的 `actionRef` 行追加"使用时页面必须声明 `meta.requiredCapabilities: [actions.upload]`"。
- [x] V79b 在 `docs/schemas/component-registry.json` upload `actionRef` 的 description 中同步追加该提示。
- [x] V79c 在 `docs/07-actions-contract.md` §7 开头补充"使用 `upload.props.actionRef` 时页面必须声明 `actions.upload`"。
- [x] V79d 执行验证：确认 `03-component-registry.md` 渲染后提示可见；`git diff --check`。
- 完成记录：已同步更新 upload 组件表格、组件注册 DSL 和 upload action 入口说明；验证执行 `git diff --check`、`npm --prefix mcp run build`、`npm --prefix mcp test`。

---

## V80（🟡）`03-component-registry.md` 作用域说明将 `permissions` 错误地列为可通过 `scope` 声明作用域

| 字段 | 值 |
|---|---|
| **位置** | `docs/03-component-registry.md`「作用域说明」段落；`docs/decisions/0004-row-level-scope.md` D2 |
| **问题** | 文档写 `visibleWhen`/`reactions`/`permissions` 均可通过 `scope` 声明，但 `permissions` 是 map 不支持 `scope` |
| **操作** | 从作用域说明中移除 `permissions`，并同步 ADR-0004 对应措辞 |
| **文件** | `docs/03-component-registry.md`；`docs/decisions/0004-row-level-scope.md` |

- [x] V80a 在 `docs/03-component-registry.md` 作用域说明中将"`visibleWhen`/`reactions`/`permissions`"改为"`visibleWhen`/`reactions`"，并补充说明 `permissions` 固定为 `$context.*` 不参与 `scope`。
- [x] V80b 在 `docs/decisions/0004-row-level-scope.md` D2 中补充或修订：`permissions` 的作用域固定为 `$context.*`，通过 `scope` 声明的部分仅适用于 `visibleWhen`/`reactions`。
- [x] V80c 执行验证：`git diff --check`；确认修改后 `03-component-registry.md` 渲染自然。
- 完成记录：已统一组件正文与 ADR-0004 对 `permissions` 的定位为 `$context.*` 固定权限语义；验证执行 `git diff --check`、`npm --prefix mcp run build`、`npm --prefix mcp test`。

---

## V81（🟡）权限扩展动作键声明机制仅存于文档要求，无执行入口

| 字段 | 值 |
|---|---|
| **位置** | `docs/01-node-protocol.md`；`docs/schemas/node.schema.json`；`docs/decisions/0003-context-namespace-and-visible-when.md` |
| **问题** | 文档要求扩展键"需在组件契约中声明"，但注册表无声明入口，L2 也不校验 |
| **操作** | 采用选项 A：删除"需在组件契约中声明"要求，改为由项目级 CI/Review 约束 |
| **文件** | `docs/01-node-protocol.md`；`docs/schemas/node.schema.json`；`docs/decisions/0003-context-namespace-and-visible-when.md` |

- [x] V81a 决策：选择选项 A（放宽要求，删除"需在组件契约中声明"）。
- [x] V81b 按决策结果同步修改 `docs/01-node-protocol.md` §3.9 中扩展键说明。
- [x] V81c 按决策结果同步修改 `docs/schemas/node.schema.json` `Permissions.additionalProperties` description。
- [x] V81d 按决策结果同步修改 `docs/decisions/0003-context-namespace-and-visible-when.md` D5 中扩展键说明。
- [x] V81e 若选 B，补充 `component-registry.json` 组件级 `allowedPermissionKeys` 字段与 L2 校验。（选项 A，不适用）
- [x] V81f 执行验证：`git diff --check`。
- 完成记录：已将扩展权限键口径统一为“协议层不限制键名，项目侧自行约束”，避免继续声称存在未实现的组件契约声明入口；验证执行 `git diff --check`、`npm --prefix mcp run build`、`npm --prefix mcp test`。

---

## V82（🟡）`protocol.get_doc` 示例 docId 与实际白名单不一致

| 字段 | 值 |
|---|---|
| **位置** | `docs/mcp/v1-design.md` §3.2 |
| **问题** | 示例写 `"docId": "02-reaction-expression"`，白名单实际为 `reaction-expression` |
| **操作** | 同步示例 docId 为白名单值 |
| **文件** | `docs/mcp/v1-design.md` |

- [x] V82a 将 `docs/mcp/v1-design.md` §3.2 示例中的 `"02-reaction-expression"` 改为 `"reaction-expression"`。
- [x] V82b 执行验证：`git diff --check`；确认单个修改即可消除矛盾（白名单表已正确）。
- 完成记录：已修正文档示例，使 `protocol.get_doc` 输入样例与实现白名单一致；验证执行 `git diff --check`、`npm --prefix mcp run build`、`npm --prefix mcp test`。

---

## V83（🟡）`protocol.get_doc` 截断时未返回"章节建议"

| 字段 | 值 |
|---|---|
| **位置** | `mcp/src/core/protocol-index.ts`；`docs/mcp/v1-design.md` §3.2 |
| **问题** | 设计文档承诺截断时返回"章节建议"，实现只返回 `truncated: true` |
| **操作** | 在 `getDoc()` 返回中补充 `availableSections` 字段，同步文档与测试 |
| **文件** | `mcp/src/core/protocol-index.ts`；`docs/mcp/v1-design.md`；`mcp/tests/search.test.ts` 或新增 `mcp/tests/docs.test.ts` |

- [x] V83a 修改 `mcp/src/core/protocol-index.ts` `getDoc()`：截断时在返回对象中附加 `availableSections` 为当前文档的 section 标题数组。
- [x] V83b 同步 `docs/mcp/v1-design.md` §3.2 输出描述，增加 `availableSections` 字段说明。
- [x] V83c 补充单测：通过临时注入超大白名单文档，断言 `truncated: true` 时 `availableSections` 非空且为字符串数组。
- [x] V83d 执行验证：`npm --prefix mcp run build`；`npm --prefix mcp test -- search.test.ts`。
- 完成记录：已为 `protocol.get_doc` 增加 `availableSections` 输出，并用临时大文档测试覆盖截断分支，避免断言依赖仓库正文体积；验证执行 `git diff --check`、`npm --prefix mcp run build`、`npm --prefix mcp test`。

---

## V84（🟢）MCP v1 设计文档 frontmatter 仍为 `status: draft`

| 字段 | 值 |
|---|---|
| **位置** | `docs/mcp/v1-design.md` frontmatter |
| **问题** | 已发布接口文档仍标记为 draft，与 v1-implementation-plan.md（implemented）和 README（正常引用）不一致 |
| **操作** | 将 frontmatter 状态调整为 `stable` |
| **文件** | `docs/mcp/v1-design.md` |

- [x] V84a 将 `docs/mcp/v1-design.md` frontmatter 中的 `status: draft` 改为 `status: stable`。
- [x] V84b 执行验证：`git diff --check`。
- 完成记录：已将 MCP v1 设计文档状态调整为 `stable`，与当前 README / 实施计划角色一致；验证执行 `git diff --check`、`npm --prefix mcp run build`、`npm --prefix mcp test`。

---

## 汇总

| 编号 | 严重度 | 主题 | 涉及文件 |
|---|---|---|---|
| V79 | 🟡 | upload.props.actionRef 能力声明提示未同步 | 3 份入口文档/DSL |
| V80 | 🟡 | permissions scope 表述残留 | 2 份（组件文档 + ADR） |
| V81 | 🟡 | 权限扩展键声明机制缺口 | 3 份（核心协议 + Schema + ADR）；已按选项 A 处理 |
| V82 | 🟡 | get_doc 示例 docId 不一致 | 1 份（MCP 设计文档） |
| V83 | 🟡 | get_doc 截断缺章节建议 | 2 份（MCP 实现 + 设计文档） |
| V84 | 🟢 | v1-design.md 状态 draft | 1 份（MCP 设计文档 frontmatter） |

## 建议验证命令（修复后）

```bash
# 文档修改验证
git diff --check

# MCP 构建与测试（影响 V83）
npm --prefix mcp run build
npm --prefix mcp test

# 全链路校验
npm run validate -- "pages/**/*.yaml" 2>/dev/null || echo "无需 pages 目录时跳过"
```
