---
status: checklist
date: 2026-07-08
based_on: 0016-2026-07-08-review.md
---

# 审计 0016 跟踪清单

> 本清单覆盖 4 个在 0015 归档后再次全量复审发现的问题，编号 V14-V17（延续 0015 的 V9-V13 编号体系）。

## 使用说明

- `[ ]` 待修复 / `[x]` 已完成
- 每项修复完成后，在右侧填写对应文件的修改摘要和 commit hash

---

## V14（🔴）`text` 组件的 `content` / `contentKey` 必填关系在 DSL 内部自相矛盾

| 字段 | 值 |
|---|---|
| **位置** | `schemas/component-registry.json` `text.props.content` 与 `text.anyOf` |
| **问题** | `content.required: true` 与组件级 `anyOf(content/contentKey)` 同时存在，导致只提供 `contentKey` 是否合法出现冲突 |
| **操作** | 保留组件级 `anyOf` 作为二选一约束，将 props 级 `content.required` 改为 `false` |
| **文件** | `docs/schemas/component-registry.json` |

- [x] V14a 将 `text.props.content.required` 改为 `false`
- [x] V14b 确认 `text.anyOf` 仍表达 `content` / `contentKey` 至少一个
- [x] V14c 核对 `03-component-registry.md` 中 `content / contentKey` 必填表述与 DSL 一致

---

## V15（🟡）`scope: row` 下 `dependencies` 的语义在 reaction schema 中仍偏向 `$deps.*`

| 字段 | 值 |
|---|---|
| **位置** | `schemas/reaction.schema.json` `dependencies.description` |
| **问题** | 描述强调 `$deps.*` 必须列出，但未说明 `scope: row` 下 `dependencies` 声明的是 `$row.*` 行内字段依赖 |
| **操作** | 补充 `scope: form` / `scope: row` 分支语义，并明确 `scope: row` 不注入表单级 `$deps` |
| **文件** | `docs/schemas/reaction.schema.json` |

- [x] V15a 更新 `dependencies.description`，明确 `scope: form` 对应 `$deps.*`
- [x] V15b 更新 `dependencies.description`，明确 `scope: row` 对应 `$row.*` 行内字段路径
- [x] V15c 与 `03-component-registry.md` / `05-scenarios/data-table.md` 中 `visibleField` 展开示例交叉核对

---

## V16（🟡）Renderer expr-eval 示例把 `contains` 表达成函数模型

| 字段 | 值 |
|---|---|
| **位置** | `08-renderer-spec.md` §5.2 最小集成示例与 §5.4 安全约束 |
| **问题** | 示例使用 `parser.functions.contains`，安全约束表写“除 `contains` 外不允许任何函数调用”，与协议中 `contains` 是二元运算符的语义冲突 |
| **操作** | 区分协议语法与底层库适配，删除函数调用例外表述 |
| **文件** | `docs/08-renderer-spec.md` |

- [x] V16a 修改示例注释，明确协议语法是 `a contains b` 运算符而非 `contains(a, b)` 函数
- [x] V16b 将 `parser.functions.contains` 相关示例改为实现适配说明，或替换为不引入函数调用例外的伪代码
- [x] V16c 将安全约束表中的“除 `contains` 外不允许任何函数调用”改为“不允许任何函数调用”

---

## V17（🟡）Renderer 最小集成示例未覆盖当前协议的完整变量白名单

| 字段 | 值 |
|---|---|
| **位置** | `08-renderer-spec.md` §5.2 最小集成示例 |
| **问题** | 示例主要覆盖 `$deps.*` / `$context.*`，未说明 `$self` / `$row.*` / `$parentRow.*` 由表达式位置和 `scope` 决定如何注入 |
| **操作** | 补充示例边界说明，或增加 `scope: row` 求值上下文示例 |
| **文件** | `docs/08-renderer-spec.md` |

- [x] V17a 在最小集成示例中标注当前代码覆盖 form/context 场景
- [x] V17b 补充 `$self` / `$row` / `$parentRow` 应由调用方按表达式位置注入的说明
- [x] V17c 与 `02-reaction-expression.md` 变量可见性矩阵交叉引用或核对

---

## 汇总

| 编号 | 严重度 | 类型 | 状态 |
|------|--------|------|------|
| V14 | 🔴 | DSL 约束冲突 | [x] |
| V15 | 🟡 | Schema 描述歧义 | [x] |
| V16 | 🟡 | Renderer 示例语义漂移 | [x] |
| V17 | 🟡 | Renderer 示例覆盖不足 | [x] |

> **全部项目完成后，** 将本审计文件移入 `audit/archived/` 并在 `archived/README.md` 添加索引。

---

*生成时间：2026-07-08*