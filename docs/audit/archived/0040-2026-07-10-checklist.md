---
status: checklist
date: 2026-07-10
based_on: 0040-2026-07-10-review.md
---

# 审计 0040 跟踪清单

## V125（🟡）统一组件 DSL 嵌套对象的封闭字段策略

- [x] V125a 盘点 `component-registry.json` 中所有带 `properties` 的嵌套 object/array item，区分封闭协议对象与开放业务字典。
- [x] V125b 为 `tabs.props.items[]`、`table.props.pagination`、`select.props.options[]` 等封闭对象补充 `additionalProperties: false`。
- [x] V125c 明确 DSL 默认开放/封闭策略；解释器已执行显式标记，无需修改 `validateNestedObject()`，`tagMap` 动态业务键保持开放而映射项仍封闭。
- [x] V125d 更新 `docs/03-component-registry.md` 与 `docs/06-validation.md` 的 DSL 嵌套对象说明。
- [x] V125e 增加 MCP/L2 反例：`pagination.unexpected`、`tabs.items[].unexpected`、`select.options[].unexpected` 均失败；合法正例继续通过。
- [x] V125f 执行根 L2/统一入口聚焦验证、`npm --prefix mcp test`，并记录修改文件与验证结果。
- 完成记录：修改 `docs/schemas/component-registry.json`、`docs/03-component-registry.md`、`docs/06-validation.md` 与 `mcp/tests/validate-content.test.ts`。三个最小 L2 反例均按预期拒绝；四个官方场景 L0-L4 通过；MCP 全量 55 项测试通过。

## V126（🟡）闭合 MCP 临时目录创建异常边界

- [x] V126a 将临时目录创建纳入 `validateContent()` 的结构化异常捕获。
- [x] V126b 确保仅在临时目录创建成功后执行清理，清理失败仍保持 best-effort 且不覆盖校验结果。
- [x] V126c 对外返回不含 Node 堆栈和内部路径细节的 `internalError`，保持 ADR-0007 D11 字段结构。
- [x] V126d 增加可控的临时目录创建失败测试，断言函数不抛异常且 `passed: false` / `internalError` 非空。
- [x] V126e 核对 `docs/mcp/v1-design.md` 与 ADR-0007，实现仅补齐既定口径，无需修改文档。
- [x] V126f 执行 `npm --prefix mcp run build`、`npm --prefix mcp test`、`npm --prefix mcp run smoke:tools`，并记录结果。
- 完成记录：修改 `mcp/src/core/validation-runner.ts` 与 `mcp/tests/validate-content.test.ts`，临时目录创建器可在测试中注入；创建失败现在返回结构化 `internalError`，清理仅在目录已创建后执行。MCP build、55 项测试与 tools smoke 均通过。

## V127（🟡）统一 MCP Node 运行时下限与编译产物语法

- [x] V127a 选择 A：继续支持 Node 18，并移除编译产物中的不兼容 JSON import attributes 语法。
- [x] V127b 使用 `createRequire(import.meta.url)` 读取 `mcp/package.json` 版本，保持 `engines.node: >=18`。
- [x] V127c 核对根 `README.md`、`docs/mcp/README.md`、`docs/mcp/v1-design.md` 与 ADR-0007；运行时下限未改变，无需修改文档。
- [x] V127d 使用 Node 18.19 执行完整 tools smoke，避免只依赖 Node 24 Docker 镜像。
- [x] V127e 执行 `npm --prefix mcp run build`、`npm --prefix mcp test`、`npm --prefix mcp run smoke:tools`。
- [x] V127f 使用 Node 18.19 启动编译产物并完成五工具 smoke。
- 完成记录：修改 `mcp/src/server.ts`，并在 `docs/CHANGELOG.md` Unreleased 记录 0040 修复。Node 18.19 与当前 Node 均成功列出五个冻结工具名；MCP build 和 55 项测试通过。

## V128（🟢）回填 0001/0002 归档清单终态

- [x] V128a 根据 0004/0005 已记录的历史事实，将 0001 C1/C2/C3 标记完成并注明 ADR-0003/0004 去向。
- [x] V128b 将 0001 C4 标记为未来触发型终态，注明当前不实现的决策依据，不改写原始问题。
- [x] V128c 将 0002 E1 标记完成并注明 ADR 已产出。
- [x] V128d 扫描全部 `docs/audit/archived/*-checklist.md`，确认不再存在无解释的 `[ ]`。
- [x] V128e 核对 `docs/audit/archived/README.md` 的“全部已完成”描述与清单状态一致。
- 完成记录：修改归档 0001/0002 checklist；C1-C3/E1 标记完成，C4 使用既有 `[-]` 状态记录为未来触发型议题。全量归档清单扫描无裸 `[ ]`。

## V129（🟢）修复归档报告相对链接

- [x] V129a 修正 0013 review 的 `CHANGELOG.md` 相对路径。
- [x] V129b 修正 0014 review 的归档索引相对路径。
- [x] V129c 修正 0021/0026 review 的表达式规范相对路径，并在 0026 保留“原 §13、现 §14”的历史语义。
- [x] V129d 修正 0025 review 的三条校验脚本相对路径。
- [x] V129e 执行全部 `docs/**/*.md` 相对链接目标检查，确认不存在失效目标。
- [x] V129f 记录修改文件与链接检查结果。
- 完成记录：修改 0013、0014、0021、0025、0026 五份归档 review，共修复 7 个链接；全部文档相对链接目标检查通过。

## 汇总

| 编号 | 严重度 | 主题 | 状态 |
|---|---|---|---|
| V125 | 🟡 | 组件 DSL 嵌套对象放行未知字段 | ✅ |
| V126 | 🟡 | MCP 临时目录异常绕过 `internalError` | ✅ |
| V127 | 🟡 | Node `>=18` 声明与产物语法不兼容 | ✅ |
| V128 | 🟢 | 0001/0002 归档清单终态未回填 | ✅ |
| V129 | 🟢 | 归档报告存在 7 个失效相对链接 | ✅ |

## 建议验证命令（修复后）

```powershell
# V125 聚焦反例与完整校验链
npm run validate -- <file-or-glob>
npm run validate:l2 -- <file-or-glob>

# MCP 构建、测试与工具 smoke
npm --prefix mcp run build
npm --prefix mcp test
npm --prefix mcp run smoke:tools

# V127：按最终声明的最低 Node 版本启动 dist/server.js 或运行 tools smoke
<minimum-supported-node> mcp/dist/server.js

# 文档与审计证据链
# 扫描归档 checklist 的空复选框，并检查 docs/**/*.md 相对链接目标

git diff --check
```

## 最终验证结果

- 四个官方场景通过根统一入口 L0-L4。
- `npm --prefix mcp run build`：通过。
- `npm --prefix mcp test`：通过，3 个测试文件、55 项测试全部通过。
- `npm --prefix mcp run smoke:tools`：通过。
- Node 18.19 执行 `mcp/tests/smoke-tools.mjs`：通过。
- 全部归档 checklist 无裸 `[ ]`，全部 `docs/**/*.md` 相对链接目标存在。
- VS Code Problems：无错误。
- `git diff --check`：无空白错误；仅输出工作区既有 LF/CRLF 转换提示。
- Docker smoke：未执行；本轮未修改 Dockerfile 或镜像使用文档，当前 Node 与最低支持 Node 的 stdio tools smoke 已覆盖启动行为。
