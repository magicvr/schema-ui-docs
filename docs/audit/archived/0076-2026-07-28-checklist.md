---
status: archived
date: 2026-07-28
based_on: 0076-2026-07-28-review.md
scope: focus=all；主类型=实现/契约漂移；次类型=MCP lock / 发布门禁卫生
---

# 审计 0076 跟踪 Checklist

> 本 checklist 跟踪 v2.6.0 后 MCP lock `file:..` 父包版本与 `mcp-release-check` 门禁（V362）。  
> 生产消费者仅作 out-of-repo 跟踪；未经单独授权，不修改 `allinme.web-client` 或 `allinme.core-api`。  
> 不把本审计路径加入 `protocol-manifest.json`、协议制品、MCP 搜索索引或 Docker 运行镜像。

## 汇总

| 编号 | 级别 | 主题 | 状态 |
|---|---|---|---|
| V362 | 🟢 P2 | MCP lock `packages[".."]` 仍为 2.5.3；`mcp-release-check` 未断言 | **已关闭** |

**计数：** 0×P0 + 0×P1 + **1×P2** + 0×P3 **全部关闭**。  
**问题号：** 全局续编 `V361` → `V362`。  
**审计号：** `0076`（archived）。

## 基线

- [x] 创建前确认无活跃 audit；全局最大审计号为 `0075`（archived）。
- [x] 确认 `V362` 位于全局最大 `V361` 之后，且不使用历史冲突的 V1–V4。
- [x] 记录正式 `v2.6.0` content `a2c6b4a4…` / artifact `e132d225…` / fixture `3a9cd233…`。
- [x] 记录工作树 content `5999720a…` / artifact `d561af31…`（0075 informative 分叉；不强制 PATCH）。
- [x] 步骤 1 本地全量门禁绿（含 conformance 35/35、MCP 134、制品链接、GHCR smoke）。
- [x] 修复后重跑受影响门禁并勾选关闭证据。
- [x] 用户确认后归档（步骤 3）。

---

## V362 — MCP lock 父包版本残留 2.5.3；release 门禁漏检（P2）— 已关闭

**优先：** 单独立项；不改协议制品语义。

- [x] `mcp/package-lock.json`：`packages[".."].version` 自 `2.5.3` → `2.6.0`（与根 `package.json` 一致）
- [x] `scripts/mcp-release-check.js`：断言 `packages[".."]` 存在，且 `name` / `version` 等于根 `package.json`
- [x] 确认 `packages['']` / lock root 仍为 MCP `2.6.0`；运行时 `npm ls` 仍为 workspace@2.6.0
- 验证命令：
  - `npm run release:check:mcp` → 通过
  - `node -e "…"` → `packages[".."].version === "2.6.0"`
- 关闭证据：
  - lock 仅 1 行版本字段变更；门禁断言已落地
  - **不**改写正式 `v2.6.0` / `mcp-v2.6.0` tag
  - **不**进入协议制品 contentDigest（`scripts/**` 与 `mcp/**` 均在 manifest excluded）

---

## 归档门禁

- [x] `review` / `checklist` 移入 `docs/audit/archived/`
- [x] frontmatter `status: archived`
- [x] `docs/audit/archived/README.md` 顶部登记 0076
- [x] `docs/audit/README.md` 重置为无活跃审计，更新「最近归档」
- [x] `git diff --check` 与受影响门禁最终确认
