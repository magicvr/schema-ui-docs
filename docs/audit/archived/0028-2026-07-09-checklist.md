---
status: checklist
date: 2026-07-09
based_on: 0028-2026-07-09-review.md
---

# 审计 0028 跟踪清单

## V49（🔴）`DataRef` 的“三选一”来源语义未被 Schema 强制，且额外字段未被禁止

| 字段 | 值 |
|---|---|
| **位置** | `docs/01-node-protocol.md` §3.3；`docs/schemas/node.schema.json` `definitions.DataRef` |
| **问题** | 正文声明 `source: static/ref/api` 是三选一来源形态，但 Schema 未禁止不同来源字段混用，也未禁止未知额外字段 |
| **操作** | 明确 `DataRef` 各来源允许字段集合，并同步 Schema 与正文 |
| **文件** | `docs/01-node-protocol.md`；`docs/schemas/node.schema.json`；必要时补充校验反例 |

- [x] V49a 决定 `source: ref` 是否允许携带本地 `responseMapping` 覆盖引用数据源，并在正文中写清。处理：`source: ref` 仅允许 `ref`，并可在引用目标为 API 数据源时携带本地 `responseMapping` 覆盖响应解析；不允许混入 `url` / `method` / `params` / `value`。
- [x] V49b 为 `DataRef` 补充 `additionalProperties: false`。修改：`docs/schemas/node.schema.json` 已补充。
- [x] V49c 为 `static` / `api` / `ref` 分支补充互斥字段约束，拒绝来源字段混用。修改：`DataRef.oneOf` 分支分别限制来源字段集合。
- [x] V49d 用最小反例验证：`source: static` 携带 `url` / `params` 会被 L0/L1 拒绝；合法 `static` / `api` / `ref` 仍通过。验证：AJV 内存样例确认非法 mixed static 被拒绝，合法 static/ref 通过。

---

## V50（🟡）`data.params` 中 `$deps.*` 的作用域规则未进入表达式规范的使用位置与变量矩阵

| 字段 | 值 |
|---|---|
| **位置** | `docs/02-reaction-expression.md` §1 / §2 / 附录 A；`docs/04-datasource-contract.md` §3.2；`docs/08-renderer-spec.md` §2.3；`docs/06-validation.md` §1 |
| **问题** | 04/08 定义了 `data.params` 的 `$deps.*` 作用域边界，但 02 的使用位置与变量矩阵没有覆盖，06 也未明确校验归属 |
| **操作** | 将 `data.params` 纳入变量可见性矩阵和校验层级说明 |
| **文件** | `docs/02-reaction-expression.md`；`docs/06-validation.md`；必要时同步校验脚本 |

- [x] V50a 在 `02-reaction-expression.md` §1 使用位置表中补充 `data.params`。修改：新增“数据请求参数”行。
- [x] V50b 在变量命名空间说明或附录 A 中补充 `data.params` 的 `$deps.*` 可见性规则。修改：补充参数值替换说明和表单/非表单 `data.params` 矩阵行。
- [x] V50c 在 `06-validation.md` 中明确 `data.params` 的 `$deps.*` 静态作用域检查属于哪一层校验。修改：L3a 描述补充 `data.params` 作用域检查。
- [x] V50d 如校验工具尚未覆盖，补充最小反例验证：独立 `chart.data.params` 使用 `$deps.*` 应被拒绝，表单内子节点使用 `$deps.*` 应通过。修改：`scripts/validate-l3a-expressions.js` 扫描 `data.params`；验证：非法独立 chart 返回 `NON_FORM_DATA_PARAMS`，表单内 table 通过。

---

## V51（🟡）ADR-0004 仍使用旧的 `columns[].key` / `$row[column.key]`，与当前 `ColumnDef.field` 不一致

| 字段 | 值 |
|---|---|
| **位置** | `docs/decisions/0004-row-level-scope.md` D1 / D2b |
| **问题** | Accepted ADR 仍使用早期 `columns[].key` 示例和 `$row[column.key]` 语义，但当前协议字段为 `field` |
| **操作** | 将 ADR 中旧字段名同步为当前协议字段名 |
| **文件** | `docs/decisions/0004-row-level-scope.md` |

- [x] V51a 将 ADR-0004 示例中的 `key: status` 改为 `field: status`。修改：`docs/decisions/0004-row-level-scope.md` 示例已同步。
- [x] V51b 将 `$row[column.key]` 改为 `$row[column.field]`。修改：`$self` 语义描述已同步。
- [x] V51c 全文搜索确认 ADR-0004 不再残留 `columns[].key` 语义。验证：已搜索 `column.key` / `key: status` / `$row[column.key]`，无残留。

---

## V52（🟡）`tagMap` 正文要求 `{text, tone}`，但组件 DSL 未要求 `text` 且未禁止额外字段

| 字段 | 值 |
|---|---|
| **位置** | `docs/03-component-registry.md` `ColumnDef`；`docs/schemas/component-registry.json` `tagMap` |
| **问题** | 正文描述 `tagMap` 值为 `{text, tone}`，但 DSL 接受缺少 `text` 或带额外字段的映射项 |
| **操作** | 收敛 `tagMap` 映射项的最小结构 |
| **文件** | `docs/schemas/component-registry.json`；必要时 `docs/03-component-registry.md` |

- [x] V52a 决定 `tagMap` 映射项中 `text` 是否必填；建议按当前正文设为必填。处理：按正文 `{text, tone}` 口径，`text` 必填，`tone` 可选。
- [x] V52b 在 `component-registry.json` 中为 `tagMap` 映射项补充 `required: ["text"]`。
- [x] V52c 在 `component-registry.json` 中为 `tagMap` 映射项补充 `additionalProperties: false`。
- [x] V52d 用最小样例验证：缺少 `text` 的 `tagMap` 项被 L2 拒绝，合法 `{ text, tone }` 通过。验证：临时样例已覆盖非法/合法两种情况。

---

## V53（🟡）`action.schema.json` 未按 `type` 禁止不相关字段，机器契约接受混合 Action

| 字段 | 值 |
|---|---|
| **位置** | `docs/07-actions-contract.md` §3-§8；`docs/schemas/action.schema.json` |
| **问题** | 各 action 类型的字段在 Schema 顶层共享，只补条件必填，未禁止当前类型不相关字段 |
| **操作** | 将 Action Schema 收敛为按类型隔离字段的结构 |
| **文件** | `docs/schemas/action.schema.json`；必要时补充说明或反例 |

- [x] V53a 选择实现方式：`oneOf` 类型分支或现有 `if/then` 上补 `not` 约束。处理：采用 `oneOf` + `RequestAction` / `NavigateAction` / `ModalAction` / `UploadAction` / `CustomAction` 分支。
- [x] V53b 确保 `request`、`navigate`、`modal`、`custom`、`upload` 各自只接受本类型字段。修改：各分支均设置 `additionalProperties: false`。
- [x] V53c 保留 `OutcomeBehavior` 的 `toast.message` / `navigate.url` 条件必填语义。修改：保留原 `OutcomeBehavior` `allOf` 条件必填。
- [x] V53d 用最小反例验证：`navigate` 携带 `bodyMapping` / `fieldName` 被 L0/L1 拒绝；合法各类型 action 仍通过。验证：AJV 内存样例确认混合 navigate 被拒绝，合法 request/upload 通过；全链路正例覆盖 request/navigate/modal/upload/custom。

---

## V54（🟢）`04-datasource-contract.md` 元数据与内部章节引用未随 0027 修复同步

| 字段 | 值 |
|---|---|
| **位置** | `docs/04-datasource-contract.md` frontmatter / §3.1 |
| **问题** | 0027 已修改该文件，但 `last_updated` 仍为 2026-07-08；§3.1 中 `select.optionsSource.params` 仍指向旧 §8 |
| **操作** | 同步文档元数据和章节引用 |
| **文件** | `docs/04-datasource-contract.md` |

- [x] V54a 将 frontmatter `last_updated` 更新为 `2026-07-10`。修改：按当前实际修复日期同步。
- [x] V54b 将 §3.1 中 `select.optionsSource.params` 的引用从 §8 修正为 §9，或改为稳定锚点链接。修改：已修正为 §9。
- [x] V54c 检查同文件是否还有 `select.optionsSource` 旧章节号引用。验证：已搜索 `select.optionsSource.params.*§8`，无残留。

---

## 汇总

| 编号 | 严重度 | 状态 |
|---|---|---|
| V49 | 🔴 | [x] 已修复并验证 |
| V50 | 🟡 | [x] 已修复并验证 |
| V51 | 🟡 | [x] 已修复并验证 |
| V52 | 🟡 | [x] 已修复并验证 |
| V53 | 🟡 | [x] 已修复并验证 |
| V54 | 🟢 | [x] 已修复并验证 |