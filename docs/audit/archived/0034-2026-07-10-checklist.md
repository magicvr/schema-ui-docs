---
status: checklist
date: 2026-07-10
based_on: 0034-2026-07-10-review.md
---

# 审计 0034 跟踪清单

## V85（🔴）补齐 `submitAction` / `upload.props.actionRef` 的 L2 引用完整性校验

| 字段 | 值 |
|---|---|
| **位置** | `scripts/validate-l2-components.js`；`docs/06-validation.md`；`mcp/tests/**/*` |
| **问题** | L2 已校验 `RowAction.actionRef`，但不校验 `form.props.submitAction` 是否存在，也不校验 `upload.props.actionRef` 是否存在且目标为 `type: upload` |
| **操作** | 扩展 L2 页面级 action 引用扫描，同步校验文档与 MCP 反例测试 |
| **文件** | `scripts/validate-l2-components.js`；`docs/06-validation.md`；`mcp/tests/validate-content.test.ts`；`mcp/tests/test-utils.ts` |

- [x] V85a 在 `scripts/validate-l2-components.js` 增加 action 引用扫描：`form.props.submitAction` 必须存在于 `doc.actions`。
- [x] V85b 同一扫描中校验 `upload.props.actionRef`：目标必须存在，且 `type` 必须为 `upload`。
- [x] V85c 遍历范围覆盖 `body`、`tabs.items[].content`、`actions[].type: modal` 的 `content` Node。
- [x] V85d 更新 `docs/06-validation.md` L2 描述与自检清单，写明 `submitAction` / `upload.actionRef` 的引用规则。
- [x] V85e 新增 MCP `validate_content` 反例：缺失 `submitAction` 目标、`upload.actionRef` 类型错误。
- [x] V85f 验证：
  - 构造最小 YAML 确认错误引用被 L2 拒绝、合法引用通过
  - `node scripts/validate-l2-components.js <test-yaml> --json` → 正确报告 `submitAction` / `actionRef` 违规
  - `npm --prefix mcp run build` → 通过
  - `npm --prefix mcp test` → 24/24 通过
- 完成记录：2026-07-10

---

## V86（🟡）补齐 `data.ref` / `form.props.targetTable` 的 L2 引用存在性校验

| 字段 | 值 |
|---|---|
| **位置** | `scripts/validate-l2-components.js`；`docs/06-validation.md`；`mcp/tests/**/*` |
| **问题** | 悬空 `data.ref` 与错误 `targetTable` 可通过 L2；运行时才暴露 |
| **操作** | 在 L2 增加 datasources / table id 引用存在性校验，并同步文档与测试 |
| **文件** | `scripts/validate-l2-components.js`；`docs/06-validation.md`；`mcp/tests/validate-content.test.ts`；`mcp/tests/test-utils.ts` |

- [x] V86a 在 L2 校验 `data.source: ref` 时，要求 `data.ref` 存在于 `doc.datasources`。
- [x] V86b 在 L2 校验 `form.mode: search` 的 `targetTable` 时，要求页面 Node 树存在 `id` 匹配且 `type: table` 的节点。
- [x] V86c 遍历范围与 V85 保持一致（含 modal content / tabs content）。
- [x] V86d 更新 `docs/06-validation.md` L2 描述与自检清单。
- [x] V86e 新增 MCP `validate_content` 反例：悬空 `data.ref`、错误 `targetTable`。
- [x] V86f 验证：
  - 构造最小 YAML 确认悬空引用被拒绝、合法引用通过
  - `node scripts/validate-l2-components.js <test-yaml> --json` → 正确报告 `data.ref` / `targetTable` 违规
  - `npm --prefix mcp run build` → 通过
  - `npm --prefix mcp test` → 24/24 通过
- 完成记录：2026-07-10

---

## V87（🟢）同步 0033 触达文档的 `last_updated`

| 字段 | 值 |
|---|---|
| **位置** | `docs/03-component-registry.md`；`docs/07-actions-contract.md` |
| **问题** | 0033 已修改正文，frontmatter 仍为 `2026-07-09` |
| **操作** | 更新为 `2026-07-10` |
| **文件** | `docs/03-component-registry.md`；`docs/07-actions-contract.md` |

- [x] V87a 更新 `docs/03-component-registry.md` `last_updated: 2026-07-10`。
- [x] V87b 更新 `docs/07-actions-contract.md` `last_updated: 2026-07-10`。
- [x] V87c 若本轮修改 `docs/06-validation.md`，确认其 `last_updated` 保持正确（当前为 `2026-07-10`）。
- [x] V87d 验证：`git diff --check`（无尾随空白/冲突标记）。
- 完成记录：2026-07-10

---

## 汇总

| 编号 | 严重度 | 主题 | 涉及文件 |
|---|---|---|---|
| V85 | 🔴 | submitAction / upload.actionRef 引用完整性 | L2 脚本 + 06 文档 + MCP 测试 |
| V86 | 🟡 | data.ref / targetTable 引用存在性 | L2 脚本 + 06 文档 + MCP 测试 |
| V87 | 🟢 | last_updated 元数据漂移 | 2 份协议正文 frontmatter |

## 建议验证命令（修复后）

```bash
# L2 / 全链路（对临时反例与官方场景）
npm run validate:l2 -- "<file-or-glob>"
npm run validate -- "docs/05-scenarios/**/*.md" 2>nul || npm --prefix mcp test

# MCP 构建与测试
npm --prefix mcp run build
npm --prefix mcp test

# 文档 diff 检查
git diff --check
```
