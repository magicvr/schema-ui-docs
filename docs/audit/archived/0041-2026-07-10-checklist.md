---
status: checklist
date: 2026-07-10
based_on: 0041-2026-07-10-review.md
---

# 审计 0041 跟踪清单

## V130（🔴）闭合普通表单 GET request 的字段传输语义

- [x] V130a 记录最终决策：A）v0.2 禁止普通表单 `submitAction` 引用 GET request；或 B）通过新 ADR 定义普通 Action query 映射。
- [x] V130b 按决策同步 `docs/07-actions-contract.md` 的 RequestAction 字段与 method 约束。
- [x] V130c 同步 `docs/08-renderer-spec.md` 的普通表单 Action 执行流程，明确字段进入 body/query 的唯一规则。
- [x] V130d 在 L2 按 `form.props.submitAction` 解析目标 Action，并拒绝不可执行组合；不得影响 RowAction 已定义的 `requestMapping.query`。
- [x] V130e 更新 `docs/06-validation.md` 和 `docs/CHANGELOG.md` Unreleased。
- [x] V130f 增加 GET 表单反例、POST/PUT/PATCH 正例和行级 GET query 回归测试。
- [x] V130g 执行 `npm run validate:l2 -- <fixtures>`、统一入口和 `npm --prefix mcp test`，记录修改文件与结果。
- 完成记录：选择 A，v0.2 普通表单 request 继续采用 JSON body，不引入未定义的 queryMapping；L2 拒绝 GET，行级 GET + `requestMapping.query` 保持合法。修改 `scripts/validate-l2-components.js`、`docs/07-actions-contract.md`、`docs/08-renderer-spec.md`、`docs/06-validation.md`、`docs/CHANGELOG.md` 和 MCP 测试；GET 反例、POST/PUT/PATCH/DELETE 与行级 GET 正例全部通过预期。

## V131（🟡）拒绝非表单 visibleWhen 中无绑定的 `$self`

- [x] V131a 将非表单 `visibleWhen` 的 L3a 变量边界收紧为仅 `$context.user.*` / `$context.features.*`。
- [x] V131b 增加 `$self`、`$deps.*`、`$row.*`、`$parentRow.*` 非法反例和 `$context.*` 正例。
- [x] V131c 确认表单字段 reactions、表格列 `scope: row` 的合法 `$self` 行为不回归。
- [x] V131d 更新 `docs/06-validation.md` 并执行 L3a/MCP 聚焦测试。
- 完成记录：修改 L3a 非表单 visibleWhen 变量白名单，同步 `docs/02-reaction-expression.md` / `docs/06-validation.md`；四类非法变量与 `$context.features.*` 正例已纳入测试，dateRangePicker 和表格列 `$self` 既有测试继续通过。

## V132（🟡）按完整点路径校验 row dependencies

- [x] V132a 修改 `$row.*` 依赖提取，保留 `$row.` 后完整点路径。
- [x] V132b 明确 dependencies 采用精确路径匹配；仅声明父路径不能隐式覆盖子路径。
- [x] V132c 增加嵌套路径正例、父路径不足反例、多路径和顶层字段回归测试。
- [x] V132d 同步 `docs/02-reaction-expression.md` / `docs/06-validation.md`，执行 L3a/MCP 测试。
- 完成记录：`$row.customer.status` 现在按 `customer.status` 精确匹配 dependencies；父路径 `customer` 不再误当作完整声明，顶层字段既有回归保持通过。修改 L3a、表达式/校验文档与 MCP 测试。

## V133（🟡）递归扫描 params 数组元素

- [x] V133a 使 `scanDataParams()` 统一递归 object、array 和 scalar，输出稳定数组索引路径。
- [x] V133b 覆盖 `data.params`、`optionsSource.params`、`datasources.*.params` 三类挂载点。
- [x] V133c 增加合法字面量数组、合法表单 `$deps.*` 数组及 `$context/$row/$self/$parentRow` 反例。
- [x] V133d 更新 `docs/06-validation.md`，执行 L3a、统一入口和 MCP 测试。
- 完成记录：`scanDataParams()` 递归对象与数组并生成 `[index]` 路径；三个挂载点均有测试，合法字面量/表单 `$deps.*` 与非法变量行为已验证。同步 `docs/02-reaction-expression.md` / `docs/06-validation.md`。

## V134（🟡）使 L4 识别开放业务 map

- [x] V134a 盘点协议中的开放业务 map：DataRef params、`optionsSource.params`、RowAction `requestMapping.path/query/body` 等。
- [x] V134b 调整 L4 遍历，跳过开放 map 的业务键名判定，同时不削弱其值内部协议对象的检查。
- [x] V134c 增加 `color/width/style` 业务参数正例及真实 props/映射项内部样式键反例。
- [x] V134d 更新 `docs/06-validation.md`，执行 L4 聚焦测试和四个官方场景统一验证。
- 完成记录：L4 新增集中式开放业务 map 路径规则，DataRef/optionsSource params 与 RowAction requestMapping 的业务键不再误报；真实 props 和 tagMap 映射项内部样式键仍受检查。四个官方场景 L0-L4 通过。

## V135（🟡）拒绝静态 datasource 上的 responseMapping

- [x] V135a 在 L2 解析 `source: ref` 目标类型，静态目标声明本地 `responseMapping` 时返回明确违规。
- [x] V135b 保持 API 目标的本地优先/继承映射行为及 list/total 条件必填不回归。
- [x] V135c 同步 `docs/01-node-protocol.md`、`docs/06-validation.md` 和 `node.schema.json` 描述。
- [x] V135d 增加静态目标反例、API 目标本地映射/继承正例并执行 L2/MCP 测试。
- 完成记录：L2 在引用完整性遍历中解析 datasource 类型，拒绝静态目标本地映射；API 目标的本地优先、继承及 list/total 测试继续通过。协议正文、Schema 描述与校验文档已同步。

## V136（🟡）补齐 table.titleKey i18n 字段

- [x] V136a 在 `component-registry.json` table props 中增加可选 `titleKey`。
- [x] V136b 更新 `docs/03-component-registry.md` table 字段表及 `title` / `titleKey` 替代关系。
- [x] V136c 增加仅 `titleKey`、仅 `title` 和两者均省略的 L2 正例；核对同时声明行为与通用 i18n 约定一致。
- [x] V136d 执行 L2、统一入口和 MCP 相关测试。
- 完成记录：table DSL 与字段表新增可选 `titleKey`，不改变标题本身可省略及通用 i18n 的宽松替代行为；组件测试、MCP 测试和官方场景通过。

## V137（🟡）执行日期 min/max 格式约束

- [x] V137a 决定校验强度：仅严格 `YYYY-MM-DD` 语法，或同时验证真实日历日期；记录与“ISO 日期”措辞的对应关系。
- [x] V137b 为组件 DSL/L2 增加所需的 `pattern` 或日期专用语义校验能力。
- [x] V137c 同步 datePicker/dateRangePicker 的 `min` / `max` 机器约束和 DSL 支持关键字说明。
- [x] V137d 增加合法日期、非法格式、非法月份/日期和闰日边界测试。
- [x] V137e 更新 `docs/03-component-registry.md` / `docs/06-validation.md`，执行 L2/统一入口/MCP 测试。
- 完成记录：选择严格格式 + 真实日历日期校验；因自定义 DSL 尚不支持 `pattern`，由 L2 日期组件语义校验执行，避免声明未实现关键字。闰日 `2024-02-29` 通过，斜杠格式、非法月份/日期及非闰年 2 月 29 日均被拒绝。

## V138（🟡）限制 validate_content 完整工具响应不超过 20KB

- [x] V138a 定义结构化裁剪字段：每层总数、返回数、省略数和 `truncated` 语义，保持向后可理解。
- [x] V138b 在最终工具文本预算内裁剪违规数组，不得直接截断 JSON 字符串或破坏 UTF-8。
- [x] V138c 保留 parseError/internalError 的完整最小结构，并保证 suggested docs 与摘要和裁剪统计一致。
- [x] V138d 更新 TypeScript 返回类型、ADR-0007 D10 和 MCP v1 design。
- [x] V138e 增加高基数单层/多层违规、中文路径/消息和未超限正例，直接断言 transport 文本 UTF-8 字节数。
- [x] V138f 执行 `npm --prefix mcp run build`、`npm --prefix mcp test`、`npm --prefix mcp run smoke:tools`。
- [x] V138g 若实现影响镜像内容，构建镜像并执行 Docker smoke；否则记录未执行原因和剩余风险。
- 完成记录：最终 handler 按层公平裁剪，新增 `layerStats.total/returned/omitted` 与 `truncated`，每个违规层至少保留一项；字符串按 UTF-8 安全压缩兜底。300 条单层违规和 3×120 条中文多层违规响应均不超过 20KB。MCP build、76 项测试、tools smoke、`schema-ui-mcp:audit-0041` Docker build/smoke 通过，测试镜像已删除。

## V139（🟡）区分合法根标量与输入解析失败

- [x] V139a 让预解析返回解析值，并在 runner 中识别非普通对象根值。
- [x] V139b 对合法根标量/数组只返回 L0/L1 结构违规，保持 `parseError: null` 并跳过不适用的 L2-L4。
- [x] V139c 确保非法 YAML/JSON 仍返回 parseError 且 layers 为空，内部异常仍进入 internalError。
- [x] V139d 评估并加固根 L2/L3a/L4 脚本对 null/标量的直接调用，避免 TypeError 泄漏。
- [x] V139e 增加 JSON null/字符串/数组、空 YAML、合法对象和非法语法回归测试。
- [x] V139f 执行 MCP build/test/tools smoke，并记录错误分类结果。
- 完成记录：runner 复用预解析值，非对象根只执行 L0/L1；JSON null/字符串/数组与空 YAML 均返回结构违规且 `parseError: null`。三个单层脚本增加根对象守卫，直接传入 null 时均稳定返回 0 violations / 0 parseErrors。既有 parseError/internalError 测试继续通过。

## V140（🟢）补齐 0011 归档索引

- [x] V140a 在 0012 与 0010 之间添加 0011 的主题、review/checklist 链接和关键修复摘要。
- [x] V140b 比较归档 review 编号集合与 README 标题集合，确认 0001-0040 全部覆盖且无重复。
- [x] V140c 检查新增链接目标并记录结果。
- 完成记录：`docs/audit/archived/README.md` 已补 0011 主题、文件和 W1-W4 摘要；PowerShell 集合比对确认归档 review 与索引编号完全一致，0011 两个目标文件存在。

## V141（🟢）修复 0002 失效章节锚点

- [x] V141a 将 0002 review 的“错误约定”链接更新为当前有效标题锚点。
- [x] V141b 对仓库内 Markdown 相对链接执行文件目标与 fragment 双重检查。
- [x] V141c 记录检查范围、脚本/命令和结果。
- 完成记录：0002 链接更新为当前 §6 标题 fragment；仓库级检查确认该 fragment 可定位。检查器另报的 4 条当前文档链接经目标标题核对为反引号/斜杠导致的 slug 计算假阳性，不属于失效链接，未修改。

## 汇总

| 编号 | 严重度 | 主题 | 状态 |
|---|---|---|---|
| V130 | 🔴 | 普通表单 GET 请求不可执行 | ✅ |
| V131 | 🟡 | 非表单 visibleWhen 放行 `$self` | ✅ |
| V132 | 🟡 | row 嵌套依赖路径被截断 | ✅ |
| V133 | 🟡 | params 数组绕过变量限制 | ✅ |
| V134 | 🟡 | 开放业务 map 触发 L4 误报 | ✅ |
| V135 | 🟡 | 静态 datasource 可携带 responseMapping | ✅ |
| V136 | 🟡 | table 缺少 `titleKey` | ✅ |
| V137 | 🟡 | 日期格式约束未机器执行 | ✅ |
| V138 | 🟡 | validate_content 响应超过 20KB | ✅ |
| V139 | 🟡 | 合法根标量误归类 parseError | ✅ |
| V140 | 🟢 | 归档索引遗漏 0011 | ✅ |
| V141 | 🟢 | 0002 历史章节锚点失效 | ✅ |

## 建议验证命令（修复后）

```powershell
# 根校验链（使用修复时创建的聚焦正反例）
npm run validate -- <file-or-glob>
npm run validate:l2 -- <file-or-glob>
npm run validate:l3a -- <file-or-glob>
npm run lint:l4 -- <file-or-glob>

# MCP 构建、测试与工具 smoke
npm --prefix mcp run build
npm --prefix mcp test
npm --prefix mcp run smoke:tools

# 涉及镜像内容时
npm --prefix mcp run smoke:docker -- <image>

# 审计证据链
# 比较 archived review 编号与 README 索引标题，并检查 Markdown 文件目标和 fragment

git diff --check
```

## 最终验证结果

- 四个官方场景通过根统一入口 L0-L4。
- `npm --prefix mcp run build`：通过。
- `npm --prefix mcp test`：通过，3 个测试文件、76 项测试全部通过。
- `npm --prefix mcp run smoke:tools`：通过，五个冻结工具名均存在。
- `schema-ui-mcp:audit-0041` Docker 镜像构建与 stdio smoke：通过；测试镜像已删除。
- 5 份 Schema/DSL JSON 解析通过；VS Code Problems 无错误。
- 归档 review 编号与索引标题集合一致，0011 索引和 0002 fragment 可达。
- `git diff --check`：无空白错误；仅输出工作区既有 LF/CRLF 转换提示。