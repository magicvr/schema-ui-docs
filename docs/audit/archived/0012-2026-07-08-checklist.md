---
status: checklist
date: 2026-07-08
based_on: 0012-2026-07-08-review.md
---

# 审计 0012 跟踪清单

## 使用说明

- `[ ]` 待决策 / 待处理
- `[x]` 已完成
- 本清单用于跟踪“是否需要产出 ADR / 是否已产出 ADR / 是否已合入正文”，不要求一次性全部实现。
- A 组为当前应优先进入 ADR 的决策点；B 组为未来触发型决策点。

---

## A1（🔴）标准化 `responseMapping` 响应体字段名映射

| 字段 | 值 |
|---|---|
| **位置** | `04-datasource-contract.md` §4.1、`audit/README.md` 跟踪项 |
| **问题** | 遗留系统 API 响应字段名与协议默认 `list` / `total` 不一致时，目前只有 Renderer 扩展层临时方案 |
| **操作** | 已产出 ADR，标准化映射声明位置、语法、适用数据形态和兼容策略 |
| **建议 ADR** | `decisions/0005-response-mapping.md` |

- [x] A1 明确 ADR 编号与文件名：`decisions/0005-response-mapping.md`
- [x] A1 决策映射声明位置：`data.responseMapping`，与 `params` 同级
- [x] A1 决策映射适用范围：列表类 Node `data` 响应包裹层；`select.optionsSource` 与单值字段映射暂不复用
- [x] A1 决策未配置映射时的默认行为与错误提示
- [x] A1 ADR 通过后更新 `04-datasource-contract.md`
- [x] A1 ADR 通过后更新相关 Schema / 示例（如需要）
- [x] A1 关闭或改写 `audit/README.md` 中的活跃跟踪项

---

## A2（🔴）定义表达式读写求值时序模型

| 字段 | 值 |
|---|---|
| **位置** | `01-node-protocol.md` §3.10、`decisions/0003-context-namespace-and-visible-when.md` 遗留问题 |
| **问题** | `visibleWhen` / `permissions` 读取字段与 `reactions.fulfill.value` 写字段发生在同一渲染周期时，读写先后未定义 |
| **操作** | 已产出 ADR，定义表达式引擎的求值阶段、快照模型、冲突处理与静态校验策略 |
| **建议 ADR** | `decisions/0006-expression-evaluation-order.md` |

- [x] A2 明确 ADR 编号与文件名：`decisions/0006-expression-evaluation-order.md`
- [x] A2 决策 `visibleWhen` / `permissions` 读取本轮开始时的稳定快照
- [x] A2 决策 `reactions.fulfill.value` 与可见性判断的执行阶段：本轮末尾批量提交，下一轮读取新值
- [x] A2 决策同周期读写冲突采用固定行为：本轮读旧快照，本轮末尾提交写入
- [x] A2 决策多个 reaction 写同一字段时的冲突处理：后写优先并开发期警告
- [x] A2 ADR 通过后更新 `01-node-protocol.md` 的已知限制段落
- [x] A2 ADR 通过后更新 `02-reaction-expression.md` / `08-renderer-spec.md`（如需要）

---

## A3（🟡）设计跨作用域联合判断标准语法

| 字段 | 值 |
|---|---|
| **位置** | `02-reaction-expression.md` §9.1、`decisions/0004-row-level-scope.md` 遗留问题 |
| **问题** | 当前不支持同一表达式同时依赖表单级字段和行级 `$row` 字段 |
| **操作** | 根据真实需求决定是否产出 ADR；若产出，应定义跨作用域依赖声明、求值与静态校验规则 |
| **建议 ADR** | 待需求触发后编号 |

- [x] A3 本轮未发现真实跨作用域业务场景，暂不启动 ADR
- [x] A3 本轮维持后端预计算行字段作为推荐规避方案
- [x] A3 本轮不新增表格级传入属性、显式绑定参数或作用域链
- [x] A3 本轮不定义表单字段变化触发行级表达式重算的新机制
- [x] A3 本轮保持 `$deps` / `$row` 互斥规则不变
- [x] A3 作为未来触发型议题保留，待真实需求出现后另开 ADR

---

## A4（🟡）设计 `scope: row` 下行内字段联动赋值 / 回写模型

| 字段 | 值 |
|---|---|
| **位置** | `02-reaction-expression.md` §9.3、`decisions/0004-row-level-scope.md` 遗留问题 |
| **问题** | `scope: row` 下当前静态禁止 `value`，不支持行内数据回写 |
| **操作** | 根据真实需求决定是否产出 ADR；若产出，应定义写回目标、状态生命周期与远程数据交互 |
| **建议 ADR** | 待需求触发后编号 |

- [x] A4 本轮未发现行内字段联动赋值真实场景，暂不启动 ADR
- [x] A4 本轮不定义单元格、整行、临时 UI 状态或提交 payload 写回目标
- [x] A4 本轮不定义分页、排序、筛选、重新加载后的状态保留策略
- [x] A4 本轮不扩展 `scope: row` 下的 `reactions` 写入能力
- [x] A4 本轮确认 A2 仅覆盖表单级 `value` 写入时序，不引入行级回写
- [x] A4 保持 `02-reaction-expression.md` 现有静态禁止规则不变

---

## A5（🟡）评估 `compute` 纯计算字段

| 字段 | 值 |
|---|---|
| **位置** | `audit/archived/0001-2026-07-07-plan.md` C4 |
| **问题** | 历史审计已记录 `compute` 作为未来演进方向，但当前正文未定义该能力 |
| **操作** | 根据真实只读计算需求决定是否产出 ADR；若产出，应定义 `compute` 与 `reactions` 的边界 |
| **建议 ADR** | 待需求触发后编号（历史建议的 0005 已被 `responseMapping` 占用） |

- [x] A5 本轮未发现只读派生值真实场景，暂不启动 ADR
- [x] A5 本轮维持现有 Node 结构或后端预计算作为优先方案
- [x] A5 本轮不新增独立 `compute` 字段
- [x] A5 本轮不扩展表达式能力边界
- [x] A5 本轮不定义计算结果进入提交 payload 或仅展示的新语义
- [x] A5 作为未来触发型议题保留，待真实需求出现后另开 ADR

---

## A6（🟡）设计跨组件数据依赖 / 参数传递机制

| 字段 | 值 |
|---|---|
| **位置** | `04-datasource-contract.md` §3.2 |
| **问题** | 当前禁止在非表单数据节点中使用 `$deps.*`；未来若组件之间需要互相驱动，缺少标准机制 |
| **操作** | 根据真实仪表盘联动场景决定是否产出 ADR |
| **建议 ADR** | 待需求触发后编号 |

- [x] A6 本轮未发现组件间数据依赖真实场景，暂不启动 ADR
- [x] A6 本轮不定义组件显式导出可依赖参数机制
- [x] A6 本轮不定义下游组件依赖声明与参数映射
- [x] A6 本轮不引入加载拓扑排序或循环依赖处理机制
- [x] A6 本轮不定义上游失败、空值、刷新时的下游联动行为
- [x] A6 作为未来触发型议题保留，待真实需求出现后另开 ADR

---

## B1（🟢）评估 `$context` 响应式刷新与刷新粒度

| 字段 | 值 |
|---|---|
| **位置** | `decisions/0003-context-namespace-and-visible-when.md` 遗留问题 |
| **触发条件** | 页面运行中需要感知用户角色或功能开关变化 |

- [x] B1 本轮未发现响应式 `$context` 真实需求，保持未来触发型
- [x] B1 本轮不支持运行时刷新 `$context`
- [x] B1 本轮不拆分 `$context.user` 与 `$context.features` 刷新时机
- [x] B1 保留在 ADR-0003 遗留问题中，待真实需求触发

---

## B2（🟢）评估容器级 `permissions.edit` / `delete` 级联语义

| 字段 | 值 |
|---|---|
| **位置** | `decisions/0003-context-namespace-and-visible-when.md` 遗留问题 |
| **触发条件** | 需要容器级批量禁用、只读或操作权限级联 |

- [x] B2 本轮未发现容器级批量禁用 / 只读真实场景，保持未来触发型
- [x] B2 本轮不定义 `edit=false` 级联到子节点
- [x] B2 本轮不定义 `delete` 与自定义动作键级联
- [x] B2 本轮不定义父子权限冲突仲裁规则
- [x] B2 待真实需求触发后另开 ADR 并更新权限章节

---

## B3（🟢）评估 `$context.features` 粒度与新命名空间扩展

| 字段 | 值 |
|---|---|
| **位置** | `02-reaction-expression.md` §11、`decisions/0003-context-namespace-and-visible-when.md` 遗留问题 |
| **触发条件** | 现有 `$context.user` / `$context.features` 无法承载新上下文需求 |

- [x] B3 本轮未发现新 `$context` 命名空间或 features 分组需求，保持未来触发型
- [x] B3 本轮维持现有 `user` / `features` 命名空间
- [x] B3 本轮不新增 `$context` 根命名空间
- [x] B3 待真实需求触发后另开 ADR 并更新变量命名空间白名单

---

## B4（🟢）评估三层及以上嵌套表格跨层引用

| 字段 | 值 |
|---|---|
| **位置** | `decisions/0004-row-level-scope.md` 遗留问题 |
| **触发条件** | 出现真实三层及以上嵌套表格，需要跨层访问父行 |

- [x] B4 本轮未发现三层嵌套表格真实场景，保持未来触发型
- [x] B4 本轮维持交互设计或数据扁平化作为优先规避方案
- [x] B4 本轮不引入 `$parentRow(2)` 或其他层级引用方式
- [x] B4 待真实需求触发后另开 ADR 并更新行级作用域文档

---

## B5（🟢）评估 `visibleField` 下线策略

| 字段 | 值 |
|---|---|
| **位置** | `decisions/0004-row-level-scope.md` 遗留问题 |
| **触发条件** | `visibleField` 与 `$row` / `visibleWhen` 出现语义冲突或维护成本上升 |

- [x] B5 本轮未发现 `visibleField` 与 `$row` 共存成本失控，保持未来触发型
- [x] B5 本轮不启动 `visibleField` 下线 ADR
- [x] B5 待真实需求触发后再更新组件注册表、场景示例与 CHANGELOG

---

## B6（🟢）评估同一份数据多套展示的 `variant` 机制

| 字段 | 值 |
|---|---|
| **位置** | `decisions/0002-why-not-two-schema-uischema.md` 权衡章节 |
| **触发条件** | 出现强烈的同一数据结构多套展示复用需求 |

- [x] B6 本轮未发现多套展示复用真实场景，保持未来触发型
- [x] B6 本轮维持现有 Node 组合为优先方案
- [x] B6 本轮不引入 Node `variant` 字段
- [x] B6 明确不得无评审回退到 schema + uiSchema 双树模型

---

## B7（🟢）评估错误响应 `code` 的前端程序化消费

| 字段 | 值 |
|---|---|
| **位置** | `04-datasource-contract.md` §5 |
| **触发条件** | Renderer 需要根据业务错误码执行跳转、特殊 UI 或恢复动作 |

- [x] B7 本轮未发现前端程序化消费 `code` 的真实场景，保持未来触发型
- [x] B7 本轮维持 `code` 仅用于调试日志和错误追踪
- [x] B7 本轮不定义错误码注册方式与默认行为
- [x] B7 待真实需求触发后另开 ADR 并更新错误约定

---

## 索引与收尾

- [x] 更新 `audit/README.md` 当前活跃审计为 0012
- [x] 若 0012 后续全部关闭，归档 `0012-2026-07-08-review.md` 与 `0012-2026-07-08-checklist.md`
- [x] 归档时更新 `audit/archived/README.md` 索引
