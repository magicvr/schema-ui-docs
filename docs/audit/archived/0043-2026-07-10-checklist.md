---
status: checklist
date: 2026-07-10
based_on: 0043-2026-07-10-review.md
---

# 审计 0043 跟踪清单

## V155（🔴）执行 tabs content 的完整 Node 契约

- [x] V155a 为 `node.schema.json` 完整引用增加普通对象检查和 L2 Node 递归。
- [x] V155b 将 tabs content 遍历改为属性存在性判断，覆盖 null/false/0/空字符串。
- [x] V155c 增加字符串、null、布尔、数字、数组反例和合法 Node 正例。
- [x] V155d 同步 `06-validation.md` 并执行 L2、统一入口和 MCP 测试。
- 完成记录：`validateKnownRef()` 受控执行完整 Node 引用，非普通对象在 `body.props.items[0].content` 报 L2 违规；tabs 递归改用 `hasOwnProperty`，合法嵌套 Node 继续进入完整组件校验。新增 6 组回归测试，完整 MCP 测试通过。

## V156（🟡）保持嵌套结构错误的正确分类

- [x] V156a 为 Node props 增加普通对象守卫，结构错误不得使 L2 抛异常。
- [x] V156b 增加 props null/string/array 回归测试，断言 L0/L1 有违规且 parseError 为 null。
- [x] V156c 核对合法根标量、非法 YAML 和内部异常既有分类不回归。
- [x] V156d 执行 MCP 聚焦测试和完整测试。
- 完成记录：`validateNode()` 只对普通对象执行 Node 语义，显式非对象 props 归一为空对象供 L2 跳过；L0/L1 仍报告原始结构路径。MCP v1 design 补充嵌套结构错误分类，既有根标量、非法 YAML、internalError 测试全部通过。

## V157（🟡）拒绝 YAML 非有限数值

- [x] V157a 将 DSL number 定义收紧为 JavaScript 有限数。
- [x] V157b 确保 minimum 只在有限数上执行且错误信息稳定。
- [x] V157c 增加 `.nan`、`.inf`、`-.inf` 反例，并保留 0/正数既有正例。
- [x] V157d 同步组件 DSL 关键字说明和 `06-validation.md`。
- 完成记录：`checkType()` 使用 `Number.isFinite()`，错误信息区分 NaN/Infinity/-Infinity；`checkNumberBounds()` 跳过非有限值，避免重复或无意义比较。临时 `.nan` 探针和三组 MCP 回归测试均按预期失败关闭。

## V158（🟡）支持固定选项 labelKey

- [x] V158a 为 `select.options[]` 增加 `labelKey` 并将 label/labelKey 改为至少一个。
- [x] V158b 保持 value 必填与 option 外壳 additionalProperties false。
- [x] V158c 同步 select 字段表，增加 label、labelKey、二者同时存在的正例和二者均缺失反例。
- [x] V158d 执行 L2、统一入口和 MCP 测试。
- 完成记录：组件 DSL 的 option item 改为 `required: [value]` + label/labelKey `anyOf`，外壳继续封闭；`03-component-registry.md` 同步类型和 i18n 说明。三类正例通过，缺失两种文案的反例被 L2 拒绝。

## V159（🟡）传播统一入口调用错误退出码

- [x] V159a 聚合 L2/L3a/L4 code 2，最终优先返回 2。
- [x] V159b 为 AJV 结果保留可用退出码并区分调用错误与内容违规。
- [x] V159c 更新 `06-validation.md` 的 0/1/2 说明。
- [x] V159d 验证无参数=2、无匹配=2、内容违规=1、合法输入=0。
- 完成记录：统一入口检测任一结果 `code === 2` 后保留调用错误身份；AJV 未安装显式返回 2，子进程错误保留 `err.status`。命令级验证结果为 `valid=0`、`violation=1`、`no-match=2`、`no-args=2`。

## V160（🟢）修正 0042 plan 索引范围

- [x] V160a 将归档索引说明从 V142-V152 改为 V142-V154。
- [x] V160b 核对 0042 review/checklist/plan 的范围描述一致。
- 完成记录：0042 文件表中的 plan 说明已改为 V142-V154，索引中不再存在旧范围文本。

## V161（🟡）落实历史问题复合身份引用

- [x] V161a 将 0010 摘要中的 V1-V4 改为 `0010/V1`-`0010/V4`。
- [x] V161b 将 0013 摘要中的 V1-V3 改为 `0013/V1`-`0013/V3`。
- [x] V161c 将 0014 文件表与摘要中的 V4 改为 `0014/V4`。
- [x] V161d 扫描归档索引，确认冲突项不再使用裸 V1-V4。
- 完成记录：归档入口共 9 处冲突身份引用已复合化；裸 `^- V[1-4]:` 和 `审视报告 — V4(` 扫描结果均为空，历史 review 原文未重编号。

## 汇总

| 编号 | 严重度 | 主题 | 状态 |
|---|---|---|---|
| V155 | 🔴 | tabs content 完整 Node 未执行 | ✅ |
| V156 | 🟡 | 嵌套结构错误误报 parseError | ✅ |
| V157 | 🟡 | 非有限数绕过 DSL | ✅ |
| V158 | 🟡 | 固定选项 labelKey 被拒绝 | ✅ |
| V159 | 🟡 | 调用错误码 2 被吞掉 | ✅ |
| V160 | 🟢 | 0042 plan 索引范围漂移 | ✅ |
| V161 | 🟡 | 裸 V1-V4 身份歧义 | ✅ |

## 建议验证命令（修复后）

```powershell
npm run validate -- <file-or-glob>
npm run validate:l2 -- <file-or-glob>
npm --prefix mcp run build
npm --prefix mcp test
npm --prefix mcp run smoke:tools
git diff --check
```

## 最终验证结果

- 5 份 Schema/组件 DSL JSON 经 Node `JSON.parse` 全部通过。
- `npm --prefix mcp run build`：通过。
- `npm --prefix mcp test`：通过，3 个测试文件、103 项测试全部通过。
- `npm --prefix mcp run smoke:tools`：通过，五个冻结工具名均存在。
- 统一入口退出码：合法输入 0、内容违规 1、无匹配 2、无参数 2。
- VS Code Problems：本轮修改文件无错误。
- `git diff --check`：通过。
- 未执行 Docker smoke：本轮未修改 Dockerfile、镜像打包内容、运行时路径或 stdio transport；MCP 构建与本地 stdio tools smoke 已覆盖受影响边界。
