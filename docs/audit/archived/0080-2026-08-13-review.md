---
status: archived
date: 2026-08-13
based_on: schema-ui-protocol 2.8.1 / protocolVersion 2.8；正式 tag v2.8.1 / mcp-v2.8.1 @ ceb099f；不改写 v2.8.0 @ 521cff8
scope: focus=all；主类型=发布完整性；次类型=实现/契约漂移；不修改生产消费者仓库
---

# 审计报告 0080：v2.8.0 正式身份纠偏与当前线入口扫版

## 结论

正式 **`v2.8.0` / `mcp-v2.8.0` @ `521cff8`** 已发布，机器契约（421 fixtures、41 conformance、MCP 134 tests、GHCR 四 tag 同 digest）无新回归。

0079 在实际打 tag **之前** 把 H4 commit `593f625` 写成「正式 tag」，随后 ADR 发布后注记进入制品路径，tag 打在 `521cff8`。文档与消费者仍引用 `593f625` / content `40690917…` / artifact `594207e0…`；GitHub Release 与工作树为 content `4fae4605…` / artifact `6cdbffcc…`。同类复发 [0068/V309](./0068-2026-07-24-review.md)。

本轮 **2×P1 + 5×P2（V379–V385）**：in-repo **已全部关闭**。制品内 informative 修正以 **2.8.1 PATCH** 固化，**未**改写 `v2.8.0`。正式 `v2.8.1` / `mcp-v2.8.1` @ `ceb099f` 已发布。消费者重 pin 仍为 out-of-repo 剩余风险。

本文不是协议权威来源。`docs/audit/**` 不进入 `protocol-manifest.json`、协议制品、MCP 索引或 Docker 镜像。

## 范围与基线

### 审计类型

| 类型 | 本轮角色 |
|---|---|
| **发布完整性** | **主类型** — 正式 tag SHA / content / artifact 与文档 pin 不一致 |
| 实现/契约漂移 | 次类型 — 制品内当前线指针与提案状态滞后 |
| 提案评审 | 不适用 — ADR-0034–0037 已 accepted |

### 范围与覆盖率

| 批次 | 覆盖 | 置信度 |
|---|---|---|
| A 规范 | charter、manifest、`00`/`06`/`08`/`10`、ADR 状态、release-goals、提案 | 高（未通读全部历史 ADR 正文） |
| B conformance | README、421 计数、双语言 41 runner | 高（未逐条手审 expected） |
| C MCP/validator | 版本/lock/DOC_MAP、134 tests、GHCR smoke | 高 |
| D 发布 | workflows、tag、Release、四镜像 tag | 高 |
| E 消费者 | `schema-ui-core` pin 只读 | 中（未跑消费者 CI） |

### 基线验证

| 命令 | 结果 |
|---|---|
| `check:links` | 262 / 758 |
| `release:check` / `verify:protocol-artifact` | 421 · fixture `7aacf133…` · content `4fae4605…` · artifact `6cdbffcc…` |
| `test:conformance:all` | 41/41 |
| MCP test / smoke / GHCR | 134；镜像 index `07dc2e14…`（`2.8.0`/`2.8`/`latest`/`521cff8`） |

## 问题清单（汇总表）

| ID | 级别 | 状态 | 位置 | 所属类型 |
|---|---|---|---|---|
| V379 | P1 | 已关闭（消费者 out-of-repo） | ADR-0034 注记、提案 H4、消费者 provenance | 发布完整性 |
| V380 | P1 | 已关闭 | `conformance/README.md` | 实现/契约漂移 |
| V381 | P2 | 已关闭 | `release-goals/README.md`、`next-admin-lifecycle.md` §8 | 实现/契约漂移 |
| V382 | P2 | 已关闭 | `docs/mcp/README.md` | 实现/契约漂移 |
| V383 | P2 | 已关闭 | `docs/proposals/next-host-app-interoperability.md` | 实现/契约漂移 |
| V384 | P2 | 已关闭 | `docs/audit/README.md`、`archived/README.md` | 发布完整性 |
| V385 | P2 | 已关闭 | `docs/CHANGELOG.md` v2.8.0 生产 evidence | 实现/契约漂移 |

## 问题详情（V379–V385）

### V379（P1）正式 v2.8.0 身份被误记为 `593f625` / `40690917…`

**回归自 0068/V309。** 正式 tag 与 Release 为 `521cff8` / content `sha256:4fae46058d01bb62d8ff5a17b35f57021a417302c9d8b932916e17ab8acf3c30` / artifact `sha256:6cdbffccaa0da1f9e05487b7def93aaef14b7fbae7b14dc8761fc661c9946f31`。fixture 树未变（`7aacf133…`）。ADR-0034 发布后注记、提案 H4 与消费者 `provenance-v2.8.json` 引用 H4 预备 commit。

**修复：** 权威文档改为正式 pin；不改写 tag；消费者 out-of-repo。

### V380（P1）conformance README 仍写当前线 `"2.7"`

`conformance/README.md` 称算法 suite 与 `release:check` 强制值为 `"2.7"`。实际 fixtures 与门禁为 `"2.8"`。该文件进入协议制品。0079 F1 只扫了根 README。

### V381（P2）release-goals 入口仍以 v2.7 为当前线

`release-goals/README.md` 漏列 `v2.8.md` 且称 v2.7 为当前线。`next-admin-lifecycle.md` §8 仍写 `protocol-manifest` 为 `2.7.0` / `"2.7"`。

### V382（P2）MCP 文档仍 pin `2.7.0`

同类复发 0077/V368。`docs/mcp/README.md` 示例与「工作树/正式 MCP」仍写 `2.7.0`。

### V383（P2）Host/App 提案仍把已发布 ADR 标为 proposed

`docs/proposals/next-host-app-interoperability.md` `status: active`，能力表仍为 proposed，并复述错误正式 digest。

### V384（P2）审计索引未登记 0079

活跃/归档 README 最近归档仍写 0078；0079 文件已在 `archived/`。

### V385（P2）CHANGELOG v2.8.0 仍写候选 digest 与 candidate 文件名

生产 evidence 段仍引用 `2d802a58…` / `2d1a13e1…` 与 `provenance-v2.8-candidate.json`。

## 与历史审计关系

- **0079：** 把 `593f625` 记成正式 tag（当时 tag 尚未打在最终 merge）。不改写 0079 正文。
- **0068/V309：** 同一制品版本多 contentDigest。本次为文档 pin 早于实际 tag。
- **0077/V368：** MCP 文档未跟版。

## 处理顺序与防复发

1. 纠正正式 pin（V379/V383/V385）→ 当前线入口（V380/V381/V382）→ 审计索引（V384）。
2. 制品内修正与版本升 PATCH 同提交。
3. 防复发：正式身份以 `git rev-parse v*` + Release asset digest + 制品内 `manifest.json` 为准，禁止用「准备发布」commit 冒充 tag；`release:check` 可后续断言 conformance README 当前线（本轮先改正文）。

生产消费者默认 out-of-repo：`schema-ui-core` 应改 pin 正式 **`v2.8.1` @ `ceb099f`**（content `25d13328…` / artifact `3e40808c…`），或至少改到正式 `v2.8.0` @ `521cff8`。

## 归档条件

in-repo V379–V385 已关闭；正式 `v2.8.1` / `mcp-v2.8.1` 证据链已闭合。用户确认后归档。
