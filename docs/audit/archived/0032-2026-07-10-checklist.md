---
status: checklist
date: 2026-07-10
based_on: 0032-2026-07-10-review.md
---

# 审计 0032 跟踪清单

## V71（🟡）`source: ref` 的本地 `responseMapping` 绕过 L2 语义校验

| 字段 | 值 |
|---|---|
| **位置** | `scripts/validate-l2-components.js`；`docs/01-node-protocol.md`；`docs/04-datasource-contract.md`；`docs/06-validation.md` |
| **问题** | `source: ref` 可携带本地 `responseMapping`，但 L2 只在 `source: api` 时执行 `list` / `total` 条件必填 |
| **操作** | 让 `source: ref + responseMapping` 进入列表/分页语义校验，并补 CI/MCP 回归 |
| **文件** | `scripts/validate-l2-components.js`；`mcp/tests/test-utils.ts`；`mcp/tests/validate-content.test.ts`；可选 `docs/06-validation.md` |

- [x] V71a 决定 `source: ref` 校验策略：对已声明本地 `responseMapping` 的 `api` / `ref` 数据源执行同一组条件必填规则。处理：不解析引用目标，按本地映射声明保守校验。
- [x] V71b 修改 `scripts/validate-l2-components.js`，让 `table` / `chart` 的 `source: ref + responseMapping` 缺少 `list` 时失败。
- [x] V71c 确保 `table.props.pagination.mode: server` 且 `source: ref + responseMapping` 缺少 `total` 时失败。
- [x] V71d 补充正反例：缺 `list` 失败；服务端分页缺 `total` 失败；提供 `list/total` 的 ref 映射通过。验证：临时 YAML 运行 `node scripts/validate-l2-components.js <tmp>.yaml --json`。
- [x] V71e 补充 MCP `validate_content` 回归测试，确认同类非法配置返回 `passed: false` 且落在 L2。修改：`mcp/tests/test-utils.ts`、`mcp/tests/validate-content.test.ts`。
- [x] V71f 同步 `docs/06-validation.md` 的 `responseMapping` 条件必填描述，明确 `source: api` / `source: ref` 本地映射均适用。
- [x] V71g 执行验证：`node scripts/validate-l2-components.js <tmp>.yaml --json`；`npm --prefix mcp test -- validate-content.test.ts`；`npm --prefix mcp run build`。

---

## V72（🟡）`table.props.actions[].permissions` 未进入 L3a 扫描

| 字段 | 值 |
|---|---|
| **位置** | `scripts/validate-l3a-expressions.js`；`docs/03-component-registry.md`；`docs/schemas/component-registry.json` |
| **问题** | RowAction permissions 在文档/DSL 中存在，但 L3a 只扫描列级 permissions，不扫描 action permissions |
| **操作** | 补齐 action permissions 遍历，并覆盖节点/列/action 三类权限测试 |
| **文件** | `scripts/validate-l3a-expressions.js`；`mcp/tests/test-utils.ts`；`mcp/tests/validate-content.test.ts`；可选 `docs/06-validation.md` |

- [x] V72a 在 `scripts/validate-l3a-expressions.js` 的 table `actions[]` 循环中扫描 `action.permissions.*`。
- [x] V72b 为 RowAction permissions 使用 `location: 'permission'`，确保与节点/列级权限共用同一变量约束。
- [x] V72c 补充反例：`table.props.actions[].permissions.view` 使用 `$self` 时失败。验证：临时 YAML 运行 `node scripts/validate-l3a-expressions.js <tmp>.yaml --json`。
- [x] V72d 补充 MCP `validate_content` 回归，覆盖 RowAction permissions 路径。修改：`mcp/tests/test-utils.ts`、`mcp/tests/validate-content.test.ts`。
- [x] V72e 执行验证：`node scripts/validate-l3a-expressions.js <tmp>.yaml --json`；`npm --prefix mcp test -- validate-content.test.ts`。

---

## V73（🟡）`permissions.*` 未完整限制为仅 `$context.*`

| 字段 | 值 |
|---|---|
| **位置** | `scripts/validate-l3a-expressions.js`；`docs/01-node-protocol.md`；`docs/02-reaction-expression.md` |
| **问题** | L3a 只拒绝 `$deps.*`，仍放过 `$self` / `$row.*` / `$parentRow.*` |
| **操作** | 把 permissions 变量规则改成 allowlist：只允许合法 `$context.*` |
| **文件** | `scripts/validate-l3a-expressions.js`；`mcp/tests/test-utils.ts`；`mcp/tests/validate-content.test.ts`；可选 `docs/06-validation.md` |

- [x] V73a 修改 L3a：`location === 'permission'` 时拒绝任何非 `$context.*` 变量。规则名：`PERM_CONTEXT_ONLY`。
- [x] V73b 保留 `$context.user.*` / `$context.features.*` 正例通过。验证：临时 YAML 中 `$context.user.roles` + `$context.features.newDashboard` 无 L3a 违规。
- [x] V73c 补充节点级、行级 action permissions 的 `$self` / `$row.*` 反例。修改：`mcp/tests/test-utils.ts`、`mcp/tests/validate-content.test.ts`。
- [x] V73d 同步 `docs/06-validation.md`，把人工自检清单强化为“是否只使用 `$context.user.*` / `$context.features.*`”。
- [x] V73e 执行验证：`node scripts/validate-l3a-expressions.js <tmp>.yaml --json`；`npm --prefix mcp test -- validate-content.test.ts`。

---

## V74（🟡）`$context` 根命名空间白名单未被 L3a enforce

| 字段 | 值 |
|---|---|
| **位置** | `scripts/validate-l3a-expressions.js`；`docs/02-reaction-expression.md` |
| **问题** | 实现按 `$context.` 前缀放行，未限制根命名空间为 `user` / `features` |
| **操作** | 增加 `$context` 根白名单校验，补合法/非法路径测试 |
| **文件** | `scripts/validate-l3a-expressions.js`；`mcp/tests/test-utils.ts`；`mcp/tests/validate-content.test.ts`；可选 `docs/02-reaction-expression.md` |

- [x] V74a 在 L3a 中校验 `$context.<root>`，仅允许 `user` / `features`。规则名：`UNKNOWN_CONTEXT_NAMESPACE`。
- [x] V74b 未改变 `$context.user.*` / `$context.features.*` 文档边界，因此无需修改 `02-reaction-expression.md`。
- [x] V74c 补充反例：`$context.tenant.id` 被拒绝。修改：`mcp/tests/test-utils.ts`、`mcp/tests/validate-content.test.ts`。
- [x] V74d 补充正例：`$context.user.roles contains 'admin'` 与 `$context.features.newDashboard == true` 通过。验证：临时 YAML 运行 L3a 无违规。
- [x] V74e 执行验证：`node scripts/validate-l3a-expressions.js <tmp>.yaml --json`；`npm --prefix mcp test -- validate-content.test.ts`。

---

## V75（🟡）MCP 子脚本异常/非 JSON 输出被归类为协议违规

| 字段 | 值 |
|---|---|
| **位置** | `mcp/src/core/validation-runner.ts`；`docs/decisions/0007-mcp-protocol-reader-validator.md` |
| **问题** | 子脚本异常或非 JSON 输出属于 MCP 内部故障，却被塞进 L2/L3a/L4 violation |
| **操作** | 将子脚本内部异常映射为结构化 `internalError`，补测试 |
| **文件** | `mcp/src/core/validation-runner.ts`；`mcp/tests/validate-content.test.ts`；可选 `docs/mcp/v1-design.md` |

- [x] V75a 调整 `runLayerScript()` 返回结构或异常处理，使非 JSON 输出/脚本崩溃可被上层识别为内部错误。修改：`mcp/src/core/validation-runner.ts` 新增 `internalError` 路径。
- [x] V75b 修改 `validateContent()` 汇总逻辑：一旦子脚本内部错误发生，返回 `passed: false`、`internalError: { message }`，避免伪装成协议违规。
- [x] V75c 确认正常 L2/L3a/L4 违规仍进入对应 layer，不影响用户配置错误路径。验证：`npm --prefix mcp test -- validate-content.test.ts` 中既有违规测试继续通过。
- [x] V75d 补充测试：通过 `setLayerScriptExecutorForTest()` 模拟子脚本非 JSON 输出，断言 `internalError` 为对象且 `layers.L2` 为空。
- [x] V75e 执行验证：`npm --prefix mcp run build`；`npm --prefix mcp test -- validate-content.test.ts`。

---

## V76（🟡）`protocol.list_components.category` 示例与实现/测试不一致

| 字段 | 值 |
|---|---|
| **位置** | `docs/mcp/v1-design.md`；`mcp/src/core/component-registry.ts`；`mcp/tests/components.test.ts` |
| **问题** | v1 设计示例写英文 `data`，实现和测试返回中文 `数据` |
| **操作** | 统一 category 取值口径，并同步文档/测试/实现 |
| **文件** | `docs/mcp/v1-design.md`；可选 `mcp/src/core/component-registry.ts`；可选 `mcp/tests/components.test.ts` |

- [x] V76a 决定 category 保留中文章节名，沿用当前实现和 `components.test.ts` 锁定值。
- [x] V76b 更新 `docs/mcp/v1-design.md` 示例为 `数据`，并说明分类来源于当前文档章节标题。
- [x] V76c 未改为英文枚举，因此无需更新实现与测试断言。
- [x] V76d 执行验证：`npm --prefix mcp run build`；后续全量 `npm --prefix mcp test` 覆盖 `components.test.ts`。

---

## V77（🟢）MCP v1 实施计划状态与实现/CI 漂移

| 字段 | 值 |
|---|---|
| **位置** | `docs/mcp/v1-implementation-plan.md`；对照 `mcp/src/**/*`、`mcp/tests/**/*`、README MCP 说明 |
| **问题** | 实施计划仍为 draft 且任务未勾选，但 MCP v1 实现、测试、Docker smoke 脚本已存在 |
| **操作** | 将该文档转为已落地/归档状态，或勾选已完成任务并标注剩余项 |
| **文件** | `docs/mcp/v1-implementation-plan.md` |

- [x] V77a 将 frontmatter `status` 调整为 `implemented`，并在正文开头说明本文保留为实施记录和验收对照。
- [x] V77b 勾选 P1-P7 中已由当前代码和测试完成的任务。
- [x] V77c 对 Docker smoke 自动化脚本状态写明“已提供，执行状态见发布流水线或本地命令”，避免空复选框误导读者。
- [x] V77d 执行验证：人工核对 `mcp/src/server.ts`、`mcp/tests/*`、`mcp/Dockerfile` 与实施计划状态一致；最终执行 `git diff --check`。

---

## V78（🟡）`07-actions-contract.md` §3.1 完整页面示例不可执行

| 字段 | 值 |
|---|---|
| **位置** | `docs/07-actions-contract.md` §3.1；`docs/schemas/component-registry.json` table props |
| **问题** | 示例 `body.type: table` 缺少必填 `pagination` 和 `columns` |
| **操作** | 补齐最小 table 必填 props，并验证示例可通过当前校验链 |
| **文件** | `docs/07-actions-contract.md`；可选 `mcp/tests/validate-content.test.ts` |

- [x] V78a 在 §3.1 示例中补齐 `pagination` 最小配置。修改：`docs/07-actions-contract.md`。
- [x] V78b 在 §3.1 示例中补齐至少一列 `columns`，字段与 `requestMapping.path.orderId` 保持一致。
- [x] V78c 抽取该 YAML 示例运行 `node scripts/validate-all.js <tmp.yaml> --json`，确认通过。
- [x] V78d 本轮不新增示例抽取回归；已有修复通过临时抽取验证，后续可统一建设文档示例校验器。
- [x] V78e 执行验证：`node scripts/validate-all.js <tmp.yaml> --json`；最终执行 `git diff --check`。

---

## 汇总

| 编号 | 严重度 | 状态 |
|---|---|---|
| V71 | 🟡 | [x] 已修复并验证 |
| V72 | 🟡 | [x] 已修复并验证 |
| V73 | 🟡 | [x] 已修复并验证 |
| V74 | 🟡 | [x] 已修复并验证 |
| V75 | 🟡 | [x] 已修复并验证 |
| V76 | 🟡 | [x] 已修复并验证 |
| V77 | 🟢 | [x] 已修复并验证 |
| V78 | 🟡 | [x] 已修复并验证 |

## 建议验证命令（修复后）

```bash
# L2 / L3a / 全链路校验
npm run validate:l2 -- "<tmp-or-fixture>.yaml"
npm run validate:l3a -- "<tmp-or-fixture>.yaml"
npm run validate -- "<tmp-or-fixture>.yaml"

# MCP 实现与回归
npm --prefix mcp run build
npm --prefix mcp test
npm --prefix mcp run smoke:tools
```

若未触及 Dockerfile 或镜像内容，可不执行 `npm --prefix mcp run smoke:docker -- <image>`；若后续改动 Docker 打包或镜像文档，再补充 Docker smoke 状态。
