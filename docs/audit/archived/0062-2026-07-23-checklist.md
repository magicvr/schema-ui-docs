---
status: archived
date: 2026-07-23
based_on: 0062-2026-07-23-review.md
---

# 审计 0062 整改 Checklist

> 本 checklist 仅跟踪协议层整改。生产 Renderer、Host 应用和业务后端不属于本轮范围。

## 基线

- [x] 确认审计编号 `0062` 延续已归档的 `0061`。
- [x] 确认问题编号 `V267` 至 `V272` 延续 `V266`。
- [x] 确认本审计开始前工作树干净。
- [x] 记录全量 JS/Python conformance、release check 和官方 2.1/2.2 示例校验均已通过。
- [x] 确认 capability 门控的 2.2 功能使用 `protocolVersion: "2.1"` 是有意设计，不构成问题。

## V267（P1）未解析的 URL 模板

- [x] 为 URL 占位符与 `path` mapping 定义共享的 fail-closed 规则。
- [x] 将该规则应用到行级导航、记录加载与批量 request。
- [x] 对齐 JavaScript 与 Python reference 的错误码和错误路径。
- [x] 为所有受影响 request 类型补充缺失占位符 mapping 与多余 path 键的负向向量。
- [x] 同步 ADR-0021、ADR-0022、action/renderer 文档和 L2 诊断。

## V268（P1）Toolbar 表达式校验

- [x] 为 toolbar `visibleWhen` 与 `permissions` 定义精确的 L3a 上下文策略。
- [x] 在 L3a 中遍历 toolbar Trigger 表达式。
- [x] 接受允许的 `$context.*` 表达式。
- [x] 拒绝 `$row.*`、`$deps.*`、模板字符串和未支持的表达式作用域。
- [x] 增加 L3a 正反向向量，包括 toolbar permissions。
- [x] 同步 ADR-0020、组件注册文档和校验指南。

## V269（P1）Batch Trigger 的选择前置条件

- [x] 要求带有 `batchMapping` 的 toolbar Trigger 所属 table 配置 `selection.mode: multiple`。
- [x] 裁决 batch Trigger 是否也必须设置 `requiresSelection: true`，并在 ADR-0022 中记录决定。
- [x] 在 L2 与组件 DSL 文档中执行该决定。
- [x] 增加缺失 selection 的负向页面 fixture 和合法 batch toolbar fixture。
- [x] 确认 capability 校验仍要求同时具备 `table.selection` 与 `actions.batch.request`。

## V270（P1）显式 Record Source Method

- [x] 让 JavaScript 与 Python reference 对缺失 `recordSource.method` fail-closed。
- [x] 为该缺失字段定义一个稳定错误码和路径。
- [x] 增加缺失 method 的 conformance 向量，并保留非 GET 向量。
- [x] 验证 reference 行为与组件 DSL、L2 的必填字段处理一致。
- [x] 若文档定义了错误语义，同步 ADR-0021 与 request-construction 文档。

## V271（P1）Selection Key 不变量

- [x] 为 selection key 定义规范的标量谓词和相等语义。
- [x] 过滤 null、undefined、object、array 和非有限 number key。
- [x] 去重时保留首次选择顺序。
- [x] 将 `count` 规范化为结果 key 数组长度。
- [x] 在 JavaScript 与 Python table-state reference 中实现相同行为。
- [x] 增加重复、非法 key、混合标量类型和 count 规范化向量。
- [x] 确认批量 request 构造只消费规范化后的 selection 状态。

## V272（P2）ActionTrigger 非 Request 覆盖

- [x] 为页面 Trigger 导航和 modal 打开定义可观测结果。
- [x] 为三种 ActionTrigger 类型定义确认顺序和取消行为。
- [x] 添加框架无关的 JS/Python reference 支持或专门执行模型。
- [x] 添加包含确认场景的 request、navigate 和 modal conformance 向量。
- [x] 同步 ADR-0020、renderer 规范和 conformance 文档。

## 完成门禁

- [x] 每个已解决条目都有规范或 ADR 依据。
- [x] 组件 DSL/Schema 与 L2/L3a 一致地拒绝非法页面。
- [x] JavaScript 与 Python reference 对每个新增向量结果一致。
- [x] `npm run test:conformance:all` 通过。
- [x] `npm run validate:conformance` 通过。
- [x] `npm run validate:scenarios` 通过。
- [x] `npm run release:check` 通过（183 versioned cases；fixture digest `sha256:c78c732fc941612075c2e9ebc3da35180df54e85219d441d51901e24b177cf4e`）。
- [x] 审计索引更新后，`npm run check:links` 通过。
- [x] 在全部条目关闭或正式延期后，将报告和 checklist 移入 `docs/audit/archived/`，将 frontmatter 设为 `archived`，并更新两个审计索引。
