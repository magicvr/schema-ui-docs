---
status: archived
date: 2026-07-11
based_on: 0050-2026-07-11-review.md
---

# 审计 0050 跟踪清单

## V202（🟡）同步 `page.schema.json` DatasourceDeclaration.params 整值替换口径

- [x] V202a 更新 `docs/schemas/page.schema.json` `DatasourceDeclaration.properties.params.description`：
  - 与 `node.schema.json` DataRef.params 对齐：URL query、字面量或完整单个 `$deps.<path>` 整值替换、禁止模板拼接、不做条件判断。
  - 指向 `docs/04-datasource-contract.md` §3.1 与 `docs/08-renderer-spec.md` §2.3。
  - 保留 `not.required: ["responseMapping"]`（ADR-0005 D1）。
  - 完成：description 已同步。
- [x] V202b 可选：在 description 中注明页面级 `datasources` 无 form 上下文，完整 `$deps.*` 仍由 L3a `NON_FORM_DATA_PARAMS` 拒绝。
  - 完成：已写入 description。
- [x] V202c 文本抽查：`page.schema.json` 与 `node.schema.json` params 描述口径一致；`03`/`component-registry` 无回退。
- [x] V202d 在 `docs/CHANGELOG.md` Unreleased 记录本项，并在文件清单中列入 `docs/schemas/page.schema.json` 与本轮审计路径。

## V203（🟡）修正 Renderer expr-eval 示例的嵌套 `$deps` 路径

- [x] V203a 更新 `docs/08-renderer-spec.md` §5.2：
  - `$deps` 正则改为捕获完整点路径。
  - 值读取沿点路径从 `context.deps` 下钻；缺失短路为 `undefined`。
  - `dependencies` 校验使用路径**首段**。
  - 完成。
- [x] V203b 示例注释说明：`dependencies` 写字段名；表达式可访问 `$deps.<field>.…` 子路径。
  - 完成。
- [x] V203c 检查 `$context` 替换示例：改为完整点路径下钻，并限制 `user` / `features` 根。
  - 完成。
- [x] V203d 将 `docs/08-renderer-spec.md` frontmatter `last_updated` 更新为 `2026-07-11`（若当日已是该日则确认未回退）。
  - 确认：已是 `2026-07-11`，无需改动。
- [x] V203e 在 `docs/CHANGELOG.md` Unreleased 记录本项。

## 验证（两项共用）

- [x] V202/V203 验证：实现行为未改（仅 Schema 描述与文档示例）时执行：
  - 文本抽查：`page.schema.json` params 含「完整单个」/「禁止模板拼接」；`08` §5.2 不再使用 `/\$deps\.(\w+)/g` 作为唯一捕获。
  - `npm --prefix mcp run build` ✅
  - `npm --prefix mcp test` ✅（108/108）

## 汇总

| 编号 | 严重度 | 主题 | 状态 |
|---|---|---|---|
| V202 | 🟡 | `page.schema.json` datasources.params 整值替换残留 | ✅ |
| V203 | 🟡 | Renderer 嵌套 `$deps` 示例 | ✅ |

## 已执行验证

```text
npm --prefix mcp run build
npm --prefix mcp test   # 108/108
```