---
status: checklist
date: 2026-07-08
based_on: 0011-2026-07-08-review.md
---

# 审计 0011 跟踪清单

## 使用说明

- `[ ]` 待修复 / `[x]` 已完成
- 每项修复完成后，在右侧填写对应文件的修改摘要
- 修复完成并验证通过后，再将本轮审计文件移入 `archived/`

---

## W1（🟡）统一 L3 表达式校验的静态校验时机

| 字段 | 值 |
|---|---|
| **位置** | `06-validation.md` §1 校验层级、§2 CI 建议流程 |
| **问题** | L3 被定位为 Renderer 运行时，但 `02` / `08` 已要求加载配置时静态拒绝非法表达式 |
| **操作** | 将 L3 拆分为静态校验与运行时求值，或至少把 L3 时机改为加载配置时 / CI 可选前置 |
| **文件** | `06-validation.md` |

- [x] W1 更新 L3 校验层级表，拆分为 L3a 静态校验 + L3b 运行时求值
- [x] W1 更新 CI / 运行时流程图，区分表达式静态校验与运行时求值
- [x] W1 核对 `02-reaction-expression.md` §10 与 `08-renderer-spec.md` §5.4 的措辞一致

---

## W2（🟡）收紧 ADR-0004 中跨作用域变通方案残留

| 字段 | 值 |
|---|---|
| **位置** | `decisions/0004-row-level-scope.md` D3、后果章节 |
| **问题** | accepted ADR 仍描述“props 传入”变通方案，但当前正文未定义该协议机制 |
| **操作** | 删除或改写该变通方案，明确其不属于 v0.2 标准能力；与当前正文保持一致 |
| **文件** | `decisions/0004-row-level-scope.md` |

- [x] W2 ADR-0004 D3 不再把未标准化的 props 传入描述为当前可落地协议方案
- [x] W2 ADR-0004 与 `02-reaction-expression.md` §9.1 保持一致：后端预计算行字段或另起 ADR
- [x] W2 如保留历史讨论，明确标注为未标准化 / 不属于 v0.2 协议能力

---

## W3（🟢）更新数据表格场景中的 `visibleField` 旧过渡措辞

| 字段 | 值 |
|---|---|
| **位置** | `05-scenarios/data-table.md` 开头变更提示 |
| **问题** | 开头仍写“不引入 $row 表达式”，与当前 `visibleField` 语法糖语义不一致 |
| **操作** | 改为说明 `visibleField` 是 `scope: row` 的 `visibleWhen` 兼容语法糖，新项目推荐 `$row` 写法 |
| **文件** | `05-scenarios/data-table.md` |

- [x] W3 开头变更提示改用语法糖说明，不再使用旧措辞
- [x] W3 文案明确 `visibleField` 等价展开为 `scope: row` 的 `visibleWhen`
- [x] W3 后文"推荐写法 / 兼容写法"与开头说明保持一致

---

## W4（🟢）清理当前规范与 Schema 描述中的过程性审计编号

| 字段 | 值 |
|---|---|
| **位置** | `07-actions-contract.md`、`schemas/component-registry.json` |
| **问题** | 当前规范和机器可读描述仍引用“审计 §x.x / 计划 Bx”等过程性编号 |
| **操作** | 改为结果导向描述；历史编号保留在 `CHANGELOG.md` / `audit/archived/` |
| **文件** | `07-actions-contract.md`、`schemas/component-registry.json` |

- [x] W4 `07-actions-contract.md` 顶部不再引用过程性审计条目 / 计划条目
- [x] W4 `component-registry.json` `statCard.valueField.description` 改为当前协议描述
- [x] W4 `component-registry.json` `select.optionsSource.description` 改为当前协议描述
- [x] W4 检查当前规范与 Schema 中是否仍有不必要的"审计 § / 计划 B"残留（一并清理了场景文档中的残留）

---

## 验证

- [x] 修复完成后重新运行全量 JSON 解析检查，确认所有 `.json` 文件可解析
- [x] 修复完成后重新运行 Markdown 本地链接检查，确认断链列表为空
- [x] 人工核对 `06-validation.md` 中 L3 静态校验 / 运行时求值边界清晰
- [x] 人工核对 ADR-0004 与 `02-reaction-expression.md` §9.1 的跨作用域边界一致
- [x] 检查 `0011-2026-07-08-review.md` 中所有问题均已在本清单覆盖