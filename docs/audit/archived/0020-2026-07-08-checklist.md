---
status: checklist
date: 2026-07-08
based_on: 0020-2026-07-08-review.md
---

# 审计 0020 跟踪清单

> 本清单覆盖 4 个在 0019 归档后再次全量复审发现的问题，编号 V24-V27（延续 0019 的 V22-V23 编号体系）。

## 使用说明

- `[ ]` 待修复 / `[x]` 已完成
- 每项修复完成后，在右侧填写对应文件的修改摘要和 commit hash

---

## V24（🟡）`upload` 组件文档缺失 `placeholder` 字段定义

| 字段 | 值 |
|---|---|
| **位置** | `docs/03-component-registry.md` `upload` 组件 props 表格 |
| **问题** | `placeholder` 字段在 `component-registry.json` 中已定义（`since: "0.2"`），但 doc 表格中缺失 |
| **操作** | 在 `upload` 组件的 props 表格中补充 `placeholder` 行 |
| **文件** | `docs/03-component-registry.md` |

- [x] V24a 在 `upload` 组件 props 表格中补充 `placeholder` 行，标注 `(since 0.2)`。修改：`docs/03-component-registry.md` upload 表格，在 `action` 行与 `description` 行之间插入 `placeholder` 行。

---

## V25（🟢）`datePicker` / `dateRangePicker` 的 `placeholder` 缺少 `(since 0.2)` 标注

| 字段 | 值 |
|---|---|
| **位置** | `docs/03-component-registry.md` `datePicker` 和 `dateRangePicker` 组件 props 表格 |
| **问题** | `(since 0.2)` 标注缺失，但 `component-registry.json` 中有 `since: "0.2"`，且同文档其他组件的 `placeholder` 均有标注 |
| **操作** | 在两处表格的 `placeholder` 行补充 `(since 0.2)` |
| **文件** | `docs/03-component-registry.md` |

- [x] V25a 在 `datePicker` 组件的 `placeholder` 行补充 `（since 0.2）`。修改：`docs/03-component-registry.md` datePicker 表格。
- [x] V25b 在 `dateRangePicker` 组件的 `placeholder` 行补充 `（since 0.2）`。修改：`docs/03-component-registry.md` dateRangePicker 表格。

---

## V26（🟢）`02-reaction-expression.md` §9.1 交叉引用漂移

| 字段 | 值 |
|---|---|
| **位置** | `docs/02-reaction-expression.md` §9.1 |
| **问题** | `$row` 与 `$deps` 互斥规则引用 `§10.1`，但 §10.1 仅覆盖非表单 `visibleWhen` 场景，不直接对应互斥规则 |
| **操作** | 将引用从 `（见 §10.1）` 改为 `（见 §10 静态校验规则）` 或删除引用 |
| **文件** | `docs/02-reaction-expression.md` |

- [x] V26a 修正交叉引用。修改：`docs/02-reaction-expression.md` §9.1 中 `（见 §10.1）` → `（见 §10 静态校验规则）`。

---

## V27（🟢）`01-node-protocol.md` §3.3 `responseMapping` 章节引用精度

| 字段 | 值 |
|---|---|
| **位置** | `docs/01-node-protocol.md` §3.3 `data` 字段注释 |
| **问题** | 引用 `04-datasource-contract.md §4.1`，但 `responseMapping` 的具体内容在 §4.1.1 |
| **操作** | 将 `§4.1` 改为 `§4.1.1` |
| **文件** | `docs/01-node-protocol.md` |

- [x] V27a 修正章节号。修改：`docs/01-node-protocol.md` §3.3 中 `§4.1` → `§4.1.1`。

---

## 汇总

| 编号 | 严重度 | 类型 | 状态 |
|------|--------|------|------|
| V24 | 🟡 | 文档字段盲区 | [x] |
| V25 | 🟢 | `since` 标注遗漏 | [x] |
| V26 | 🟢 | 交叉引用漂移 | [x] |
| V27 | 🟢 | 章节引用精度 | [x] |

> 跟踪清单中所有项标记为 `[x]` 后，将本审计文件移入 `audit/archived/` 并在 `archived/README.md` 添加索引。

---

*生成时间：2026-07-08*
