---
status: checklist
date: 2026-07-10
based_on: 0035-2026-07-10-review.md
---

# 审计 0035 跟踪清单

## V88（🟡）L2 解析 `source: ref` 继承的 `datasources.*.responseMapping` 并做条件必填

| 字段 | 值 |
|---|---|
| **位置** | `scripts/validate-l2-components.js`；`docs/06-validation.md`；`mcp/tests/**/*` |
| **问题** | V71 只检查节点本地 `responseMapping`；映射写在 `datasources` 且节点仅 `source: ref` 时，table/chart 缺少 `list`/`total` 仍可通过 |
| **操作** | L2 以“生效映射”（本地覆盖优先，否则继承预声明）执行列表/分页条件必填；同步文档与 MCP 反例 |
| **文件** | `scripts/validate-l2-components.js`；`docs/06-validation.md`；`mcp/tests/validate-content.test.ts`；`mcp/tests/test-utils.ts` |

- [x] V88a 在 `validateResponseMapping()`（或等价路径）中，对 `data.source: ref` 解析生效 `responseMapping`：本地声明优先，否则读取 `doc.datasources[data.ref].responseMapping`。
- [x] V88b 仅当生效映射存在时：`table`/`chart` 必须有 `list`；`table.props.pagination.mode: server` 必须有 `total`。
- [x] V88c 保持既有本地映射行为不变：节点本地缺 `list`/`total` 仍拒绝；无任何映射时不强制（沿用默认字段名语义）。
- [x] V88d 更新 `docs/06-validation.md` L2 / `responseMapping` 相关说明，写明“本地或继承的映射均进入条件必填”。
- [x] V88e 新增 MCP `validate_content` 反例：`table` / `chart` 通过 `source: ref` 继承不完整 `datasources.*.responseMapping` 应失败；合法继承/本地覆盖应通过。
- [x] V88f 验证：
  - 构造最小 YAML：继承缺 `list` → L2 拒绝；继承完整 `list`+`total` → 通过；本地覆盖缺 `list` → 拒绝；无映射 → 通过
  - `node scripts/validate-l2-components.js <test-yaml> --json` → 正确报告违规
  - `npm --prefix mcp run build` → 通过（`schema-ui-mcp@0.2.8`）
  - `npm --prefix mcp test` → 28/28 通过
- 完成记录：2026-07-10
- 修改摘要：新增 `getEffectiveResponseMapping()`；`validateResponseMapping()` 按生效映射校验；MCP 增加继承/覆盖反例与正例

---

## V89（🟡）同步 v0.2.8 版本声明（CHANGELOG / 总纲 / MCP 包与文档示例）

| 字段 | 值 |
|---|---|
| **位置** | `docs/06-validation.md`；`docs/CHANGELOG.md`；`docs/00-overview.md`；`mcp/package.json` 及版本示例 |
| **问题** | `06` 已写 v0.2.8 引用完整性变更，但 CHANGELOG 无条目、总纲仍写 v0.2.7、MCP 包/Docker 示例仍为 0.2.7 |
| **操作** | 补齐 v0.2.8 发布闭环；协议补丁与 MCP `0.2.8` 同步 |
| **文件** | `docs/CHANGELOG.md`；`docs/00-overview.md`；`docs/06-validation.md`；`mcp/package.json`；`mcp/package-lock.json`；`README.md`；`docs/mcp/README.md`；`docs/mcp/v1-design.md`；`docs/mcp/v1-implementation-plan.md`；`docs/decisions/0007-mcp-protocol-reader-validator.md` |

- [x] V89a 在 `docs/CHANGELOG.md` 顶部新增 `v0.2.8` 条目：覆盖 0034 引用完整性校验（V85/V86）与本轮 V88 及文档同步项。
- [x] V89b 更新 `docs/00-overview.md` §5：最新补丁版本改为 `v0.2.8`，共享 `protocolVersion` 列表补上 `v0.2.8`。
- [x] V89c 确认 `docs/06-validation.md` 的 v0.2.8 变更说明与最终合入范围一致（含继承 `responseMapping` 语义）；验证时修复列表换行损坏。
- [x] V89d 将 `mcp/package.json`（及 lock 中包版本字段）升至 `0.2.8`。
- [x] V89e 同步根 `README.md`、`docs/mcp/README.md`、`docs/mcp/v1-design.md`、`docs/mcp/v1-implementation-plan.md`、ADR-0007 中的镜像/示例 tag 为 `0.2.8`；验证时补修 `docs/mcp/README.md` 末尾残留 `0.2.7` 文案。
- [x] V89f 验证：
  - 入口文档最新补丁/示例 tag 为 `0.2.8`（`00-overview` 历史共享列表中保留 `v0.2.7` 属预期）
  - `npm --prefix mcp run build` → 通过
  - `npm --prefix mcp test` → 28/28 通过
  - `git diff --check` → 无冲突/尾随空白问题
- 完成记录：2026-07-10

---

## V90（🟢）同步 `08-renderer-spec.md` §5.5 permissions 白名单描述

| 字段 | 值 |
|---|---|
| **位置** | `docs/08-renderer-spec.md` §5.5 |
| **问题** | 仍写 permissions 只允许任意 `$context.*`；L3a / `06` 已收紧为 `$context.user.*` / `$context.features.*` |
| **操作** | 更新 Renderer 静态校验清单中的 permissions 条目 |
| **文件** | `docs/08-renderer-spec.md` |

- [x] V90a 将 §5.5 permissions 条目改为仅允许 `$context.user.*` / `$context.features.*`，并写明禁止 `$deps.*` / `$self` / `$row.*` / `$parentRow.*` / 未登记 `$context` 根。
- [x] V90b 更新 `docs/08-renderer-spec.md` 的 `last_updated`（当前为 `2026-07-10`）。
- [x] V90c 验证：与 `docs/06-validation.md` L3a 描述、L3a 脚本规则口径一致；§5.4 变量白名单同步注明 permissions 额外受限。
- 完成记录：2026-07-10

---

## 汇总

| 编号 | 严重度 | 主题 | 涉及文件 | 状态 |
|---|---|---|---|---|
| V88 | 🟡 | ref 继承 responseMapping 条件必填 | L2 脚本 + 06 文档 + MCP 测试 | ✅ |
| V89 | 🟡 | v0.2.8 版本声明闭环 | CHANGELOG + 总纲 + MCP 包/文档示例 | ✅ |
| V90 | 🟢 | Renderer permissions 白名单描述 | `08-renderer-spec.md` | ✅ |

## 建议验证命令（修复后）

```bash
# L2 / 临时反例
node scripts/validate-l2-components.js ".tmp-audit-check/*.yaml" --json
npm run validate -- ".tmp-audit-check/*.yaml"

# MCP 构建与测试
npm --prefix mcp run build
npm --prefix mcp test

# 版本与文档一致性（人工抽查）
# - docs/00-overview.md 最新补丁 v0.2.8
# - docs/CHANGELOG.md 含 v0.2.8
# - mcp/package.json version 0.2.8

git diff --check
```