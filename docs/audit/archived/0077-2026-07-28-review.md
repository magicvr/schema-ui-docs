---
status: archived
date: 2026-07-28
based_on: schema-ui-protocol 2.7.0 / protocolVersion 2.7；正式 tag v2.7.0 / mcp-v2.7.0 @ ca9e5fe；延续已归档 0076
scope: focus=all；主类型=实现/契约漂移；次类型=发布完整性（v2.7 证据链）；不修改生产消费者仓库
---

# 审计报告 0077：v2.7.0 落地后 MCP/validator 身份、入口投影与发布证据

## 结论

本轮全量复审确认：工作树已交付 **v2.7.0**（ADR-0029–0033 / `form.controls.advanced` / 316 fixtures），核心规范、Schema、L2、conformance 与协议制品本地门禁绿。正式 **`v2.7.0` / `mcp-v2.7.0` 证据链已闭合**（2026-07-28）。

新发现问题 **4×P1 + 4×P2 + 1×P3（V363–V371）**：**全部关闭**。V363–V365、V367–V371 in-repo 闭合；**V366** 经 PR [#32](https://github.com/magicvr/schema-ui-docs/pull/32) → tag/CD 闭合。

| 判定 | 说明 |
|---|---|
| 正式 2.7.0 身份 | content `sha256:2170d612…` · artifact `sha256:45ceb27a…` · fixture `sha256:f9589dc0…`（316）— **不可变 pin** |
| MCP 镜像 | index `sha256:37749442…`（`2.7.0` / `2.7` / `latest` / `ca9e5fe…` 同 digest）；远端 smoke 通过 |
| 本轮关闭 | lock、validator SemVer、DOC_MAP、入口卫生、正式 tag/CD |
| 未改写 | **未**改写 `v2.6.0` / `mcp-v2.6.0` |

有效修复已沉淀到 lock、`validator/package.json`、MCP 源码/测试、README 与 conformance 说明；**本文不是协议权威来源**。`docs/audit/**` 不进入 `protocol-manifest.json`、协议制品、MCP 索引或 Docker 镜像。

## 范围与基线

### 审计类型

| 类型 | 本轮角色 |
|---|---|
| **实现/契约漂移** | **主类型** — MCP lock、validator SemVer、DOC_MAP、入口/文档投影 |
| **发布完整性** | **次类型** — v2.7.0 正式 tag/CD 未闭合（V366） |
| 提案评审 | 不适用 — ADR-0029–0033 已 accepted |

### 当前基线（步骤 1）

| 项 | 结果 |
|---|---|
| 工作树 / 分支 | `dev` @ `f048dde`（干净；ahead `origin/dev` by 1） |
| 工作树版本 | 根/manifest/MCP package **2.7.0**；页面协议 **`"2.7"`**；validator 误升 **2.7.0**（应独立 **1.0.0**） |
| 正式 tag | `v2.6.0` / `mcp-v2.6.0` |
| 活跃审计 | 创建前 **无**；本轮 **0077** |
| 问题编号 | 全局最大 **V362**（0076），本轮续编 **V363–V371** |

### 基线验证（步骤 1）

| 命令或证据 | 结果 |
|---|---|
| `npm run check:links` | 247 Markdown、702 相对链接通过 |
| `npm run release:check` | 316 cases；fixture `f9589dc0…` |
| `npm run verify:protocol-artifact` | reproducible；108 文件 |
| `npm run check:protocol-artifact-links` | 79 MD / 421 链接通过 |
| `npm run validate:scenarios` / `validate:conformance` | 官方 6 场景；16 versioned suites 绿 |
| `npm run test:conformance:all` | **35/35** |
| `release:check:mcp` | **失败** — lock `2.6.0` ≠ `2.7.0` |
| `release:check:validator` | 通过（仅 protocol 捆绑） |
| MCP `build` / `test` / `smoke:tools` | **134/134**；5 tools（DOC_MAP 仍指 v2.6） |
| 生产消费者 | 相邻仓只读：仍 pin **`v2.4.1`**（out-of-repo） |

## 覆盖率与验证

| 批次 | 已覆盖 | 未覆盖 / 低置信度 | 置信度 |
|---|---|---|---|
| A 规范与权威边界 | charter、manifest、`00`–`09`、Schema、ADR 0028–0033、v2.7 门禁 | 未通读全部历史 ADR | 高 |
| B 校验与 conformance | 全量 runner、advanced sample L0–L4 | reference 未全文 diff | 高 |
| C MCP 与 validator | lock、SemVer pin、DOC_MAP、build/test | Docker 2.7 未建 | 高 |
| D 自动化与发布 | workflows、远端 Release 列表 | **2.7 正式 CD 无** | 高（缺口） |
| E 生产消费者 | pin 只读 | 未跑消费者 CI | 中 |
| 历史审计 | 索引 + 0074–0076 查重；最大 NNNN=0076 / V=362 | 未通读 0001–0073 全文 | 索引高 |

## 问题清单（汇总表）

| 编号 | 级别 | 主题 | 状态 | 类型 |
|---|---|---|---|---|
| V363 | 🔴 P1 | MCP lockfile 仍为 2.6.0；`release:check:mcp` fail-closed | **已关闭** | 实现/契约漂移 |
| V364 | 🔴 P1 | validator SemVer 误升 2.7.0，与 MCP `schemaUiValidator` 1.0.0 冲突 | **已关闭** | 实现/契约漂移 |
| V365 | 🔴 P1 | MCP `v2-release-goals` 仍指向 v2.6 | **已关闭** | 实现/契约漂移 |
| V366 | 🟡 P1 | v2.7.0 正式 tag/CD/远端证据链未闭合 | **已关闭** | 发布完整性 |
| V367 | 🟢 P2 | 根 README 辅助入口仍停在 2.6 | **已关闭** | 文档/入口 |
| V368 | 🟢 P2 | `docs/mcp/README.md` 示例仍为 2.6.0 | **已关闭** | 文档/入口 |
| V369 | 🟢 P2 | `conformance/README.md` 仍写当前为 `"2.6"` | **已关闭** | 文档 |
| V370 | 🟢 P2 | `01-node-protocol` 类型注释仍写制品 `"2.6"` | **已关闭** | 规范正文卫生 |
| V371 | ⚪ P3 | 场景 README 缺 `form-controls-advanced` 门禁投影说明 | **已关闭** | 文案 |

**计数：** 0×P0、**4×P1**、**4×P2**、**1×P3**；**全部关闭**。  
**Out-of-repo（不编号）：** 生产消费者仍 pin `v2.4.1`。

## 问题详情（V363–V371）

### V363（🔴 P1）MCP lockfile 未随 2.7.0 升版 — 已关闭

**位置：** `mcp/package-lock.json`（`version` / `packages[""]` / `packages[".."]`）。

**证据：** `release:check:mcp` → `MCP lockfile version mismatch '2.6.0' !== '2.7.0'`。  
**修复：** 三处版本同步为 **2.7.0**（与 `mcp/package.json` 及根包一致）。  
**关闭证据：** `release:check:mcp` 绿。  
**查重：** 0076/V362（parent 门禁已在；本轮为 lock 内容未跟版）。

### V364（🔴 P1）validator SemVer 误升 — 已关闭

**位置：** `validator/package.json`；`mcp/package.json` → `schemaUiValidator`。

**证据：** v2.6 正式态 validator 为独立 **1.0.0**；`f048dde` 将 `version` 改为 2.7.0，MCP pin 仍为 1.0.0。  
**修复：** `validator.version` **恢复 `1.0.0`**；保留 `schemaUiProtocol` → 2.7.0。  
**关闭证据：** `release:check:mcp` / `release:check:validator` 绿；bundledValidatorVersion `1.0.0`。  
**查重：** 独立 SemVer 政策（README / 历史 2.6 线）。

### V365（🔴 P1）DOC_MAP 当前线仍 v2.6 — 已关闭

**位置：** `mcp/src/core/protocol-index.ts`；`mcp/tests/search.test.ts`。  
**修复：** `v2-release-goals` → `docs/release-goals/v2.7.md`；单测期望同步。  
**关闭证据：** `npm --prefix mcp test` **134/134**。  
**查重：** 0074/V351。

### V366（🟡 P1）正式 2.7.0 证据链 — 已关闭

**关闭证据（路径 1）：**

| 项 | 结果 |
|---|---|
| PR | [#32](https://github.com/magicvr/schema-ui-docs/pull/32) → `main` `ca9e5fe` |
| tag `v2.7.0` | Protocol Release [30329060753](https://github.com/magicvr/schema-ui-docs/actions/runs/30329060753) success |
| tag `mcp-v2.7.0` | MCP CD [30329060412](https://github.com/magicvr/schema-ui-docs/actions/runs/30329060412) success |
| 正式 tar | artifact `sha256:45ceb27a…`；content `sha256:2170d612…`；fixture `sha256:f9589dc0…` |
| GHCR | index `sha256:37749442…`（`2.7.0` / `2.7` / `latest` 同 digest）；smoke 通过 |

**禁止**改写 `v2.6.0` / `mcp-v2.6.0`（已遵守）。

### V367–V371 — 已关闭

入口 README、`docs/mcp/README`、conformance README、`01` 注释、场景 README advanced 注记；模式同 0074/V353–V358、0075/V360。

## 与历史审计关系

| 历史 | 关系 |
|---|---|
| 0074 V351–V356 | DOC_MAP / 入口 / conformance / 01 注释同模式在 2.7 复发 |
| 0076 V362 | lock parent 门禁仍有效；本轮 lock 根版本未跟 2.7 |
| 0074 V352 | 正式 tag 证据链模板 → V366 |

## 处理顺序与防复发

1. **先** V363+V364（否则 `release:check:mcp` 无法绿）→ V365 → 入口 P2/P3。  
2. **后** V366 发布（用户授权 push/tag；本审计不自动打 tag）。  
3. **防复发：** MINOR 升版清单须含 `mcp/package-lock.json` 三字段、`validator` 仅升 `schemaUiProtocol`、DOC_MAP 当前线、根/MCP/conformance 入口文案。
