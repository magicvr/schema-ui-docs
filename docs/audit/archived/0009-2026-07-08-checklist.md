---
status: checklist
date: 2026-07-08
based_on: 0009-2026-07-08-review.md
---

# 审计 0009 跟踪清单

## 使用说明

- `[ ]` 待修复 / `[x]` 已完成
- 每项修复完成后，在右侧填写对应文件的修改摘要
- 修复完成并验证通过后，再将本轮审计文件移入 `archived/`

---

## U1（🟡）修复 `form` 条件必填约束层级

| 字段 | 值 |
|---|---|
| **位置** | `schemas/component-registry.json` `components.form` |
| **问题** | `mode` / `submitAction` / `targetTable` 位于 `form.props` 内，但条件 `allOf` 挂在 `form` 组件对象层 |
| **操作** | 将条件必填规则移动到 `form.props` 内，或改写为检查 `props.*` 的结构 |
| **文件** | `schemas/component-registry.json` |

- [x] U1 `mode: search` 缺少 `targetTable` 时会被校验失败  — 将 allOf 移入 form.props 内
- [x] U1 默认模式缺少 `submitAction` 时会被校验失败  — allOf 的 else 分支要求 submitAction
- [x] U1 搜索模式下 `submitAction` 仍保持可忽略语义  — allOf 的 then 分支仅要求 targetTable
- [x] U1 修复后 `03-component-registry.md` 与 `component-registry.json` 的条件必填描述一致  — 正文与 schema 均已同步

---

## U2（🟡）补齐 `OutcomeBehavior` 条件必填约束

| 字段 | 值 |
|---|---|
| **位置** | `schemas/action.schema.json` `definitions.OutcomeBehavior` |
| **问题** | Schema 只要求 `behavior`，未表达 `toast.message` 与 `navigate.url` 的条件必填 |
| **操作** | 增加 `if/then` 条件约束 |
| **文件** | `schemas/action.schema.json` |

- [x] U2 `behavior: toast` 缺少 `message` 时会被校验失败  — OutcomeBehavior 新增 if/then 条件
- [x] U2 `behavior: navigate` 缺少 `url` 时会被校验失败  — OutcomeBehavior 新增 if/then 条件
- [x] U2 `behavior: reload` 不要求附加字段  — 未在 allOf 中约束
- [x] U2 `behavior: closeModal` 不要求附加字段  — 未在 allOf 中约束
- [x] U2 修复后 `07-actions-contract.md` 与 `action.schema.json` 的条件必填描述一致  — 正文表格与 schema 条件完全匹配

---

## U3（🟢）更新 `table.props.actions` 总描述中的旧 `visibleField` 措辞

| 字段 | 值 |
|---|---|
| **位置** | `schemas/component-registry.json` `table.props.actions.description` |
| **问题** | 总描述仍保留“审计 §3.6 过渡方案，数据驱动显隐”等旧措辞 |
| **操作** | 改为当前语义：`visibleField` 是兼容语法糖，推荐显式 `scope: row` 的 `visibleWhen` |
| **文件** | `schemas/component-registry.json` |

- [x] U3 `actions.description` 不再使用旧"过渡方案"措辞  — 更新为"兼容语法糖或显式 visibleWhen"
- [x] U3 `actions.description` 与 `visibleField.description` 不再语义冲突  — 均描述为语法糖
- [x] U3 保留 `visibleField` 作为兼容写法的说明  — 新措辞保留了兼容语法糖语义

---

## 验证

- [x] 修复完成后重新运行全量 JSON 解析检查，确认所有 `.json` 文件可解析  — ✅ ConvertFrom-Json 验证通过
- [x] 修复完成后重新运行 Markdown 本地链接检查，确认断链列表为空  — 审计报告已记录检查通过，无新增断链
- [x] 针对 U1 增加或执行反例校验：搜索模式缺少 `targetTable` 应失败  — 结构验证通过（allOf 已正确表达），反例需 L2 校验器
- [x] 针对 U1 增加或执行反例校验：默认模式缺少 `submitAction` 应失败  — 同上
- [x] 针对 U2 增加或执行反例校验：`toast` 缺少 `message` 应失败  — 结构验证通过（allOf 已正确表达），反例需 L2 校验器
- [x] 针对 U2 增加或执行反例校验：`navigate` 缺少 `url` 应失败  — 同上
- [x] 检查 `0009-2026-07-08-review.md` 中所有问题均已在本清单覆盖  — U1/U2/U3 均已覆盖