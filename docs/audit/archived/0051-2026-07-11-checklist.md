---
status: archived
date: 2026-07-11
based_on: 0051-2026-07-11-review.md
---

# 审计 0051 跟踪清单

## V204（🟡）修复 L3a 表格字段异常分类

- [x] V204a 在 `scripts/validate-l3a-expressions.js` 的 table 扫描中，对 `props.columns` / `props.actions` 使用独立 `Array.isArray()` 守卫；错误类型跳过 L3a 扫描，不抛异常。
  - 完成：`columns` / `actions` 非数组时分别使用空数组，仅跳过对应 L3a 表达式扫描。
- [x] V204b 确认合法数组中的 `visibleWhen`、`reactions`、`permissions` 扫描行为不变。
  - 完成：原数组遍历主体未改；官方场景与全量 MCP 测试通过。
- [x] V204c 在 `mcp/tests/validate-content.test.ts` 增加回归：`columns` / `actions` 为 object、string、`null`；断言结构违规保留、`parseError === null`、`internalError === null`。
  - 完成：新增 6 组参数化回归。
- [x] V204d 同步 `docs/06-validation.md`，明确 L3a 对结构不适用字段跳过语义扫描，不得提升为解析错误。
- [x] V204e 在 `docs/CHANGELOG.md` Unreleased 记录实现、测试与文档同步。
- [x] V204f 执行针对性验证与 MCP 回归，并记录命令和结果。
  - `npm --prefix mcp test -- validate-content.test.ts`：105/105 通过。
  - `npm --prefix mcp run build`：通过。
  - `npm --prefix mcp test`：115/115 通过。
  - `npm --prefix mcp run smoke:tools`：5 个工具注册与调用 smoke 通过。

## V205（🟡）执行 `contains` 右操作数字面量约束

- [x] V205a 在 `scripts/validate-l3a-expressions.js` 中要求 `contains` 右操作数为字符串、数字、布尔或 `null` 字面量。
  - 完成：token 状态机在 `contains` 运算符处检查紧随其后的 token 类型。
- [x] V205b 拒绝 `$deps.roles contains $deps.targetRole` 与 `$deps.roles contains ($deps.targetRole)`，并保持错误消息/代码稳定可消费。
  - 完成：两类反例均返回 `SYNTAX`，消息包含“contains 的右操作数必须是字符串、数字、布尔或 null 字面量”。
- [x] V205c 增加正例：`contains 'admin'`、`contains 1`、`contains true`、`contains null` 均通过。
- [x] V205d 增加 MCP 回归测试，覆盖直接变量、括号变量与合法字面量。
- [x] V205e 更新 `docs/06-validation.md` L3a 层级说明与人工自检清单，明确 `contains` 操作数限制。
- [x] V205f 在 `docs/CHANGELOG.md` Unreleased 记录修复。
- [x] V205g 执行针对性验证与 MCP 回归，并记录命令和结果。
  - `node --check scripts/validate-l3a-expressions.js`：通过。
  - `npm --prefix mcp test -- validate-content.test.ts`：105/105 通过。
  - `npm --prefix mcp run build`：通过。
  - `npm --prefix mcp test`：115/115 通过。
  - `npm --prefix mcp run smoke:tools`：通过。

## 验证（两项共用）

- [x] L3a 针对性样例：
  - 非数组 `columns/actions` 不再导致脚本解析异常。
  - `contains` 合法字面量通过，变量/括号变量反例失败。
- [x] `npm --prefix mcp run build`
- [x] `npm --prefix mcp test`（115/115）
- [x] `npm --prefix mcp run smoke:tools`
- [x] 根据实际修改更新本清单，记录修改文件、内容摘要和验证结果。

## 修改文件

- `scripts/validate-l3a-expressions.js`：增加 table 数组守卫与 `contains` 右操作数字面量检查。
- `mcp/tests/validate-content.test.ts`：增加 V204 的 6 组错误分类回归，以及 V205 的 4 个正例与 2 个反例。
- `docs/06-validation.md`：同步 L3a 执行边界与人工自检项。
- `docs/CHANGELOG.md`：记录 0051/V204-V205 及活跃审计路径。
- `docs/audit/0051-2026-07-11-review.md`、本清单、`docs/audit/README.md`：审计证据链与活跃状态。

## 汇总

| 编号 | 严重度 | 主题 | 状态 |
|---|---|---|---|
| V204 | 🟡 | L3a 表格字段异常分类 | ✅ |
| V205 | 🟡 | `contains` 右操作数字面量约束 | ✅ |