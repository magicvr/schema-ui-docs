---
status: checklist
date: 2026-07-08
based_on: 0018-2026-07-08-review.md
---

# 审计 0018 跟踪清单

> 本清单覆盖 2 个在 0017 归档后再次全量复审发现的问题，编号 V20-V21（延续 0017 的 V18-V19 编号体系）。

## 使用说明

- `[ ]` 待修复 / `[x]` 已完成
- 每项修复完成后，在右侧填写对应文件的修改摘要和 commit hash

---

## V20（🟡）`responseMapping` 的条件必填语义没有落入校验清单

| 字段 | 值 |
|---|---|
| **位置** | `04-datasource-contract.md` §4.1.1；`schemas/node.schema.json` `ResponseMapping`；`06-validation.md` §1 与 §6；`decisions/0005-response-mapping.md` D3/D4 |
| **问题** | 正文要求列表类接口必须提供 `responseMapping.list`，服务端分页表格必须提供 `responseMapping.total`，但 L1 Schema 只能表达 `minProperties: 1`，`06-validation.md` 未把条件必填落入语义校验清单 |
| **操作** | 在校验文档中明确该规则需要结合组件类型和 props 做 L2/L4 或人工 review，避免把 `node.schema.json` 的结构下限误认为完整契约 |
| **文件** | `docs/06-validation.md`，必要时补充 `docs/04-datasource-contract.md` 或 `docs/schemas/node.schema.json` 描述 |

- [x] V20a 在 `06-validation.md` 的校验层级中说明 `responseMapping` 的 `list` / `total` 条件必填属于语义校验，不能只靠 L1 Schema 完成
- [x] V20b 在 `06-validation.md` 自检清单中新增列表类接口必须声明 `responseMapping.list` 的检查项
- [x] V20c 在 `06-validation.md` 自检清单中新增 `table.props.pagination.mode: server` 必须声明 `responseMapping.total` 的检查项
- [x] V20d 核对 `04-datasource-contract.md` 与 ADR-0005 的条件必填表述是否无需调整

---

## V21（🟢）0017 修复链触达的文档未同步 `last_updated`

| 字段 | 值 |
|---|---|
| **位置** | `03-component-registry.md` frontmatter；`06-validation.md` frontmatter；0017 V18 修复记录 |
| **问题** | 0017 V18 已更新 `03-component-registry.md` 与 `06-validation.md` 的 DSL / L2 校验说明，但两个文档的 `last_updated` 仍为 `2026-07-07` |
| **操作** | 将已触达文档的 frontmatter 更新时间同步为 `2026-07-08` |
| **文件** | `docs/03-component-registry.md`、`docs/06-validation.md` |

- [x] V21a 将 `03-component-registry.md` 的 `last_updated` 更新为 `2026-07-08`
- [x] V21b 将 `06-validation.md` 的 `last_updated` 更新为 `2026-07-08`
- [x] V21c 核对 `07-actions-contract.md` 是否确有 2026-07-08 修复链触达；若无直接证据则保持现状

---

## 汇总

| 编号 | 严重度 | 类型 | 状态 |
|------|--------|------|------|
| V20 | 🟡 | 语义校验缺口 | [x] |
| V21 | 🟢 | 元数据漂移 | [x] |

> **全部项目完成后，** 将本审计文件移入 `audit/archived/` 并在 `archived/README.md` 添加索引。

---

*生成时间：2026-07-08*