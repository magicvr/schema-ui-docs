---
status: archived
date: 2026-07-11
based_on: 0053-2026-07-11-review.md
---

# 审计 0053 跟踪清单

## V207（🟡）修正 `$context`「所有位置」与 params 禁令矛盾

- [x] V207a 修改 `docs/02-reaction-expression.md` §2：`$context.user.*` / `$context.features.*` 的可用位置改为条件表达式挂载点，明确不含 `params` 值替换。
  - 完成：可用位置改为 `reactions` / `visibleWhen` / `permissions` / 表格列与操作表达式，并排除三类 params。
- [x] V207b 交叉引用附录 A 或 §10.7，避免读者以为 params 可用 `$context.*`。
  - 完成：§2 行内引用附录 A / §10.7。

## V208（🟡）将 `contains` 右操作数约束写入权威静态规则

- [x] V208a `02` §3 枚举右操作数允许类型：字符串 / 数字 / 布尔 / `null`。
  - 完成：§3 正文补充字面量类型枚举并禁止变量/分组。
- [x] V208b `02` 新增 §10.8：`contains` 右操作数必须为 §4 字面量；拒绝变量与分组表达式。
  - 完成：新增 `### 10.8 contains 右操作数字面量约束`，含正例与 L3a 对齐说明。
- [x] V208c `08` §5.5 补一条与 `06` / L3a 同口径的 bullet。
  - 完成：§5.5 增加 contains 右操作数条目并链到 §10.8。

## V209（🟡）同步 `reaction.schema.json` dependencies 描述

- [x] V209a 更新 `dependencies.description`：form 写字段名（无 `$deps.` 前缀）；row 写 `$row.` 之后完整点路径（无 `$row.` 前缀）；未引用 `$deps.*` / `$row.*` 时可为空数组。
  - 完成：与 `02` §8.1 对齐，补充仅引用 `$self`、`$context.*` 或不引用变量的空数组场景，并禁止带前缀字符串。

## V210（🟢）修正 MCP README 的 CHANGELOG 路径

- [x] V210a `docs/mcp/README.md`：仓库根 `CHANGELOG.md` → `docs/CHANGELOG.md`（相对路径 `../CHANGELOG.md`）。
  - 完成：改为 `[docs/CHANGELOG.md](../CHANGELOG.md)`。

## V211（🟢）统一 `$parentRow`「静态拒绝」措辞

- [x] V211a `03-component-registry.md` RowAction.requestMapping：暂不支持 → 静态拒绝。
  - 完成：改为「v0.2 静态拒绝 `$parentRow.*`（无嵌套表格挂载结构）」。
- [x] V211b `component-registry.json` 对应 description 同步。
  - 完成：DSL 描述同口径。

## V212（🟢）修正 `02` §1 对 `04` 整值替换章节的链接

- [x] V212a 「数据请求参数」行主链改为 `04` §3.1。
  - 完成：主链 §3.1，并保留 §3.2 作用域次链。

## 元数据与验证

- [x] M1 `docs/CHANGELOG.md` Unreleased 记录 0053 / V207–V212，并列入审计路径。
  - 完成：补 6 条修复说明；涉及文件清单增加 `reaction.schema.json` 与归档后的 0053 路径。
- [x] M2 更新 `docs/audit/README.md` 当前活跃审计与跟踪项。
  - 完成：活跃表与跟踪项已写入 0053 / V207–V212。
- [x] M3 触达文档 `last_updated` 按需同步（`02` / `03` / `08`）。
  - 完成：三份文档 frontmatter 已为 `2026-07-11`，无需再改日期。
- [x] V 验证：文档抽查 + MCP 最小回归。
  - 抽查：§2 无「所有位置」；§10.8 / `08` §5.5 / Schema description / MCP CHANGELOG 路径 / parentRow 静态拒绝 / §3.1 主链均在位。
  - `git diff --check`：通过（仅 Git 行尾转换提示，无差异错误）。
  - `reaction.schema.json` JSON 解析：通过。
  - `dependencies: []` + 常量表达式 `true == true` 的 L3a 探针：通过。
  - `npm --prefix mcp run build`：通过。
  - `npm --prefix mcp test`：115/115 通过。
  - `npm --prefix mcp run smoke:tools`：5 个工具 smoke 通过。

## 修改文件

- `docs/02-reaction-expression.md`：V207 / V208 / V212
- `docs/08-renderer-spec.md`：V208c
- `docs/schemas/reaction.schema.json`：V209
- `docs/mcp/README.md`：V210
- `docs/03-component-registry.md`、`docs/schemas/component-registry.json`：V211
- `docs/CHANGELOG.md`：Unreleased 与文件清单
- `docs/audit/archived/0053-2026-07-11-review.md`、本清单、`docs/audit/README.md`、`docs/audit/archived/README.md`：审计证据链

## 汇总

| 编号 | 严重度 | 主题 | 状态 |
|---|---|---|---|
| V207 | 🟡 | `$context` 可用位置 vs params | ✅ |
| V208 | 🟡 | contains 右操作数进 §10 / §5.5 | ✅ |
| V209 | 🟡 | reaction.schema dependencies 描述 | ✅ |
| V210 | 🟢 | MCP CHANGELOG 路径 | ✅ |
| V211 | 🟢 | parentRow 静态拒绝措辞 | ✅ |
| V212 | 🟢 | 02§1 → 04§3.1 链接 | ✅ |
