---
status: draft
owner: AI 助手
date: 2026-07-07
based_on: 0002-2026-07-07-review.md
---

# 第二次协议审计修复跟踪清单

> 来源：[0002 审视报告](./0002-2026-07-07-review.md) | 创建日期：2026-07-07
>
> 状态标记：`[ ]` 待处理 · `[x]` 已完成 · `[~]` 进行中 · `[-]` 不予处理（需注明原因）

---

## P0：立即修复

- [x] **D1. `04-datasource-contract.md` 第 7 节"静态数据"重复**（审视报告 §D1）
  - **问题：** 第 101 行和第 138 行完全重复两次，导致编号错乱，第 8 节引用链断裂
  - **修复方式：** 删除第 138 行开始的重复段（第 101–135 行内容保留不动）
  - **涉及文件：** `docs/04-datasource-contract.md`
  - **验证方法：** 确认 `## 7. 静态数据` 在文档中只出现一次，第 8 节（optionsSource 契约）编号正确

---

## P1：v0.2.1

- [x] **D2. 缺少顶层文档结构 `page.schema.json`**（审视报告 §D2）
  - **问题：** 现有 `schemas/` 只能校验 Node/Action/Reaction，无顶层 `meta`+`datasources`+`body`+`actions` 的完整文档 Schema
  - **影响：** `meta.protocolVersion`（必填）无法自动校验，`DataSourceDef` 无对应 Schema
  - **修复方式：** 新增 `schemas/page.schema.json`，定义完整页面文档结构
  - **涉及文件：** 新建 `schemas/page.schema.json`；更新 `06-validation.md` 中 L1 校验的文档引用
  - **参考内容：** 审视报告 §D2 中的示例 JSON Schema

---

## P2：v0.2.1

- [x] **D3. `01-node-protocol.md` 中 `tabs` 仍被列为使用 `children` 的容器**（审视报告 §D3）
  - **位置：** `01-node-protocol.md §3.4`，第 3 句
  - **修复方式：** 将 `tabs` 从列举中移除，或加上脚注说明"since 0.2 改由 `items[].content` 承载，不再使用 `children`"
  - **涉及文件：** `docs/01-node-protocol.md`

- [x] **D4. `table.props.columns` 缺少完整 items Schema 定义**（审视报告 §D4）
  - **问题：** `component-registry.json` 中 `table.props.columns` 仅定义为 `{ type: "array", required: true }`，缺少 `field`/`label`/`format`/`tagMap` 等子字段定义
  - **涉及文件：** `schemas/component-registry.json`
  - **参考内容：** `03-component-registry.md` 中 ColumnDef 表格的完整字段

- [x] **D5. 错误响应体 `code` 字段语义未定义**（审视报告 §D5）
  - **位置：** `04-datasource-contract.md §5`
  - **修复方式：** 补充说明 `code` 是否被前端用于程序化判断，或注明仅用于调试日志
  - **涉及文件：** `docs/04-datasource-contract.md`

---

## P3：v0.2.2

- [x] **D6. `reaction.schema.json` 的 `dependencies.minItems: 1` 不允许仅 `$self` 场景**（审视报告 §D6）
  - **修复方式：** 将 `minItems: 1` 改为 `minItems: 0` 或移除该约束
  - **涉及文件：** `schemas/reaction.schema.json`

- [x] **D7. `DataRef.method` 枚举与 `action.schema.json` 不一致**（审视报告 §D7）
  - **问题：** 前者仅 `["GET", "POST"]`，后者 `["GET", "POST", "PUT", "DELETE", "PATCH"]`
  - **修复方式：** 同步两者枚举值；若有意限制则加注释说明理由
  - **涉及文件：** `schemas/node.schema.json`

- [ ] **E1. C 组 ADR（C1–C4）仍无进展**（审视报告 §E1）
  - **建议：** 优先启动 C1 ADR（`visibleWhen` + `$context`），中后台系统普遍需求
  - **需产出文件：** `docs/decisions/0003-context-namespace-and-visible-when.md`
  - **依赖：** 无，可独立启动

- [x] **E2. `supportsReactions` 未在文档中标准化为表格列**（审视报告 §E2）
  - **修复方式：** 在 `03-component-registry.md` 的"如何阅读本表"部分增加 `reactions` 支持列
  - **涉及文件：** `docs/03-component-registry.md`

---

## 查看归属速查

| 审视编号 | 优先级 | 类型 | 建议版本 |
|---|---|---|---|
| D1 | 🔴 P0 | 文档错误 | 立即修复（PATCH） |
| D2 | 🟠 P1 | Schema 缺口 | v0.2.1 |
| D3 | 🟡 P2 | 文档矛盾 | v0.2.1 |
| D4 | 🟡 P2 | Schema 缺口 | v0.2.1 |
| D5 | 🟡 P2 | 文档不明确 | v0.2.1 |
| D6 | 🟢 P3 | Schema 缺陷 | v0.2.2 |
| D7 | 🟢 P3 | Schema 不一致 | v0.2.2 |
| E1 | 🟢 P3 | 未完成项 | v0.3 规划 |
| E2 | 🟢 P3 | 文档改进 | v0.2.2 |
