---
status: checklist
date: 2026-07-07
based_on: 0007-2026-07-07-review.md
---

# 审计 0007 跟踪清单

## 使用说明

- `[ ]` 待修复 / `[x]` 已完成
- 每项修复完成后，在右侧填写对应文件的修改摘要

---

## S1（🔴）`select` 组件补充 `required` 和 `defaultVisible`

| 字段 | 值 |
|---|---|
| **位置** | `03-component-registry.md` select 表格 + `schemas/component-registry.json` `select.props` |
| **操作** | 在文档和 Schema 的 `select.props` 中增加 `required`（boolean, 可选）和 `defaultVisible`（boolean, 可选）两个字段 |
| **文件** | `03-component-registry.md`、`component-registry.json` |

- [x] S1 `03-component-registry.md` select 表格补充 `required` 行（类型 boolean，说明"是否必填"）
- [x] S1 `03-component-registry.md` select 表格补充 `defaultVisible` 行（类型 boolean，说明"初始是否可见"）
- [x] S1 `component-registry.json` `select.props` 增加 `required` 和 `defaultVisible` 字段定义

---

## S2（🔴）`dateRangePicker` 的 `reactions` 语义定义

| 字段 | 值 |
|---|---|
| **位置** | `03-component-registry.md` dateRangePicker 章节 + `schemas/component-registry.json` |
| **操作** | 明确 `dateRangePicker` 在 reactions 语义中的求值行为，需在文档中补充"reactions 注意事项"章节 |
| **文件** | `03-component-registry.md`（必要时更新 `02-reaction-expression.md`） |

**需要澄清的问题（至少需覆盖以下三个）：**
1. `$deps` 引用：其他字段引用 `dateRangePicker` 时，`dependencies` 应填写 `startField` 的值、`endField` 的值、还是二者均可？
2. `$self` 语义：`dateRangePicker` 自己的 `reactions` 中使用 `$self` 时，代表什么？
3. 取值范围：`$deps.xxx` 取到的是什么类型的数据结构？

- [x] S2 `03-component-registry.md` dateRangePicker 章节补充 `reactions` 注意事项说明
- [x] S2 确认 `02-reaction-expression.md` 无需额外修改（dateRangePicker 的 `$self` 语义已在本组件文档中定义）

---

## S3（🔴）`02-reaction-expression.md` §9.3 措辞修正

| 字段 | 值 |
|---|---|
| **位置** | `02-reaction-expression.md` §9.3 |
| **操作** | `"scope: row 的 reactions/otherwise"` → `"scope: row 的 fulfill/otherwise"` |
| **文件** | `02-reaction-expression.md` |

- [x] S3 `02-reaction-expression.md` §10.4 中 `reactions`/`otherwise` → `fulfill`/`otherwise`（实际问题位置在 §10.4 而非 §9.3）

---

## S4（🟡）明确 `data.params` 中 `$deps.*` 的作用域边界

| 字段 | 值 |
|---|---|
| **位置** | `04-datasource-contract.md` §3.1 + `08-renderer-spec.md` §2.3 |
| **操作** | 补充规则：`data.params` 中的 `$deps.*` 引用仅在表单上下文有效；非表单上下文中引用 `$deps.*` 时静态校验拒绝（与 `visibleWhen` 的非表单约束一致） |
| **文件** | `04-datasource-contract.md`、`08-renderer-spec.md` |

**已决策：方案 A（严格）— 非表单上下文 `data.params` 中禁止 `$deps.*`，静态校验拒绝。设计理由：与 `visibleWhen`/`permissions.*` 的非表单约束策略一致，在不该出现的位置使用就是配置错误。**

- [x] S4 决策 `data.params` 中 `$deps.*` 的作用域策略后再修改（方案 A，已决策）
- [x] S4 `04-datasource-contract.md` §3.2 补充作用域规则
- [x] S4 `08-renderer-spec.md` §2.3 补充作用域规则

---

## S5（🟡）修正搜索模式示例结构

| 字段 | 值 |
|---|---|
| **位置** | `03-component-registry.md` form 组件章节，搜索模式示例 |
| **操作** | 将 `form` 和 `table` 包裹在一个容器节点中作为 `body` 的单一 Node |
| **文件** | `03-component-registry.md` |

- [x] S5 `03-component-registry.md` 搜索模式示例改为合法的单一 `body` Node 结构（包裹在 `section` 中）

---

## S6（🟢）`CHANGELOG.md` 移除"方案 A"措辞

| 字段 | 值 |
|---|---|
| **位置** | `CHANGELOG.md` v0.2.1 |
| **操作** | `"searchForm（方案 A）"` → `"searchForm（`form` 新增 `mode: search` 模式）"`；或保留方案 A 标识但补充"（方案 B 因复杂度原因未采纳，详见审计 0003 记录）" |
| **文件** | `CHANGELOG.md` |

- [x] S6 `CHANGELOG.md` v0.2.1 中 "searchForm（方案 A）" → "searchForm"

---

## S7（🟢）`component-registry.json` 中 `placeholder` 补充 `since` 标注

| 字段 | 值 |
|---|---|
| **位置** | `schemas/component-registry.json` `datePicker.props`、`dateRangePicker.props`、`upload.props` |
| **操作** | 为三个组件中 `placeholder` 字段补充 `since: "0.2"` 标注（与同组件的 `description`/`tooltip` 保持一致） |
| **文件** | `component-registry.json` |

- [x] S7 `datePicker.props.placeholder` 补充 `since: "0.2"`
- [x] S7 `dateRangePicker.props.placeholder` 补充 `since: "0.2"`
- [x] S7 `upload.props.placeholder` — 实际不存在 `placeholder` 字段，无需修改

---

## S8（🟢）`05-scenarios/` 目录增加索引文件

| 字段 | 值 |
|---|---|
| **位置** | `docs/05-scenarios/` |
| **操作** | 创建 `README.md` 索引文件，列出各场景文件的用途和阅读顺序 |
| **文件** | `05-scenarios/README.md`（新建） |

- [x] S8 创建 `05-scenarios/README.md` 索引文件

---

## 汇总

| 编号 | 严重度 | 状态 |
|------|--------|------|
| 编号 | 严重度 | 状态 |
|------|--------|------|
| S1 | 🔴 | [x] |
| S2 | 🔴 | [x] |
| S3 | 🔴 | [x] |
| S4 | 🟡 | [x] |
| S5 | 🟡 | [x] |
| S6 | 🟢 | [x] |
| S7 | 🟢 | [x] |
| S8 | 🟢 | [x] |

---

## 优先级与建议版本

| 编号 | 严重度 | 类型 | 建议版本 | 建议处理方式 |
|---|---|---|---|---|
| S1 | 🔴 | 组件遗漏 | v0.2.3 | 立即修复（一行级修改，低风险） |
| S2 | 🔴 | 语义未定义 | v0.2.3 | 需协议决策层讨论后补充文档说明 |
| S3 | 🔴 | 措辞错误 | v0.2.3 | 立即修复（三处字符替换，零风险） |
| S4 | 🟡 | 规则缺失 | v0.2.3 | 需团队讨论决策作用域策略后再修改 |
| S5 | 🟡 | 示例错误 | v0.2.3 | 立即修复（示例结构调整） |
| S6 | 🟢 | 措辞优化 | v0.2.3 | 低优先级，一行修改 |
| S7 | 🟢 | 标注补齐 | v0.2.3 | 低优先级，三处 JSON 补字段 |
| S8 | 🟢 | 目录完善 | v0.2.3 | 新建 README 文件 |

> 建议优先处理 **S1、S3、S5**（修复成本低、影响明确），然后按团队节奏处理 **S2、S4**（需决策），**S6–S8** 可在碎片时间完成。

---

*生成时间：2026-07-07*
