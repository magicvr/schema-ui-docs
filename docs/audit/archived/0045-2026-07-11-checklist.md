---
status: archived
date: 2026-07-11
based_on: 0045-2026-07-11-review.md
---

# 审计 0045 跟踪清单

## V175（🟡）表单 visibleWhen 拒绝 `$self`

- [x] V175a L3a：表单上下文 `visibleWhen` 仅允许 `$deps.*` 与 `$context.user.*` / `$context.features.*`。
- [x] V175b 增加 MCP 反例：`formVisibleWhenSelfYaml` → `FORM_VISIBLEWHEN_VARS`。
- [x] V175c 执行 `npm --prefix mcp test`（含 L3a 路径）。
- 完成记录：`scripts/validate-l3a-expressions.js` 新增规则 7b；`mcp/tests/test-utils.ts` + `validate-content.test.ts` 覆盖。

## V176（🟡）表格 actions 任意 scope 拒绝 `$self`

- [x] V176a L3a：`location === 'tableAction' && hasSelfRef` 即拒。
- [x] V176b 同步 `02` §10.3 / §9.2 与 `08` §5.5。
- [x] V176c 增加 form-scope actions `$self` 反例 `tableActionFormScopeSelfYaml`。
- [x] V176d MCP 测试通过。
- 完成记录：规则 8 去掉 scope 限制；文档与测试已同步。

## V177（🟡）删除「表单字段 scope:row」死路径

- [x] V177a 改写 `02` §9.3 状态键表。
- [x] V177b 改写 `08` §5.5。
- [x] V177c 与 V190 一并在 §10.6 写明挂载限制。
- 完成记录：状态键表仅两列；`02` §9 作用域表亦收紧 `$self` 可用位置。

## V178（🟡）对齐 requestMapping `$` 规则

- [x] V178a 重写 `07` §3.1：任意位置含 `$` 须整段 `$row.*`。
- 完成记录：与 L2 `includes('$')` 一致。

## V179（🟡）对齐 01/08 非表单 visibleWhen

- [x] V179a 更新 `01` §3.8。
- [x] V179b 更新 `08` §5.5。
- 完成记录：表单/非表单 visibleWhen 白名单与 `02`/L3a 对齐。

## V180（🟡）MCP 官方场景覆盖 search/upload

- [x] V180a `it.each` 加入两个新场景路径。
- [x] V180b `npm --prefix mcp test` 通过（108 项）。
- 完成记录：`validate-content.test.ts` 官方场景现为 6 个。

## V181（🟡）ADR 同步 V167

- [x] V181a 更新 ADR-0004 D2a。
- [x] V181b 更新 ADR-0006 D5 注记。
- 完成记录：表格任意 scope 禁 required/value 已写入 ADR。

## V182（🟡）ADR-0004 同步 V164/V171

- [x] V182a D2 dependencies 精确写法。
- [x] V182b 附录矩阵补 actions form-scope 两行。
- 完成记录：与 `02` §8.1 / 附录 A 对齐。

## V183（🟡）06 自检清单

- [x] V183a 扩展 §6 勾选项；同步 L3a 表描述与 L4 扫描范围（V188）。
- 完成记录：含 `$row` deps、表格任意 scope、form visibleWhen、actions `$self`。

## V184（🟢）CHANGELOG 路径

- [x] V184a 0044 路径改为 archived；追加 0045 Unreleased。
- 完成记录：`docs/CHANGELOG.md` Unreleased 已更新。

## V185（🟢）lock 版本

- [x] V185a 根与 mcp lock 对齐 0.2.8。
- 完成记录：`package-lock.json` / `mcp/package-lock.json` 的 `version` 与 `".."` 均为 `0.2.8`。

## V186（🟢）去 BOM

- [x] V186a 两份新场景去 BOM。
- 完成记录：UTF-8 无 BOM 重写。

## V187（🟢）upload 场景文案

- [x] V187a description 改为「每文件不超过 10MB」。
- 完成记录：`form-with-upload.md`。

## V188（🟢）L4 文档范围

- [x] V188a 更新 `06` §3（fulfill/otherwise + 深度扫描）。
- 完成记录：与 `lint-l4-banned-props.js` 一致。

## V189（🟢）MCP 实施计划目录树

- [x] V189a 更新 `v1-implementation-plan.md` §2 为当前结构。
- 完成记录：注明以仓库为准。

## V190（🟢）§10 ROW_SCOPE_MOUNT

- [x] V190a 在 `02` §10.6 增加挂载点规则。
- 完成记录：与 L3a / `06` / `08` 一致。

## 汇总

| 编号 | 严重度 | 主题 | 状态 |
|---|---|---|---|
| V175 | 🟡 | 表单 visibleWhen `$self` | ✅ |
| V176 | 🟡 | actions form-scope `$self` | ✅ |
| V177 | 🟡 | 表单 scope:row 死路径 | ✅ |
| V178 | 🟡 | requestMapping `$` | ✅ |
| V179 | 🟡 | 非表单 visibleWhen 口径 | ✅ |
| V180 | 🟡 | MCP 场景回归 | ✅ |
| V181 | 🟡 | ADR V167 | ✅ |
| V182 | 🟡 | ADR V164/V171 | ✅ |
| V183 | 🟡 | 06 自检 | ✅ |
| V184 | 🟢 | CHANGELOG 路径 | ✅ |
| V185 | 🟢 | package-lock | ✅ |
| V186 | 🟢 | BOM | ✅ |
| V187 | 🟢 | upload 文案 | ✅ |
| V188 | 🟢 | L4 文档 | ✅ |
| V189 | 🟢 | 实施计划目录 | ✅ |
| V190 | 🟢 | ROW_SCOPE_MOUNT | ✅ |

## 最终验证结果

- `npm --prefix mcp run build`：通过
- `npm --prefix mcp test`：3 个文件、**108** 项全部通过（含 6 官方场景 + V175/V176 反例）
- `npm --prefix mcp run smoke:tools`：5 工具列出正常
- 未执行 Docker smoke：本轮未改 Dockerfile / 镜像打包

## 建议验证命令

```powershell
npm --prefix mcp run build
npm --prefix mcp test
npm --prefix mcp run smoke:tools
```
