---
status: checklist
date: 2026-07-10
based_on: 0042-2026-07-10-review.md
---

# 审计 0042 跟踪清单

## V142（🔴）统一非 GET DataRef.params 的传输位置

- [x] V142a 确认并记录 v0.2 决策：DataRef `params` 始终进入 query；或通过 ADR 定义 method/body 分流。
- [x] V142b 同步 `01-node-protocol.md`、`04-datasource-contract.md`、`08-renderer-spec.md`，删除 query/body 歧义。
- [x] V142c 更新 `node.schema.json` 描述和 CHANGELOG；核对 optionsSource.params 是否仍只使用 query。
- [x] V142d 增加 GET/POST/PUT/PATCH/DELETE 参数构造契约示例或测试，确认空值省略规则一致。
- [x] V142e 执行相关文档/Schema 校验并记录修改文件和结果。
- 完成记录：选择 DataRef `params` 始终编码为 URL query，所有 method 均不隐式生成 body；同步 Node/数据源/Renderer/Schema/CHANGELOG，并补 method→请求形态表。5 份 Schema/DSL JSON 解析通过。

## V143（🔴）要求搜索 targetTable 具有有效 API 数据源

- [x] V143a 定义“有效 API 数据源”：内联 `source: api` 或 `source: ref` 指向 API datasource。
- [x] V143b 修改 L2，拒绝无 data、source static、ref static 目标，保留 API/ref API 正例。
- [x] V143c 同步 form 搜索数据流和 `06-validation.md`，使用“有效数据源”而非硬编码 `data.url`。
- [x] V143d 增加四类反例和两类正例到 MCP/聚焦测试。
- [x] V143e 执行 `npm run validate:l2 -- <fixtures>`、统一入口和 `npm --prefix mcp test`。
- 完成记录：L2 的 Node id 索引保留目标节点并解析有效 datasource；无 data/static/ref static 被拒绝，内联 API/ref API 通过。同步 03/06/CHANGELOG，MCP 完整测试通过。

## V144（🟡）闭合 dateRangePicker reaction value 写入语义

- [x] V144a 记录决策：v0.2 禁止 dateRangePicker reaction `value`，或通过 ADR 定义 `{start,end}` 写入结构。
- [x] V144b 按决策同步表达式、组件和校验文档。
- [x] V144c 在 L2/L3a 最合适层执行限制，不影响 `$self.start/end` 读取及其他状态键。
- [x] V144d 增加 fulfill/otherwise value 反例和 visible/required/disabled 正例。
- [x] V144e 执行 L2/L3a、统一入口和 MCP 测试。
- 完成记录：选择 v0.2 禁止双字段目标不明确的 value 写入，由 L2 同时检查 fulfill/otherwise；`$self.start/end` 及其他状态键保持既有行为。同步 02/03/06 并通过测试。

## V145（🟡）定义 upload 组件与 Action 重复约束

- [x] V145a 决定 actionRef 模式下 `accept/maxSize/multiple` 的唯一来源或合并规则。
- [x] V145b 同步 upload 组件、Action 正文、DSL 描述和 `06-validation.md`。
- [x] V145c 在 L2 跨引用校验冲突/重复字段，不影响直接 action 模式。
- [x] V145d 增加 actionRef 重复字段反例、Action 单一配置和直接 action 正例。
- [x] V145e 执行 L2、统一入口和 MCP 测试。
- 完成记录：actionRef 模式以 UploadAction 为三项约束唯一来源，L2 对组件重复声明逐字段报错；直接 action 模式不变。同步 03/07/06/DSL，三字段反例和既有正例通过。

## V146（🟡）定义 HTTP 标准错误处理与 onError 组合顺序

- [x] V146a 决定 400/401/403/404/其他 4xx/5xx 与 `onError` 的覆盖或追加矩阵。
- [x] V146b 明确 `400 + errors` 下 navigate/closeModal 是否禁止，以及 toast message 优先级。
- [x] V146c 同步数据源、Action、Renderer 文档；需要时新增 ADR。
- [x] V146d 增加至少三个端到端时序示例：字段错误、认证失败、普通业务失败。
- [x] V146e 更新 CHANGELOG 并验证所有交叉引用。
- 完成记录：先执行协议级安全/字段错误处理，再执行不冲突的 onError；401/403 忽略 onError，400+errors 抑制跳转类行为，toast 配置消息替代默认消息。同步 04/07/08/CHANGELOG，无需新增公共结构 ADR。

## V147（🟡）定义部分 bodyMapping 的字段选择语义

- [x] V147a 决定部分 mapping 是白名单还是局部重命名，并记录安全理由。
- [x] V147b 同步 Action、Renderer 和 action Schema 描述。
- [x] V147c 增加缺省、空对象、部分映射、完整映射请求体示例。
- [x] V147d 核对普通表单 method 约束和后端 400 errors 字段名映射是否受影响。
- [x] V147e 更新 CHANGELOG 并执行 Schema/示例验证。
- 完成记录：bodyMapping 声明时采用源字段白名单，缺省时全字段原名发送；补缺省/部分/完整/空对象示例。同步 07/08/action Schema/CHANGELOG，Schema 解析通过，GET 禁令和 errors.field 表单字段语义不变。

## V148（🟡）跳过 search 模式被忽略的 submitAction

- [x] V148a 修改 `validatePageActionRefs()`，仅默认表单解析 submitAction 引用和 GET 限制。
- [x] V148b 更新 `06-validation.md`，明确引用完整性规则按 form mode 分支。
- [x] V148c 增加 search + 不存在/GET submitAction 正例及 default 对照反例。
- [x] V148d 执行 L2、统一入口和 MCP 测试。
- 完成记录：`mode: search` 跳过 submitAction 引用与 method 校验，默认表单 V85/V130 规则保持；测试和官方场景通过。

## V149（🟡）将 select option value 视为不透明业务值

- [x] V149a 调整 L4 路径分类，跳过 `select.options[].value` 内部业务键。
- [x] V149b 保持 option 外壳额外字段拒绝和真实 props CSS 禁令不回归。
- [x] V149c 增加对象/数组 option value 正例及 option 外壳/普通 props 反例。
- [x] V149d 同步 `06-validation.md` 并执行 L4、统一入口和 MCP 测试。
- 完成记录：L4 新增不透明业务值路径，只跳过 option value 内部；既有 option 外壳封闭性、tagMap 映射项和普通 props CSS 测试继续通过。同步 06，MCP 测试通过。

## V150（🟡）拒绝非法 RowAction URL 花括号模板

- [x] V150a 定义并实现 URL 模板词法检查，仅接受完整 `{identifier}`。
- [x] V150b 拒绝连字符、数字开头、空、嵌套、孤立和未闭合花括号。
- [x] V150c 保持合法多占位符与 requestMapping.path 双向完整性检查。
- [x] V150d 同步 Action/校验文档并增加正反例。
- [x] V150e 执行 L2、统一入口和 MCP 测试。
- 完成记录：L2 先执行 URL 花括号词法校验，再执行既有 path 双向完整性；六类非法模板均有测试，合法 RowAction 场景继续通过。同步 07/06。

## V151（🟡）为 upload.props.maxSize 增加非负下界

- [x] V151a 为自定义 DSL/L2 增加受控 `minimum` 数值约束支持，或实现等价专用校验。
- [x] V151b 将 upload props `maxSize` 下界设为 0，与 UploadAction Schema 一致。
- [x] V151c 同步组件和校验文档的 DSL 关键字边界。
- [x] V151d 增加负数、0、正数和错误类型测试。
- [x] V151e 执行 L2、统一入口和 MCP 测试。
- 完成记录：DSL/L2 支持数值 `minimum`，upload maxSize 设为 0；负数拒绝、0/正数通过、既有类型检查保留。同步 03/06/DSL，测试通过。

## V152（🟡）闭合 MCP 子进程 stdout 容量边界

- [x] V152a 确定 `maxBuffer` 上限及其与 1 MiB 输入/20KB 最终输出预算的关系。
- [x] V152b 修改 validation runner，稳定处理 `ENOBUFS`，不得误报为 JSON 解析失败。
- [x] V152c 评估让子脚本提前限制结构化输出，避免无界 stdout；记录本轮采用方案。
- [x] V152d 增加真实子进程高基数边界测试，断言违规统计和最终 transport 预算。
- [x] V152e 同步 MCP v1 design/ADR（如契约变化）。
- [x] V152f 执行 `npm --prefix mcp run build`、`npm --prefix mcp test`、`npm --prefix mcp run smoke:tools`。
- [x] V152g 若影响镜像运行或打包内容，构建镜像并执行 `smoke:docker`；否则记录未执行原因。
- 完成记录：本轮采用显式 16MB 有界 maxBuffer，并把 ENOBUFS 分类为专用 internalError；保留未来子脚本提前限量作为长期优化。真实 6000 条 L2 违规保留 total=6000，最终文本 ≤20KB。MCP build、90 项测试、tools smoke、audit-0042 镜像 build/Docker smoke 通过，镜像已删除。

## V153（🟡）恢复历史 V1-V4 的可定位身份

- [x] V153a 不重编号历史正文，建立 `NNNN/Vn` 复合引用规则和冲突说明。
- [x] V153b 修正已确认的无审计号歧义引用，例如 0035 的“历史上 V1”。
- [x] V153c 在 audit README 增加问题定义数量与唯一编号数量检查要求。
- [x] V153d 扫描全部审计引用，确认 V1-V4 均可由审计号限定或上下文唯一定位。
- [x] V153e 记录最大编号仍为 V154，后续编号不得因历史冲突重置。
- 完成记录：不重编号历史标题；audit/archived README 增加冲突说明与定义数/唯一数检查，0035 两处歧义引用改为 `0013/V1`。0010/0013/0014 内部引用由文件上下文定位；后续最大号继续为 V154。

## V154（🟢）统一 Windows glob 文件匹配

- [x] V154a 选择统一入口规范化或共享文件匹配 helper，避免四个脚本各自漂移。
- [x] V154b 支持 Windows 绝对反斜杠 glob，同时保留直接文件和 POSIX/相对 glob 行为。
- [x] V154c 增加正反斜杠、绝对/相对、直接文件和无匹配测试。
- [x] V154d 更新 `06-validation.md` 的 Windows CLI 说明。
- [x] V154e 在 Windows 执行统一入口和各分层命令并记录结果。
- 完成记录：新增 `scripts/file-patterns.js`，直接文件保留原路径，仅 Windows glob 规范化分隔符；统一入口和 L2/L3a/L4 共用。绝对反斜杠 glob 四条命令均找到同一临时正例并通过，临时文件已删除；相对/正斜杠/直接文件由官方场景和 MCP 调用覆盖。

## 汇总

| 编号 | 严重度 | 主题 | 状态 |
|---|---|---|---|
| V142 | 🔴 | 非 GET DataRef.params 传输位置 | ✅ |
| V143 | 🔴 | 搜索目标表格不可重新请求 | ✅ |
| V144 | 🟡 | dateRangePicker value 写入歧义 | ✅ |
| V145 | 🟡 | upload 重复约束无合并规则 | ✅ |
| V146 | 🟡 | HTTP 错误与 onError 无顺序 | ✅ |
| V147 | 🟡 | 部分 bodyMapping 边界未定义 | ✅ |
| V148 | 🟡 | search submitAction L2 误报 | ✅ |
| V149 | 🟡 | option value 触发 L4 误报 | ✅ |
| V150 | 🟡 | 非法 URL 模板绕过 L2 | ✅ |
| V151 | 🟡 | upload maxSize 缺少下界 | ✅ |
| V152 | 🟡 | MCP 子进程 stdout 溢出 | ✅ |
| V153 | 🟡 | 历史 V1-V4 身份冲突 | ✅ |
| V154 | 🟢 | Windows glob 行为不一致 | ✅ |

## 建议验证命令（修复后）

```powershell
# 根校验链，使用修复时创建的聚焦正反例
npm run validate -- <file-or-glob>
npm run validate:l2 -- <file-or-glob>
npm run validate:l3a -- <file-or-glob>
npm run lint:l4 -- <file-or-glob>

# MCP 构建、测试与工具 smoke
npm --prefix mcp run build
npm --prefix mcp test
npm --prefix mcp run smoke:tools

# 涉及镜像行为或打包内容时
npm --prefix mcp run smoke:docker -- <image>

# 文档与审计状态
git diff --check
```

## 最终验证结果

- 5 份 Schema/组件 DSL JSON 解析通过。
- 四个官方场景通过 MCP 内置完整 L0-L4 校验；Windows 绝对反斜杠 glob 经统一入口和 L2/L3a/L4 单层命令验证通过。
- `npm --prefix mcp run build`：通过。
- `npm --prefix mcp test`：通过，3 个测试文件、90 项测试全部通过。
- `npm --prefix mcp run smoke:tools`：通过，五个冻结工具名均存在。
- `schema-ui-mcp:audit-0042` Docker build 与 stdio smoke：通过；测试镜像已删除。
- VS Code Problems：无错误；临时测试文件无残留。
- `git diff --check`：通过。