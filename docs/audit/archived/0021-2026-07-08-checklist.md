---
status: checklist
date: 2026-07-08
based_on: 0021-2026-07-08-review.md
---

# 审计 0021 跟踪清单

> 本清单覆盖 2 个在 0020 归档后再次全量复审发现的问题，编号 V28-V29（延续 0020 的 V24-V27 编号体系）。

## 使用说明

- `[ ]` 待修复 / `[x]` 已完成
- 每项修复完成后，在右侧填写对应文件的修改摘要和 commit hash

---

## V28（🟡）`03-component-registry.md` 表格列/操作"作用域说明"缺少"仅限表单上下文"前提限定

| 字段 | 值 |
|---|---|
| **位置** | `docs/03-component-registry.md` — `table` 组件"作用域说明（since 0.2.1）"段落 |
| **问题** | `scope: form` 描述声明可访问 `$deps.*`，但未注明仅在 `form.children` 上下文中合法；独立表格使用 `scope: form` + `$deps.*` 会被静态校验拒绝 |
| **操作** | 在 `scope: form`（默认）条目中补充"仅当表格本身位于 `form.children` 内时，`$deps.*` 才合法"的前提说明 |
| **文件** | `docs/03-component-registry.md` |

- [x] V28a 在"作用域说明（since 0.2.1）"段落的 `scope: form` 条目中，追加"注意：仅当表格本身位于 `form.children` 内（如搜索表单嵌入表格场景）时，`$deps.*` 才合法；独立表格的列/操作中即使声明 `scope: form`，`$deps.*` 仍被静态校验拒绝（见 `02-reaction-expression.md` §9.1）"说明。修改文件：`docs/03-component-registry.md`。

---

## V29（🟢）`CHANGELOG.md` v0.2.4 条目缺少"涉及的文档与 Schema"段落

| 字段 | 值 |
|---|---|
| **位置** | `docs/CHANGELOG.md` — `v0.2.4` 版本条目末尾 |
| **问题** | 其他所有版本（v0.1.0、v0.2.0–v0.2.3）均有"涉及的文档与 Schema："段落，唯 v0.2.4 缺失 |
| **操作** | 在 v0.2.4 条目末尾补充该段落，列出受影响的协议文档、Schema 文件和新增决策文件 |
| **文件** | `docs/CHANGELOG.md` |

- [x] V29a 在 `v0.2.4` 条目末尾追加"涉及的文档与 Schema："段落，内容包含：协议文档（`01`/`02`/`04`/`08`）、JSON Schema（`node.schema.json`）、新增决策（`decisions/0005-response-mapping.md`、`decisions/0006-expression-evaluation-order.md`）。修改文件：`docs/CHANGELOG.md`。

---

## 汇总

| 编号 | 严重度 | 状态 |
|---|---|---|
| V28a | 🟡 中等 | [x] 已完成 |
| V29a | 🟢 轻微 | [x] 已完成 |
