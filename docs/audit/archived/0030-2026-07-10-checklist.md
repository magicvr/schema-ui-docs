---
status: checklist
date: 2026-07-10
based_on: 0030-2026-07-10-review.md
---

# 审计 0030 跟踪清单

## V64（🔴）`modal.content` 不进入 L2 / L3a / L4

| 字段 | 值 |
|---|---|
| **位置** | `scripts/validate-l2-components.js`；`scripts/validate-l3a-expressions.js`；`scripts/lint-l4-banned-props.js`；`docs/06-validation.md`；MCP `validate_content` |
| **问题** | `actions[].type: modal` 的 `content` Node 只做 L0/L1 结构校验，不进组件契约/表达式作用域/CSS 禁用词扫描 |
| **操作** | 三层脚本递归扫描 `doc.actions[*].content`，并同步 `06` 描述与反例验证 |
| **文件** | 上述脚本 + `docs/06-validation.md`；可选 `mcp/tests/validate-content.test.ts` |

- [x] V64a L2：遍历 `doc.actions`，对 `type: modal` 且存在 `content` 的节点调用 `validateNode(content, 'actions.<id>.content', ...)`。修改：`scripts/validate-l2-components.js`。
- [x] V64b L2：`validateRowActionRefs` / `validateRequiredCapabilities` 进入 `modal.content` 子树（覆盖嵌套 table/upload 能力声明）。修改：同上。
- [x] V64c L3a：`validatePage` 对每个 `actions[*].content` 调用 `scanNode(..., parentIsForm=false, tableDepth=0)`。修改：`scripts/validate-l3a-expressions.js`。
- [x] V64d L4：`scanPage` 对每个 `actions[*].content` 调用 `scanNode`。修改：`scripts/lint-l4-banned-props.js`。
- [x] V64e `06-validation.md` L2/L3a/L4 描述补充覆盖 `actions[].type: modal` 的 `content` Node。修改：`docs/06-validation.md`。
- [x] V64f 反例验证：`modal.content` 内未知 `type` → L2 exit 1（`未知组件类型 "unknownWidget"`）；`$row` 误用 → L3a exit 1（`SCOPE_ISOLATION`）；`fulfill.margin` → L4 exit 1（`margin`）；`body` 内同等合法 form 样例仍 exit 0。
- [x] V64g MCP 回归：13 个测试全部通过，含官方 4 场景 + 能力缺失 + L3a 作用域反例。

---

## V65（🟡）`datasources.*.params` 中 `$deps.*` 未纳入 L3a

| 字段 | 值 |
|---|---|
| **位置** | `scripts/validate-l3a-expressions.js`；`docs/06-validation.md`；可选 `02`/`04`；`scripts/lint-l4-banned-props.js` 注释 |
| **问题** | 页面级 `datasources` 无 form 上下文，但 `params: $deps.*` 全链路静默通过 |
| **操作** | L3a 扫描 datasources.params（固定非 form）；同步文档与注释 |
| **文件** | `scripts/validate-l3a-expressions.js`；`docs/06-validation.md`；可选 `docs/02-reaction-expression.md`、`docs/04-datasource-contract.md` |

- [x] V65a L3a `validatePage` 遍历 `doc.datasources`，对 `DataRef.params` 调用 `scanDataParams(..., hasFormContext=false)`。修改：`scripts/validate-l3a-expressions.js`。
- [x] V65b `06-validation.md` L3a 描述补充 `datasources.*.params`。修改：`docs/06-validation.md`。
- [x] V65c 可选：`02` §10.6 / 附录 A 或 `04` §1 明确页面级 `datasources.params` 不允许 `$deps.*`。决策：不额外修改 `02`/`04`，以 `06-validation.md` L3a 作为页面级 `datasources.*.params` 静态校验边界说明；0031 V70 回补 checklist 状态。
- [x] V65d 修正 L4 注释中“datasources 的 params 也要扫描”的虚假描述（与实现一致）。修改：`scripts/lint-l4-banned-props.js`。
- [x] V65e 反例验证：`datasources.*.params.status: $deps.status` → L3a exit 1（`NON_FORM_DATA_PARAMS`）；字面量 params 样例 exit 0；全链路 MCP 测试 13/13 通过。

---

## V66（🟢）ADR-0004 附录矩阵未同步 V58 form 前提

| 字段 | 值 |
|---|---|
| **位置** | `docs/decisions/0004-row-level-scope.md` 附录变量可见性矩阵 |
| **问题** | 仍写「表格列 scope:form → `$deps.*` ✅」，无 form 内限定 |
| **操作** | 与 `02` 附录 A 对齐 |
| **文件** | `docs/decisions/0004-row-level-scope.md` |

- [x] V66a 将「表格列 scope:form」改为「仅表格位于 form.children 内」并 `$deps.*` ✅。修改：ADR-0004 附录。
- [x] V66b 新增「独立表格列 scope:form」行，`$deps.*` ❌（静态校验拒绝）。修改：同上。
- [x] V66c 对照 `02-reaction-expression.md` 附录 A，确认无其他矩阵行残留。验证：`table` 行已对齐为 4 行（form 内 + 独立 + row + 嵌套）。

---

## 汇总

| 编号 | 严重度 | 状态 |
|---|---|---|
| V64 | 🔴 | [x] 已修复并验证 |
| V65 | 🟡 | [x] 已修复并验证 |
| V66 | 🟢 | [x] 已修复并验证 |

## 建议验证命令（修复后）

```bash
# 文档/脚本
npm run validate:l2 -- "<tmp-or-fixture>"
npm run validate:l3a -- "<tmp-or-fixture>"
npm run lint:l4 -- "<tmp-or-fixture>"
npm run validate -- "<tmp-or-fixture>"

# 若触及 MCP 测试
npm --prefix mcp run build
npm --prefix mcp test
```
