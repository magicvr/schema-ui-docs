---
status: checklist
date: 2026-07-10
based_on: 0039-2026-07-10-review.md
---

# 审计 0039 跟踪清单

## V117（🔴）实现 `dateRangePicker` 的受控 `$self.start` / `$self.end`

- [x] V117a 明确 L3a 上下文元数据，使校验器可区分 `dateRangePicker` 自身 reactions、普通字段 reactions 与表格列表达式。
- [x] V117b 修改词法与变量白名单：接受精确 `$self`；仅在 `dateRangePicker` 自身 reactions 中接受 `$self.start` / `$self.end`。
- [x] V117c 拒绝 `$self.unknown`、普通组件 `$self.start`、表格列任意 `$self.*`，避免扩大协议边界。
- [x] V117d 更新 `docs/06-validation.md` 的 L3a 说明，并核对 `docs/02-reaction-expression.md` / `docs/03-component-registry.md` 口径。
- [x] V117e 增加脚本或 MCP 正反例测试；执行统一校验入口与 MCP 相关测试。
- [x] V117f 记录修改文件、内容摘要和验证命令。
- 完成记录：`scripts/validate-l3a-expressions.js` 为普通 Node reactions 传入 `componentType`，仅允许 `dateRangePicker` 使用 `$self.start` / `$self.end`，并拒绝未知属性、普通组件属性路径及 `$selfish` 伪命名空间；同步 `docs/02-reaction-expression.md`、`docs/06-validation.md`、`docs/08-renderer-spec.md`。新增 MCP 正反例并通过 `npm --prefix mcp test`（51/51）。

## V118（🟡）执行表单内 `visibleWhen.dependencies` 条件必填

- [x] V118a 在 L2 按 Node 树位置判断表单上下文；表单内 visibleWhen 缺少 dependencies 时返回明确违规。
- [x] V118b 决定 L3a 是否保留同一防线；若保留，确保错误不重复或输出仍稳定可消费。
- [x] V118c 保持非表单 `$context.*` visibleWhen 可省略 dependencies；表单内 `dependencies: []` 合法。
- [x] V118d 更新 `docs/06-validation.md`，说明该条件必填属于 L2/语义层而非基础 Schema 可独立表达的约束。
- [x] V118e 更新 MCP `validate_content` 测试：表单内缺失反例、非表单正例、空数组正例。
- [x] V118f 执行 `npm run validate:l2` 的针对性样例、`npm --prefix mcp test` 与相关统一入口验证。
- [x] V118g 记录修改文件、内容摘要和验证命令。
- 完成记录：条件必填由能感知 Node 树位置的 L2 单层执行，避免 L2/L3a 重复错误；覆盖普通 Node 和 form 子树内 table columns/actions 的 `scope: form` visibleWhen。非表单 `$context.*` 可省略，表单内 `dependencies: []` 正例通过。修改 `scripts/validate-l2-components.js`、`docs/01-node-protocol.md`、`docs/06-validation.md` 和 MCP 测试；聚焦 41 项与全量 51 项测试通过。

## V119（🟡）使 L4 区分业务字典键与协议字段

- [x] V119a 调整 L4 遍历策略，跳过 `table.props.columns[].tagMap` 的映射键本身。
- [x] V119b 保持对 tagMap 映射项内部对象的检查，确保非法样式字段仍被 L2/L4 拒绝。
- [x] V119c 增加 `tagMap.color` / `tagMap.width` 合法正例，以及映射项内部 `color` 非法反例。
- [x] V119d 更新 `docs/06-validation.md` 的 L4 扫描边界说明。
- [x] V119e 执行 `npm run lint:l4 -- <fixtures>` 或等价针对性命令，并复跑官方场景统一校验。
- [x] V119f 记录修改文件、内容摘要和验证命令。
- 完成记录：`scripts/lint-l4-banned-props.js` 在 `.tagMap` 层跳过业务映射键，但继续递归每个映射项；`tagMap.color` / `tagMap.width` 正例通过，项内 `color` 仍进入 L4。同步 `docs/06-validation.md`，相关 MCP 校验测试与四个官方场景通过。

## V120（🔴）收敛 `$parentRow` 与嵌套表格可表达性

- [x] V120a 记录最终决策：A）为 v0.2 定义唯一嵌套表格挂载结构；或 B）v0.2 暂停公开 `$parentRow` 能力并保守拒绝。
- [x] V120b 按决策同步 `docs/02-reaction-expression.md` 的变量白名单、作用域规则与附录矩阵。
- [x] V120c 按决策同步 `docs/03-component-registry.md`、`docs/schemas/component-registry.json` 与 `scripts/validate-l2-components.js`。
- [x] V120d 核对并同步 `docs/07-actions-contract.md` 中 RowAction `requestMapping` 的 `$parentRow.*` 边界。
- [x] V120e 同步 `docs/08-renderer-spec.md` 和 `docs/06-validation.md`，明确 Renderer 与 L2/L3a 的统一行为。
- [x] V120f 更新 L3a：只接受最终协议可构造的 `$parentRow` 上下文，删除不可达分支或实现新挂载点遍历。
- [x] V120g 增加至少一个通过 L0-L4 的完整正例（若选择 A）和普通表格/非法层级反例；若选择 B，增加明确拒绝反例。
- [x] V120h 更新 MCP `validate_content` 测试，并执行根校验链、MCP build/test/smoke。
- [x] V120i 回补 0026/V43c 的关系说明，不改写其历史事实，只说明本轮如何补齐全链路证据。
- [x] V120j 记录修改文件、决策摘要和验证命令。
- 决策结果：选择 B。行业实践上，嵌套数据网格需要明确的 detail/expanded-row 挂载 API、父行生命周期与虚拟化边界；在这些结构尚未定义时仅开放变量会制造不可互操作扩展，因此 v0.2 暂停 `$parentRow`，待新 ADR 成套恢复。
- 完成记录：正文、组件 DSL、L2/L3a、Renderer、动作契约、ADR-0004/0008 均改为保守拒绝；删除不可达 `tableDepth`/allowParentRow 分支，MCP 增加 `PARENT_ROW_UNSUPPORTED` 与 requestMapping 反例。0026/V43 历史文件未改写，本轮 review/checklist 明确说明此前 L3a-only 证据不足。build、51 项测试、tools smoke 与 Docker smoke 均通过。

## V121（🟡）拒绝链式比较与链式 `contains`

- [x] V121a 在 L3a token 语法校验中识别同一比较表达式内的连续比较运算符。
- [x] V121b 拒绝 `a contains b contains c`、`a == b contains c` 等比较链。
- [x] V121c 保持 `a contains b && c == d`、括号分组和一元 `!` 的合法行为。
- [x] V121d 更新 `docs/06-validation.md` 的表达式语法检查说明。
- [x] V121e 增加脚本/MCP 正反例并执行 L3a、完整 MCP 测试。
- [x] V121f 记录修改文件、内容摘要和验证命令。
- 完成记录：L3a 按括号层级跟踪比较运算符，`&&` / `||` 重置当前比较段；链式 `contains` 与混合比较链拒绝，逻辑分隔正例通过。同步 `docs/02-reaction-expression.md`、`docs/06-validation.md` 并通过全量测试。

## V122（🟡）统一 `$context` 刷新生命周期

- [x] V122a 记录最终决策：A）初始化时一次性注入、刷新靠重挂载；或 B）允许同实例显式更新并触发新求值轮次。
- [x] V122b 更新 ADR-0003 D1、后果与遗留问题，使旧决策与最终口径一致；如被替代，增加明确的 superseded 说明。
- [x] V122c 更新 ADR-0006 D2 的触发源和 Snapshot 定义。
- [x] V122d 同步 `docs/02-reaction-expression.md` §14 与 `docs/08-renderer-spec.md` 初始化/调度说明。
- [x] V122e 增加至少一个生命周期时序示例，明确新旧 context 在哪一轮可见。
- [x] V122f 全文搜索“上下文刷新”“整页重新挂载”“一次性注入”，确认不存在冲突残留。
- [x] V122g 记录修改文件、决策摘要和人工核对结果。
- 决策结果：选择 A。不可变初始化配置是通用 Renderer 的低耦合默认；若未来需要同实例刷新，应先定义显式 `updateContext` API、原子替换与并发求值语义，而不是把“上下文刷新”作为未定义事件。
- 完成记录：ADR-0003 明确保留初始化快照，ADR-0006 与 `docs/02-reaction-expression.md` 删除同实例上下文刷新触发源，`docs/08-renderer-spec.md` 明确重挂载创建新实例；残留扫描只命中一致的重挂载说明。

## V123（🟡）闭合 Renderer 请求初始化接口

- [x] V123a 决定 `RendererRequestConfig` 在 `RendererOptions` 中采用顶层展开还是命名字段组合，并在正文只保留一个权威类型关系。
- [x] V123b 更新附录 A，纳入 `credentials`、`requestTimeout`、`requestInterceptor`、`onAuthFailure` 及其默认值/签名。
- [x] V123c 核对正文所有 `new Renderer({...})` 示例与最终接口一致。
- [x] V123d 视发布说明策略更新 `docs/CHANGELOG.md` 的 `Unreleased`。
- [x] V123e 执行文档引用/关键词核对并记录修改摘要。
- 完成记录：采用 TypeScript `interface RendererOptions extends RendererRequestConfig`，由 §2.6 作为请求字段唯一权威定义，附录不再重复可漂移字段表；同步 context 重挂载说明与 `CHANGELOG.md` Unreleased。

## V124（🟡）将 MCP 20KB 预算施加到完整工具文本

- [x] V124a 选择唯一预算层：最终 `handleGetDoc()` 工具文本，或可证明等价的预序列化预算函数。
- [x] V124b 调整截断算法，使正文、截断提示、元数据、JSON 转义/缩进和 `availableSections` 总和不超过 20KB。
- [x] V124c 保持 UTF-8 边界安全、`truncated` 语义和精确 section 请求行为。
- [x] V124d 更新 `mcp/tests/search.test.ts`，直接断言 `handleGetDoc(...).content[0].text` 的 UTF-8 字节数。
- [x] V124e 覆盖中文多字节、章节数量较多、无需截断和 section 精确请求四类测试。
- [x] V124f 更新 `docs/mcp/v1-design.md` 与 ADR-0007 D10，明确 20KB 指完整工具文本。
- [x] V124g 执行 `npm --prefix mcp run build`、`npm --prefix mcp test`、`npm --prefix mcp run smoke:tools`。
- [x] V124h 如实现或文档影响镜像内容，执行 `npm --prefix mcp run smoke:docker -- <image>` 或记录未执行原因与风险。
- [x] V124i 记录修改文件、内容摘要和验证命令。
- 完成记录：预算层选在 `handleGetDoc()` 最终 JSON 文本；按真实 UTF-8 字节数二分裁剪正文，元数据、转义/缩进、提示和章节列表均计入预算，必要时裁减过长章节列表。新增真实大文档、中文多字节和“正文未超限但元数据使总响应超限”测试。`component-registry` / `renderer-spec` 工具文本均不超过 20KB；build、51 项测试、tools smoke 与 `schema-ui-mcp:audit-0039` Docker smoke 通过。

## 汇总

| 编号 | 严重度 | 主题 | 状态 |
|---|---|---|---|
| V117 | 🔴 | dateRangePicker `$self.*` 实现漂移 | ✅ |
| V118 | 🟡 | visibleWhen 条件必填缺口 | ✅ |
| V119 | 🟡 | L4 tagMap 业务键误报 | ✅ |
| V120 | 🔴 | `$parentRow` / 嵌套表格不可表达 | ✅ |
| V121 | 🟡 | contains 比较链被放行 | ✅ |
| V122 | 🟡 | `$context` 生命周期冲突 | ✅ |
| V123 | 🟡 | Renderer 初始化参数遗漏 | ✅ |
| V124 | 🟡 | MCP 完整响应超 20KB | ✅ |

## 建议验证命令（修复后）

```powershell
# 根校验链（针对修复时创建的临时正反例分别执行）
npm run validate -- <file-or-glob>
npm run validate:l2 -- <file-or-glob>
npm run validate:l3a -- <file-or-glob>
npm run lint:l4 -- <file-or-glob>

# MCP 构建、测试与工具 smoke
npm --prefix mcp run build
npm --prefix mcp test
npm --prefix mcp run smoke:tools

# 涉及镜像内容时
npm --prefix mcp run smoke:docker -- <image>

git diff --check
```