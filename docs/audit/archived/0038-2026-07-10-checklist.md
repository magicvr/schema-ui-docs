---
status: checklist
date: 2026-07-10
based_on: 0038-2026-07-10-review.md
---

# 审计 0038 跟踪清单

## V114（🟡）补齐当前最新 PATCH 的发布说明

- [x] V114a 明确当前仓库版本语义：补写 `v0.2.8`、新增 `v0.2.9`，或引入 `Unreleased` 段，避免继续让最新条目落后于仓库状态。
- [x] V114b 更新 `docs/CHANGELOG.md`，使最新版本条目准确覆盖当前仓库已合入的正文/脚本/MCP 变更。
- [x] V114c 对照 `README.md`、`docs/00-overview.md`、`docs/mcp/README.md` 的“当前最新 PATCH”表述，确保入口与 CHANGELOG 同步。
- [x] V114d 如最终调整了 MCP 对外版本示例，同步核对镜像 tag、启动命令与说明文字。
- [x] V114e 记录修改文件、内容摘要与人工核对结果。
- 完成记录：采用 `Unreleased` 段收敛“当前工作区已落地但未单独切 PATCH”的变更；`README.md` 与 `docs/mcp/README.md` 明确区分“已发布版本 `0.2.8`”和 `CHANGELOG.md` 顶部未发布修订，未改动现有镜像 tag 示例。

## V115（🟡）修正 `docs/schemas/` 的入口层描述

- [x] V115a 更新 `README.md` 仓库说明，把 `docs/schemas/` 从笼统的“JSON Schema”改为“标准 JSON Schema + 组件 DSL”或等价表述。
- [x] V115b 更新 `README.md` 目录树注释，明确 `component-registry.json` 属于自定义 DSL。
- [x] V115c 更新 `docs/00-overview.md` 文档地图中 `schemas/` 的说明，区分标准 Schema 与组件 DSL。
- [x] V115d 如有必要，补充指向 `docs/03-component-registry.md` 的交叉引用，提示 DSL 边界说明所在位置。
- [x] V115e 记录修改文件、内容摘要与人工核对结果。
- 完成记录：`README.md` 与 `docs/00-overview.md` 已统一改为“标准 JSON Schema + 组件注册 DSL”；入口层已明确 `component-registry.json` 不属于可直接按 AJV 处理的标准 Schema 集合。

## V116（🟢）固定 MCP SDK 依赖版本策略

- [x] V116a 将 `mcp/package.json` 中 `@modelcontextprotocol/sdk` 从 `latest` 改为明确版本策略（优先固定具体 PATCH）。
- [x] V116b 同步更新 `mcp/package-lock.json`，确保锁文件与 manifest 一致。
- [x] V116c 视需要在 `docs/mcp/README.md` 补一句依赖/发布稳定性说明，避免与对外“固定 PATCH”口径割裂。
- [x] V116d 执行验证：`npm --prefix mcp run build`、`npm --prefix mcp test`、`npm --prefix mcp run smoke:tools`。
- [x] V116e 记录修改文件、内容摘要与验证结果。
- 完成记录：`mcp/package.json` / `mcp/package-lock.json` 已将 `@modelcontextprotocol/sdk` 固定为 `1.29.0`；`docs/mcp/README.md` 补充“不使用 latest 作为包清单策略”说明。已执行 `npm --prefix mcp run build`、`npm --prefix mcp test`（43/43 通过）、`npm --prefix mcp run smoke:tools`，并补做 `Select-String` 文档/依赖核对；`git diff --check` 仅出现 CRLF 警告，无空白错误。

## 汇总

| 编号 | 严重度 | 主题 | 状态 |
|---|---|---|---|
| V114 | 🟡 | 最新 PATCH 发布说明 | ✅ |
| V115 | 🟡 | schemas 入口索引口径 | ✅ |
| V116 | 🟢 | MCP SDK 依赖策略 | ✅ |

## 建议验证命令（修复后）

```powershell
# 文档与版本说明核对
Select-String -Path README.md,docs\*.md,docs\mcp\*.md,mcp\package.json,mcp\package-lock.json -Pattern '0\.2\.8|Unreleased|JSON Schema|component-registry|@modelcontextprotocol/sdk|latest' | Select-Object -First 80 | Out-String

# MCP 构建与测试
npm --prefix mcp run build
npm --prefix mcp test
npm --prefix mcp run smoke:tools

git diff --check
```
