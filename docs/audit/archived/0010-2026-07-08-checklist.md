---
status: checklist
date: 2026-07-08
based_on: 0010-2026-07-08-review.md
---

# 审计 0010 跟踪清单

## 使用说明

- `[ ]` 待修复 / `[x]` 已完成
- 每项修复完成后，在右侧填写对应文件的修改摘要
- 修复完成并验证通过后，再将本轮审计文件移入 `archived/`

---

## V1（🟡）修复 Renderer 并行加载示例的 `body` 数组结构

| 字段 | 值 |
|---|---|
| **位置** | `08-renderer-spec.md` §2.1 默认并行加载 |
| **问题** | 示例将顶层 `body` 写成数组，违反 `body: Node` |
| **操作** | 用 `grid` / `section` 等容器节点包裹多个并行加载节点 |
| **文件** | `08-renderer-spec.md` |

- [x] V1 `body` 不再直接写成 YAML 数组  — 已修复（改用 `grid` 容器包裹）
- [x] V1 示例仍能表达多个独立 API 请求默认并行发起  — 已确认
- [x] V1 修复后与 `01-node-protocol.md` 顶层结构保持一致  — 已确认

---

## V2（🟡）修复 Renderer `$deps.*` 数据依赖示例的上下文冲突

| 字段 | 值 |
|---|---|
| **位置** | `08-renderer-spec.md` §2.3 数据依赖声明 |
| **问题** | 独立 `table` 示例使用 `$deps.*`，但同节声明独立 `table` / `chart` 应静态拒绝 `$deps.*` |
| **操作** | 改为明确的表单/搜索上下文示例，或移除独立 `table` 示例中的 `$deps.*` |
| **文件** | `08-renderer-spec.md` |

- [x] V2 示例明确 `$deps.*` 来源于表单/搜索上下文  — 已修复（`form` 包含 `select`、`dateRangePicker`、`table`）
- [x] V2 示例不再与 `04-datasource-contract.md` §3.2 冲突  — 已确认
- [x] V2 修复后仍保留空值省略、去抖、自动重新请求等 Renderer 行为说明  — 已确认

---

## V3（🟡）收紧跨作用域联合判断的未定义变通描述

| 字段 | 值 |
|---|---|
| **位置** | `02-reaction-expression.md` §9.1 作用域隔离规则 |
| **问题** | “用 scope: form 表达式算出静态布尔值并传入 props”缺少协议级挂载机制定义 |
| **操作** | 改为明确说明 v0.2 不提供协议级变通；需要后端预计算或另起 ADR |
| **文件** | `02-reaction-expression.md` |

- [x] V3 删除或改写未定义的"静态布尔值传入 props"机制  — 已修复（改为后端预计算或 ADR）
- [x] V3 保留 `$row` 与 `$deps` 互斥的边界说明  — 已确认
- [x] V3 如保留替代方案，应明确其不属于 v0.2 标准语法  — 已确认（明确说明应由后端预计算或通过 ADR 新增标准机制）

---

## V4（🟢）清理 `tabs` 当前规范和注册表中的“方案 A”历史措辞

| 字段 | 值 |
|---|---|
| **位置** | `03-component-registry.md` `tabs`、`schemas/component-registry.json` `tabs.props.items.description` |
| **问题** | 当前规范正文和机器可读描述仍引用“方案 A”历史方案 |
| **操作** | 改为结果导向的当前协议描述 |
| **文件** | `03-component-registry.md`、`schemas/component-registry.json` |

- [x] V4 `03-component-registry.md` 当前规范不再出现"方案 A"残留  — 已清理
- [x] V4 `component-registry.json` 当前描述不再引用"方案 A / 审计 §2.4"  — 已清理
- [x] V4 如需保留历史方案编号，仅保留在 `CHANGELOG.md` 等历史记录中  — 已确认（仅存档审计和 CHANGELOG 中保留）

---

## 验证

- [x] 修复完成后重新运行全量 JSON 解析检查，确认所有 `.json` 文件可解析  — ✅ 全部通过
- [x] 修复完成后重新运行 Markdown 本地链接检查，确认断链列表为空  — ✅ 全部通过
- [x] 人工核对 `08-renderer-spec.md` §2.1 示例符合 `body: Node` 结构  — ✅ 已确认（`grid` 单 Node 包裹）
- [x] 人工核对 `08-renderer-spec.md` §2.3 示例不再违反 `$deps.*` 表单上下文限制  — ✅ 已确认（`form` 上下文）
- [x] 检查 `0010-2026-07-08-review.md` 中所有问题均已在本清单覆盖  — ✅ 全部覆盖