---
status: plan
date: 2026-07-09
based_on: 0024-2026-07-09-review.md
---

# 审计 0024 修复计划

## 目标

补齐 MVP 协议冻结前的稳定性缺口：PATCH 级能力协商、L3a 表格 scope 静态校验、`text` 数据兜底语义、`RowAction.key` 执行边界。

## 处理项

1. V35：新增 `meta.requiredCapabilities`，Renderer 新增 `supportedCapabilities` 说明，L2 校验器实现 `actions.upload` 能力声明检查。
2. V36：修复 L3a 表格列/行内操作表达式默认 scope，使 `$row.*` 必须显式声明 `scope: row`。
3. V37：澄清 `text.content/contentKey` 与 `data` 的关系，保持现有 DSL 二选一约束不变。
4. V38：澄清 `RowAction.key` 仅做前端本地分发，不定义后端请求执行模型。
5. 更新 CHANGELOG 与审计归档索引。
6. 运行反例、正例与场景示例校验。