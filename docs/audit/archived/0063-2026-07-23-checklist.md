---
status: archived
date: 2026-07-23
based_on: 0063-2026-07-23-review.md
---

# 审计 0063 整改 Checklist

> 本 checklist 仅跟踪协议层整改。生产 Renderer、Host 应用和业务后端不属于本轮范围。

## 基线

- [x] 确认审计编号 `0063` 延续已归档的 `0062`。
- [x] 确认问题编号 `V273` 至 `V281` 延续 `V272`。
- [x] 确认本审计开始前工作树干净（`dev` @ `f8b1680`）。
- [x] 确认 0062（V267–V272）已归档且不重开。
- [x] 确认无 P0；本轮 3×P1 + 6×P2。

## V273（P1）formRecord 缺失路径 null/undefined

- [x] 裁决可观测值：JSON `null` vs 运行时 `undefined`，写入 ADR-0021 D5a。
- [x] 统一 JS/Python `response-mapping` reference 与 fixture 期望。
- [x] 明确「等价空初始值、不中止整次回填」及与提交投影的关系。
- [x] 同步 `03` form.recordSource 相关摘要。
- [x] CHANGELOG Unreleased 记录裁决。

## V274（P1）batch count 与 keys 长度不变量

- [x] 在 batch request 构造入口定义：规范化 selection（与 table-query-state 同一谓词）。
- [x] JS/Python `request-construction` 行为一致。
- [x] 增加 count/keys 不一致与非标量/重复键的 fixture；保留合法 batch 正向向量。
- [x] 同步 ADR-0022 D3/D5 与 `07` §3.5。
- [x] 与 V281 协调：同一入口完成 keys 规范化 + count 对齐。

## V275（P1）2.2 版本/迁移/打包策略

- [x] 明确 2.2 正式发布后：`protocolVersion: "2.1"` + batch capabilities 是否仍为合法页面（否：必须 `"2.2"`）。
- [x] 明确官方样例与算法 fixtures 升至 `"2.2"` 的时机与 release:check 规则（打 `2.2.0` tag 时）。
- [x] 将策略写入 ADR-0022 状态段与 `13-v2.2-release-goals.md`。
- [x] 与 V277 交付物清单对齐。
- [x] CHANGELOG 说明 Unreleased 中 0022 与 2.1 制品的关系。

## V276（P2）文档状态过期

- [x] ADR-0020 / ADR-0021 示例 `protocolVersion` 改为 `"2.1"`。
- [x] `admin-list-edit-lifecycle.md` 删除或改写「2.1 发布前可声明 2.0」过期句。
- [x] `11-next-admin-lifecycle-goals.md` §7 将 0020/0021 标为 accepted，与 §8 一致。
- [x] 相关交叉链接抽查无失效。

## V277（P2）Phase C 交付物

- [x] 新增 `migrations/2.1-to-2.2.md`。
- [x] 扩展示例场景 `admin-list-batch.md` + `_samples/order-list-batch.yaml`。
- [x] `2.2.0` 制品打包策略写入 `13`（tag 时执行；当前不阻塞审计关闭）。
- [x] 更新 `11` Phase C 勾选与 CHANGELOG Unreleased。

## V278（P2）L2 recordSource.method 诊断

- [x] 缺失 method 与非法 method 分码或分 message。
- [x] 补充静态校验负例 `_samples/audit-0063-record-source-missing-method-bad.yaml`。
- [x] 同步 `06` 自检清单一句。

## V279（P2）L3a `$context.route` 报错语义

- [x] `route` 纳入已知 `$context` 根；expression 挂载点明确拒绝。
- [x] 错误 message 区分「未知根」与「本挂载点禁止」。
- [x] 负例：`_samples/audit-0063-context-route-in-visible-when-bad.yaml`。
- [x] 同步 `02` §11.3 / L3a 文件头注释。

## V280（P2）modal + recordSource route 注入

- [x] 在 ADR-0021 D7 写明：modal 内 route 注入非 2.2 门禁；若使用 route 则快照形状对齐整页；MVP 推荐字面量。

## V281（P2）batch keys 二次规范化

- [x] 与 V274 同入口：过滤非标量、去重保序、重算 count。
- [x] fixture 覆盖非标量/重复键输入。

## 完成门禁

- [x] 每个已解决条目都有规范或 ADR 依据（或书面范围内延期）。
- [x] 受影响 Schema/L2/L3a 与双端 reference 一致。
- [x] 新增/变更 fixtures 双端通过。
- [x] `npm run test:conformance:all` 通过。
- [x] `npm run validate:conformance` 通过。
- [x] `npm run validate:scenarios` 通过。
- [x] `npm run release:check` 通过（186 cases；fixture digest `sha256:0775cc6f3a93646f3412d8c1f1ee7ca5f76552e5dfad8f56a2eadd825a016e98`）。
- [x] `npm run check:links` 通过。
- [x] CHANGELOG Unreleased 记录本轮关闭的 V 编号。
- [x] 全部条目关闭或正式延期后：将 review/checklist 移入 `docs/audit/archived/`，frontmatter `status: archived`，更新 `docs/audit/README.md` 与 `archived/README.md`。
