---
status: checklist
date: 2026-07-09
based_on: 0024-2026-07-09-review.md
---

# 审计 0024 跟踪清单

## V35（🔴）PATCH 级执行能力缺少显式协商机制

- [x] V35a `01-node-protocol.md` 顶层 `meta` 结构新增 `requiredCapabilities`
- [x] V35b `00-overview.md` 版本稳定性说明补充 PATCH 级能力协商规则
- [x] V35c `08-renderer-spec.md` 增加 `supportedCapabilities` 与能力匹配规则
- [x] V35d `schemas/page.schema.json` 增加 `meta.requiredCapabilities` 结构约束
- [x] V35e `validate-l2-components.js` 校验 `actions.upload` 能力声明
- [x] V35f 反例验证：使用 upload 但未声明 `actions.upload` 时 L2 失败
- [x] V35g 正例验证：声明 `actions.upload` 后 L2 通过

## V36（🔴）L3a 表格表达式默认 `scope: row` 导致缺少显式 scope 也能通过

- [x] V36a `validate-l3a-expressions.js` 将表格列/行内操作表达式默认 scope 改为 `form`
- [x] V36b 反例验证：`$row.*` 省略 `scope: row` 时 L3a 失败
- [x] V36c 场景示例回归验证通过

## V37（🟡）`text` 组件数据驱动语义与必填关系需澄清

- [x] V37a `03-component-registry.md` 明确 `content/contentKey` 在数据场景下为兜底文案
- [x] V37b `component-registry.json` 同步描述

## V38（🟡）`RowAction.key` 执行边界需澄清

- [x] V38a `03-component-registry.md` 明确 `RowAction.key` 仅用于前端预注册处理器本地分发
- [x] V38b `component-registry.json` 同步描述
- [x] V38c 明确后端请求型行操作需要后续 ADR，不纳入当前 MVP 稳定承诺

## 汇总

| 编号 | 严重度 | 状态 |
|---|---|---|
| V35 | 🔴 | [x] 已修复并验证 |
| V36 | 🔴 | [x] 已修复并验证 |
| V37 | 🟡 | [x] 已澄清 |
| V38 | 🟡 | [x] 已澄清 |