---
status: archived
date: 2026-07-28
based_on: schema-ui-protocol 2.7.0 / protocolVersion 2.7；正式 tag v2.7.0 / mcp-v2.7.0 @ ca9e5fe；HEAD b764eb3；延续已归档 0077
scope: focus=all；主类型=实现/契约漂移（informative 轨道入口 + MCP smoke 默认）；不修改生产消费者仓库；不强制 PATCH / 不改写 tag
---

# 审计报告 0078：v2.7.0 发布后轨道叙事与 Docker smoke 默认

## 结论

本轮全量复审确认：正式 **`v2.7.0` / `mcp-v2.7.0`** 证据链完整；核心规范、Schema、L2、conformance（316）、MCP 134 tests 与 GHCR digest **`sha256:37749442…`** 与 0077 关闭结论一致，**无 P0/P1**，0077 已关项**未回归**。

新发现问题 **2×P2 + 1×P3（V372–V374）**：**全部关闭**（步骤 3）。`next-admin-lifecycle` 已对齐 2.7；`smoke-docker.mjs` 默认跟 `mcp/package.json`；总纲 D.1 阅读建议已修正。于 2026-07-28 归档。

| 判定 | 说明 |
|---|---|
| 正式 2.7.0 身份 | content `2170d612…` · artifact `45ceb27a…` · fixture `f9589dc0…`（316）— **不可变 pin** |
| 工作树（修复后） | content `d67f6cef…` · artifact `b94449eb…` · fixture **仍** `f9589dc0…`（informative 分叉；**不**强制 PATCH） |
| MCP 镜像 | index `sha256:37749442…`（`2.7.0` / `2.7` / `latest` 同 digest）；显式 smoke 通过 |
| 本轮关闭 | V372–V374 in-repo；**不**改写 `v2.7.0` / `mcp-v2.7.0` |
| 发布策略 | 与 0075/0076 同类 informative 卫生；**不**强制 PATCH |

有效修复已沉淀到 `docs/release-goals/next-admin-lifecycle.md`、`docs/00-overview.md`、`mcp/tests/smoke-docker.mjs`；**本文不是协议权威来源**。`docs/audit/**` 不进入 `protocol-manifest.json`、协议制品、MCP 索引或 Docker 镜像。

## 范围与基线

### 审计类型

| 类型 | 本轮角色 |
|---|---|
| **实现/契约漂移** | **主类型** — informative 发布元数据与工具默认相对 2.7.0 滞后 |
| 发布完整性 | 残差观察 — 正式 pin 齐备；本轮不重新开 tag/CD |
| 提案评审 | 不适用 — 无 proposed ADR |

### 当前基线（步骤 1）

| 项 | 结果 |
|---|---|
| 工作树 / 分支 | `dev` @ `b764eb3`（干净；ahead `origin/dev` by 1） |
| 工作树版本 | 根/manifest/MCP **2.7.0**；页面协议 **`"2.7"`**；validator **1.0.0** |
| 正式 tag | `v2.7.0` / `mcp-v2.7.0` → `ca9e5fe` |
| 活跃审计 | 创建前 **无**；本轮 **0078** |
| 问题编号 | 全局最大 **V371**（0077），本轮续编 **V372–V374** |

### 基线验证（步骤 1）

| 命令或证据 | 结果 |
|---|---|
| `npm run check:links` | 249 Markdown、705 相对链接通过 |
| `npm run release:check` | 316 cases；fixture `f9589dc0…`；content `2170d612…`；artifact `45ceb27a…` |
| `npm run verify:protocol-artifact` | reproducible；108 文件 |
| `npm run check:protocol-artifact-links` | 79 MD / 421 链接通过 |
| `npm run validate:scenarios` / `validate:conformance` | 官方 6 场景；16 versioned suites 绿 |
| `npm run test:conformance:all` | **35/35** |
| `release:check:mcp` / `:validator` | 绿；lock parent **2.7.0** |
| MCP `build` / `test` / `smoke:tools` | **134/134**；5 tools |
| GHCR 显式 `smoke:docker` `2.7.0`/`2.7`/`latest` | 通过；digest `37749442…` |
| 无参 `smoke:docker` | **失败**（默认 `schema-ui-mcp:2.0.0`） |
| 生产消费者 | 相邻仓只读：仍 pin **`v2.4.1`**（out-of-repo） |

## 覆盖率与验证

| 批次 | 已覆盖 | 未覆盖 / 低置信度 | 置信度 |
|---|---|---|---|
| A 规范与权威边界 | charter、manifest、`00`–`09`、Schema、ADR 0029–0033、v2.7 门禁、**next-admin-lifecycle 深挖** | 未通读全部历史 ADR | 高 |
| B 校验与 conformance | 全量 runner、advanced sample L0–L4 | reference 未全文 diff | 高 |
| C MCP 与 validator | lock、DOC_MAP、build/test、**smoke 默认**、GHCR | MCP 无 v2.7 L2 专用负例（与 extended 同缺口） | 高 |
| D 自动化与发布 | workflows、Release、GHCR | 未改写历史 tag | 高 |
| E 生产消费者 | pin 只读 | 未跑消费者 CI | 中 |
| 历史审计 | 索引 + 0077 查重；最大 NNNN=0077 / V=371 | 未通读 0001–0076 全文 | 索引高 |

## 问题清单（汇总表）

| 编号 | 级别 | 主题 | 状态 | 类型 |
|---|---|---|---|---|
| V372 | 🟢 P2 | `next-admin-lifecycle` §8/导言/frontmatter 仍停在 2.6，F1e 误标 backlog | **已关闭** | 实现/契约漂移 |
| V373 | 🟢 P2 | `smoke-docker.mjs` 无参默认 `schema-ui-mcp:2.0.0` | **已关闭** | 实现/契约漂移（工具卫生） |
| V374 | ⚪ P3 | `00-overview` 阅读建议仍写「D.1 待立项」 | **已关闭** | 文档卫生 |

**计数：** 0×P0、0×P1、**2×P2**、**1×P3**；**全部关闭**。  
**Out-of-repo（不编号）：** 生产消费者仍 pin `v2.4.1`。  
**残差（不编号）：** form-controls-advanced 有意不进官方六场景；MCP 无 advanced L2 专用负例；工作树 content/artifact 与正式 `v2.7.0` 因 informative 分叉。

## 问题详情

### V372（🟢 P2）next-admin-lifecycle 当前状态与 F1e 叙事滞后 — 已关闭

**位置：** `docs/release-goals/next-admin-lifecycle.md`（frontmatter、导言、§5、§7、§8）。

**初审行为：** frontmatter / 导言 / §8 仍停在 2.6 且 F1e 标 residual，与同文 D.1.0b 及 `protocol-manifest` 2.7.0 冲突。

**修复：** 对齐 post-v2.7 轨道叙事；§5 写入 `2.7`；§7 增 `v2.7.md` 与 0029–0033；§8 协议线 `2.7.0`/`"2.7"`，残留不含 F1e。

**验证：** `check:links` 通过；§8 与 manifest 一致。

### V373（🟢 P2）smoke-docker 无参默认镜像仍为 2.0.0 — 已关闭

**位置：** `mcp/tests/smoke-docker.mjs`。

**初审行为：** 硬编码 `schema-ui-mcp:2.0.0`；无参 smoke 失败。CD 显式传 tag 不受影响。

**修复：** 默认从 `mcp/package.json` `version` 推导 `schema-ui-mcp:<version>`；argv 仍可覆盖。

**验证：** 默认解析 `schema-ui-mcp:2.7.0`；`ghcr.io/magicvr/schema-ui-mcp:2.7.0` 显式 smoke 通过。

### V374（⚪ P3）总纲阅读建议 D.1 表述过粗 — 已关闭

**位置：** `docs/00-overview.md` §2 阅读建议末条。

**修复：** 标明 F1/进阶已随 v2.6/v2.7 交付，残留见 `next-admin-lifecycle` §4 / §8。

**验证：** `check:links` 通过。

## 与历史审计关系

- **0077（V363–V371）：** v2.7.0 落地后 MCP/validator/DOC_MAP/发布证据 — **已关闭未回归**。
- **0075/0076：** 发布后 informative 卫生、不强制 PATCH — 本轮沿用同一策略。
- **0036（V94a）：** smoke 默认镜像跟版 — **回归式**维护缺口，本轮以 V373 处理（不复用旧 V 号）。
- 新问题自 **V372** 递增；审计编号 **0078**。

## 处理顺序与防复发

1. V372 轨道叙事（制品 informative，优先）
2. V373 smoke 默认（跟 `mcp/package.json`，防再硬编码）
3. V374 总纲一句

**防复发：** MINOR 交付后同步 `next-admin-lifecycle` §8 与 frontmatter；smoke 默认禁止硬编码历史版本。

## 关闭条件

- [x] V372–V374 checklist 可验收项全部 `[x]`（归档移动除外）
- [x] `check:links`、`release:check` 绿；smoke 默认跟包版本
- [x] 用户确认后归档；**不**自动打 tag / 改写 `v2.7.0`

## 步骤 3 关闭摘要

| 项 | 结果 |
|---|---|
| V372–V374 | in-repo 全部关闭 |
| fixture digest | **不变** `f9589dc0…`（316） |
| content / artifact | 工作树 `d67f6cef…` / `b94449eb…`（vs 正式 `2170d612…` / `45ceb27a…`） |
| 正式 tag | **未**改写 |
| 生产消费者 | 仍 out-of-repo pin `v2.4.1` |
