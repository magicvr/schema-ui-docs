---
status: archived
date: 2026-07-28
based_on: 0072-2026-07-28-review.md
scope: focus=all；主类型=发布完整性与门禁卫生
---

# 审计 0072 跟踪 Checklist

> 本 checklist 跟踪 v2.5.1 发布后入口卫生与门禁观察面（V345–V348），以及 **2.5.2** PATCH 发布。  
> 生产消费者仅作 out-of-repo 跟踪；未经单独授权，不修改 `allinme.web-client` 或 `allinme.core-api`。  
> 不把本审计路径加入 `protocol-manifest.json`、协议制品、MCP 搜索索引或 Docker 运行镜像。

## 汇总

| 编号 | 级别 | 主题 | 状态 |
|---|---|---|---|
| V345 | 🟡 P2 | MCP 文档仍写「需等 `mcp-v2.5.1` 发布」 | **已关闭** |
| V346 | 🟡 P2 | `release-check` 未对权威 `17` 做 `applies_to` 门禁 | **已关闭** |
| V347 | ⚪ P3 | Protocol CI 未观察 L0–L4 等校验脚本路径 | **已关闭** |
| V348 | ⚪ P3 | CHANGELOG 措辞 + 2.5.2 身份与发布 | **已关闭** |

**计数：** 2×P2、2×P3 **全部关闭**。  
**问题号：** 全局续编 `V344` → `V345`–`V348`。  
**审计号：** `0072`（archived）。

## 基线

- [x] 创建前确认无活跃 audit；全局最大审计号为 `0071`。
- [x] 确认 `V345`–`V348` 位于全局最大 `V344` 之后，且不使用历史冲突的 V1–V4。
- [x] 记录初审工作树 `2.5.1` 与正式 `v2.5.1` / `mcp-v2.5.1` @ `6191c14`。
- [x] 记录初审本地门禁与远端 2.5.1 证据（见 review）。
- [x] 修复后工作树升级为 **2.5.2** 并完成正式发布。

---

## V345 — MCP 文档仍写「需等 `mcp-v2.5.1` 发布」（P2）— 已关闭

- [x] 更新 `docs/mcp/README.md`：去掉错误「需等」口径；发版后与 `mcp-v2.5.2` 对齐。
- [x] 全文检索无残留「需等」。
- [x] 示例与 `mcp/package.json` **2.5.2** 一致。
- 验证命令：`npm run check:links`；正式镜像 smoke。
- 关闭证据：MCP 文档已改；`docs/mcp` 不进协议制品。

---

## V346 — `release-check` 未对权威 `17` 做 `applies_to` 门禁（P2）— 已关闭

- [x] `docs/17-app-manifest.md` 与 `docs/00-overview.md` 纳入 `coreSpecPaths`。
- [x] `npm run release:check` 通过。
- [x] 负向探针：破坏 `17` 的 `applies_to` → fail-closed 后恢复。
- 验证命令：`npm run release:check`。
- 关闭证据：门禁代码 + 负向探针。

---

## V347 — Protocol CI 未观察 L0–L4 等校验脚本路径（P3）— 已关闭

- [x] `protocol-ci.yml` PR/push paths 使用 `scripts/**`。
- [x] 两处 paths 一致。
- 验证命令：workflow 审查；PR #27 CI 绿。
- 关闭证据：workflow diff。

---

## V348 — CHANGELOG 措辞与 2.5.2 身份（P3）— 已关闭

- [x] `v2.5.1`「发布准备」→「已发布」；`v2.5.0` Unreleased 指针 → v2.5.1。
- [x] 升 **2.5.2**（根包、manifest、lockfile、MCP、validator、README、overview、CHANGELOG）。
- [x] **不**改写 `v2.5.1` / `mcp-v2.5.1`。
- [x] **发布证据：** PR [#27](https://github.com/magicvr/schema-ui-docs/pull/27) → `main` `2ed9dfc`；tag `v2.5.2` Protocol Release [30312103413](https://github.com/magicvr/schema-ui-docs/actions/runs/30312103413) success；tag `mcp-v2.5.2` MCP CD [30312103712](https://github.com/magicvr/schema-ui-docs/actions/runs/30312103712) success。
- [x] 正式资产：artifact `sha256:df7d095d…` = 本地；content `sha256:dc06a637…`；fixture `sha256:712f6c42…`（同 2.5.1）；MCP 镜像 `sha256:2c44682c…`（`2.5.2`/`2.5`/`latest` 同 digest，label 协议 2.5.2，revision `2ed9dfc`）；Docker smoke 通过。
- 验证命令：`release:check`、`verify:protocol-artifact`、正式 pull/smoke。
- 关闭证据：上表远端 run + digest。

---

## 回归与归档门禁

- [x] V345–V348 全部勾选。
- [x] 本地全量门禁 + PR CI 绿 + 正式 tag/CD。
- [x] 用户授权发布与归档。
- [x] 移至 `docs/audit/archived/`；frontmatter `status: archived`；更新 `docs/audit/README.md` 与 `archived/README.md`。
- [x] 无应关闭而未勾选的裸 `[ ]`（out-of-repo 跟踪除外）。

## Out-of-repo（不在本仓关闭）

- [ ] （跟踪）`allinme.web-client`：评估自 `v2.4.1` 升级至 `v2.5.2` 的 pin、suite 数、CI ref。
- [ ] （跟踪）`allinme.core-api`：同步 `pin.go`、制品 SHA、校验器检出 tag。

## 变更日志

| 日期 | 说明 |
|---|---|
| 2026-07-28 | 创建 0072；记录步骤 1 复审基线与 V345–V348 开放项。 |
| 2026-07-28 | 步骤 3 in-repo：V345–V347 关闭；V348 升 2.5.2。 |
| 2026-07-28 | **发布与归档：** PR #27 → `v2.5.2` / `mcp-v2.5.2`；V348 全关；0072 归档。 |
