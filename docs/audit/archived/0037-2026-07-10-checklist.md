---
status: checklist
date: 2026-07-10
based_on: 0037-2026-07-10-review.md
---

# 审计 0037 跟踪清单

## V96（🔴）统一表格表达式 `scope: form` / `scope: row` 口径

- [x] V96a 更新 `docs/01-node-protocol.md` §3.5：仅使用行上下文时要求显式 `scope: row`；表单内表格允许 `scope: form` + `$deps.*`。
- [x] V96b 同步 `docs/00-overview.md` reactions 术语概括，避免继续写成所有表格表达式都必须 row scope。
- [x] V96c 对照 `docs/03-component-registry.md` 与 ADR-0004，确认四处口径一致。
- [x] V96d 运行 Markdown 链接检查或人工核对相关交叉引用。
- 完成记录：`00` / `01` 已统一口径；当前文档链接与锚点检查通过（29 个文件，0 失败）。

## V97（🔴）执行 row scope 挂载边界与行依赖完整性

- [x] V97a 在 L3a 仅允许表格 columns/actions 的表达式声明 `scope: row`，普通 Node reactions/visibleWhen 使用时报告明确规则。
- [x] V97b 提取 `$row.*` / `$parentRow.*` 依赖路径并核对 `dependencies`；保留 `$row.__index` / `$row.__key` 的明确策略。
- [x] V97c 补充普通 input row scope、缺失行依赖、未声明父行依赖反例及合法正例。
- [x] V97d 更新 `docs/06-validation.md` L3a 规则说明。
- [x] V97e 执行 `node scripts/validate-l3a-expressions.js <fixtures> --json`、MCP 相关测试和构建。
- 完成记录：L3a 新增 `ROW_SCOPE_MOUNT` / `UNDECLARED_ROW_DEP` / `UNDECLARED_PARENT_ROW_DEP`；MCP 回归测试和行为探针通过。

## V98（🔴）拒绝空表达式与畸形变量路径

- [x] V98a 为 reaction/VisibleWhen 的 `when` 增加非空 Schema 约束。
- [x] V98b 将 L3a 所有 when 调用点改为按字段存在性校验，使空字符串进入 `validateExpression()`。
- [x] V98c 为 `$deps` / `$row` / `$parentRow` / `$context` / `$self` 定义完整变量词法规则，拒绝空段、连续点和伪命名空间。
- [x] V98d 补充 Node、table column、RowAction 的空 when 与畸形路径反例。
- [x] V98e 执行 L0/L1、L3a、MCP 测试与 build。
- 完成记录：Schema 增加 `minLength: 1`，L3a 拒绝空字符串、`$row.` 与 `$row..status`；相关回归测试通过。

## V99（🔴）执行组件 DSL 嵌套布尔 required

- [x] V99a 修改 `validateNestedObject()`，检查 `properties.*.required === true`。
- [x] V99b 扫描 component-registry 中全部嵌套 required 用法，确认实现覆盖且无双重报错。
- [x] V99c 增加 `table.pagination: {}` 反例和合法 pagination 正例。
- [x] V99d 更新 `docs/06-validation.md` L2 DSL 解释边界。
- [x] V99e 执行 L2、MCP 测试与 build。
- 完成记录：`pagination: {}` 现报告 `body.props.pagination.mode` 缺失；合法官方场景继续通过。

## V100（🔴）收紧 RowAction requestMapping 值与父行上下文

- [x] V100a requestMapping 字符串只要包含 `$`，就必须整体匹配允许的单个行引用；拒绝模板/拼接残留。
- [x] V100b RowAction 遍历携带 tableDepth，仅嵌套表格允许 `$parentRow.*`。
- [x] V100c 覆盖 path/query/body 三段的模板、顶层 parentRow、合法字面量和嵌套父行正例。
- [x] V100d 更新 `docs/06-validation.md` L2 规则说明。
- [x] V100e 执行 L2、MCP 测试与 build。
- 完成记录：`prefix-$row.id` 与顶层 `$parentRow.id` 均被拒绝，合法 `$row.id` 正例通过。

## V101（🔴）禁止或正式定义 datasource 引用链

- [x] V101a 选择并记录收敛方案：优先禁止顶层 `datasources.*.source: ref`；若保留则先补 ADR 定义链式解析与循环检测。
- [x] V101b 按选定方案更新 page/node Schema，避免 Node DataRef 与 datasource declaration 过度复用。
- [x] V101c 同步 `01`、`04`、`08` 对 datasource 声明和 Node ref 的边界。
- [x] V101d 增加自引用、双节点循环、合法 api/static datasource 与 Node ref 测试。
- [x] V101e 执行 L0/L1、L2、MCP 测试与 build。
- 完成记录：新增 `DatasourceDeclaration`，仅允许 api/static，并保留 api/value 互斥；自引用及非法混合配置回归通过。

## V102（🟡）校验页面内 Node id 唯一性

- [x] V102a L2 收集 id 时检测重复，错误包含首个与重复节点路径且不覆盖首项。
- [x] V102b 覆盖 body、tabs content、modal content 之间的跨挂载点重复。
- [x] V102c 增加重复 id 反例与合法 targetTable 正例。
- [x] V102d 更新 `docs/06-validation.md` 引用完整性清单并运行 L2/MCP 测试。
- 完成记录：重复 id 现报告首次与重复路径；既有合法引用测试继续通过。

## V103（🟡）约束 request.bodyMapping 值类型

- [x] V103a `action.schema.json` 将 bodyMapping 值限制为非空 string。
- [x] V103b `07-actions-contract.md` 明确源字段名到目标请求字段名的方向及空值边界。
- [x] V103c 增加嵌套对象/数组/数字反例和字符串映射正例。
- [x] V103d 执行 L0/L1 与 MCP 测试。
- 完成记录：Schema 使用 `additionalProperties: { type: string, minLength: 1 }`；嵌套对象反例被 L0/L1 拒绝。

## V104（🟡）修复 Renderer source:ref 示例根结构

- [x] V104a 将 `08` §2.1.1 示例改为单根 Node，保留 datasource/ref 语义。
- [x] V104b 抽取修改后的 YAML 并执行 `npm run validate -- <fixture>`。
- [x] V104c 检查 `08` 其他 `body:` 示例是否仍有直接数组。
- 完成记录：示例补齐 meta 并以 section 作为单根 Node；抽取后 L0-L4 全部通过，未发现其他 body 数组。

## V105（🟡）按 Node 树位置描述 visibleWhen 上下文

- [x] V105a 修正 `02` §10.1，不再把“未声明 dependencies”等同于非表单上下文。
- [x] V105b 同步 ADR-0003 D3a，并明确表单内缺失 dependencies 是配置错误。
- [x] V105c 对照 `01` §3.8 与 L3a `isFormContext()`，确认术语一致。
- 完成记录：`02` 与 ADR-0003 均改为按 Node 树位置判断表单上下文。

## V106（🟡）重写 ADR-0006 为协议可表达的求值示例

- [x] V106a 用当前字段 value/状态写入的合法配置替换“country 清空 province”示例。
- [x] V106b 修正 D3 中“不同节点写同一目标字段”的不可表达描述，只保留协议实际支持的冲突仲裁。
- [x] V106c 对照 Renderer 求值时序章节，确认稳定快照、commit 与 next-tick 结论不变。
- [x] V106d 抽取 ADR 示例进行结构校验。
- 完成记录：ADR 改为 status 自身规范化示例，删除跨节点目标写入语义；YAML 可解析为两个 Node。

## V107（🟢）清理 ADR-0004 旧术语

- [x] V107a 将 `reactions/otherwise` 改为 `fulfill/otherwise`。
- [x] V107b 全文搜索同类错误组合并确认无正文残留。
- 完成记录：ADR-0004 术语已修正，当前正文无同类残留。

## V108（🟡）按 UTF-8 字节限制 MCP get_doc 返回

- [x] V108a 使用 UTF-8 byte length 判断是否超出 20KB。
- [x] V108b 实现不会切断多字节字符的字节边界截断，并决定提示文本是否计入上限。
- [x] V108c 增加少于 20,480 字符但超过 20KB 的中文文档测试，断言 truncated 与 availableSections。
- [x] V108d 执行 `npm --prefix mcp test -- search.test.ts`、完整测试与 build。
- 完成记录：按 UTF-8 边界截断且提示计入 20KB；中文测试断言无替换字符、总字节不超限。

## V109（🟡）将文档导言纳入 MCP 搜索

- [x] V109a `splitSections()` 返回首个二级标题前的非空导言区段。
- [x] V109b 避免 H1/文档标题重复计分，并保持现有稳定排序。
- [x] V109c 增加导言关键词命中、section 标识和现有 row scope 排序回归测试。
- [x] V109d 执行 MCP search 测试、完整测试与 build。
- 完成记录：新增“导言”区段；导言关键词和既有稳定排序测试均通过。

## V110（🟡）映射调用方 filename 并隐藏 MCP 临时路径

- [x] V110a finalize/runLayerScript 链路把 L2/L3a/L4 violation.file 统一映射为调用方 filename。
- [x] V110b 未传 filename 时省略 file，不返回 os.tmpdir/UUID 路径。
- [x] V110c 增加 L2/L3a/L4 文件名断言与 parseError 回归测试。
- [x] V110d 如需补充设计口径，更新 `docs/mcp/v1-design.md` / ADR-0007。
- [x] V110e 执行 validate-content 测试、完整 MCP 测试、build 与 smoke:tools。
- 完成记录：调用方 filename 映射与无 filename 省略测试通过；完整 MCP 测试和 smoke:tools 通过。

## V111（🟢）修复 4 个 Markdown 锚点

- [x] V111a 修复 `02-reaction-expression.md` 使用位置表中的 3 个失效锚点。
- [x] V111b 修复 `08-renderer-spec.md` §2.3 的数据源 §3.2 锚点。
- [x] V111c 运行 Markdown 本地链接/锚点检查，确认无新漂移。
- 完成记录：当前活跃文档范围 29 个 Markdown 文件链接/锚点检查 0 失败。

## V112（🟢）同步审计命名格式中的 plan 类型

- [x] V112a 将 `00-overview.md` 命名格式更新为 `{review,checklist,plan}` 或引用审计 README。
- [x] V112b 对照实际归档文件确认三类命名一致。
- 完成记录：总纲已列出 review/checklist/plan 并链接审计规则。

## V113（🟢）归档时更新索引最后更新时间

- [x] V113a 修复完成且用户确认后，将 `docs/audit/archived/README.md` 最后更新时间更新为 2026-07-10。
- [x] V113b 在归档索引顶部新增 0037 条目时再次核对日期；不得在最终确认前提前归档或修改归档索引。
- 完成记录：用户已授权“确认无误的话，归档”；归档步骤同步更新顶部索引与最后更新时间。

## 汇总

| 编号 | 严重度 | 主题 | 状态 |
|---|---|---|---|
| V96 | 🔴 | 表格 scope 正文口径 | ✅ |
| V97 | 🔴 | row scope 挂载与 dependencies | ✅ |
| V98 | 🔴 | 空 when / 变量词法 | ✅ |
| V99 | 🔴 | 嵌套 required | ✅ |
| V100 | 🔴 | requestMapping 模板/parentRow | ✅ |
| V101 | 🔴 | datasource 引用循环 | ✅ |
| V102 | 🟡 | Node id 唯一性 | ✅ |
| V103 | 🟡 | bodyMapping 值类型 | ✅ |
| V104 | 🟡 | Renderer body 示例 | ✅ |
| V105 | 🟡 | visibleWhen 上下文描述 | ✅ |
| V106 | 🟡 | ADR-0006 示例 | ✅ |
| V107 | 🟢 | ADR-0004 术语 | ✅ |
| V108 | 🟡 | get_doc UTF-8 字节上限 | ✅ |
| V109 | 🟡 | 搜索导言覆盖 | ✅ |
| V110 | 🟡 | MCP filename 映射 | ✅ |
| V111 | 🟢 | Markdown 锚点 | ✅ |
| V112 | 🟢 | 审计 plan 命名 | ✅ |
| V113 | 🟢 | 归档更新时间 | ✅ |

## 建议验证命令（修复后）

```powershell
# 针对审计反例/正例的根校验
npm run validate -- ".tmp-audit-check/*.yaml"
npm run validate:l2 -- ".tmp-audit-check/*.yaml" --json
npm run validate:l3a -- ".tmp-audit-check/*.yaml" --json
npm run lint:l4 -- ".tmp-audit-check/*.yaml" --json

# MCP 构建与测试
npm --prefix mcp run build
npm --prefix mcp test
npm --prefix mcp run smoke:tools

# 仅在 Dockerfile/镜像内容受影响或需要发布验证时
# npm --prefix mcp run smoke:docker -- schema-ui-mcp:0.2.8

git diff --check
```