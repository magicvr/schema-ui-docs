---
status: checklist
date: 2026-07-10
based_on: 0029-2026-07-10-review.md
---

# 审计 0029 跟踪清单

## V55（🔴）`02-reaction-expression.md` §2 禁止事项交叉引用漂移到错误的 §9.x

| 字段 | 值 |
|---|---|
| **位置** | `docs/02-reaction-expression.md` §2「禁止事项」 |
| **问题** | `permissions` / 非表单 `visibleWhen` / 表格 `actions` 的 `$self` 分别误指 §9.3 / §9.2 / §9.4；实际在 §10.2 / §10.1 / §10.3 |
| **操作** | 修正交叉引用，删除不存在的 §9.4 |
| **文件** | `docs/02-reaction-expression.md` |

- [x] V55a 将 `permissions.*` 引用从 §9.3 改为 §10.2。修改：`docs/02-reaction-expression.md` §2。
- [x] V55b 将非表单 `visibleWhen` 引用从 §9.2 改为 §10.1。修改：同上。
- [x] V55c 将表格 `actions` 的 `$self` 引用从 §9.4 改为 §10.3。修改：同上。
- [x] V55d 搜索确认 §2 禁止事项不再残留错误 §9.x 静态校验引用。验证：协议正文仅保留合法的 §9.1 作用域隔离引用。

---

## V56（🔴）术语/协议边界矛盾：`reactions`「仅表单」vs 表格列/行内 `reactions`

| 字段 | 值 |
|---|---|
| **位置** | `docs/00-overview.md` §3；`docs/01-node-protocol.md` §3 / §3.5 |
| **问题** | 总纲与核心协议仍写 reactions 仅表单/仅字段类 Node，但 `03` 与 ADR-0004 已允许表格列/行内 reactions |
| **操作** | 统一 reactions 术语与挂载点描述 |
| **文件** | `docs/00-overview.md`；`docs/01-node-protocol.md` |

- [x] V56a 更新 `00-overview.md` 术语表中 `reactions` 定义，覆盖表单字段与表格列/行内操作挂载点。修改：`docs/00-overview.md`。
- [x] V56b 更新 `01-node-protocol.md` §3 YAML 注释与 §3.5 正文，改为“支持 reactions 的组件位置（见组件注册表）”。修改：`docs/01-node-protocol.md`。
- [x] V56c 在 §3.5 明确 `scope: form`（表单字段）与 `scope: row`（表格列/行内，fulfill 仅 visible/disabled）边界。修改：新增挂载位置表与 `scope: row` 状态键限制说明。
- [x] V56d 确认 `03` / ADR-0004 描述无需反向收紧，仅同步总纲与核心协议。确认：无需反向修改。

---

## V57（🟡）ADR-0003 与正文不同步：`$context.user` 最小字段集

| 字段 | 值 |
|---|---|
| **位置** | `docs/decisions/0003-context-namespace-and-visible-when.md` D1 |
| **问题** | ADR 仍写「不规定内部字段」，与 `02` §11.1 / CHANGELOG v0.2.5 的强制最小字段冲突 |
| **操作** | 更新 ADR D1 与正文对齐 |
| **文件** | `docs/decisions/0003-context-namespace-and-visible-when.md` |

- [x] V57a 将 `$context.user` 改为协议规定最小字段 `id` / `name` / `roles`。修改：ADR-0003 D1。
- [x] V57b 保留项目扩展约定，并写明不得覆盖最小集名称。修改：同上。
- [x] V57c 注明最小字段集自 v0.2.5 生效，并交叉引用 `02-reaction-expression.md` §11.1。修改：同上。
- [x] V57d 同步 ADR-0003 后果(Consequences) 段旧结论，删除“`$context.user` 内部字段协议不做规定”。修改：后果段改为最小字段集 + 扩展字段仍可能不一致的取舍说明。

---

## V58（🟡）表格列 `scope: form` + `$deps.*` 三方不一致

| 字段 | 值 |
|---|---|
| **位置** | `docs/02-reaction-expression.md` 附录 A；`docs/03-component-registry.md`；`scripts/validate-l3a-expressions.js` |
| **问题** | 附录 A 无 form 前提，`03` 要求表格在 form 内，L3a 对 tableColumn 固定 hasFormContext=false 且不拒绝独立表格 `$deps` |
| **操作** | 以 `03` 为准同步矩阵与 L3a |
| **文件** | `docs/02-reaction-expression.md`；`scripts/validate-l3a-expressions.js` |

- [x] V58a 附录 A「表格列 scope: form」补充「仅表格位于 form.children 内」前提；并增加独立表格拒绝行。修改：`docs/02-reaction-expression.md` 附录 A。
- [x] V58b L3a 表格列/操作扫描继承 `inFormCtx`，不再写死 `hasFormContext: false`。修改：`scripts/validate-l3a-expressions.js`。
- [x] V58c 对 `tableColumn`/`tableAction` 在 `!hasFormContext` 且出现 `$deps.*` 时拒绝。修改：新增规则 `NON_FORM_TABLE_DEPS`。
- [x] V58d 用最小样例验证：独立表格列 `$deps` 被拒；`form` 内表格列 `scope: form` + `$deps` 通过。验证：非法样例 exit=1（`NON_FORM_TABLE_DEPS`），合法样例 exit=0。

---

## V59（🟡）`select.optionsSource.params` 未纳入 L3a

| 字段 | 值 |
|---|---|
| **位置** | `docs/06-validation.md`；`scripts/validate-l3a-expressions.js`；`docs/02-reaction-expression.md`；`docs/04-datasource-contract.md` |
| **问题** | 文档要求 optionsSource.params 与 data.params 同规则，但 L3a 只扫 data.params |
| **操作** | 补齐 L3a 扫描与文档描述 |
| **文件** | `scripts/validate-l3a-expressions.js`；`docs/06-validation.md`；`docs/02-reaction-expression.md`；`docs/04-datasource-contract.md` |

- [x] V59a L3a 扫描 `props.optionsSource.params`，复用 `scanDataParams`。修改：`scripts/validate-l3a-expressions.js`。
- [x] V59b `06-validation.md` L3a 描述补充 `optionsSource.params`。修改：`docs/06-validation.md`。
- [x] V59c §10.6 / 附录 A / `04` §3.2 显式覆盖 `optionsSource.params` 与 `data.params` 同规则。修改：`02` / `04`。
- [x] V59d 用最小样例验证：非表单/非法变量被拒，表单内合法 `$deps` 通过。验证：非法样例 exit=1（`DATA_PARAMS_VARIABLE`），合法样例 exit=0。
- [x] V59e 收紧 `isFormContext`：仅 `type === 'form'` 开启表单上下文，子节点只靠 `parentIsForm` 继承；不得用节点自身 `reactions.dependencies` 推断。修改：`scripts/validate-l3a-expressions.js`。
- [x] V59f 新增 `NON_FORM_REACTION_DEPS`：非 form 上下文的 `reactions.when` 禁止 `$deps.*`。验证：根节点 `select` 伪造 `reactions.dependencies` + `optionsSource.params.$deps` + `visibleWhen.$deps` 返回 3 处违规（`NON_FORM_DATA_PARAMS` / `NON_FORM_REACTION_DEPS` / `NON_FORM_VISIBLEWHEN`）；`form` 内合法样例 exit=0。
- [x] V59g 验证口径收紧：L3a 只接受 JSON/YAML 页面配置，不得用 `docs/05-scenarios/*.md` 作为“通过”证据。

---

## V60（🟡）`DataRef.method` 缺省语义未定义

| 字段 | 值 |
|---|---|
| **位置** | `docs/01-node-protocol.md` §3.3；`docs/04-datasource-contract.md`；`docs/schemas/node.schema.json` |
| **问题** | method 可选但无默认值说明，示例可省略，实现可能分叉 |
| **操作** | 明确缺省为 GET，并同步 Schema 描述 |
| **文件** | `docs/01-node-protocol.md`；`docs/04-datasource-contract.md`；`docs/schemas/node.schema.json` |

- [x] V60a 在 `01` §3.3 写明 `source: api` 时 `method` 缺省为 `GET`。修改：`docs/01-node-protocol.md`。
- [x] V60b 在 `04` 数据源契约同步缺省说明。修改：`docs/04-datasource-contract.md` §2。
- [x] V60c `node.schema.json` 的 method 增加 `"default": "GET"` 与 description。修改：`docs/schemas/node.schema.json`。
- [x] V60d 确认现有省略 method 的示例仍合法。确认：`grid-dashboard` 等省略 method 的示例与缺省 GET 一致。

---

## V61（🟡）upload action `method` 默认 POST 未进入 Schema

| 字段 | 值 |
|---|---|
| **位置** | `docs/07-actions-contract.md` §7；`docs/schemas/action.schema.json` `UploadAction` |
| **问题** | 文档默认 POST，Schema 无 default |
| **操作** | Schema 补充 default/description |
| **文件** | `docs/schemas/action.schema.json` |

- [x] V61a `UploadAction.method` 增加 `"default": "POST"` 与 description。修改：`docs/schemas/action.schema.json`。
- [x] V61b 确认 `07` 正文「默认 POST」与 Schema 一致。确认：`07` §7 已声明默认 POST，无需改文。

---

## V62（🟢）`08-renderer-spec.md` §5.1 运算符白名单章节引用歧义

| 字段 | 值 |
|---|---|
| **位置** | `docs/08-renderer-spec.md` §5.1 |
| **问题** | 「§3 运算符白名单」在 08 内会误指版本协商章节 |
| **操作** | 改为显式指向 `02-reaction-expression.md` §3 |
| **文件** | `docs/08-renderer-spec.md` |

- [x] V62a 将 §5.1 中「§3 运算符白名单」改为 `02-reaction-expression.md` §3。修改：`docs/08-renderer-spec.md`。
- [x] V62b 搜索 `08` 中是否还有无文档前缀的模糊 § 引用。验证：问题引用已消除；`last_updated` 同步为 2026-07-10。

---

## V63（🟢）场景示例 frontmatter `last_updated` 漂移

| 字段 | 值 |
|---|---|
| **位置** | `docs/05-scenarios/form-with-reactions.md`；`docs/05-scenarios/grid-dashboard.md` |
| **问题** | last_updated 仍为 2026-07-07 |
| **操作** | 同步为 2026-07-10 |
| **文件** | 上述两个场景文件 |

- [x] V63a 更新 `form-with-reactions.md` 的 `last_updated` → `2026-07-10`。
- [x] V63b 更新 `grid-dashboard.md` 的 `last_updated` → `2026-07-10`。

---

## 汇总

| 编号 | 严重度 | 状态 |
|---|---|---|
| V55 | 🔴 | [x] 已修复并验证 |
| V56 | 🔴 | [x] 已修复并验证 |
| V57 | 🟡 | [x] 已修复并验证 |
| V58 | 🟡 | [x] 已修复并验证 |
| V59 | 🟡 | [x] 已修复并验证 |
| V60 | 🟡 | [x] 已修复并验证 |
| V61 | 🟡 | [x] 已修复并验证 |
| V62 | 🟢 | [x] 已修复并验证 |
| V63 | 🟢 | [x] 已修复并验证 |