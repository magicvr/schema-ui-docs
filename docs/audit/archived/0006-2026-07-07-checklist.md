---
status: checklist
date: 2026-07-07
based_on: 0006-2026-07-07-review.md
---

# 审计 0006 跟踪清单

## 使用说明

- `[ ]` 待修复 / `[x]` 已完成
- 每项修复完成后，在右侧填写对应文件的修改摘要和 commit hash

---

## M1（🔴）CHANGELOG v0.2.0 B8 字段名修正

| 字段 | 值 |
|---|---|
| **位置** | `CHANGELOG.md` v0.2.0 B8 |
| **操作** | `supportsData: true` → `supportsStates: true` |
| **文件** | `CHANGELOG.md` |

- [x] M1 修复完成（`CHANGELOG.md`：`supportsData` → `supportsStates`）

---

## M2（🔴）CHANGELOG v0.2.1 补充 ADR-0004 合并记录

| 字段 | 值 |
|---|---|
| **位置** | `CHANGELOG.md` v0.2.1 "已合并的 ADR" |
| **操作** | 追加 `decisions/0004-row-level-scope.md` 条目 |
| **文件** | `CHANGELOG.md` |

- [x] M2 修复完成（`CHANGELOG.md`：追加 ADR-0004 条目）

---

## M3（🔴）`00-overview.md` 版本声明同步

| 字段 | 值 |
|---|---|
| **位置** | `00-overview.md §5` |
| **操作** | 同步为 v0.2.2（或从 CHANGELOG 中回退 v0.2.2 条目——需决策） |
| **文件** | `00-overview.md`（以及可能的 `CHANGELOG.md` 回退） |

**决策：** v0.2.2 已在 CHANGELOG 中记录，视为已发布，同步概述中的版本声明。

- [x] 已发布方案：`00-overview.md §5` 中的 `v0.2.1` → `v0.2.2`

---

## M4（🟡）`07-actions-contract.md` 补充 `modal.content` 版本标注

| 字段 | 值 |
|---|---|
| **位置** | `07-actions-contract.md §5.1` |
| **操作** | `content` 字段补充 `(since 0.2.1)` 标注 |
| **文件** | `07-actions-contract.md` |

- [x] M4 修复完成（`07-actions-contract.md`：`content` 后追加 `(since 0.2.1)`）

---

## M5（🟡）`08-renderer-spec.md` frontmatter 修正

| 字段 | 值 |
|---|---|
| **位置** | `08-renderer-spec.md` frontmatter |
| **操作** | 1) `applies_to` 改为 `v0.2.1`；2) `date` 改为 `last_updated` |
| **文件** | `08-renderer-spec.md` |

- [x] M5 修复完成（`08-renderer-spec.md`：frontmatter `applies_to` + `date` → `last_updated`）

---

## M6（🟡）`03-component-registry.md` 表格字段 `since` 标注精度评估

| 字段 | 值 |
|---|---|
| **位置** | `03-component-registry.md` RowAction/ColumnDef 表格 |
| **操作** | 评估是否将 `(since 0.2)` 改为更精确的 `(since 0.2.1)` |
| **文件** | `03-component-registry.md` |

**注意事项：**
- 协议约定 `protocolVersion` 只用 MAJOR.MINOR，因此 `since 0.2` 严格语义上是正确的
- 但 `03-component-registry.md` 中已有使用 `since 0.2.1` 的前例（`form.mode`、`form.targetTable`、`inputNumber.min`/`max`/`step`/`precision`）
- 建议统一策略：所有标注要么全用 MAJOR.MINOR，要么全用 MAJOR.MINOR.PATCH

- [x] M6 评估完成
- [x] M6 修复完成（`03-component-registry.md`：`(since 0.2)` → `(since 0.2.1)` 共 5 处）

---

## M7（🟢）`component-registry.json` 废弃标注能力验证

| 字段 | 值 |
|---|---|
| **位置** | `component-registry.json` + `03-component-registry.md` |
| **操作** | 评估是否需要为 B9 能力补充测试用例，或标记为"已定义待验证" |
| **文件** | 可能不涉及文件修改 |

**注意事项：**
- 该能力本身无 bug，只是零使用。是否修补取决于团队策略：
  - 策略 A：接受现状，不做修改（无 bug，不构成问题）
  - 策略 B：在 `03-component-registry.md` 中补充说明该能力目前无实际使用
- 建议采用策略 A（当前无组件需要废弃，无需强行创造使用场景）

- [x] M7 评估完成（策略 A：接受现状）

---

## M8（🟢）`04-datasource-contract.md` responseMapping 段落格式优化

| 字段 | 值 |
|---|---|
| **位置** | `04-datasource-contract.md §4.1` |
| **操作** | 精简嵌套 blockquote，将临时变通方案独立为标题段落 |
| **文件** | `04-datasource-contract.md` |

- [x] M8 修复完成（`04-datasource-contract.md`：嵌套 blockquote 精简，插入分隔线）

---

## 汇总

| 编号 | 严重度 | 状态 |
|------|--------|------|
| M1 | 🔴 | [x] |
| M2 | 🔴 | [x] |
| M3 | 🔴 | [x] |
| M4 | 🟡 | [x] |
| M5 | 🟡 | [x] |
| M6 | 🟡 | [x] |
| M7 | 🟢 | [x] |
| M8 | 🟢 | [x] |

> **✅ 全部 8 项已完成。** 可将本审计文件移入 `audit/archived/` 并在 `archived/README.md` 添加索引。**

---

*生成时间：2026-07-07*
