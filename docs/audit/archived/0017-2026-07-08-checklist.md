---
status: checklist
date: 2026-07-08
based_on: 0017-2026-07-08-review.md
---

# 审计 0017 跟踪清单

> 本清单覆盖 2 个在 0016 归档后再次全量复审发现的问题，编号 V18-V19（延续 0016 的 V14-V17 编号体系）。

## 使用说明

- `[ ]` 待修复 / `[x]` 已完成
- 每项修复完成后，在右侧填写对应文件的修改摘要和 commit hash

---

## V18（🟡）`component-registry.json` 自定义 DSL 混用 JSON Schema 组合关键字，但支持边界未被规范化

| 字段 | 值 |
|---|---|
| **位置** | `schemas/component-registry.json`；`03-component-registry.md` §“机器可读注册表”；`06-validation.md` L2 组件契约校验 |
| **问题** | 文档说明 `component-registry.json` 是自定义 DSL、非标准 JSON Schema，但文件内部使用 `anyOf` / `oneOf` / `allOf` / `if` / `then` / `else` / 数组形式 `required` 等 JSON Schema 风格组合关键字，且未说明 L2 校验器必须支持的关键字子集 |
| **操作** | 明确 DSL 支持的组合约束关键字及其语义，或改写为 DSL 自有约束字段 |
| **文件** | `docs/03-component-registry.md`、`docs/06-validation.md`、必要时 `docs/schemas/component-registry.json` |

- [x] V18a 在 `03-component-registry.md` 中补充 `component-registry.json` 的 DSL 关键字白名单
- [x] V18b 明确字段级布尔 `required` 与组合约束数组 `required` 的语义差异
- [x] V18c 在 `06-validation.md` 中说明 L2 组件契约校验不能只读取 props 字段表，必须处理组合约束
- [x] V18d 核对 `form`、`select`、`tabs`、`statCard`、`text` 等现有组合约束是否被新说明覆盖

---

## V19（🟢）`DataRef.params` 空值规则的交叉引用指向 `04-datasource-contract.md §8`，章节号已漂移

| 字段 | 值 |
|---|---|
| **位置** | `01-node-protocol.md` `DataRef` 结构示例；`04-datasource-contract.md` §3.1 与 §8 |
| **问题** | `DataRef.params` 注释将空值规则指向 `04-datasource-contract.md §8`，但通用规则实际位于 §3.1；§8 是 `select.optionsSource` 专门章节 |
| **操作** | 将引用改为 `04-datasource-contract.md §3.1`，必要时补充 `select.optionsSource.params` 同样适用 |
| **文件** | `docs/01-node-protocol.md` |

- [x] V19a 修改 `DataRef.params` 注释中的章节引用
- [x] V19b 核对 `08-renderer-spec.md` 中空值规则引用是否仍指向 §3.1
- [x] V19c 核对 `04-datasource-contract.md` §3.1 标题与锚点是否稳定

---

## 汇总

| 编号 | 严重度 | 类型 | 状态 |
|------|--------|------|------|
| V18 | 🟡 | DSL 解释边界 | [x] |
| V19 | 🟢 | 交叉引用漂移 | [x] |

> **全部项目完成后，** 将本审计文件移入 `audit/archived/` 并在 `archived/README.md` 添加索引。

---

*生成时间：2026-07-08*