---
status: archived
date: 2026-07-11
based_on: 0044-2026-07-11-review.md
---

# 审计 0044 跟踪清单

## V162（🔴）对齐 `08` 与 `02` / ADR-0006 的 value 冲突规则

- [x] V162a 重写 `08-renderer-spec.md` §5.3 末句：仅同字段 `reactions` 数组顺序后写优先。
- [x] V162b 删除或改写「Node 深度优先遍历」表述，避免跨 Node value 合并暗示。
- [x] V162c 交叉引用 `02` §14.1 与 ADR-0006 D3；核对 `01` 求值时序段无同类漂移。
- [x] V162d `git diff --check` 与相关段落人工对照。
- 完成记录：`08` §5.3 已改为同字段数组顺序后写优先并引用 `02`/ADR-0006；无深度优先表述残留。

## V163（🟡）修正 ColumnDef / RowAction 的 scope 强制措辞

- [x] V163a 将 `03-component-registry.md` 列/操作字段表改为条件表述（`$row`/`$self` 才需 `scope: row`）。
- [x] V163b 与同页「作用域说明」及 `01` §3.5 措辞对齐。
- [x] V163c 抽检：纯 `$context` 列显隐示例在文档语气上被接受。
- 完成记录：ColumnDef/RowAction 字段表与作用域说明已同步条件表述；状态键限制独立成条。

## V164（🟡）把 `$row` dependencies 规则写入权威文档

- [x] V164a 在 `02-reaction-expression.md` §8 或 §9 增加：dependencies 为 `$row.` **之后**完整点路径；禁止写 `"$row.xxx"`。
- [x] V164b 正例：`[canRefund]`、`[user.id]`、`[__index]`；反例：`["$row.canRefund"]`。
- [x] V164c `01-node-protocol.md` §3.5 补一句 row 作用域 dependencies 语义。
- [x] V164d `06-validation.md` L3a 描述改为与上一致（明确无 `$row.` 前缀）。
- [x] V164e 用 L3a 复验正反例：`dependencies: [canRefund]` 通过；`["$row.canRefund"]` 失败。
- 完成记录：新增 `02` §8.1 规则表；`01` 注意与 `06` L3a 已对齐。既有 MCP 测试 `uses exact nested row dependency paths` 覆盖精确路径。

## V165（🟡）统一总纲的已发布 / Unreleased 叙事

- [x] V165a 在 `00-overview.md` §5 增加与 README 一致的 published-vs-workspace 说明。
- [x] V165b （可选）若本轮修复随版本发布，将 Unreleased 收敛为下一 PATCH 并同步总纲「最新补丁」句。
- 完成记录：总纲 §5 已区分已发布 `0.2.8` 与 Unreleased；本轮仍记入 Unreleased，未切新 PATCH。

## V166（🟢）补齐 `input` 能力页脚

- [x] V166a 在 `03-component-registry.md` 的 `input` 节末增加：支持 children：否；data：否；reactions：是；states：否。
- [x] V166b 与 `component-registry.json` 中 `input` 的 supports* 标记交叉核对。
- 完成记录：页脚已补齐，与 DSL supportsReactions:true 一致。

## V167（🟡）闭合表格列/操作 form-scope 的 required/value

- [x] V167a 决策：一律禁止表格 columns/actions reactions 的 `required`/`value`（推荐），或书面定义 form-scope 语义。
- [x] V167b 同步 `02` §9.3、`03` 列/操作说明、`06` L2 描述。
- [x] V167c 若选禁止：L2 `validateStateMap` 对 table column/action 挂载点强制仅 `visible`/`disabled`。
- [x] V167d 增加 form-scope 下列/操作 `fulfill.value` / `required` 反例与合法 `visible` 正例（MCP 或脚本测试）。
- [x] V167e 执行 L2、统一入口与 `npm --prefix mcp test`。
- 完成记录：选择一律禁止；L2 按路径识别 columns/actions fulfill/otherwise；新增 `tableFormScopeReactionForbiddenStateYaml` 与 MCP 测试；104 项测试通过。

## V168（🟢）更新 ADR-0005 的执行层标注

- [x] V168a 将 `params.responseMapping` 禁令主路径改为 L1 结构 + L2 语义拒绝。
- [x] V168b 与 `06-validation.md` 当前描述交叉引用一致。
- 完成记录：ADR-0005 D1 已更新。

## V169（🟡）补 search / upload 端到端场景

- [x] V169a 新增搜索表单 + 服务端分页表格完整场景（`mode: search`、`targetTable`、API 数据源）。
- [x] V169b 新增或扩展 upload 完整场景（至少覆盖 `action` 或 `actionRef`+`actions.upload` 之一，并声明 `requiredCapabilities`）。
- [x] V169c 更新 `05-scenarios/README.md` 索引；若「四类场景」表述变化则同步 `00-overview.md`。
- [x] V169d 对新增完整 YAML 跑 `npm run validate` 全部通过。
- 完成记录：`search-form-table.md`、`form-with-upload.md`（含 action 对照片段）已添加；索引与总纲场景列表已更新；全部完整场景 YAML code=0。

## V170（🟢）修复 `06` v0.2.8 blockquote

- [x] V170a 为 Node id 唯一性与 requestMapping 模板两条恢复 `>` 前缀。
- [x] V170b 预览渲染确认整段仍在同一引用块内。
- 完成记录：两条已恢复 `>`，并追加 0044 收敛说明。

## V171（🟢）附录 A 补 actions form-scope 行

- [x] V171a 在 `02` 附录 A 增加「表格 actions（scope: form）」变量可见性行。
- [x] V171b 与正文 §9.1 / 列 form-scope 行规则对称（含独立表格禁 `$deps`）。
- 完成记录：附录 A 增加 form-scope actions 两行（form 内 / 独立表格）。

## V172（🟢）根 package 版本叙事

- [x] V172a 决策：根 `package.json` 版本对齐协议 PATCH，或文档声明 tooling-only。
- [x] V172b 落实到 `package.json` 和/或 `README.md`。
- 完成记录：根包版本改为 `0.2.8`，description 注明对齐协议/MCP 与 Unreleased 关系。

## V173（🟢）调整 CHANGELOG `v0.1.0` 排序

- [x] V173a 将 `v0.1.0` 移出 0.2.x 中间位置，使 0.2.x 倒序连贯。
- [x] V173b 确认无内部锚点/链接依赖旧顺序。
- 完成记录：`v0.1.0` 已移至全部 `0.2.x` 条目之后。

## V174（🟢）标注历史 `$parentRow` 已被 0039 取代

- [x] V174a 在 Unreleased 或下一 PATCH 说明：v0.2.7 `$parentRow` 表述由 0039/V120 取代，当前拒绝。
- [x] V174b 不重写历史事实条目正文（仅加 superseded 注记）。
- 完成记录：Unreleased 增加 `$parentRow` superseded 说明。

## 汇总

| 编号 | 严重度 | 主题 | 状态 |
|---|---|---|---|
| V162 | 🔴 | `08` value 冲突规则与 ADR-0006 矛盾 | ✅ |
| V163 | 🟡 | 表格字段表过度要求 `scope: row` | ✅ |
| V164 | 🟡 | `$row` dependencies 未进权威文档 | ✅ |
| V165 | 🟡 | `00` 版本叙事缺 Unreleased | ✅ |
| V166 | 🟢 | `input` 缺能力页脚 | ✅ |
| V167 | 🟡 | 表格 form-scope required/value 未闭合 | ✅ |
| V168 | 🟢 | ADR-0005 执行层过时 | ✅ |
| V169 | 🟡 | search/upload 缺端到端场景 | ✅ |
| V170 | 🟢 | `06` blockquote 断裂 | ✅ |
| V171 | 🟢 | 附录 A 缺 actions form-scope | ✅ |
| V172 | 🟢 | 根 package 版本脱节 | ✅ |
| V173 | 🟢 | CHANGELOG `v0.1.0` 排序 | ✅ |
| V174 | 🟢 | 历史 `$parentRow` 易误导 | ✅ |

## 建议验证命令（修复后）

```powershell
npm run validate -- <file-or-glob>
npm run validate:l2 -- <file-or-glob>
npm run validate:l3a -- <file-or-glob>
npm --prefix mcp run build
npm --prefix mcp test
npm --prefix mcp run smoke:tools
git diff --check
```

## 最终验证结果

- 全部 6 个官方场景完整 YAML：`npm run validate` 退出码 0。
- `npm --prefix mcp test`：通过，3 个测试文件、**104** 项测试全部通过（含 V167 form-scope 反例）。
- `git diff --check`：见修复提交前检查。
- 未执行 Docker smoke：本轮未修改 Dockerfile / 镜像打包 / stdio transport。
