---
status: active
date: 2026-07-28
based_on: 工作树 schema-ui-protocol 2.5.0 / 2.5（dev @ e901adb）；正式远端仍为 v2.4.1 / mcp-v2.4.1；延续已归档 0069（V313–V330）与 0068（V309–V312）
scope: focus=all；主类型=发布完整性；次类型=文档/入口卫生；含 v2.5 accept 落地后的全量门禁与证据链；不含生产 Renderer/Host 实现与消费者仓代码修改
---

# 审计报告 0070：v2.5.0 落地后发布完整性与入口卫生

## 结论

**工作树内 v2.5 机器契约与本地门禁健康；V331 / V333–V335 已在步骤 3 关闭；V332（main / tag / Release / MCP 跟发）仍开放。**

- 工作树根 / MCP / `protocol-manifest` 均为 **`2.5.0` / `"2.5"`**；ADR-0025/0026/0027 为 `accepted` 并已投影。
- **修复后** `check:protocol-artifact-links` / `check:links` / `release:check` / `verify:protocol-artifact` **全绿**；fixtureDigest 未变（`26179a46…`）。contentDigest `d8d81a19…` / artifact `81bc11fc…`。
- **远端仍未闭合：** `origin/main` = **2.4.1**；无 `v2.5.0` / `mcp-v2.5.0`；GitHub Releases 最新 **2.4.1**。V332 证据齐备前 **不得归档**。
- **未发现**会静默破坏合法 `2.4` 页面语义的 P0 契约矛盾。

本文档是过程记录，不是协议权威来源。权威仍以已接受 ADR、核心规范、Schema、versioned fixtures 与（打 tag 后的）协议制品 digests 为准。

## 范围与基线

### 审计类型

| 类型 | 本轮角色 |
|---|---|
| **发布完整性** | **主类型** — tag/main/CI 制品链接/Release/MCP 跟发 |
| 实现/契约漂移 | 次要抽查 — 未发现新 P0/P1 语义漂移 |
| 提案评审 | 不适用 — 0025/0026/0027 已 accepted 并投影 |

### 纳入

- 根 / MCP / validator `package.json`、lockfile、`protocol-manifest.json`、`README.md`、`PROJECT_CHARTER.md`
- `docs/00-overview.md`、`CHANGELOG.md`、`RELEASE.md`、`release-goals/v2.5.md`、`migrations/2.4-to-2.5.md`、`17-app-manifest.md`
- ADR-0025/0026/0027、`0011` D3 指针；`08` capability 表；`docs/schemas/*`
- `conformance/fixtures`（16 versioned + 1 array）、runners、reference JS/Python
- `.github/workflows/*`（尤其 `check:protocol-artifact-links`）
- MCP 源码测试与 `docs/mcp/README.md`
- 相邻消费者 `allinme.web-client` / `allinme.core-api`（只读 pin）
- 历史索引：`docs/audit/README.md`、`archived/README.md`；正文抽查 0068/0069

### 排除

- 生产 Renderer / Host / 业务鉴权实现
- 默认修改消费者仓库（out-of-repo 跟踪）
- 通读 0001–0067 全部归档正文
- 正式 GHCR 镜像 digests 列表（API 缺 `read:packages`；且尚无 2.5 镜像）

### 基线验证（2026-07-28，`dev`）

| 项 | 结果 |
|---|---|
| 工作树协议版本 | `2.5.0` / `"2.5"` |
| 本地 HEAD | `e901adb`（`docs(prompts): 强化全量审计闭环…`）；含 `f0ff2f0 feat(v2.5): …2.5.0` |
| `origin/main` | **2.4.1**（`df973ec` PR #23）；**未含** 2.5 交付 |
| 正式 tag / Release | `v2.4.1` / `mcp-v2.4.1`；**无** `v2.5.0` / `mcp-v2.5.0` |
| 活跃审计 | 无 → 本轮新建 **`0070`** |
| 问题编号 | 自全局 `V330` 续编 → **`V331`–`V335`** |
| versioned fixtures | 16 类合计 **282**；fixtureDigest `sha256:26179a46712de6ccd4f655dc236a25164f0f55d25a563cfc6e5245435a560512` |
| 工作树 contentDigest | `sha256:5d138d273ce8bcf9f048beb7a3da91d05b6a549d96dd845cf25bbfd88961e8b2` |
| 工作树 artifactDigest | `sha256:6a1f05163ecb782627623e765c386b2987e4cfa89d3e3146dc827460677f9b89`（94 文件） |
| `npm run release:check` | 通过 |
| `npm run verify:protocol-artifact` | `reproducible: true` |
| `npm run check:links` | 221 Markdown / 621 相对链接通过 |
| **`npm run check:protocol-artifact-links`** | **失败：2 missing targets**（V331） |
| `npm run validate:scenarios` | 六官方场景 L0–L4 通过 |
| `npm run validate:conformance` | 16 versioned + 1 allowlist 通过 |
| `npm run test:conformance:all` | **35/35** 通过 |
| `release:check:mcp` / `validator` | 通过；MCP/协议线 **2.5** |
| MCP build / test / smoke:tools | 通过；**128** 测试；5 tools |
| Docker smoke / GHCR 2.5 | 未作为通过项；远端 packages API 403/缺口 |

## 覆盖率与验证

| 批次 | 覆盖 | 置信度 |
|---|---|---|
| A 规范/权威 | charter、manifest、00/08/17、三 ADR、migrations、v2.5 门禁、场景索引与抽样 | 高 |
| B conformance | fixtures 清单、全量 runners、digest | 高 |
| C MCP/validator | package、Dockerfile 头、build/test/smoke:tools；未完成 smoke:docker 全流程 | 中高 |
| D 发布/CI | workflows、RELEASE、远端 tag/Release/main | 高 |
| E 消费者 | 两端 pin 只读 | 中（pin 明确） |
| 历史 | 索引 + 0068/0069；未通读更早全文 | 索引高 |

## 问题清单（汇总表）

| 编号 | 级别 | 主题 | 状态 | 类型 |
|---|---|---|---|---|
| V331 | 🔴 P1 | 协议制品链接门禁失败（`v2.5.md` → `docs/audit/**`） | **已关闭** | 发布完整性 |
| V332 | 🔴 P1 | `2.5.0` 正式发布证据链未闭合（main / tag / Release / MCP） | **开放** | 发布完整性 |
| V333 | 🟢 P2 | 根 README 升级路径缺 `2.4 → 2.5` | **已关闭** | 文档入口 |
| V334 | 🟢 P2 | MCP 文档示例仍钉 `2.4.1`，与 `mcp@2.5.0` 不一致 | **已关闭** | 文档/MCP |
| V335 | 🟢 P2 | 场景 README 称 batch 示例为 `"2.4"`，正文已为 `"2.5"` | **已关闭** | 文档入口 |

**Out-of-repo（不单列缺陷编号）：** 生产消费者 pin `v2.4.1` 与当前**正式**发布一致；待本仓 `v2.5.0` 证据齐备后再升级跟踪。

---

## 问题详情

### V331（🔴）协议制品相对链接门禁失败 — 已关闭

**位置：** `docs/release-goals/v2.5.md`（原 L17、L215 链到 `../audit/archived/0069-…`）。

**原现象：** 源码树链接存在，但 `docs/audit/**` 被制品排除 → `check:protocol-artifact-links` 失败，阻断 Protocol CI/Release。

**修复（2026-07-28）：** 两处改为无链接的「归档审计 **0069**」叙述，并注明过程审计不进入协议制品。`release-goals` / `migrations` 无残留 audit 相对链接。

**验证：** `npm run check:protocol-artifact-links` exit 0（67 文件 / 361 链）；`check:links` / `release:check` / `verify:protocol-artifact` 通过。修复后 contentDigest `sha256:d8d81a19…`，artifact `sha256:81bc11fc…`（fixture 未变）。

---

### V332（🔴）`2.5.0` 发布证据链未闭合 — 开放

**位置 / 声称侧：**

- `docs/CHANGELOG.md`：`## v2.5.0 — 2026-07-28`（正式节，Unreleased 为空）
- `docs/00-overview.md`：当前版本 `v2.5.0`；「随 `2.5.0` 制品正式发布」
- 根 / MCP / validator / manifest：版本已升 `2.5.0` / `"2.5"`
- `docs/release-goals/v2.5.md`：G0–G5 与 §3 全勾；「下一步：tag 仪式见 RELEASE.md」

**远端事实（2026-07-28 复审）：**

| 证据 | 状态 |
|---|---|
| `origin/main` `package.json` version | **2.4.1** |
| open PR | **无** |
| tag `v2.5.0` / `mcp-v2.5.0` | **不存在** |
| GitHub Release | 最新 **Schema-UI Protocol 2.4.1** |
| main 上 2.5 Protocol CI | **未发生**（2.5 仅在 `origin/dev`） |

**影响：** 读者可能将 `dev` 工作树或 CHANGELOG 正式节当作可 pin 的发布身份；与 0068「版本字符串 vs 不可变资产」同类。

**进度（2026-07-28 步骤 3）：** V331 与入口卫生（V333–V335）已关闭，制品链接门禁不再阻断本地构建。仍缺：合入 `main`、tag、Release、MCP CD。

**剩余步骤：**

1. PR：`dev` → `main`，Protocol / MCP / Validator CI 全绿。
2. 在目标 commit：`git tag v2.5.0` → Protocol Release；核对 tar digests。
3. 跟发：`git tag mcp-v2.5.0` → MCP CD；核对 GHCR label 与捆绑协议 `2.5.0`。
4. 将正式 contentDigest / artifactDigest / fixtureDigest 与 Release run 记入关闭证据（**勿**在 CHANGELOG 自引用未定 digest 以免二次漂移，沿 0068 纪律）。

**验证：** `gh release view v2.5.0`；Protocol/MCP Release run success；`release:check:tag` / `release:check:mcp:tag`（若在 tag 上下文执行）。

**关闭条件（发布类最严）：** 本地门禁 + 分支 CI + 正式 tag/CD 远端证据齐备；不可访问远端时不得假装关闭。

---

### V333（🟢）根 README 缺 2.4→2.5 迁移入口 — 已关闭

**修复：** 根 `README.md` 升级路径首条补 `docs/migrations/2.4-to-2.5.md`，并保留 2.3→…→2.0 链。与 `00-overview` 一致。`check:links` 通过。

---

### V334（🟢）MCP 文档示例版本滞后 — 已关闭

**修复：** `docs/mcp/README.md` 示例与 tag 表改为 `2.5.0` / `2.5`，并注明正式 GHCR 拉取需等 `mcp-v2.5.0`。对照 `mcp/package.json` 无 `2.4.1` 残留示例。Docker smoke 未跑（可选）。

---

### V335（🟢）场景索引 protocolVersion 文案漂移 — 已关闭

**修复：** `docs/05-scenarios/README.md` 将 batch 示例改为随协议线声明 `"2.5"`，并注明 batch capability 仍属 2.2+。与正文 / `_samples/order-list-batch.yaml` 一致。

---

## 与历史审计关系

| 历史 | 关系 |
|---|---|
| **0069**（V313–V330） | 提案层交叉可接受性已关闭；accept 原子交付在工作树已落地。本轮**不**重开 D4a/navigate 等设计刀口；无回归证据。 |
| **0068**（V309–V312） | 发布身份 / README 迁移入口 / 文档卫生。本轮 V332 同类「证据链」、V333 同类「README 迁移入口」。 |
| **0067 / 0066** | recordView 与发布闭环；本轮抽查无回归。 |
| V 编号 | 全局最大曾用 **V330**；本轮 **V331–V335**；无复用 0010/0013/0014 的 V1–V4 冲突。 |

## 处理顺序与防复发

### 推荐顺序

1. **V331**（解锁 CI/CD 门禁）  
2. **V333 + V335**（入口文案，可与 V331 同 PR）  
3. **V334**（可与 MCP 跟发同变更）  
4. **V332**（PR → main → `v2.5.0` → `mcp-v2.5.0`；依赖 1）

### 防复发

- informative 文档（含 `release-goals`）**禁止**相对链接到 `docs/audit/**`；可在 `check:links` 或 release 脚本增加「制品包含文件不得链 excluded 路径」的显式规则（若尚未覆盖全路径，以 `check:protocol-artifact-links` 为权威）。
- CHANGELOG 正式节与包版本 bump 仅在**即将**合 main / tag 的变更集出现；或明确「仅 dev 预写、以 tag 为准」。
- 协议 MINOR 后 README 升级路径与 MCP 文档示例纳入 G5/发布检查表（可补 `release-goals` 或 RELEASE 清单一句）。

### 消费者（out-of-repo）

`allinme.web-client` / `allinme.core-api` 当前 pin **`v2.4.1`** 正确。正式 `v2.5.0` 发布后另开消费者升级项（fixture 路径、suite 计数 14→16+、commit pin）；**默认不在本审计修改消费者仓**。
