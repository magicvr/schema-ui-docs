---
status: active
date: 2026-08-14
based_on: schema-ui-protocol 2.9.0（工作树 @ feat/2.9-route-binding-readonly）
scope: v2.9 发布闭环跟踪清单（ADR-0039/0040 → 修订 → 审计 → 发布）
---

# 跟踪清单 0082：v2.9 数据源路由绑定 + 表单字段只读

## 目标

ADR-0039（`data.route-binding`）与 ADR-0040（`form.controls.readonly`）随 v2.9.0 原子交付
并发布：修订 → self 审计 → grok 独立审计 → 归档 → PR/CI → tag → release 资产 + MCP 镜像。

## 门禁

- [x] **A-001 self**（0082-review）：ADR 决策逐条核对一致；V394/V395（P2）已整改；
      V396–V398（P3）备注。
- [x] **A-002 独立（grok build · grok 4.6 · xhigh）**：conditional → F-001~F-003（P1）+
      R-001~R-004（P2）+ O-005/O-006 全部处置；复核 BLOCKING_COUNT=0，判定闭合。
- [ ] 审计归档（0082 → archived/）。
- [ ] PR → Protocol CI 全绿 → merge `main`。
- [ ] tag `v2.9.0`（Protocol Release 资产）→ tag `mcp-v2.9.0`（GHCR 镜像）→ 核对 digest。

## 证据

| 项 | 值 |
|---|---|
| ADR | `docs/decisions/0039-data-source-route-binding.md` / `0040-form-field-readonly.md` |
| 门禁文档 | `docs/release-goals/v2.9.md`（G0–G4） |
| 迁移 | `docs/migrations/2.8-to-2.9.md` |
| capability | `data.route-binding`（request-construction）/ `form.controls.readonly`（component-format） |
| fixture digest | `sha256:b6609b46…`（v2.9.0） |
| 新增用例 | request-construction +6、version-negotiation +6、MCP 负例 +4 |

## 关闭条件

- 本地门禁全绿（check:links / release:check / verify / scenarios / conformance /
  conformance:all / mcp / validator / audit）；
- A-002 独立审计 BLOCKING_COUNT=0；
- CI 全绿合并后 tag 发布，Release 资产与 GHCR 镜像 digest 可核对。
