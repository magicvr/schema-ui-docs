---
status: active
date: 2026-07-28
based_on: 0074-2026-07-28-review.md
scope: focus=all；主类型=实现/契约漂移；次类型=发布完整性
---

# 审计 0074 跟踪 Checklist

> 本 checklist 跟踪 v2.6.0（ADR-0028 F1 A+B）落地后的制品门禁、MCP 投影、入口卫生与正式发布证据（V350–V358）。  
> 生产消费者仅作 out-of-repo 跟踪；未经单独授权，不修改 `allinme.web-client` 或 `allinme.core-api`。  
> 不把本审计路径加入 `protocol-manifest.json`、协议制品、MCP 搜索索引或 Docker 运行镜像。

## 汇总

| 编号 | 级别 | 主题 | 状态 |
|---|---|---|---|
| V350 | 🔴 P1 | 协议制品相对链接门禁失败 — F1 sample 未入 manifest | **已关闭** |
| V351 | 🔴 P1 | MCP 单测仍断言 `v2-release-goals` → v2.5 | **已关闭** |
| V352 | 🟡 P1 | 工作树 2.6.0 正式 tag/CD/远端证据链未闭合 | **开放** |
| V353 | 🟢 P2 | `docs/mcp/README.md` 示例仍停在 2.5.3 | **已关闭** |
| V354 | 🟢 P2 | 根 README 升级路径缺 2.5→2.6 | **已关闭** |
| V355 | 🟢 P2 | `conformance/README.md` 仍写当前版本为 `"2.5"` | **已关闭** |
| V356 | 🟢 P2 | `01-node-protocol` YAML 注释仍写制品 `"2.5"` | **已关闭** |
| V357 | ⚪ P3 | `next-admin-lifecycle` 开篇仍写「F1 立项中」 | **已关闭** |
| V358 | ⚪ P3 | 场景 README 阅读顺序未列 `form-controls-extended` | **已关闭** |

**计数：** 3×P1 中 **2 已关闭 / 1 开放**；4×P2 + 2×P3 **全部关闭**。  
**问题号：** 全局续编 `V349` → `V350`–`V358`。  
**审计号：** `0074`（active；V352 发布证据待闭合后归档）。

## 基线

- [x] 创建前确认无活跃 audit；全局最大审计号为 `0073`（archived）。
- [x] 确认 `V350`–`V358` 位于全局最大 `V349` 之后，且不使用历史冲突的 V1–V4。
- [x] 记录正式 `v2.5.3` content `eae31dc4…` / artifact `93e6e144…` / fixture `712f6c42…`。
- [x] 记录初审工作树 content `ec668f9b…` / artifact `e7c4efaa…` / fixture `3a9cd233…`（303 cases）。
- [x] 记录步骤 1 本地门禁：`check:protocol-artifact-links` 红；MCP test 1 失败；conformance 35/35 绿。
- [x] 修复后全量本地门禁绿（含制品链接与 MCP test）。
- [ ] 正式 `v2.6.0` / `mcp-v2.6.0` 发布证据齐备（关闭 V352）。

---

## V350 — 协议制品相对链接门禁失败 — F1 sample 未入 manifest（P1）— 已关闭

### 制品清单

- [x] `protocol-manifest.json` `informative.scenarios` 增加  
      `docs/05-scenarios/_samples/user-profile-extended.yaml`
- [x] 确认源文件存在且与 `form-controls-extended.md` fence 语义一致
- [x] **不**把 audit 负例 sample 或无关 `_samples/*` 无差别全量塞进制品
- 验证命令：`npm run build:protocol`；`dist/protocol` 含 sample
- 关闭证据：制品文件数 98→**99**；sample 进入 tar

### 链接门禁

- [x] `npm run check:protocol-artifact-links` 通过（71 Markdown / 381 链接）
- [x] `npm run verify:protocol-artifact` 可复现
- 验证命令：`check:protocol-artifact-links`；`verify:protocol-artifact`；`release:check`
- 关闭证据：content `sha256:a2c6b4a4…`；artifact `sha256:e132d225…`；fixture 仍 `3a9cd233…`（机器契约未变）

---

## V351 — MCP 单测仍断言 `v2-release-goals` → v2.5（P1）— 已关闭

- [x] `DOC_MAP` 已指向 `docs/release-goals/v2.6.md`（实现侧正确）
- [x] 更新 `mcp/tests/search.test.ts`：期望路径 `v2.6.md`；内容含 `v2.6`
- [x] `npm --prefix mcp test` **134/134** 通过
- [x] `npm --prefix mcp run smoke:tools`：5 tools 列出
- 验证命令：`npm --prefix mcp test`；`smoke:tools`
- 关闭证据：search.test 全绿；smoke tools 列表完整

---

## V352 — 工作树 2.6.0 正式 tag/CD/远端证据链未闭合（P1，发布类）— 开放

> 证据真实产生前不得关闭。不可访问远端时保持开放或标「无法验证」并列剩余风险。  
> **禁止**改写已发布 `v2.5.3` / `mcp-v2.5.3` tag 或 Release 资产。

### 合入前

- [x] V350、V351 及本轮 P2/P3 已关闭
- [x] 本地全量门禁绿（见「回归与归档门禁」）
- [ ] 推送分支 / PR；Protocol CI + MCP CI + Validator CI 绿
- 验证命令：上列 npm 脚本；`gh pr checks`（或等价）
- 关闭证据：（待填）

### 正式协议发布

- [ ] `main` 目标 commit 上 tag `v2.6.0` 并 push
- [ ] Protocol Release 工作流 success；GitHub Release 含 `schema-ui-protocol-2.6.0.tar.gz` + `.sha256`
- [ ] 正式 contentDigest / artifactDigest / fixtureDigest 与发版 commit 本地一致
- 验证命令：`gh release view v2.6.0`；工作流 run URL
- 关闭证据：（待填）

### 正式 MCP 发布

- [ ] tag `mcp-v2.6.0` 并 push（MAJOR.MINOR 与协议线 2.6 对齐）
- [ ] MCP CD success；GHCR `2.6.0` / `2.6` / `latest`（及 SHA）策略符合 `RELEASE.md`
- [ ] 镜像 label 捆绑协议 `2.6.0`；记录 index digest
- 验证命令：工作流 run；`docker buildx imagetools inspect`（若可用）
- 关闭证据：（待填）

---

## V353 — `docs/mcp/README.md` 示例仍停在 2.5.3（P2）— 已关闭

- [x] pull/run/客户端 args/本地 build/smoke 示例对齐工作树 MCP **2.6.0**
- [x] 明确正式镜像未发前生产继续 pin **`2.5.3` / `mcp-v2.5.3`**
- [x] 避免「需等某 tag」后忘记收尾
- 验证命令：人工 diff + `npm run check:links`
- 关闭证据：`docs/mcp/README.md` 示例与说明已为 2.6.0 / 已发布 pin 2.5.3

---

## V354 — 根 README 升级路径缺 2.5→2.6（P2）— 已关闭

- [x] 根 `README.md` 升级段落增加 [`docs/migrations/2.5-to-2.6.md`](../migrations/2.5-to-2.6.md)
- 验证命令：`npm run check:links`
- 关闭证据：升级链以 2.5→2.6 为首

---

## V355 — `conformance/README.md` 仍写当前版本为 `"2.5"`（P2）— 已关闭

- [x] 两处「当前/现为」改为 **`"2.6"`**；过滤上一线示例改为 `"2.5"`
- [x] 与 `release:check` / `validate:conformance` 行为一致
- 验证命令：人工核对 + `validate:conformance`
- 关闭证据：文案与 303 cases @ `"2.6"` 一致

---

## V356 — `01-node-protocol` YAML 注释仍写制品 `"2.5"`（P2）— 已关闭

- [x] 顶层结构示例注释改为 **`"2.6"`**
- [x] 与同文件正文一致
- 验证命令：人工核对
- 关闭证据：`docs/01-node-protocol.md` 注释已对齐

---

## V357 — `next-admin-lifecycle` 开篇仍写「F1 立项中」（P3）— 已关闭

- [x] 引言改为 F1 A+B **已交付** / post-v2.6 残留 backlog
- [x] 与 §3 / D.1.0 一致
- 验证命令：人工核对
- 关闭证据：`docs/release-goals/next-admin-lifecycle.md` 引言已改

---

## V358 — 场景 README 阅读顺序未列 `form-controls-extended`（P3）— 已关闭

- [x] 阅读顺序追加第 11 项 F1 场景（注明 `form.controls.extended`）
- [x] 与文件列表表项一致
- 验证命令：`npm run check:links`
- 关闭证据：`docs/05-scenarios/README.md` 阅读顺序 1–11

---

## 回归与归档门禁

- [x] V350–V351、V353–V358 已勾选关闭。
- [ ] V352 远端证据齐备（或用户确认「无法验证」并接受剩余风险——本轮默认保持开放）。
- [x] 受影响命令回归（2026-07-28 步骤 3）：  
      `check:links`、`release:check`、`verify:protocol-artifact`、`check:protocol-artifact-links`、  
      `validate:scenarios`、`validate:conformance`、`test:conformance:all`（35/35）、  
      `release:check:mcp`、`npm --prefix mcp test`（134）、`smoke:tools`（5 tools）。
- [ ] 用户确认关闭摘要后，方可归档（**含 V352 关闭或明确接受剩余风险**）。
- [ ] 移至 `docs/audit/archived/`；frontmatter `status: archived`；更新两边 README。
- [ ] 无应关闭而未勾选的裸 `[ ]`（out-of-repo 与 V352 除外）。
- [ ] `git diff --check` 与最终相关门禁。

## Out-of-repo（不在本仓关闭）

- [ ] （跟踪）`allinme.web-client`：评估自 `v2.4.1` 升级至当前稳定协议 pin（目标随本轮发布后为 `v2.6.0` 或经协商的中间稳定版）
- [ ] （跟踪）`allinme.core-api`：同步 `pin.go`、制品 SHA、校验器检出 tag

## 变更日志

| 日期 | 说明 |
|---|---|
| 2026-07-28 | 创建 0074；记录步骤 1 复审基线与 V350–V358 开放项。 |
| 2026-07-28 | 步骤 3 in-repo：V350–V351、V353–V358 关闭；本地全量门禁绿；V352 仍开放。 |
