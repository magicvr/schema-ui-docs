---
status: active
date: 2026-07-28
based_on: schema-ui-protocol 2.6.0 / protocolVersion 2.6；HEAD daac9e3（dev ahead origin/dev 1）；正式 tag 仍为 v2.5.3 / mcp-v2.5.3 @ 2e81c54；延续已归档 0070–0073
scope: focus=all；主类型=实现/契约漂移；次类型=发布完整性；不修改生产消费者仓库
---

# 审计报告 0074：v2.6.0 落地后制品门禁、MCP 投影与发布证据

## 结论

本轮全量复审确认：工作树已落地 **v2.6（ADR-0028 F1 批次 A+B）** 的核心规范、Schema/DSL、L2/L3a、versioned fixtures（**303** cases）与 dual-language conformance（**35/35**）。正式 **`v2.5.3` / `mcp-v2.5.3`** 证据链仍有效且**不得改写**。

新发现问题 **3×P1 + 4×P2 + 2×P3（V350–V358）**。步骤 3 in-repo 后：

| 状态 | 编号 |
|---|---|
| **已关闭** | V350、V351、V353–V358（2×P1 + 4×P2 + 2×P3） |
| **开放** | **V352**（正式 `v2.6.0` / `mcp-v2.6.0` tag/CD 证据链） |

生产消费者仍 pin **v2.4.1**，仅作 out-of-repo 跟踪。有效修复已沉淀到 `protocol-manifest`、MCP 测试与入口文档；**本文不是协议权威来源**。`docs/audit/**` 不进入 `protocol-manifest.json`、协议制品、MCP 索引或 Docker 镜像。

| 判定 | 说明 |
|---|---|
| 工作树 2.6.0 语义 | ADR-0028 accepted；303 fixtures / 35 conformance 绿 |
| 工作树 2.6.0 门禁 | 步骤 3 后链接门禁与 MCP **134** tests 绿 |
| 正式 2.5.3 身份 | **不可变** pin：content `eae31dc4…` · artifact `93e6e144…` · fixture `712f6c42…` |
| 正式 2.6.0 身份 | **尚未产生**（V352 开放） |
| 工作树 digests（步骤 3） | content `a2c6b4a4…` · artifact `e132d225…` · fixture `3a9cd233…`（99 文件） |

## 范围与基线

### 审计类型

| 类型 | 本轮角色 |
|---|---|
| **实现/契约漂移** | **主类型** — 制品清单与场景链接、MCP 测试投影、入口文案与当前线不一致 |
| **发布完整性** | **并列** — 工作树已声明 2.6.0，正式 tag/CD/远端 digests 未齐；制品链接门禁红 |
| 提案评审 | 不适用 — ADR-0028 已 `accepted`；批次 C（`cascader`）为明确 defer，非本轮提案 |

### 当前基线（步骤 1 初审）

| 项 | 结果 |
|---|---|
| 工作树 / 分支 | `dev` @ `daac9e3`（领先 `origin/dev` 1 提交；工作树干净） |
| 工作树版本 | 根包与 manifest **2.6.0**；页面协议 **`"2.6"`**；MCP **2.6.0**；validator **1.0.0** 支持 2.6.0 |
| 正式 tag | 最新 `v2.5.3` / `mcp-v2.5.3` → `2e81c54`（`origin/main`）；**无** `v2.6.*` |
| 活跃审计 | 创建前 **无**；本轮 **0074** |
| 问题编号 | 全局最大 **V349**（0073），本轮续编 **V350–V358** |

### 基线验证（步骤 1）

| 命令或证据 | 结果 |
|---|---|
| `npm run check:links` | 233 Markdown、655 相对链接通过 |
| `npm run release:check` | 303 cases、17 fixture 文件；fixture `3a9cd233…`；content `ec668f9b…`；artifact `e7c4efaa…` |
| `npm run verify:protocol-artifact` | reproducible=true；98 文件 |
| `npm run check:protocol-artifact-links` | **失败**：`form-controls-extended.md` → `user-profile-extended.yaml` missing in `dist/protocol` |
| `npm run validate:scenarios` / `validate:conformance` | 官方 6 场景 L0–L4 通过；versioned suites 绿 |
| `npm run validate -- …/user-profile-extended.yaml` | 源码树 L0–L4 通过 |
| `npm run test:conformance:all` | **35/35** JS+Python 通过 |
| `release:check:mcp` / `:validator` | 通过（线对齐 2.6） |
| MCP `build` | 通过 |
| MCP `test` | **1 failed / 133 passed**（`search.test.ts` 期望 `v2.5.md`） |
| `smoke:tools` / 本地 Docker smoke | **未执行**（被 MCP test 阻断 / 本轮未跑镜像）— 记证据缺口 |
| 远端 Release | Latest = `v2.5.3`；无 `v2.6.0` 资产 |
| 生产消费者 | 相邻仓只读：仍 pin **`v2.4.1`** |

## 覆盖率与验证

| 批次 | 已覆盖 | 未覆盖 / 低置信度 | 置信度 |
|---|---|---|---|
| A 规范与权威边界 | charter、manifest、`00`–`09`、Schema、ADR-0028、`v2.6` 门禁、F1 场景/sample、`2.5-to-2.6` | 未逐字通读全部历史 ADR | 高 |
| B 校验与 conformance | 全量 runner、release-check fixture 门禁、L2 F1 路径抽样 | reference 未全文 diff | **高** |
| C MCP 与 validator | package 对齐、DOC_MAP→`v2.6`、build、失败单测定位 | smoke:tools / Docker 未跑 | 中高 |
| D 自动化与发布 | RELEASE 惯例、远端 2.5.3 tag/Release；2.6 证据缺口明确 | v2.6 远端 CI/CD 尚未触发 | **高**（缺口） |
| E 生产消费者 | web-client / core-api pin 只读 | 未跑消费者 CI | 中 |
| 历史审计 | 索引 + 0070–0073 查重；最大 NNNN=0073 / V=349 | 未通读 0001–0069 全文 | 索引高 |

## 问题清单（汇总表）

| 编号 | 级别 | 主题 | 状态 | 类型 |
|---|---|---|---|---|
| V350 | 🔴 P1 | 协议制品相对链接门禁失败 — F1 sample 未入 manifest | **已关闭** | 发布完整性 / 制品契约 |
| V351 | 🔴 P1 | MCP 单测仍断言 `v2-release-goals` → v2.5 | **已关闭** | 实现/契约漂移 |
| V352 | 🟡 P1 | 工作树 2.6.0 正式 tag/CD/远端证据链未闭合 | **开放** | 发布完整性 |
| V353 | 🟢 P2 | `docs/mcp/README.md` 示例仍停在 2.5.3 | **已关闭** | 文档/入口 |
| V354 | 🟢 P2 | 根 README 升级路径缺 2.5→2.6 | **已关闭** | 文档/入口 |
| V355 | 🟢 P2 | `conformance/README.md` 仍写当前版本为 `"2.5"` | **已关闭** | 文档 |
| V356 | 🟢 P2 | `01-node-protocol` YAML 注释仍写制品 `"2.5"` | **已关闭** | 规范正文卫生 |
| V357 | ⚪ P3 | `next-admin-lifecycle` 开篇仍写「F1 立项中」 | **已关闭** | 文案 |
| V358 | ⚪ P3 | 场景 README 阅读顺序未列 `form-controls-extended` | **已关闭** | 文案 |

**计数：** 0×P0、**3×P1**（2 关 / 1 开）、**4×P2**（全关）、**2×P3**（全关）。  
**Out-of-repo（不编号）：** 生产消费者仍 pin `v2.4.1`（与 0070–0073 一致；本轮不升 pin）。

---

## 问题详情

### V350（🔴 P1）协议制品相对链接门禁失败 — F1 sample 未入 manifest — 已关闭

**位置：** `protocol-manifest.json` → `informative.scenarios`；`docs/05-scenarios/form-controls-extended.md`；`docs/05-scenarios/_samples/user-profile-extended.yaml`；`scripts/build-protocol-artifact.js`（按 manifest 收录）。

**当前行为：**

- 源码树存在 `user-profile-extended.yaml`，L0–L4 通过。
- 场景 Markdown 经 `docs/05-scenarios/**/*.md` 进入协议制品，并相对链接 `./_samples/user-profile-extended.yaml`。
- manifest 仅显式列入 `order-list-batch.yaml` 与 `order-detail-lifecycle.yaml`，**未**列入 F1 sample。
- `dist/protocol` 内目标文件缺失 → `check:protocol-artifact-links` 失败。

**冲突证据：**

```text
npm run check:protocol-artifact-links
# form-controls-extended.md:101 → missing docs/05-scenarios/_samples/user-profile-extended.yaml
```

**影响：** 正式 tar 消费者跟随场景链接会 404；发布工程门禁 fail-closed；与 release-goals/v2.6「扩展示例 + sample」勾选对外承诺不一致。

**修复（步骤 3）：** manifest 增加 `user-profile-extended.yaml`；制品 99 文件；`check:protocol-artifact-links` 绿。  
**关闭证据：** content `a2c6b4a4…` / artifact `e132d225…`；fixture 不变 `3a9cd233…`。

**查重：** 0070/V331（制品相对链接门禁）。

---

### V351（🔴 P1）MCP 单测仍断言 `v2-release-goals` → v2.5 — 已关闭

**位置：** `mcp/src/core/protocol-index.ts`（`DOC_MAP`）；`mcp/tests/search.test.ts`。

**初审行为：** 测试仍期望 `v2.5.md`；`DOC_MAP` 已指向 `v2.6.md`；Vitest 1 fail / 133 pass。

**修复（步骤 3）：** `mcp/tests/search.test.ts` 期望路径 `v2.6.md`、内容含 `v2.6`。  
**关闭证据：** `npm --prefix mcp test` **134/134**；`smoke:tools` 5 tools。

**查重：** 0071/V340（DOC_MAP 投影）；测试滞后为同模式的对称面。

---

### V352（🟡 P1）工作树 2.6.0 正式 tag/CD/远端证据链未闭合 — 开放

**位置：** 根/MCP/`protocol-manifest` 版本声明；`docs/CHANGELOG.md` `## v2.6.0`；Git tags；GitHub Releases；MCP GHCR。

**当前行为：**

- 工作树与 CHANGELOG 已以 **2.6.0** 作为当前制品身份。
- 正式最新 tag/Release 仍为 **2.5.3**；无 `v2.6.0` / `mcp-v2.6.0`。
- `dev` 领先 `origin/dev` 1 提交；尚未合入 `main` 的 v2.6 CI 证据。

**影响：** 对外「已交付 2.6.0」与可 pin 的不可变资产不一致；消费者无法固定正式 tar/镜像 digest。

**步骤 3 进展：** V350/V351 与 P2/P3 已关；本地 digests 已更新为 content `a2c6b4a4…` / artifact `e132d225…`。  
**仍待：** 推送/PR → `main` 绿 → tag `v2.6.0` + `mcp-v2.6.0` → 记录正式 Release/CD digests。  
**禁止**改写 `v2.5.3` / `mcp-v2.5.3`。

**查重：** 0070/V332、0071/V344、0073/V349（发布身份/证据链）。

---

### V353（🟢 P2）`docs/mcp/README.md` 示例仍停在 2.5.3 — 已关闭

**修复：** 示例与说明对齐工作树 **2.6.0**；正式未发前生产继续 pin **2.5.3 / mcp-v2.5.3**。  
**查重：** 0072/V345、0070/V334。

### V354（🟢 P2）根 README 升级路径缺 2.5→2.6 — 已关闭

**修复：** 根 README 升级链首增加 `2.5-to-2.6`。  
**查重：** 0070/V333。

### V355（🟢 P2）`conformance/README.md` 仍写当前版本为 `"2.5"` — 已关闭

**修复：** 两处当前版本改为 `"2.6"`。

### V356（🟢 P2）`01-node-protocol` YAML 注释仍写制品 `"2.5"` — 已关闭

**修复：** 注释改为 `"2.6"`。

### V357（⚪ P3）`next-admin-lifecycle` 开篇仍写「F1 立项中」 — 已关闭

**修复：** 引言改为 F1 A+B 已交付 / post-v2.6 残留 backlog。

### V358（⚪ P3）场景 README 阅读顺序未列 `form-controls-extended` — 已关闭

**修复：** 阅读顺序追加第 11 项 F1 场景。

---

## 与历史审计关系

| 历史 | 关系 |
|---|---|
| **0073**（V349） | 2.5.3 发布已关闭；本轮确认正式 digests 仍有效；本轮主题为 **2.6.0 落地后** 缺口 |
| **0072**（V345–V348） | V345 类 MCP 发布后文案 → 本轮 **V353** 再现于 2.6 升版 |
| **0071**（V336–V344） | V340 DOC_MAP / V344 发布身份 → 本轮 **V351**（测试滞后）、**V352**（证据链） |
| **0070**（V331–V335） | V331 制品链接 → **V350**；V333 迁移入口 → **V354**；V332 发布证据 → **V352** |
| 0010/0013/0014 | 历史 V1–V4 冲突；本轮 **不**复用，自 **V350** 递增 |

**已修复且未回归（抽样）：** 2.5 线契约投影（清单下限、M3a、D1b、Schema capability、外部 root、场景 frontmatter）在 2.6 全量 conformance 下仍绿；fixture 自 292→303 为预期增量。

## 处理顺序与防复发

1. **V350** — manifest 纳入 F1 sample；制品链接门禁绿（digest 更新预期）。  
2. **V351** — MCP 测试对齐 `v2.6.md`；`npm --prefix mcp test` 全绿。  
3. **V353–V356** — 入口/文档卫生，可与 1–2 同 PR。  
4. **V357–V358** — P3 顺手或同 PR。  
5. **V352** — 合入 `main` 后正式 `v2.6.0` + `mcp-v2.6.0`；证据齐备再关。  
6. 消费者 pin：会话外跟踪，默认不改邻仓。

**防复发：**

- 新增 `_samples/*.yaml` 且被制品内 Markdown 链接时，必须同步 `protocol-manifest.json` 显式条目（或改为可审计的 glob 策略 + 门禁）。  
- 变更 `DOC_MAP` 当前线目标时，同 PR 更新对应稳定 id 测试。  
- MINOR 升版 checklist：README 迁移链、`docs/mcp` 示例、`conformance/README` 当前版本句、权威正文示例注释。  
- 工作树 `artifactVersion` 升版后，发布证据链条目保持开放直至 tag/CD 可核验。

## 关闭条件（预览，步骤 3）

| 主类型 | 关闭要求 |
|---|---|
| 实现/契约 | V350–V351、V353–V358 有代码/文档沉淀与本地门禁证据 |
| 发布完整性 | V352 具备正式 tag、Release 资产、MCP CD/镜像 digest；不可访问则保持开放或「无法验证」 |
| 归档 | 用户最终确认；移入 `archived/`；更新两边 README |
