---
status: active
date: 2026-08-13
based_on: schema-ui-protocol 2.8.0 / protocolVersion 2.8；tag v2.8.0 @ 593f625；HEAD 8ba128a（上游）、消费者 schema-ui-core f929120
scope: focus=all；主类型=发布完整性 + 实现/契约漂移；审计轮 A-001（self）+ A-002（independent，grok build）
---

# 审计报告 0079：v2.8.0 Host/App 互操作发布闭环

## 结论（A-001 self，A-002 合并中）

上游 H1–H4 全量闭环：正式 **v2.8.0**（tag `v2.8.0` @ `593f625`）证据链完整——content
`sha256:40690917…` · artifact `sha256:594207e0…` · fixture 树 `sha256:7aacf133…`（421 cases）；
conformance 41 条（JS/Python）全绿；release:check（含 tag 模式）/ mcp / validator 全绿；
MCP 134 tests；消费者 `schema-ui-core` S4 生产证据（E-004）与正式 digest 重 pin 完成。

A-001 self 发现 **2×P2 + 2×P3（V375–V378）**，P2 两项已在消费者仓修复（f929120），P3 一项
residual 已登记、一项记录不修（B0 const 已结构性覆盖的 B1 防御路径；发布后新增 fixture 将改变
已发布 digest，留待下一 MINOR）。

**A-002 independent（grok build，grok 4.5，reasoning high）进行中**，结论与合并处置见
「A-002 独立审计」节（本文件随审计闭环更新）。

## 范围与基线

### 审计类型

| 类型 | 本轮角色 |
|---|---|
| 发布完整性 | 主类型 — v2.8.0 版本线、门禁、制品 digest 与 tag 证据链 |
| 实现/契约漂移 | 主类型 — B/F/C 机器契约、双 reference、消费者生产实现与上游一致性 |
| 提案评审 | 不适用 — H0/H1 已闭合（ADR accepted） |

### 基线（步骤 1）

| 项 | 结果 |
|---|---|
| 上游工作树 | `dev` @ `8ba128a`（干净）；tag `v2.8.0` @ `593f625` |
| 消费者工作树 | `schema-ui-core` `dev` @ `f929120`（干净） |
| 正式 pin | content `40690917…` · artifact `594207e0…` · fixture `7aacf133…`（421） |
| MCP / validator | `2.8.0` / `2.8` / validator `1.0.0`（range 对齐） |

## H1 审计记录补录（ADR-0034 §H1 评审记录引用）

ADR-0034 引用的「docs/audit 本轮审计记录」此前未落盘，本报告补录事实（审计发生在 H1 变更集
`3936cf9` 评审时）：

- **独立审计方**：grok build（grok 4.5，reasoning high）；
- **结论**：阻断性意见 **0 条**（BLOCKING_COUNT=0）；
- **H1 self 评审修正**（落入 ADR 文本）：F-1 P1 跨 ADR 分类冲突（0035 D7 与 0036 D2/D3）、
  F-2 P2 bootstrap diagnostics code 映射、F-3 P2 claim 规范化序列化排序键（D1a）、
  F-4 P2 `returnIntentQueryKeys` 落点与 M1 门控。

## A-001 self 审计过程（步骤 2–4）

### 检查面

1. Schema↔ADR 逐字段（B0/F0/C0 + registry + app-manifest PageEntry）；
2. B1/F1/C1 JS/Python 输出一致（fixtures 逐字段，claim digest 跨语言交叉验证）；
3. 语法增宽同步面（2 Schema + 6 reference + ADR-0034 说明）；
4. 2.8 扫版完整性（fixtures/scenarios/frontmatter/README/overview/MCP DOC_MAP/包版本）；
5. release-check 计数（421）与 digest pin；
6. 制品 build/verify 可复现性；
7. 消费者 boot 阶段顺序、claim 绑定、a11y、零排除消费的真实性；
8. residual 登记完整性。

### 发现问题与处置

| ID | 严重级 | 位置 | 问题 | 处置 |
|---|---|---|---|---|
| V375 | P2 | 消费者 `apps/web/src/host/boot.ts` | maintenance 终态未传播 document `retryAfterSeconds` 到 failure `retry`（D4 after 语义缺失） | 已修复 f929120 |
| V376 | P2 | 消费者 `apps/api/internal/kernel/profile.go` | `core.manifest-route` Contributions.Routes 未登记 bootstrap 路由 | 已修复 f929120 |
| V377 | P3 | 消费者 E-004 | session adapter 终态来源未登记 residual | 已修复 f929120 |
| V378 | P3 | 上游 `host-bootstrap.js/.py` | `bootstrapVersion !== "1.0"` 防御分支无 fixture（B0 const 已覆盖；发布后不追加用例以保持 digest） | 记录不修，下一 MINOR 随行 |

## A-002 独立审计（grok build）

> （待 grok 审计完成回填：结论、问题清单、合并处置与 BLOCKING_COUNT。）

## 归档条件

A-002 阻断性意见归零（BLOCKING_COUNT=0）、全部合并处置落盘后，本报告与清单归档至
`docs/audit/archived/`。
