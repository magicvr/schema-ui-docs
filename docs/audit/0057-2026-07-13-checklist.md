---
status: active
date: 2026-07-13
based_on: 0057-2026-07-13-review.md
---

# 审计 0057 跟踪清单

> 0056（V225–V232）在本复审中已确认闭环，不再重复开单。  
> 本清单仅跟踪 **0057 新发现**。

## V233（🟢 P2）规范/ADR 死链：table-query-state → search-table

- [x] V233a 将 `docs/04-datasource-contract.md` 中 `conformance/fixtures/table-query-state/cases.json` 改为 `…/search-table/cases.json`（或等价相对路径）。
- [x] V233b 同步 `docs/08-renderer-spec.md` 同路径链接。
- [x] V233c 同步 `docs/decisions/0011-reserved-query-params.md` D5 向量路径说明。
- [x] V233d（可选）全库再扫 `table-query-state/cases` 断链；审计正文中的历史描述可保留。

## 验证命令（修复 V233 后）

```bash
# 不应再出现对已删路径的规范链接（审计文档除外）
rg -n 'fixtures/table-query-state/cases' docs --glob '!audit/**'
npm run release:check
```
