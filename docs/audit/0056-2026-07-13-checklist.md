---
status: active
date: 2026-07-13
based_on: 0056-2026-07-13-review.md
---

# 审计 0056 跟踪清单

## V225（🔴 P0）修正发布 fixture digest 证据并硬门禁

- [x] V225a 用当前 `release:check` 算法重算并更正所有对外/归档中的错误 digest（`7bf6db…` → 实际 `fa480b…`，或证明历史字节并记录变更）。
  - 文件：`docs/audit/archived/0055-*.md`、`docs/audit/archived/README.md`；如 CHANGELOG/发布说明有引用则同步
- [x] V225b 在 `scripts/release-check.js`（或旁路配置）增加**期望 digest 断言**（或“首次写入 + 变更需显式 bump”策略），避免再打印错误证据仍绿灯。
- [x] V225c CI/CD 已有 `release:check` 步骤确认会因 digest 不匹配失败；补充一则失败用例说明到 `docs/09-v1-release-goals.md` 或脚本注释。

## V226（🟡 P1）统一根 CLI 与 MCP 的 L0/L1 错误收集

- [x] V226a `scripts/validate-all.js` 调用 `ajv-cli` 时增加 `--all-errors`（或改为与 MCP 相同的进程内 Ajv，单一实现）。
- [x] V226b 用最小反例（缺 `body` + 非法顶层字段 + 缺 `meta.pageId/title`）断言根 CLI 与 MCP `layers["L0/L1"]` 条数/关键 keyword 一致。
- [x] V226c 在 `docs/06-validation.md` 注明 L0/L1 双入口必须 `allErrors` 对齐。

## V227（🟡 P1）版本化 fixtures 的 `protocolVersion` 与 v1 对齐策略

- [x] V227a 书面裁定：case.`protocolVersion` 表示“被测页面协议”还是“算法适用版本”；写入 `conformance/README.md`。
- [x] V227b 若表示当前稳定算法：将仍适用 1.0 的 58 条 `0.3` case 升为 `"1.0"`（保留真正只属于 0.3 行为的对照 case，若有）。
- [x] V227c `release:check` 或 `validate-fixture-suites` 增加策略校验（例如：非 version-negotiation 历史对照 suite 的 case 不得大面积停留在已退役 MINOR，或要求 `protocolVersion` ∈ supported 集合）。
- [x] V227d 重新计算 fixture digest 并走 V225 门禁。

## V228（🟡 P1）根依赖 high severity 与 audit 门禁

- [x] V228a 消除 `fast-json-patch@<3.1.1`：优先去掉对 `ajv-cli` 的运行时依赖（进程内 Ajv 对齐 MCP），或替换/上游修复路径；避免 `audit fix --force` 降级到 ajv-cli@0.6。
- [x] V228b CI `protocol` job 增加 `npm audit --audit-level=high`（或等价）；失败即阻断。
- [x] V228c 确认 Docker 生产镜像仍不打包该链（回归 `mcp/Dockerfile`）。

## V229（🟢 P2）fixture suite Schema 与数组 fixtures 治理

- [x] V229a 更新 `conformance/schemas/fixture-suite.schema.json`：删除死枚举 `errors`；按需加入 `query-serialization` / `table-query-state` 或明确永不版本化。
- [x] V229b 将仍保留的数组 fixtures 要么迁入 `fixtureVersion: "1.0"` suite，要么在 README/校验器中断言“允许的数组路径白名单”，防止静默新增游离文件。
- [x] V229c `validate:conformance` 对白名单数组文件至少检查 id 唯一与非空。

## V230（🟢 P2）消除 table-query-state / search-table 重复

- [x] V230a 选定单一权威源（建议保留 versioned `search-table`，废弃或生成 `table-query-state`）。
- [x] V230b 删除重复 runner/文档行，或改为 thin wrapper 读同一文件；避免双份 expected。
- [x] V230c 若保留 table-query-state 目录：补 Python runner 与 CI，或从 digest 路径中移除并更新文档。

## V231（🟢 P2）文档 frontmatter 与 Schema 描述升到 v1.0

- [x] V231a 七份核心规范 frontmatter `applies_to` → `schema-ui-protocol v1.0`（或项目统一写法）。
- [x] V231b `docs/schemas/page.schema.json` `protocolVersion` description 去掉“当前 RC 使用 0.3”。
- [x] V231c 抽查 `last_updated` / 过时 RC 措辞；可选：`release:check` 扫描 `applies_to:.*v0\.3`。

## V232（🟢 P2）MCP devDependency 审计

- [x] V232a 评估升级 `vitest`（及 vite/esbuild 链）到无 critical/high 的版本，或 pin 并记录接受风险。
- [x] V232b CI `mcp` job 增加 `npm audit --prefix mcp --audit-level=high`（可对仅 dev 的 advisory 使用豁免文件，但须显式）。
- [x] V232c 确认 `npm prune --omit=dev` 后镜像 audit 清洁。

## 验证命令（修复后建议）

```bash
npm ci
npm ci --prefix mcp
npm run release:check
npm run validate:conformance
npm run test:conformance:version
npm run test:conformance:version:python
# …其余 conformance 与 CI 对齐
npm --prefix mcp test
npm audit --audit-level=high
npm audit --prefix mcp --audit-level=high
```
