---
status: active
date: 2026-07-28
based_on: 0071-2026-07-28-review.md
scope: focus=all；主类型=实现/契约漂移
---

# 审计 0071 跟踪 Checklist

> 本 checklist 跟踪 v2.5 契约投影、应用清单/导航门禁、MCP 运行边界与 PATCH 发布完整性（V336–V344）。
> 生产消费者仅作 out-of-repo 跟踪；未经单独授权，不修改 `allinme.web-client` 或 `allinme.core-api`。
> 不把本审计路径加入 `protocol-manifest.json`、协议制品、MCP 搜索索引或 Docker 运行镜像。

## 汇总

| 编号 | 级别 | 主题 | 状态 |
|---|---|---|---|
| V336 | P1 | 应用清单缺少 v2.5 字段集下限 | **已关闭** |
| V337 | P1 | 导航 M3a 静态表达式校验未落地 | **已关闭** |
| V338 | P1 | API `baseURL` 与应用路由根语义冲突 | **已关闭** |
| V339 | P2 | 页面 Schema 版本说明与 capability 列表滞后 | **已关闭** |
| V340 | P2 | MCP 文档索引遗漏 v2.5 权威正文 | **已关闭** |
| V341 | P2 | MCP 可由环境变量改用外部协议或验证器根目录 | **已关闭** |
| V342 | P2 | 场景 frontmatter 版本漂移未被 runner 覆盖 | **已关闭** |
| V343 | P3 | Docker build context 未隔离且 CI 未观察隔离规则 | **已关闭** |
| V344 | P1 | 修复后制品仍声明已发布的 2.5.0 身份 | **开放** |

**计数：** 3×P1、4×P2、1×P3 已关闭；1×P1（V344）开放。
**问题号：** 全局续编 `V335` → `V336`–`V344`。
**审计号：** `0071`（active，待归档确认）。

## 基线

- [x] 创建前确认无活跃 audit；全局最大审计号为 `0070`。
- [x] 确认 `V336`–`V344` 位于全局最大 `V335` 之后，且不使用历史冲突的 V1–V4。
- [x] 记录当前工作树版本：协议 `2.5.1` / `"2.5"`，MCP `2.5.1`，validator `1.0.0`；已发布 `2.5.0` 保持不可变。
- [x] 记录本地门禁：links、release/artifact、scenario、fixture、35 个 conformance 入口、MCP 128 tests、tools smoke、Docker smoke 均通过。
- [x] 记录远端证据：`v2.5.0` / `mcp-v2.5.0`、Protocol/MCP Release 与可见 CI 均成功；GHCR version 列表缺 token scope。

---

## V336 — 应用清单缺少 v2.5 字段集下限（P1）

- [x] 裁决并写清：应用清单 `protocolVersion` 低于 `2.5` 的稳定错误码、错误路径与 M0/M1 归属。
  - 码：`PROTOCOL_VERSION_TOO_LOW`；路径：`protocolVersion`；归属：**M1**（字段集下限，非协商 `UNSUPPORTED_PROTOCOL_VERSION`）。
- [x] 在 JS 与 Python app-manifest reference 实现相同的 `>= 2.5` 语义检查。
- [x] 新增共享 fixture：完整的 `2.4` + `app.manifest` 清单必须被拒绝（`m1-protocol-version-below-2-5-rejected`）。
- [x] 保留并复核清单 `2.5` + 页面 `2.4` 解耦正例（`decoupled-manifest-2-5-page-2-4`）。
- [x] 同步 `17-app-manifest.md`、M1 说明与 Schema 描述。
- 验证命令：`npm run test:conformance:app-manifest`；`npm run test:conformance:app-manifest:python`；`npm run validate:conformance`。
- 关闭证据：两端对新增反例输出 `PROTOCOL_VERSION_TOO_LOW`；既有 2.5 及解耦案例无回归。

## V337 — 导航 M3a 静态表达式校验未落地（P1）

- [x] 定义并复用清单作用域 M3a 的输入、输出与错误格式（`SYNTAX` / `FORBIDDEN_VARIABLE` / `FORBIDDEN_CONTEXT_NAMESPACE` / `UNKNOWN_VARIABLE`）；仅 `validate` 路径结构化报告，runtime `project` 仍 fail-closed 隐藏单项。
- [x] 在 JS/Python app-navigation reference 增加相同的 M3a 校验路径。
- [x] 新增 `$deps`、`$row`、`$self`、非法语法和合法 `$context.user.*` / `$context.features.*` 共享 fixtures。
- [x] 同步 `17-app-manifest.md` M3a 表述和 conformance README 的 suite 覆盖描述。
- 验证命令：`npm run test:conformance:app-navigation`；`npm run test:conformance:app-navigation:python`；`npm run test:conformance:all`。
- 关闭证据：非法表达式被结构化拒绝；合法过滤、空组剪枝和高亮均保持通过（16 fixtures）。

## V338 — API `baseURL` 与应用路由根语义冲突（P1）

- [x] 以 `17-app-manifest.md` D1b 为唯一语义来源。
- [x] 同步 `07-actions-contract.md`、`action.schema.json` 与 `app-manifest.schema.json` 的描述（request→API baseURL；navigate/Outcome navigate→应用路由根）。
- [x] 为 API host 与应用 host 不同的基址矩阵新增 request-construction fixtures（form request / recordSource / page navigate / outcome navigate）。
- [x] reference 在提供 `baseURL` / `appRouteRoot` 时解析绝对 URL 并标注 `resolvedBase`。
- 验证命令：`npm run test:conformance:request`；`npm run test:conformance:request:python`；`npm --prefix mcp test`。
- 关闭证据：69 request fixtures 双语言通过；MCP 回归通过。

## V339 — 页面 Schema 版本说明与 capability 列表滞后（P2）

- [x] 将 `page.schema.json` 的当前协议说明更新为 `2.5`。
- [x] 补全 `app.manifest`、`app.navigation`、`table.sort` capability 说明与版本下限。
- [x] 同步 `01` 示例注释中的当前制品版本。
- 验证命令：`npm run check:links`；`npm run build:protocol`；`npm --prefix mcp test`。
- 关闭证据：Schema JSON 可解析；协议制品构建成功。

## V340 — MCP 文档索引遗漏 v2.5 权威正文（P2）

- [x] 在 `DOC_MAP` 添加 `app-manifest` → `docs/17-app-manifest.md`。
- [x] 将 current v2 release-goals 映射到 `docs/release-goals/v2.5.md`。
- [x] 补 `protocol.get_doc` / `protocol.search` 回归；确认 `docs/audit/**` 不在可检索范围。
- 验证命令：`npm --prefix mcp run build`；`npm --prefix mcp test`；`npm --prefix mcp run smoke:tools`。
- 关闭证据：134 MCP tests 通过（含 app-manifest / v2.5 goals）；tools smoke 通过。

## V341 — MCP 可由环境变量改用外部协议或验证器根目录（P2）

- [x] 生产 `NODE_ENV=production` 忽略 `SCHEMA_UI_PROTOCOL_ROOT` / `SCHEMA_UI_VALIDATOR_ROOT`；显式 `SCHEMA_UI_ALLOW_EXTERNAL_ROOTS=1` 才允许覆盖。
- [x] 默认和生产路径仅使用镜像内置协议制品及固定 validator。
- [x] 增加 `mcp/tests/paths.test.ts`；Docker smoke 注入外部根 env 并仍 `get_doc(app-manifest)`（待 daemon）。
- [x] 更新 ADR-0007 D9 与 `RELEASE.md`。
- 验证命令：`npm --prefix mcp test`（paths 4 tests 通过）。
- 关闭证据：生产忽略覆盖有单测；Docker 注入回归代码已写入 `smoke-docker.mjs`（本机 daemon 未运行，未实跑镜像）。

## V342 — 场景 frontmatter 版本漂移未被 runner 覆盖（P2）

- [x] 将 `permission-inheritance.md` 的 `applies_to` 同步为 v2.5。
- [x] 全部受控场景 `protocol_version: v2.5` 与 YAML `meta.protocolVersion: "2.5"` 对齐。
- [x] `release-check` 增加 frontmatter（`applies_to` 或 `protocol_version`）与 YAML 版本一致性门禁。
- 验证命令：`npm run release:check`；`npm run validate:scenarios`；`npm run check:links`。
- 关闭证据：release-check 通过；场景 L0–L4 通过。

## V343 — Docker build context 未隔离且 CI 未观察隔离规则（P3）

- [x] 新增根 `.dockerignore`（排除 `.env`、缓存、`docs/audit`、本地噪声；保留 Dockerfile 所需输入）。
- [x] 将 `.dockerignore` 与 `mcp/Dockerfile` 纳入 `mcp-ci.yml` paths。
- [x] 用 2 MiB `sentinel-audit-probe.bin` 验证不进入 Docker build context；构建输出为 16.61 kB，探针已自动清理。
- [x] 重建当前 `schema-ui-mcp:2.5.0` 并运行 Docker smoke；5 tools、`externalRootEnvIgnored: true`、`appManifestDocOk: true`。
- 关闭证据：`.dockerignore` 与 MCP CI paths 已落地；哨兵、镜像构建和 smoke 均通过。

## V344 — 修复后制品仍声明已发布的 2.5.0 身份（P1，发布完整性）

- [x] 确认正式 `v2.5.0` artifact digest `sha256:81bc11fc…` 与修复后工作树制品不同；不修改历史 tag / Release。
- [x] 将根 package/lockfile、manifest、MCP package/lockfile、validator compatibility 和当前入口文档同步为 `2.5.1`；页面 `protocolVersion` 保持 `"2.5"`。
- [x] CHANGELOG 新增 `v2.5.1` PATCH 节，说明 V336–V343 是对已发布 2.5.0 投影缺口的固化。
- [x] 本地 `release:check` 通过，记录 292 cases、fixture `sha256:712f6c42…`、content `sha256:3a20f417…`、artifact `sha256:3524c527…`。
- [x] 重跑 links、artifact reproducibility/links、scenarios、fixture validation、35 个 conformance 入口、MCP/validator release checks、MCP build/134 tests/tools smoke，以及 `schema-ui-mcp:2.5.1` Docker smoke。
- [ ] 分支 CI 通过后，在目标 commit 手工创建并推送 `v2.5.1`；核对 Protocol Release 的 tar、SHA-256 与本地 artifact digest。
- [ ] Protocol Release 完成后，手工创建并推送 `mcp-v2.5.1`；核对 MCP CD、GHCR version/SHA tags、OCI label、remote digest 与 smoke。
- 验证命令：`npm run release:check:tag`；`npm run release:check:mcp:tag`；远端 Release/CD 证据。
- 关闭证据：不可变 `v2.5.1` / `mcp-v2.5.1` tag、CI/CD run、Release asset 与 GHCR digest 全部可核验。

## Out-of-repo

- [ ] `allinme.web-client` 评估升级至 v2.5 的 pin、16 类 fixtures、app manifest/navigation/table sort 支持与 CI。未经单独授权不修改。
- [ ] `allinme.core-api` 评估升级至 v2.5 的 pin、页面/清单产出边界与 CI。未经单独授权不修改。

## 完成门禁（归档前）

- [ ] V336–V344 的代码、fixtures、测试、文档与发布证据均有关闭证据；没有以 audit 正文替代权威沉淀。
- [x] 重跑受影响的根、conformance、MCP 门禁（见下）。
- [x] 本地 Docker build + smoke；包含外部根目录环境变量注入和 app-manifest 查询回归。
- [x] 明确未执行项、消费者 out-of-repo 项、GHCR 远端证据缺口和剩余风险。
- [ ] 向用户展示关闭摘要并获得明确归档确认。

### 本轮验证结果（2026-07-28）

| 门禁 | 结果 |
|---|---|
| `release:check` | 通过；versionedCaseCount **292**；fixtureDigest `sha256:712f6c42…` |
| `test:conformance:all` | **35** 入口全部通过 |
| `check:links` | 225 Markdown / 624 相对链接通过 |
| `validate:scenarios` | 官方六场景 L0–L4 通过 |
| `validate:conformance` | 16 versioned + 1 allowlisted array 通过 |
| `npm --prefix mcp test` | **134** tests 通过 |
| `npm --prefix mcp run smoke:tools` | 5 工具可发现 |
| Docker build / smoke | **通过**；Docker context 16.61 kB；外部根目录 env 被 production image 忽略 |
