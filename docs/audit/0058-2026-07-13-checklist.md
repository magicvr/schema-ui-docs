---
status: active
date: 2026-07-13
based_on: 0058-2026-07-13-review.md
---

# 审计 0058 跟踪清单

> V234/V235 已完成修复，并通过本清单列出的针对性门禁验证。

## V234（🟢 P2）对齐 Node.js 支持声明与 CI 证据

- [x] V234a 决定并统一最低支持版本：若继续承诺 Node 18，则为运行时/build 增加 Node 18 兼容验证；若不再支持，则同步根/MCP `engines`、README、ADR/设计文档。
- [x] V234b CI 增加最低支持版本 job 或 matrix，至少执行 `npm ci`、`npm ci --prefix mcp`、MCP build、核心 tests/smoke；Node 24 可继续作为发布镜像版本。
- [x] V234c 处理 `vitest@3.2.7` → `vite@7.3.6` 的 Node 20+ 测试链：选择兼容依赖、拆分测试支持范围，或明确开发/测试工具链最低 Node 版本与运行时不同。
- [x] V234d 在最低支持版本上验证 MCP 子进程 L2/L3a/L4 stdout 契约，避免只验证 L0/L1/官方正例。

## V235（🟢 P2）修复归档死链并增加链接门禁

- [x] V235a 将 `docs/audit/archived/0053-2026-07-11-checklist.md` 的 `../CHANGELOG.md` 修为从归档目录可达的 `../../CHANGELOG.md`。
- [x] V235b 增加仓库内 Markdown 相对链接存在性检查，至少覆盖 `README.md`、`docs/**`、`conformance/**` 与审计活跃/归档目录。
- [x] V235c 将链接检查接入 PR/push CI；明确外部 URL、锚点、代码块和历史纯文本路径的跳过策略。
- [x] V235d 复跑全库相对链接扫描，结果应为 0 条不存在目标。

## 验证命令（修复后建议）

```bash
# 支持矩阵应在最低声明 Node 版本和 Node 24 各执行一次
npm ci
npm ci --prefix mcp
npm --prefix mcp run build
npm --prefix mcp test
npm --prefix mcp run smoke:tools

# 新增的链接检查命令（名称按最终实现）
npm run check:links

npm run release:check
npm run validate:conformance
npm run validate:scenarios
npm audit --audit-level=high
npm audit --prefix mcp --audit-level=high
```
